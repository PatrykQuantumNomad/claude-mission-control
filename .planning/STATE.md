---
gsd_state_version: 1.0
milestone: v1.2
milestone_name: Depth & Polish
status: ready_to_plan
stopped_at: Phase 22 Plan 01 complete — SQL feasibility spike resolved NO (SKLP-11 descopes to v1.3); SPIKE-FINDINGS.md committed at 07abcfa; SUMMARY at .planning/phases/22-skill-latency-overhead-spike-gated/22-01-SUMMARY.md. User must re-invoke /gsd-plan-phase 22 to author Plan 02-descope (REQUIREMENTS.md flip).
last_updated: "2026-05-08T13:28:11.319Z"
last_activity: 2026-05-08 -- Phase 22 execution started
progress:
  total_phases: 6
  completed_phases: 5
  total_plans: 18
  completed_plans: 17
  percent: 83
---

# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-05-05 after v1.1 ship)

**Core value:** A solo Claude Code developer can see what every agent session is doing, how tokens and tools are performing, what each skill costs and how often it fails, queue and approve tasks, compare two sessions side-by-side, get paged when metrics breach thresholds, and kill runaway sessions — all from one browser tab without maintaining external infrastructure.

**Current focus:** Phase 22 — skill-latency-overhead-spike-gated

## Current Position

Phase: 23
Plan: Not started
Status: Ready to plan
Last activity: 2026-05-08

Progress: [██████████] 100% (5/6 phases complete; Phase 22 spike-gated and resolved to descope per SKLP-11 v1.3 deferral; Phase 23 unblocked per ROADMAP §Phase 22 Success Criteria)

## Accumulated Context

### Decisions

Cumulative decision log lives in `.planning/PROJECT.md` Key Decisions table. v1.2 plan-execution decisions (Phase 18 Plan 01):

- **`cmc.core.time` is the canonical home for naive-UTC time concerns.** `now_utc()` returns `datetime.now(UTC).replace(tzinfo=None)`; `UTCDatetime` PlainSerializer is colocated. `cmc.api.schemas.common` re-exports `UTCDatetime` (one-line, `# noqa: F401`) so the 8 existing schema importers keep working without an 8-file cosmetic sweep (D-Pitfall-9). `cmc.core` re-exports `now_utc` for ergonomic access.
- **No speculative time helpers.** Only `now_utc` and `UTCDatetime` ship in Phase 18 (D-Module-shape). Future helpers (`today_utc`, `parse_iso_utc`) promote inline if/when Plan 02's sweep finds 3+ uses of a pattern.
- **Two-commit migration enforced.** Plan 01 creates the helper; Plan 02 owns the 22-site mechanical replace. Bisect-friendly; the sweep commit can be uniform mechanical.

Phase 18 Plan 02 (utcnow sweep, POLI-06):

- **D-Sweep atomic commit (c3d792f).** Tasks 1+2 merged into a single bisect-friendly mechanical-replacement commit. The 22-site replacement either fully reverts or fully applies — no half-migrated intermediate state on the bisect timeline. Per the locked two-commit migration: Plan 01 created the helper, Plan 02 adopts it across the codebase.
- **Docstring substring discipline.** When a verify gate uses `git grep`, prose mentioning the banned API must paraphrase. Three docstring blocks in `cmc/core/time.py` and one in `tests/test_core_time.py` were reworded ("the deprecated stdlib naive-UTC factory") to clear the POLI-06 structural verify gate while preserving explanatory intent.
- **Adjacent lint cleanup folds into the sweep when the same file is already touched.** Pre-existing I001 import-sort errors in `tests/test_core_time.py` (Plan 18-01 carry-over) blocked the pre-commit ruff hook on the sweep commit; auto-fixed via `ruff check --select I --fix` and folded into the same commit (vs. a separate "lint" commit on a single file already in the sweep's modification set).
- **Did not activate ruff DTZ** (Open-Question 3 deferred — would surface 38 unrelated DTZ findings out of POLI-06 scope).
- **Did not introduce a Field constants module / NOW_UTC sentinel.** Kept all 19 default_factory= references as direct function imports — matches D-Field-factories.

Phase 18 Plan 03 (SchedulesCard determinism, POLI-07):

- **`vi.spyOn(Date, 'now')` is the locked clock-pin mechanism for boundary-threshold tests** (NOT `vi.useFakeTimers`). Narrowest blast radius — targets exactly the one `Date.now()` call the production code reads, no interaction with React-Query or userEvent timer scheduling. Used describe-scoped in `SchedulesCard.test.tsx` with `NOW_MS = 2026-05-05T23:55:00Z`.
- **Test factories MUST default time-dependent fields to a sentinel ('never run' = `null`), never a hard-coded ISO string.** Hard-coded ISO defaults age with calendar time and silently flip "fresh" fixtures to "stale" — exactly the bit-rot that broke `SchedulesCard.test.tsx > stale row` 8 days after the original timestamp was written.
- **No cleanup-sweep migrations beyond SchedulesCard.** Audited 9 other component tests using `Date.now()`; all use it for relative timestamps without threshold/boundary assertions, so no flake risk. `RelativeTime.test.tsx` and `EmergencyStopBanner.test.tsx` left untouched per Pitfall 3 (load-bearing useFakeTimers usage).

Phase 18 Plan 04 (Playwright strict-mode + e2e README, POLI-08):

- **`data-testid` lives on the source React component, not test-only wrappers.** `data-testid="schedule-composer-name"` ships on `ScheduleComposer.tsx:193`. Specs reach it via `page.getByTestId('feature-component-element')`. Test wrappers were rejected to avoid render-layer maintenance burden.
- **`feature-component-element` kebab-case path-style is the locked testid convention** (e.g., `schedule-composer-name`, `alerts-firehose-skill-filter`, `skills-detail-projects-table`). Documented in `frontend/tests/e2e/README.md` (NOT `CONTRIBUTING.md`) per CONTEXT D-Documentation-location lock — rule lives next to the tooling that enforces it.
- **Decorate only when strict mode collides — pre-decoration is anti-pattern.** Full-suite strict-mode run found exactly one collision (`getByLabel('Name')` matched both ScheduleComposer wrap and SkillTimeline aria-label). Only that selector got a testid; `getByLabel('Advanced cron')`, `getByRole('button', {name: 'Create schedule'})`, `getByRole('button', {name: '+ New'})` all stayed as-is.
- **Steady-state alerts.spec.ts skip preserved (Pitfall 6).** README documents that "1 skipped" (alerts TEST-05a) is the baseline so verifiers don't regress on it.

Phase 18 Plan 05 (baseline-and-phase-close, phase-exit artifact):

- **BASELINE.md lives in the phase directory, not at `.planning/` root.** Per CONTEXT D-Verifier-baseline. A future "Phase 24 Polish v2" or similar would write its own baseline in *its* phase directory rather than mutating Phase 18's frozen baseline.
- **Verifier rules embedded as prose-with-bounds inside BASELINE.md** (e.g., `passed >= 566 → pass`, `warnings_datetime_utcnow > 0 → fail`, `total_warnings > 132 → warn`). Single source of truth: a downstream verifier reads one file and gets both the baseline counts AND the comparison thresholds.
- **Warning-delta is a load-bearing baseline metric.** Pytest total warnings (32) AND `datetime.utcnow`-specific warnings (0) are both recorded; the second is the load-bearing POLI-06 reverse-direction signal; the first gives a 100-warning headroom before flagging an investigation.
- **Dev-DB context capture for state-dependent skips.** Recorded `failed_tasks_total=1` and `failed_tasks_recent_5min=0` to explain the alerts.spec.ts steady-state skip. Lets future verifiers distinguish "baseline preserved" from "state drifted" (skipped >= 2 → human review).
- **Even ad-hoc inspection scripts respect the POLI-06 ban.** The dev-DB capture script uses `cmc.core.time.now_utc` (not `datetime.utcnow`) — structural enforcement is across the codebase, not just shipped code.

Phase 19 Plan 01 (migration 0003_project_key + project_key helper + ingest wiring, SKLP-08 foundation):

- **Migration 0003 INLINES the sha1[:12] backfill logic instead of importing `compute_project_key`.** Defensive against future helper refactors — Alembic migrations must remain runnable against historical revisions even if `cmc.core.project_key` is renamed/moved/restructured. Mirrors how 0002 inlined `json_extract` for session_id backfill instead of importing from `cmc.ingest.parser`. The unit test `test_matches_inline_sha1_logic` in `test_core_project_key.py` pins the formula equality so the helper and the migration cannot silently diverge.
- **Empty-string sentinel for `compute_project_key(None)` and `compute_project_key('')`.** Both return `''` (never raises). The `sessions.project_key` column is NOT NULL, so the empty string is the natural "no canonical project" marker — queries naturally exclude `WHERE project_key != ''`. Mirrors the COALESCE pattern in cost.py:168.
- **Python-loop backfill for filesystem-aware migrations.** SQLite has no `realpath` builtin, so the migration iterates rows in Python and calls `os.path.realpath` per row. Pure-SQL backfill (the 0002 idiom for session_id) was unavailable because the canonicalization crosses into the filesystem layer.
- **`_SESSION_MUTABLE_COLS` includes `project_key`.** Pitfall 9 — the migration is one-shot; ingest must keep the column fresh on every re-sync, including rows where the cwd value arrives or is corrected later. Without this, a session with cwd corrected after first sync would never get re-keyed.
- **Helper-first / wiring-second commit split (53fe578 + 95bd1df).** Two atomic commits on `main`: Task 1 lands compute_project_key + 11 unit tests in isolation; Task 2 lands the migration + sessions model + scheduler/repository wiring + 2 migration tests as one unit. Bisect-friendly: a regression in either layer is attributable to one commit.
- **Test count grew from 7 (plan) to 11 (delivered).** Each addition pins an invariant the original 7 didn't: re-export shape (`test_reexport_via_cmc_core`), formula equality vs inlined migration code (`test_matches_inline_sha1_logic`, drift guard), parametric falsy-input coverage. No deviation cost — pure-function tests run in 0.02s combined.

Phase 19 Plan 03 (SKLP-09/10 deltas + badges + DST spring-forward correctness):

- **DeltaPill is the canonical period-over-period primitive going forward.** `curr/prev/delta/delta_pct/direction` shape works for both integer counts (usage) AND Decimal cost (money). `delta_pct=None` invariant when `prev=0` — server's job to decide '—' vs '+inf%' (RESEARCH §Pattern 3, server is source of truth, frontend never re-derives). Reusable for cost forecasts (Phase 20 ANLY-08), alert frequency comparisons, compare-mode session diffs — anywhere a curr/prev/delta/direction shape is needed.
- **Python-side badge classification (over pure SQL CASE WHEN).** The plan offered both as acceptable; Python won on readability + unit-testability of the cold-start gate. The DST safety property still holds: SQL emits UTC-clean MIN/MAX(ts) (verified by grep guard) and Python does timedelta arithmetic (DST-immune by construction). Documented in `_derive_badges` docstring.
- **Cost-delta windows are LITERAL in SQL, not parameterized.** Four new dedicated SQL fragments (`_COST_DELTA_CURR_REQUEST_SQL` / `_PREV` / `_CURR_SESSION` / `_PREV_SESSION`) with hardcoded `datetime('now', '-7 days')` / `'-14 days'` bounds. Could have parameterized the existing `_COST_*_SCOPED_SQL` pair — risked Pitfall 2 (caller binds `?range=` to delta windows). Hardcoding the 7d horizon in SQL is the structural guard.
- **`_USAGE_DELTA_BADGE_SQL_PORTABLE` uses skills_seen UNION + LEFT JOIN trio.** SQLite < 3.39 lacks FULL OUTER JOIN; the portable form gives the same result and ships everywhere. The FULL OUTER form preserved as source-only documentation.
- **DST test combines Python helper assertion + structural grep guard.** Either alone is insufficient: helper-only misses an SQL regression that swaps to `'localtime'` and corrupts the inputs; grep-only misses a Python regression that drops timezone info. Together they form the load-bearing structural guard for ROADMAP success criterion #5. Adversarial-mutation verification (in-place sed in `'localtime'`, observe RED, restore, observe GREEN) proved the guard isn't vacuous.
- **The 30d window literal is NOT in route SQL.** Dormant threshold runs in Python against `MIN/MAX(ts)` hoisted out of SQL. The DST positive-presence assertion was scoped to `-7 days` / `-14 days` (which DO appear as delta CTE bounds); the `-30 days` arithmetic is pinned by the Python helper's UTC-anchored timedelta math.
- **`cost_delta` emitted on the empty-case branch.** Frontend never has to special-case "this response has no delta" — the field is always present (flat-zero pill). Mirrors Plan 19-02's path-leakage-by-construction discipline: structural property of the response shape rather than a runtime invariant the consumer must remember to check.
- **Atomic three-commit task split (ee662cb schema + ea0d1cb handlers + 68aeb5c tests).** Task 1's commit landed required new fields temporarily failing one existing test; Task 2's handlers populated them; Task 3 fixed the existing test + added 12 new ones. Bisect-friendly — a regression in any layer attributable to one commit. The intermediate test-failing state on Task 1's commit is acceptable per plan's explicit sequence (the plan never claims tests are green between commits, only that they're green at plan-close).

Phase 20 Plan 03 (cost-dashboard-route-and-cards, ANLY-06+07 frontend):

- **NEW `/cost` top-level route — Option A from 20-RESEARCH.md Open Question #1.** No existing route precedent hosting cost-forecast / per-project-cost surfaces; ROADMAP language ("the cost dashboard") implies a dedicated page. Sibling of `/skills`, `/activity`, `/alerts` (no parent layout nesting). Establishes the surface for v1.3 cost analytics (ANLY-08 confidence band, ANLY-09 per-project budgets) without further routing decisions.
- **Bias banner driven by `data.partial_month_bias` flag, NEVER by `data.days_elapsed < 7` re-derivation client-side.** Pitfall 7 from 20-RESEARCH.md. Adversarial vitest case: `days_elapsed: 10, partial_month_bias: true, insufficient_data: false` → banner SHOWS. Server is single source of truth for forecast-confidence policy; UI consumes the flag verbatim, ready for any future server-side policy divergence (e.g., a trailing-7d-volatility metric replacing the day-7 boundary). Locked structurally — TS schema makes the flag a required boolean.
- **MTD KpiTile ALWAYS rendered, even on insufficient_data branch.** Operator visibility into current month-to-date is independent of forecast confidence. Pattern: `data-testid="cost-forecast-card-mtd"` lives outside the insufficient/projection branch toggle.
- **Cache-key discipline: `qk.costBreakdown(dim, range)` keyed by BOTH dim AND range.** Phase 19 hotfix lesson (STATE.md L121, hotfix da592ff) carried forward. Without range in the key, switching the RangeToggle would render stale 7d data labelled as 30d (or vice versa) until the next 60s tick. Test: `expect(qk.costBreakdown('project', '7d')).not.toEqual(qk.costBreakdown('project', '30d'))`. Pattern locked: every TanStack-Query hook param affecting response shape MUST appear in the queryKey; future cost-derived hooks (ANLY-08, ANLY-09) inherit the rule.
- **Section-level `data-testid` on the wrapping `<section>` survives all four PanelCard branches** (loading / error / empty / data). PanelCard doesn't pass-through `data-testid`; the section wrapper gives Playwright (Plan 04) and vitest a stable hook regardless of which branch rendered. Phase 19 SKLP-08 lesson reapplied verbatim. Inner branch-specific testids (`cost-forecast-card-projected`, `cost-forecast-card-bias-banner`, `cost-forecast-card-insufficient-message`, `cost-forecast-card-mtd`, `cost-by-project-card-table`) follow Phase 18 POLI-08 kebab-case `feature-component-element` convention.
- **Decimal-as-JSON-string display via template literals only.** `${'$'}${data.projected_month_total_usd}` preserves full 4-decimal precision (e.g., `$125.4321`); `Number(value)` coercion is type-rejected at compile time (the field type is `string`) and Pitfall 5 forbidden runtime-side. Pinned by adversarial vitest case: rendered text is exactly `$125.4321`, not `$125.43`. Sort comparators may parseFloat (display passes the original string through fmtCost) — same precedent as `SkillProjectsTable.fmtCost`.
- **Cadence centralization preserved.** `useCostForecast` and `useCostBreakdown` cadence (60s/45s — same daily-aggregate bucket as `useSkillCost` / `useTokens`) lives in `queries.ts` only; panel components NEVER inline `refetchInterval`. Project convention from Phase 14 file header. Future contributors editing the cost panels CAN'T silently drift the cadence.
- **TDD RED → GREEN gate sequence: 3 commits.** `f90ec21` (feat — types + api + qk + hooks; queries.test.ts cost-key assertion went RED first then GREEN), `1fc13e1` (feat — panels + 9 vitest cases; tests authored RED, components written GREEN), `96ea120` (feat — /cost route + NavBar + routeTree codegen; NavBar.test.tsx 5-link assertion went RED then GREEN). Each task self-contained, bisect-friendly. No REFACTOR commits — code shipped clean on first GREEN.
- **TanStack Router file-route + routeTree.gen.ts codegen** is the locked pattern for new top-level surfaces. `pnpm tsc --noEmit` triggers the @tanstack/router-plugin/vite codegen; the resulting `frontend/src/routeTree.gen.ts` diff is committed alongside the route file + nav-link change. NavBar's `Link to='/cost'` is type-checked against the generated `FileRoutesByPath` interface, so a missing codegen surface fails compilation immediately.
- **Pre-existing pytest `NoDecode` collection error confirmed pre-existing**, not a Plan 20-03 regression. Verified by `git stash` + pytest reproducing the same ImportError on the stashed (pre-Plan-20-03) tree. Frontend-only changes cannot regress backend tests; the error stems from a pydantic version drift unrelated to this phase. Out of scope for Plan 20-03 — defer to Phase 23 polish/cleanup or earlier if a Plan 20-04 agent observes the same.

Phase 20 Plan 02 (cost-forecast-module-and-endpoint, ANLY-06):

- **D-01 — `days_elapsed = today.day - 1`.** Calendar interpretation, not cost-bearing-days. Day 1 → 0, day 7 → 6 (insufficient), day 8 → 7 (forecast unlocks). Resolves 20-RESEARCH.md Open Question #3 / Assumption A2. Locked by 9-case parametric unit test in `test_cost_forecast.py::test_days_elapsed_boundary`.
- **D-02 — 14d baseline EXCLUDES today.** Uses `[today-14, today-1]` (complete days only). Avoids partial-day bias on the OLS slope. Resolves 20-RESEARCH.md Open Question #5 / Assumption A3. Locked by adversarial test `test_forecast_excludes_today_from_baseline`: seeds a 999_999_999-token outlier for today against a flat 14d baseline; if the baseline included today the projection would jump by orders of magnitude — assertion `projected < mtd * 100` catches the violation.
- **D-03 — No clamp on negative-slope projections.** Raw OLS extrapolation; honest signal. Resolves 20-RESEARCH.md Open Question #4 / Assumption A4. UI handles presentation. Locked by `test_project_month_total_negative_slope_can_go_below_mtd` (slope=-2 → projection=-570).
- **D-04 — No `degenerate_baseline` flag on the response.** Zero-variance Y series returns `slope=Decimal(0)`, `intercept=mean(ys)`, `projection = mean * days_in_month`. The `partial_month_bias` flag covers user-facing volatility messaging; a separate field would be redundant. Resolves 20-RESEARCH.md Open Question #2.
- **D-05 — `insufficient_data` and `partial_month_bias` share the same `< 7` threshold.** Both flags flip together at the day-7 → day-8 boundary. Surfaced as separate booleans for forward-compat in case the policy ever diverges. Locked by `test_forecast_insufficient_data_threshold` (day 3 → both true) + `test_forecast_flags_clear_on_day_8` (day 8 → both false).
- **Decimal context inheritance from `cmc.pricing` is structurally enforced.** `cmc/cost/forecast.py` does NOT call `getcontext()`; module docstring explicitly forbids it. Pricing is imported in any path reaching forecast (`compute_cost` is the read-time entry point). Verified by `grep -nE "getcontext\(\)\.prec|getcontext\(\)\.rounding" cmc/cost/forecast.py` → 0 matches. Pattern locked: future cost-derived analytics modules in `cmc/cost/` MUST inherit context, never re-set.
- **Pure-stdlib OLS is the locked policy** for cost analytics in v1.2 (ROADMAP success criterion #1: "stdlib Decimal-only OLS"). No numpy, no scipy. Closed-form least squares: `slope = Σ(x-mx)(y-my) / Σ(x-mx)² if Σ(x-mx)² else Decimal(0)`. Defensive zero-variance guard returns `(Decimal(0), mean(ys))`; pinned by `test_decimal_ols_zero_variance_y` and `test_decimal_ols_single_point_no_crash`.
- **Two-phase route handler — MTD always, baseline only when sufficient.** `cost_forecast` always computes `month_to_date_usd` (early-return shape preserved); the 14d baseline + projection is only computed when `days_elapsed >= 7`. Reusable pattern for any future month-aware analytics endpoint. The MTD SQL uses `[month_start, today]` (today included) while the baseline SQL uses `[today-14, today-1]` (today excluded — D-02).
- **`pytest_freezer` is the locked date-pinning mechanism for forecast tests.** Already in `pyproject.toml:dev` as `pytest-freezer>=0.4`; freezegun-based; patches `datetime.now(UTC)` globally so `cmc.core.time.now_utc` is transparently mocked without cmc-internal monkeypatching. The `freezer` fixture's `move_to(iso_string)` API is the calling convention. Pattern locked: any future endpoint test that depends on the server clock should use the same pattern, NOT `monkeypatch` of `cmc.core.time`.
- **Test seed model name discipline.** `_seed_n_days_token_usage` helper uses `claude-opus-4-7` (priced via lifespan-loaded `data/pricing.json`). An earlier draft used `claude-opus-4-5` which doesn't exist in the seed → silent `compute_cost` lookup miss → `Decimal(0)` everywhere → test_forecast_excludes_today_from_baseline failed with `mtd=0, projected=0`. Fix: always reference a model that exists in `data/pricing.json` for cost-bearing test fixtures; lookup misses bump `unpriced_tokens` counter (ANLY-05) but produce silent zeros that confuse boundary tests.
- **TDD RED → GREEN gate sequence atomically committed (4 commits).** `01b25a1` (RED — 26 failing decimal_ols/days unit tests) → `10e0757` (GREEN — `cmc/cost/forecast.py` module) → `54f922b` (RED — `CostForecastResponse` schema + 5 failing endpoint tests with HTTP 404) → `2765f07` (GREEN — wired `GET /api/cost/forecast`). Two RED→GREEN gates per task, each bisect-friendly.
- **`cmc/cost/` package is the locked precedent for future cost-derived analytics.** First module: `forecast.py` (ANLY-06). Future candidates: `confidence.py` (ANLY-08 v1.3 confidence band), other read-time analytics. All MUST inherit Decimal context from `cmc.pricing`, MUST be pure-Python (no DB access in the module — the route handler does the SQL), MUST cite their semantic decisions in the module docstring (locks RESEARCH.md → code traceability).

Phase 20 Plan 01 (cost-breakdown-project-key-refactor, ANLY-07):

- **`WHERE s.project_key != ''` filter is the mandatory companion to `GROUP BY s.project_key`.** Without it, Phase 19's empty-key sentinel (legacy/cwd-missing sessions where `compute_project_key('') -> ''`) surfaces as a phantom `''` bucket — 20-RESEARCH.md Pitfall 1. Plan 02's forecast endpoint and any future per-project rollup MUST pair the GROUP BY with this WHERE filter at the SQL level (mirrors skills router `_PROJECTS_TOKEN_SQL` from Phase 19 Plan 02).
- **The `<unknown>` coercion in `cost_breakdown` handler is preserved untouched even though it is now unreachable for dim=project.** project_key is NOT NULL DEFAULT '' + WHERE filter excludes empty values, so `r["key"] or "<unknown>"` cannot fire for project rows. The branch still serves dim=model for legacy rows with NULL model. Out-of-scope removal — would risk regression on dim=model behavior unrelated to this plan's ANLY-07 contract.
- **Direct engine insert helper (`_seed_session_for_cost`) instead of conftest's `make_session_row`.** `make_session_row` predates Phase 19's project_key column and does not accept it as a kwarg (conftest.py:455-492); the plan example would have raised `TypeError`. Mirrored Phase 19's `test_skills_router.py::_seed_session_row` private helper instead. If a third caller needs the shape, a follow-up plan can promote it to a shared conftest helper.
- **Adversarial-mutation verification is the locked load-bearingness check for structural guards.** Reverted `_BREAKDOWN_BY_PROJECT_SQL` to `COALESCE(s.cwd, '<unknown>') AS key`, ran `pytest tests/test_cost_no_path_leakage.py -v` → confirmed RED with `row['key']='/tmp/super/secret/leakage/path' starts with '/'`, restored, confirmed GREEN. Without this step the guard could pass vacuously on a future implementation accidentally returning empty rows. Same protocol applied in Phase 19 Plan 03 DST guard and Plan 04 e2e path-leakage assertion.
- **Banned-fragment set chosen for distinctive multi-segment coverage.** `('/tmp/super', '/secret/leakage', 'leakage/path')` — three overlapping fragments so any partial leak (e.g. one path component slipping into a derived label field) gets caught. The 12-char hex project_key alphabet is `0-9a-f` only, so substrings `super`/`secret`/`leakage` cannot appear by construction → zero false-positive risk.
- **Structural guard lives in its own file** (`test_cost_no_path_leakage.py`), not inside `test_cost_router.py`. Mirrors `test_skill_projects_no_path_leakage` discoverability convention. Future cost endpoints (forecast, projects-list) can append tests to the same file as the structural guard surface grows.
- **TDD RED → GREEN gate sequence atomically committed.** Three commits: `96dbc9e` (RED — failing tests for project_key rollup) → `17e162f` (GREEN — SQL refactor) → `3b33b2d` (path-leakage guard test). Bisect-friendly per Phase 18 patterns; the RED commit's failing-state is intentional and documented in the commit body.
- **`<unknown>` in cost_breakdown handler now structurally unreachable for dim=project, NOT removed.** Project_key is NOT NULL DEFAULT '' + WHERE != '' filter; the coercion branch in cost.py:204 (`(r["model"] or "<unknown>")`) still serves dim=model. Leaving it intact preserves dim=model semantics on legacy rows; removing it would be Plan 02 territory.

Phase 19 Plan 04 (SKLP-08/09/10 frontend wiring — DeltaPill + SkillProjectsTable + badges + e2e):

- **Section-wrapped PanelCard for stable e2e hooks.** PanelCard does NOT pass-through `data-testid`; the wrapping `<section data-testid='skills-detail-projects-table'>` survives all four PanelCard render branches (loading/empty/error/data). The inner table only mounts on the data branch. Reusable for any future panel needing branch-stable e2e identification.
- **DeltaPill is decoupled from the wire shape.** The primitive accepts `delta: number` + `deltaPct: number | null`, NOT the full DeltaPill DTO `{ curr, prev, delta, delta_pct, direction }`. Callers Number-coerce the Decimal-as-JSON-string at the call site and pass primitives. Rationale: the primitive can be reused for client-side-derived deltas (compare diffs, etc.) without conflating with the server-side schema. The internal `direction` derives locally from `delta` so the primitive stays decoupled from the wire shape.
- **delta_pct=null renders as `(—)` (parens preserved); aria-label includes the em-dash so screen readers announce the un-comparable percent honestly rather than going silent.**
- **Dual structural guard for ROADMAP success criterion #1 (path-leakage).** Three layers now: (1) backend SkillProjectRow schema enumerates 7 fields, no cwd/path/display_path; (2) backend `test_skill_projects_no_path_leakage` programmatic key+value scan; (3) frontend vitest `container.textContent` regex `/\b\/[A-Za-z][\w/.-]+/`; (4) Playwright e2e same regex on `getByTestId('skills-detail-projects-table').textContent`. Adversarial-mutation verification done unit-side (RED on injected `/Users/foo/bar/baz`, GREEN on restore) — locks the assertion's load-bearingness for both vitest and e2e layers.
- **SkillsRegistry data-source merge by skill_name.** The registry endpoint (/api/skills) has no badges field; the SKLP-10 badges live on /api/skills/usage. Joining at render time via a `Map<name, badges[]>` from `useSkillUsage(14d, 200)` gives consistent badge state across TopSkills + SkillsRegistry without backend coupling. limit=200 widens join coverage so registry rows below the default top-10 still get markers; rows below the activity threshold render no badge by design.
- **Cost rendered to 4 decimals on per-project rollups (`$0.0021`).** Per-project sums can be sub-cent; 2 decimals would round-to-zero and look like a free run. Sort uses parseFloat-coerced value but display passes through the formatted 4-decimal string.
- **Steady-state 2-skip baseline on Playwright** — alerts.spec.ts (existing, requires recently-failed task) + skills-detail.spec.ts (new, requires ≥1 skill in dev DB). Phase verifiers compare failed counts only, not skip counts; both skips documented in `frontend/tests/e2e/README.md`.
- **Existing-test-fixture extension pattern locked.** When adding a required field to a schema, existing test fixtures get a single `_flatPill` constant (or equivalent default) inlined. No fixture-builder helper layer; comments explain that the new field is exercised by dedicated tests, not the patched existing ones. Three files patched (TopSkills.test.tsx, SkillCostCard.test.tsx, SkillLatencyTable.test.tsx).
- **Atomic three-commit task split (2333b46 primitive+types+hook + 5092e51 panel+mount+test + b729ecc wiring+e2e).** Bisect-friendly: a regression in any layer is attributable to one commit. Commit 1 includes the test-fixture patches because tsc fails without them, blocking the next commit.

Phase 19 Plan 02 (SKLP-08 per-project endpoint, GET /api/skills/{name}/projects):

- **Path-leakage prohibition is enforced TWICE — schema + runtime test.** `SkillProjectRow` enumerates exactly 7 fields (`project_key`, `count`, `p50_ms`, `p95_ms`, `cost_usd`, `cost_attribution`, `low_sample`); no `cwd`/`path`/`display_path` exists, ever. The `test_skill_projects_no_path_leakage` test programmatically scans every row's keys AND values for filesystem-shape leakage (no `/`-prefixed string, no occurrence of the seeded secret cwd, no segment substring). LOAD-BEARING for ROADMAP success criterion #1. New project-keyed responses elsewhere in cmc.api SHOULD ship the same dual guard.
- **Per-project endpoint is session-scoped only — no Path R / request-scoped fallback.** Per-project rollups aggregate across many sessions, so the request-scoped JOIN (Path R in `skill_cost`) buys no meaningful precision at the project granularity but doubles SQL cost. `cost_attribution` literal is `"session" | "approximate"` (priced vs. unpriced model), distinct from `skill_cost`'s `"request" | "session"` pair.
- **Two-CTE split (`_PROJECTS_PERCENTILE_SQL` + `_PROJECTS_TOKEN_SQL`) instead of a single mega-query.** Mixing window-function percentiles (PARTITION BY project_key for p50/p95) with SUM aggregation (GROUP BY project_key for tokens) in one CTE would force awkward double-aggregation. The split keeps each query single-purpose and reuses `_LATENCY_SQL`'s proven window pattern verbatim. Python merges the two row-sets keyed by project_key. Reusable pattern for any future per-project skill rollup (e.g. Plan 22's SKLP-11 if the spike clears).
- **Originally added a registry-existence 404 (regex 400 layered on top); removed in hotfix da592ff.** The plan's must_have read "rejects unknown skills with 404 (mirrors /skills/{name}/cost behavior)" — but `/cost`, `/latency`, `/runs` all skip the registry check, doing regex-only validation and returning empty data for unregistered names. Operator hit a 404 on `/skills/analyze/projects` for an events-only skill (skill in `otel_events.skill_activated` but absent from the catalog — legacy/uningested data). Hotfix relaxed `/projects` to mirror its peers: regex-only, returns 200 + rows=[] for unregistered names. Path-leakage and project_key-only contracts remain enforced by the schema + runtime test.
- **Range Literal `"14d"|"30d"` only — `"7d"` reserved for Plan 19-03 delta CTE.** Pitfall 2 from STATE.md is honored at the schema level (`SkillRange` already narrows correctly). The dedicated `test_skill_projects_invalid_range_returns_422` exercises both `7d` and `2d` to lock the rejection contract.
- **Test count: 7 vs. plan's 5.** Two extras (`invalid_range_returns_422` + `path_traversal_rejected`) mirror the canonical guards every existing SKIL-* router endpoint ships with — omitting them would leave a structural hole that a future verifier might flag. Pure-edge-case tests; no deviation cost.

Phase 20 Plan 04 (cost-dashboard-e2e, ANLY-06+07 final integration trust signal):

- **API-guarded preflight is the locked conditional-skip pattern for state-dependent e2e specs.** Tests 3+4 query `/api/cost/breakdown?dim=project&range=7d` via Playwright's `request` fixture; if `rows.length === 0`, `test.skip()` with a developer-actionable reason ("Run a Claude Code session inside a git project … then re-run."). Mirrors `skills-detail.spec.ts` SKLP-08 preflight verbatim. Tests 1+2 are dev-DB-state-independent and always pass — the spec degrades gracefully on a clean dev DB without false-failing.
- **Path-leakage regex `/\/[A-Za-z][\w/.-]+/` is the locked one-shape-multiple-surfaces guard.** Identical regex used in `skills-detail.spec.ts` (Phase 19 Plan 04) and `cost-dashboard.spec.ts` (this plan). Future per-project surfaces should reuse the same regex literal verbatim — regression in either page is caught by a single mental model.
- **Path-leakage prohibition is enforced at FOUR independent layers.** (1) Backend SQL: `WHERE s.project_key != ''` + `GROUP BY s.project_key` (Plan 20-01). (2) Backend pytest: `tests/test_cost_no_path_leakage.py` programmatic key+value scan (Plan 20-01). (3) Frontend vitest: `CostByProjectCard.test.tsx` `container.textContent` regex (Plan 20-03). (4) Frontend Playwright: `cost-dashboard.spec.ts` real-DOM `textContent` regex (this plan). A regression that bypasses any one layer is caught by at least one of the others.
- **`waitForRequest` matcher armed BEFORE the click — race-free network assertion.** Playwright rejects matchers registered after the network call already fired. Spec arms the matcher (regex requiring BOTH `range=30d` AND `dim=project`), then clicks the toggle. Both query-param matches are required because the forecast endpoint also fires at navigation but lacks `range`/`dim` params — single-param match would false-positive.
- **Section-level testid pattern (Plan 20-03 ship) makes Tests 1+2 dev-DB-state-independent.** `cost-forecast-card` and `cost-by-project-card` testids live on the wrapping `<section>`, NOT on the inner `<div>` that's gated by the data branch. They mount on loading/error/empty/data branches uniformly — the e2e spec's mounting assertions hold across all dev DB states. The inner `cost-by-project-card-table` testid only mounts on the data branch, hence the preflight skip on Tests 3+4.
- **Adversarial-mutation verification confirms the path-leakage guard is load-bearing.** Reverted `_BREAKDOWN_BY_PROJECT_SQL` to `COALESCE(s.cwd, '<unknown>') AS key` + `GROUP BY COALESCE(s.cwd, '<unknown>')`, killed uvicorn:8765, re-ran spec → Test 3 FAILED (RED) showing actual cwd-shaped strings (`/Users/patrykattc/work/git/PatrykQuantumNomad…`) in rendered table textContent; restored, re-ran → GREEN. Same protocol applied to Plan 20-01's structural pytest test and Phase 19 Plan 04's e2e — locked load-bearingness check for any structural guard.
- **Real-environment regression caught on first run.** Test 3 initially failed because the running uvicorn:8765 had started 1:47PM, BEFORE Plan 20-01's commit landed at 14:41PM, so `reuseExistingServer=true` reused the stale process serving pre-refactor SQL. Killing the stale process let Playwright spawn fresh and all 4 tests passed. Documented in SUMMARY as a developer-experience signal: restart `cmc start` after merging migrations or schema-shaping refactors.
- **Steady-state Playwright skip discipline locked across phases 18, 19, 20.** Three specs now have documented conditional-skip behavior in `frontend/tests/e2e/README.md`: `alerts.spec.ts` (Phase 18 Plan 04), `skills-detail.spec.ts` (Phase 19 Plan 04), `cost-dashboard.spec.ts` (this plan). Phase verifiers compare failed counts only, not skip counts. Phase 20 close baseline: passed >= 8, skipped 1-3, failed 0 (current dev DB observed at 11 passed / 2 skipped / 0 failed).

Phase 21 Plan 01 (detector-window-kind-discriminator, ALRT-13):

- **Discriminator-INSIDE-existing-function pattern locked for additive detector variants.** `params_json.window_kind` dispatches at a single small helper (`_resolve_alpha`) at the existing alpha-resolution site of `evaluate_anomaly`; recurrence body, warmup gate, and hysteresis state machine are UNCHANGED between branches. No third `rule.kind` value, no parallel detector function, no second dispatcher branch. AST static-import test (`test_only_one_anomaly_detector`) pins the single-detector invariant against future regressions — adversarially-mutation verified RED→GREEN by appending a true-duplicate `evaluate_anomaly` (sibling-named functions like `_evaluate_anomaly_helper` are tolerated by exact-equality `node.name == "evaluate_anomaly"` per Pitfall 7). Reusable for any future single-function invariant; mirrors `test_no_tasks_import` precedent in the same file.
- **Sliding reuses the EWMA-style recurrence verbatim — only `alpha` differs (sliding=1/N, ewma=2/(N+1)).** Did NOT introduce textbook Welford `M2 += (x - mean_old)*(x - mean_new)` (would change the recurrence algebra) and did NOT extract the 3-line recurrence into a helper (per-tick hot path; zero readability gain). The "Welford-style" comment at detector.py:226 is preserved verbatim because the recurrence is shared between branches.
- **Q3 INSUFFICIENT-vs-PENDING_FIRE lock honored.** ROADMAP success criterion's phrase "warmup-boundary `PENDING_FIRE` guard" describes intent (prevent spurious fires during warmup), not literal `AlertSignal` value. The detector's existing `INSUFFICIENT` return at :239-240 is the correct semantic for "no baseline yet". `PENDING_FIRE` would lie to the dispatcher (means "candidate firing but min_dwell not met"). Did NOT add a new enum value or change the existing warmup return.
- **Validator-coupled warmup-boundary guard avoids a second branch in the detector.** API-side `min_samples >= params_json.window_n` constraint (422 on violation) is the warmup-boundary guard for sliding rules; the existing detector warmup gate at :239-240 (`if new_sc < rule.min_samples: return INSUFFICIENT`) IS the boundary mechanism. No new code path in the detector required. Plan 21-02's `AlertRulePatch` adjacency (no conflict — 21-02 only appended NL parse/metrics models at end-of-file) confirmed the validator regions are merge-clean.
- **Defense in depth on enum drift.** API validator strict-rejects unknown `window_kind` values to prevent typo-masking (e.g., `"slidng"` would silently default to ewma at the detector). `_resolve_alpha` defensively defaults unknown/missing values to `"ewma"` to preserve v1.0 behavior for pre-Phase-21 persisted rules (no migration). Both layers are pinned by tests: schema validator probe + `test_anomaly_unknown_window_kind_defaults_to_ewma` unit test.
- **Sliding recurrence z-bound is a property of the algebra, not a bug.** With sliding alpha=1/N, single-tick `new_var = (1/N)*diff² + (1-1/N)*prior_var` inflates variance proportional to squared diff, so single-tick |z| is asymptotically bounded near √(N-1). For N=10 that's ≈ 3.0; the hysteresis test uses `threshold_fire=2.5` to deterministically trip PENDING_FIRE at |z|≈2.79 on a single value=80 against prior (mean=50, var=4). Documented in the test docstring; future sliding-window users should know this bound exists.
- **Off-by-one tightening on warmup boundary test.** Plan literal `min_samples=window_n=10` does NOT make all 10 ticks return INSUFFICIENT under the gate `new_sc < min_samples` (tick 10 produces new_sc=10, 10<10 is False → gate clears). Test uses `min_samples=window_n+1=11` (still satisfies validator's `min_samples >= window_n`) so the warmup-boundary test literally exercises "feed window_n ticks; all INSUFFICIENT". Documented in the test docstring as a tightening of plan intent (the success criterion is "no fire during warmup" — both shapes satisfy it).
- **Atomic single-commit ship per plan output (c2a7793).** Helper + line replace + validator + 5 detector tests + 1 AST guard test all land in one commit. Bisect-friendly — a regression in any layer is attributable to one commit. Diff: 362 insertions / 3 deletions across 4 files; detector.py specifically is +26/-3 (~15 lines added per plan estimate).
- **Parallel-execution rebase mid-run.** Plan 21-02 (commits dfeb6fa + ef2a3d7) landed on `main` while this plan was executing; my unstaged edits to `cmc/api/schemas/alerts.py` were overwritten when 21-02's commit landed. The execution_context anticipated this exact scenario; re-applied the validator-region edits cleanly because 21-02 only appended models at end-of-file (zero region overlap as plan_context predicted). Lesson locked: parallel plans landing on `main` need to either commit-as-you-go OR confirm the file has not changed under foot before final commit. The plan_context conflict-coordination guidance was correct and useful.

Phase 21 Plan 02 (NL parser + parse-nl/metrics routes, ALRT-14):

- **Lazy `from anthropic import AsyncAnthropic` INSIDE `parse_alert_nl` — verbatim mirror of `cmc/schedules/nlcron.py:30`.** Module is import-side-effect-free without `ANTHROPIC_API_KEY` (verified `grep -c "^import anthropic\|^from anthropic" cmc/alerts/nl_parser.py == 0`). Tests monkeypatch via the `_patched_import` shim mirror of `tests/test_dispatcher.py:2362-2454`; module-level import would break those tests. Locked pattern for ALL future Anthropic-Haiku NL endpoints.
- **Hard-validation pipe via existing `AlertRuleCreate` Pydantic v2 model_validator — no parallel validation surface.** Pydantic v2 `ValidationError` IS a `ValueError`, so single `(ValueError, TypeError)` catch covers both pure-Python coercion errors AND validator failures (`is_known_metric`, `threshold_clear < threshold_fire`, the new ALRT-13 `window_kind` enum). Single source of truth at the schema layer; parser does NOT re-implement validation.
- **Returns `None` on hallucination — NO fallback rule, NO best-guess save path (PITFALLS lockout).** Verified by 6 dedicated `*_returns_none` parser tests (no-key / INVALID literal / malformed JSON / hallucinated metric / validator failure / non-dict JSON / empty content). Router maps `None → 503` per V11 / SCHD-06 collapsed-failure-mode contract; body literal `"natural-language alerts unavailable"`. **Single 503 collapses both failure modes (missing API key + rejected Haiku output) — wire shape identical, semantic distinction documented in test names** (Pitfall 6 recommendation A — distinguishing the two modes would leak env config to localhost callers per Security V11).
- **System prompt built at CALL TIME from `sorted(_SCOPE_EXTRACTORS.keys())` via private `_build_system_prompt()` helper.** Adding a metric to `cmc/alerts/scopes.py` automatically updates the prompt without editing `nl_parser.py`. Reusable convergence pattern for future schema-driven NL endpoints.
- **Vocabulary endpoint shape: `GET /api/alerts/metrics → {"metrics": ["cost_usd_24h", "dispatcher_failed_tasks_5m", "skill_p95_latency_ms"]}` (alphabetical sort).** Sort makes ordering deterministic for Plan 21-03's CI drift-guard test (frontend `KNOWN_METRICS` must match this exact list verbatim). Locks the canonical wire shape for backend↔frontend metric/skill-name sync.
- **`log.warning` assertions via direct `MagicMock` patch on `nl_parser.log.warning` (NOT pytest's caplog).** The FastAPI lifespan in `test_alerts_router.py`'s `client` fixture calls `cmc.core.logging.configure_logging` which executes `root.handlers.clear()` — removing the caplog handler installed by pytest. The three caplog-based tests fail with `assert False` when run alongside `test_alerts_router.py` in the same session; pass in isolation. Direct mock on the module-level `log` object is propagation-independent. Reusable lesson: caplog is fragile in any test file that runs after a router test using a FastAPI lifespan that calls `configure_logging`. **Future tests asserting log calls should default to direct `monkeypatch.setattr(module.log, "method", MagicMock())` over caplog.**
- **`r.json()["error"]` body shape (NOT FastAPI default `r.json()["detail"]`) per `cmc/core/errors.py` HTTPException handler — corrected from the plan's example test code.** The app wraps `detail` into `{"error": detail, "request_id": ...}`; the plan's example assertion key would have silently false-passed. Mirrored the existing `test_schedules_router.py:308-345` pattern verbatim.
- **Three new schemas APPENDED at the END of `schemas/alerts.py` (after `AlertAckRequest`).** Disjoint from Plan 21-01's validator regions (lines 55-176). Plans were designed merge-clean per the conflict-coordination contract — landing-order independence verified by both plans converging on `main` without rebase. Reusable parallel-execution pattern: APPEND-only schema additions never collide with validator-region edits.
- **Two-commit task split (dfeb6fa feat + ef2a3d7 test).** Helper module + schemas + routes in commit 1; parser unit tests + router integration tests in commit 2. Bisect-friendly — a regression in either layer is attributable to one commit. Plan's stated "single atomic commit" relaxed to a clean two-commit split because the test commit landed cleanly only after a reset/re-apply due to a parallel-write interaction with Plan 21-01's working-tree (the SUMMARY documents the recovery sequence).

Phase 21 Plan 03 (frontend-nl-input-and-metrics-sync, ALRT-14 user-shippable half):

- **Pitfall 5 single-source-of-truth save semantics, locked at the type level by the test contract.** On preview-modal Save, `useCreateAlertRule.mutate(parsedRule)` fires the parsed rule DIRECTLY — never merged with the manual draft. Pinned by an assertion in the new vitest case that the captured POST body has `kind='anomaly'` while the manual default is `'threshold'`: a silent merge would have left `kind='threshold'`, so the test fails immediately on the regression. Reusable contract for any future "AI-suggests / user-confirms" flow: the suggestion is the authoritative payload OR the user edits it explicitly — there is no third "soft-merge" option.
- **PITFALLS lockout on hallucination — the inline message is hand-authored UX text, NOT the raw 503 body.** The frontend's `m.isError` branch renders **"Could not parse — please rephrase or use the manual form below."** verbatim. The 503 body literal (`"natural-language alerts unavailable"`) stays at the network layer where the contract lives; surfacing it in UI would tell the user what HTTP status they hit, not what to do. Mirrored from NLCronInput's pattern but the literal text is hand-authored because NL prompts are user-input where actionability matters more than the SCHD-06 body-literal-pinning convention.
- **`useAlertMetrics` is `staleTime: Infinity` + `gcTime: Infinity` + `refetchInterval: false` + `refetchOnWindowFocus: false`.** The vocabulary changes only on backend deploys, so polling and stale-revalidation are pure waste. The `FALLBACK_KNOWN_METRICS` constant is the loading-window fallback (sub-second window before the first response lands) — NOT a stub. Reusable cadence-bucket guidance: any backend-vocabulary endpoint (skill names, scope keys, kinds enum) qualifies for this aggressive caching profile.
- **`KNOWN_METRICS` rename to `FALLBACK_KNOWN_METRICS` is BOTH a semantic rename AND the regex anchor for the cross-language drift guard.** The pytest matches either name (forward-portable), but the `FALLBACK_` prefix is the documenting form: future maintainers see the constant and immediately understand it's the loading-window fallback, not the source of truth. The header comment block explicitly points readers at `useAlertMetrics()` for runtime sync and `test_alerts_metrics_sync.py` for the CI guard.
- **Cross-language drift-guard pytest reads frontend .tsx file as text — no JS / node dependency at the backend layer.** `_extract_frontend_metric_keys()` lives in `backend/tests/test_alerts_metrics_sync.py` (NOT frontend) because the test imports the Python source-of-truth (`_SCOPE_EXTRACTORS` from `cmc.alerts.scopes`). The `Path(__file__).resolve().parents[2]` repo-root walk + `tsx_path.read_text()` is the entire FS dependency. Reusable pattern for any future cross-language constant-pair (e.g., enum keys mirrored across Python schema + TypeScript types) — pin in the language with the source-of-truth, regex-extract the mirror.
- **Drift-guard regex MUST filter single-line `//` comments before extracting `value:` strings (Rule 1 fix during execution).** The plan's original regex matched `value:` substrings even inside `//`-commented lines, so a developer commenting out a metric to verify drift would see the test FALSELY PASS — defeating the entire purpose of the guard. Added a line-level filter dropping lines whose first non-whitespace chars are `//` BEFORE running the value regex. Negative-test re-verified post-fix: comment-out a metric → test FAILS with `Only in backend: ['skill_p95_latency_ms']` diagnostic; revert → test PASSES. Documented in the test source so future maintainers don't undo the filter. Reusable testing lesson: any test that asserts equality between an extracted set and a source-of-truth set must also include a negative-mutation step in its done criteria — not just the positive-case happy path.
- **Mocking pattern: `vi.spyOn(globalThis, 'fetch')` with URL+method routing, NOT `vi.mock('../../lib/api')` module mock.** Mirrored the existing fetch-spy pattern in `AlertRuleForm.test.tsx` (and every other panel test in `__tests__/`). The plan's prose suggested module-mock; its later sentence said "mirror whatever module-mock pattern the existing AlertRuleForm.test.tsx already uses" — followed the latter. Locked the URL-router helper (`installRoutedFetch(opts)`) for any future panel test that needs to compose multi-route mocks.
- **Two-commit task split (b902661 feat + 379a673 test) preserves the bisect-friendly precedent set by Plan 21-02.** Implementation in commit 1 (api.ts + queries.ts + AlertRuleForm.tsx); tests + drift-guard pytest in commit 2 with the comment-filter Rule 1 fix folded in. A regression in either layer is attributable to one commit.

Phase 22 Plan 01 (skill-latency-overhead-feasibility-spike, SKLP-11 gate):

- **SKLP-11 descopes to v1.3 — STRONG (structural) failure mode confirmed.** CT-1 (Coverage) failed: only 1 row in `otel_events` with `event_name='skill_activated'` (Phase 12 spike artifact, 2026-05-02), and that row's `body.record.attributes` does NOT carry a `duration_ms` key. Confirmed by direct attribute-key enumeration via `json_each` over `body.record.attributes` — the absence is upstream (Claude Code emitter), not an extraction-idiom bug. Required threshold `MIN_LATENCY_SAMPLES = 30` (single source of truth at `cmc/api/routes/skills.py:606`) cannot be met. CT-3 / CT-4 recorded N/A per failure-isolation rule. v1.3 re-evaluation requires the host CLI to start emitting `duration_ms` on `skill_activated` events; the user can monitor via the attribute-key enumeration query in `22-01-SPIKE-FINDINGS.md` §Negative Finding.
- **Sequential-branching strategy locked: NO Plan 22-02 written this round.** Per 22-RESEARCH.md §Architecture Patterns Option 1, ROADMAP Success Criterion #1 verbatim requires success-branch plans to cite SQL-column / temporal-JOIN derivation sources in front-matter — those citations cannot exist until the spike commits a YES outcome. Pre-branching would force placeholder citations into Plan 22-02-success front-matter, directly violating the locked anti-fake-decomposition prohibition. User must re-invoke `/gsd-plan-phase 22` to author Plan 22-02-descope, which anchors on `## Outcome: NO` banner + §Negative Finding sections. Reusable pattern for any future spike-gated phase: do NOT pre-write the branch plan; let the planner read the outcome banner and pick the template.
- **Anti-fake-decomposition guarantee enforced by regex-pinned verify gate.** Forbidden tokens (TBD / placeholder / best-effort / approximate decomposition / `\bratio guess\b`) are blocked from SPIKE-FINDINGS.md by negative-grep at task-verify time. Side-effect: §Anti-Fake-Decomposition Reaffirmation paraphrases "ratio guesswork" → "fractional-of-total estimation" to avoid even a near-miss against the regex word boundary. Reusable: any spike findings document that takes a binary outcome should pin the outcome banner with `^## Outcome: (YES|NO)` AND blocklist forbidden inconclusive-tokens via negative grep — together they make ambiguity machine-detectable.
- **Read-only probes against `data/cmc.db` accepted as the spike's blast-radius.** No INSERT/UPDATE/DELETE; no source code touched (`git show --stat 07abcfa` confirms only SPIKE-FINDINGS.md, +195 lines). Threat model T-22-02 mitigation honored: SPIKE-FINDINGS.md captures only attribute-key NAMES (not values) from the existing row, and the COALESCE projection on CT-1 returns skill names only — no user-identity columns leaked. Reusable for any future SQL spike against production-like data: probe queries must project counts and column names, not raw `body` JSON, unless explicit redaction is performed.

Phase 19 in-browser hotfixes (during human verification, 2026-05-06):

- **Hotfix dad754a — `/api/skills/usage` limit cap 50 → 200 + key `qk.skillUsage` by limit.** `SkillsRegistry.tsx` calls `useSkillUsage('14d', 200)` to widen the badge-join coverage; backend was capped at `Query(le=50)` → 422. Independently, `qk.skillUsage(range)` omitted `limit` from the queryKey so the four production callers (limit=1/10/20/200) shared a single cache entry — the 422 from the limit=200 caller corrupted the cache for the limit=1 `SkillCostCardForTopSkill` caller, surfacing as "Couldn't load skill cost data" on /skills even though the failing URL was the usage endpoint. Both fixes shipped together; pinned by an updated `test_skills_usage_limit_clamping` (201 fails, 200 succeeds) and a new `qk.skillUsage('14d', 1) !== qk.skillUsage('14d', 200)` test in queries.test.ts.
- **Hotfix da592ff — `/skills/{name}/projects` registry 404 → 200 + rows=[].** Plan 19-02 implementation diverged from its own must_have ("mirrors /skills/{name}/cost behavior"). Operator hit a spurious 404 on /skills/analyze for an events-only skill. Removed the registry lookup so /projects matches /cost, /latency, /runs (regex-only validation). Path-leakage guard preserved.
- **Cache-key discipline locked.** Whenever a TanStack-Query hook accepts a query parameter that affects the response shape, that parameter MUST be in the `queryKey`. Otherwise siblings with the same key fight over a single cache entry and a failure in one corrupts all. This is the second time we've shipped this bug class; future hooks should be code-reviewed against the rule explicitly.
- **Plan must_haves that say "mirrors X" must be verified against X's actual behavior, not its docstring.** Plan 19-02 cited "mirrors /cost" but added a stricter check than /cost actually has. Future plan-checker should grep the cited reference's source for the claimed behavior before locking the must_have.

v1.2 roadmap-time decisions:

- **Phase 22 is spike-gated for SKLP-11.** Phase opens with a mandatory feasibility spike (`tools` temporal JOIN against `skill_activated.duration_ms`); negative finding descopes SKLP-11 to v1.3 cleanly without blocking Phase 23. No fake decomposition ships under any circumstance (PITFALLS Pitfall 10).
- **Migration `0003_project_key` is owned by Phase 19.** Both SKLP-08 (Phase 19) and ANLY-07 (Phase 20) need `project_key` normalization (sha1[:12] of `realpath(cwd.rstrip('/'))`); migration ships in Phase 19 since it runs first (PITFALLS Pitfall 7 prevails over ARCHITECTURE's "zero migrations" claim).
- **ALRT-13 extends `evaluate_anomaly` via `params_json.window_kind`.** No third `kind` value, no parallel detector function, no second dispatch branch — single-detector invariant carried from v1.1 (PITFALLS Pitfall 1; SUMMARY Conflict 1 resolution).
- **ALRT-14 returns `None` on Haiku hallucination.** No fallback rule, no "best-guess" save path; UI surfaces honest "could not parse" message. Hard-validation against `_SCOPE_EXTRACTORS.keys()` via `is_known_metric()`.
- **Phase ordering follows ARCHITECTURE: 18 → 19 → (20 ‖ 21) → 22 → 23.** Phase 18 first for green CI baseline; Phase 19 before Phase 22 to establish CTE patterns SKLP-11 reuses; Phase 23 last to close the milestone.
- **Zero net-new dependencies.** Every v1.2 feature implementable with existing tools (stdlib `math`/`Decimal` for OLS + Welford, SQLAlchemy CTEs, existing `cmdk` + `anthropic` SDK 0.97, vitest + Playwright). STACK research confirmed.

v1.1 carried decisions (still active):

- Cost stored as tokens, $ computed at read time (window logic via `effective_from`/`effective_until`)
- ALRT-12 invariant: alert engine NEVER imports `cmc.dispatcher.tasks`
- Stable `dedup_key = f"alert:{rule_id}:{scope_key}"` reusing `notification_log` UNIQUE
- `UTCDatetime` PlainSerializer with `when_used='json'` gate
- CMPR-04 over-cap = render branch (HTTP 200 + `over_cap=true`), not error branch
- CMPR-05 tabular-only compare (no diff library, no raw message rendering)
- Wave-1/wave-2 single-writer convention for REQUIREMENTS.md

### Pending Todos

- ~~Phase 19 plan owns migration `0003_project_key`~~ — **landed in Phase 19 Plan 01 (commit 95bd1df, 2026-05-06)**. `sessions.project_key VARCHAR(12) NOT NULL DEFAULT ''` + `idx_sessions_project_key` + Python-loop backfill; ingest path keeps it fresh via scheduler.py and repository.py. Phase 20 ANLY-07 unblocked.
- ~~Phase 19 Plan 02 (SKLP-08 endpoint, per-project rollup)~~ — **landed in Phase 19 Plan 02 (commits b6d73a7 schemas + 056141b endpoint+7tests, 2026-05-06)**. `GET /api/skills/{name}/projects` returns the path-leakage-resistant per-project rollup; ROADMAP success criterion #1 satisfied on the backend side.
- ~~Phase 19 Plan 03 (SKLP-09/10 deltas + badges + DST spring-forward unit test)~~ — **landed in Phase 19 Plan 03 (commits ee662cb DeltaPill schema + ea0d1cb handlers/CTEs + 68aeb5c 12 tests, 2026-05-06)**. `usage_delta` + `badges` on `/api/skills/usage`; `cost_delta` on `/api/skills/{name}/cost`; ROADMAP success criteria #3, #4, #5 satisfied on the backend side. DST test load-bearing — adversarial-mutation verified.
- ~~Phase 19 Plan 04 (frontend wiring — DeltaPill + SkillProjectsTable + badges on TopSkills/SkillsRegistry + Playwright e2e)~~ — **landed in Phase 19 Plan 04 (commits 2333b46 DeltaPill primitive + api/queries hook + 5092e51 SkillProjectsTable + mount + b729ecc TopSkills/SkillCostCard/SkillsRegistry wiring + Playwright e2e, 2026-05-06)**. ROADMAP success criteria #1, #3, #4 satisfied user-visible. Phase 19 closed; Phase 20 unblocked.
- ~~Phase 20 Plan 01 (cost-breakdown-project-key-refactor, ANLY-07)~~ — **landed in Phase 20 Plan 01 (commits 96dbc9e RED tests + 17e162f GREEN SQL refactor + 3b33b2d structural path-leakage guard, 2026-05-06)**. `_BREAKDOWN_BY_PROJECT_SQL` GROUP BY s.project_key + WHERE s.project_key != ''; `test_cost_no_path_leakage.py` adversarial-mutation-verified RED→GREEN. Phase 19 SKLP-08 path-leakage discipline now extends to the cost router. ROADMAP Phase 20 success criterion #3 backend portion satisfied.
- ~~Phase 20 Plan 02 (cost-forecast-module-and-endpoint, ANLY-06)~~ — **landed in Phase 20 Plan 02 (commits 01b25a1 RED unit tests + 10e0757 GREEN forecast module + 54f922b RED schema/endpoint tests + 2765f07 GREEN endpoint, 2026-05-06)**. New `cmc/cost/` package with `forecast.py` (pure-stdlib Decimal OLS + calendar helpers); `GET /api/cost/forecast` endpoint returning `CostForecastResponse` (8 fields, Decimal-as-JSON-string). Locked decisions D-01..D-05 (days_elapsed = today.day - 1; 14d baseline excludes today; no negative-projection clamp; no degenerate flag; single threshold for both bias flags). ROADMAP Phase 20 success criteria #1 + #2 backend portion satisfied. Frontend consumption in Plan 20-03.
- ~~Phase 20 Plan 03 (cost-dashboard-route-and-cards)~~ — **landed in Phase 20 Plan 03 (commits f90ec21 types+api+qk+hooks + 1fc13e1 panels+9 vitest cases + 96ea120 /cost route+NavBar+codegen, 2026-05-06)**. New `/cost` top-level route mounting `CostForecastCard` (ANLY-06) above `CostByProjectCard` (ANLY-07); NavBar wired with Cost link between Skills and Alerts. Bias banner driven by `data.partial_month_bias` flag (server-source-of-truth; Pitfall 7 adversarially tested). Cache-key discipline: `qk.costBreakdown` keyed by both dim AND range. vitest 306 → 316 (+10). ROADMAP success criteria #1, #2, #3 user-visible; Plan 20-04 e2e is the final coverage layer.
- ~~Phase 20 Plan 04 (cost-dashboard-e2e)~~ — **landed in Phase 20 Plan 04 (commit 0ad412a test — frontend/tests/e2e/cost-dashboard.spec.ts + e2e README skips inventory, 2026-05-06)**. Playwright spec ships 4 tests (mounting both panels, NavBar Cost link nav, path-leakage guard on cost-by-project-card-table, 7d→30d toggle network-fire). Mirrors Phase 19 `skills-detail.spec.ts` verbatim with API-guarded preflight + identical path-leakage regex + conditional-skip discipline. Adversarial-mutation verification performed locally (cwd-restoration backend mutation produces RED on Test 3, restoration produces GREEN). Path-leakage prohibition now enforced at FOUR defense layers (SQL filter + structural pytest + vitest container regex + Playwright real-DOM regex). Steady-state Playwright baseline: 11 passed / 2 skipped / 0 failed (Phase 19 was 7/2/0; +4 from new spec); vitest 316/0 unchanged; pytest 632/0 unchanged. ROADMAP Phase 20 success criteria #1, #2, #3, #4 satisfied user-shippable end-to-end. Phase 20 closed.
- ~~Phase 21 Plan 01 (detector-window-kind-discriminator, ALRT-13)~~ — **landed in Phase 21 Plan 01 (commit c2a7793 feat — `_resolve_alpha` helper + window_kind validator + 5 sliding-window tests + AST guard, 2026-05-07)**. `params_json.window_kind` discriminator dispatched INSIDE `evaluate_anomaly` via `_resolve_alpha(rule, n)` helper (ewma → `2/(N+1)`, sliding → `1/N`); recurrence body, warmup gate, and hysteresis state machine UNCHANGED — single function, single dispatch branch. `WindowKind = Literal["ewma", "sliding"]` at API boundary; `AlertRuleCreate` / `AlertRulePatch` validators reject unknown values with HTTP 422 and enforce `min_samples >= window_n` for sliding rules (warmup-boundary guard). 5 sliding-window detector tests (seed → INSUFFICIENT shared with EWMA; uniform-alpha recurrence vs EWMA on identical inputs proves discriminator dispatches: sliding `new_mean=10.2` vs ewma `new_mean≈10.36`; warmup-boundary guard with `min_samples=window_n+1` makes all `window_n` ticks INSUFFICIENT; hysteresis state machine shared on |z|; defensive default `_resolve_alpha` of unknown/missing window_kind to ewma). `test_only_one_anomaly_detector` AST guard mirrors `test_no_tasks_import` precedent — exact-equality `node.name == "evaluate_anomaly"` (NOT startswith, per Pitfall 7); adversarially-mutation verified RED→GREEN by appending true-duplicate `evaluate_anomaly`. Pytest 644 → 650 (+6) / 0 failed / 32 warnings; pyright + ruff clean on the four touched files. Phase 18 BASELINE.md floor of 632 preserved. Single atomic commit per plan output. Plan 21-02 (NL parser + parse-nl/metrics routes, ALRT-14) landed in parallel earlier (commits dfeb6fa + ef2a3d7) — schema region overlap with 21-01 was zero by design (21-02 only appended models at end-of-file); merge-clean as plan_context predicted.
- ~~Phase 21 Plan 03 (frontend-nl-input-and-metrics-sync, ALRT-14 user-shippable half)~~ — **landed in Phase 21 Plan 03 (commits b902661 feat — alertsParseNl + alertMetrics + useParseAlertNl + useAlertMetrics + AlertNlInput + AlertDialog preview modal + FALLBACK_KNOWN_METRICS rename + manualDisabled gate; 379a673 test — 4 new vitest cases + new backend/tests/test_alerts_metrics_sync.py with single-line `//`-comment filter, 2026-05-07)**. ROADMAP success criteria #3 frontend (preview modal showing parsed AlertRule before save), #4 frontend (inline "could not parse" message on 503; never auto-saves a fallback rule), #5 (KNOWN_METRICS sync via BOTH useAlertMetrics() runtime path AND test_alerts_metrics_sync.py static-time drift guard) satisfied user-shippable end-to-end. Vitest 320 / 0 (vs Phase 18 floor 293; +4 new cases over prior 316). Pytest 650 / 1 pre-existing flaky (test_emergency_stop.py::test_estop02_validate_pid_is_claude_positive — passes in isolation, environment-dependent PID lookup; logged in deferred-items.md). Drift-guard negative-test verified locally (comment-out a metric → FAILS with "Only in backend: [...]" diagnostic; revert → PASSES). Phase 21 awaits verifier sweep before close.
- ~~Phase 21 Plan 02 (nl-parser-route-and-metrics, ALRT-14)~~ — **landed in Phase 21 Plan 02 (commits dfeb6fa feat — `cmc/alerts/nl_parser.py` + 3 schemas appended + 2 routes; ef2a3d7 test — 8 parser unit tests + 4 router tests, 2026-05-07)**. `parse_alert_nl(prompt) -> AlertRuleCreate | None` ships in `cmc/alerts/nl_parser.py` with lazy `from anthropic import AsyncAnthropic` INSIDE the function (mirror nlcron.py:30; `grep -c "^import anthropic" == 0`). Output piped through `is_known_metric()` + `AlertRuleCreate(**parsed)` Pydantic v2 model_validator — single source of truth. Returns `None` on hallucination — NO fallback rule, NO best-guess save path (PITFALLS lockout). System prompt built at CALL TIME from `sorted(_SCOPE_EXTRACTORS.keys())` via `_build_system_prompt()` helper. `POST /api/alerts/parse-nl` returns 200 + `{rule, description}` envelope; 503 `"natural-language alerts unavailable"` on `None` (V11 / SCHD-06 collapsed-failure-mode contract collapses missing-API-key + rejected-Haiku-output to a single failure mode per Pitfall 6 recommendation A). `GET /api/alerts/metrics` returns `sorted(_SCOPE_EXTRACTORS.keys())` for Plan 21-03's `useAlertMetrics` query and CI drift-guard. 8 parser unit tests via `_patched_import` shim (mirror tests/test_dispatcher.py:2362-2454) covering no-key / happy / INVALID / malformed JSON / hallucinated metric / validator failure / non-dict / empty content. 4 router tests via `monkeypatch.setattr("cmc.api.routes.alerts.parse_alert_nl", AsyncMock(...))` bind-replace (mirror tests/test_schedules_router.py:308-345). Log-warning assertions via direct `MagicMock` patch on `nl_parser.log.warning` (NOT caplog) — sidesteps FastAPI lifespan's `configure_logging` clearing root handlers, locking a reusable lesson for any future log-assertion test that runs alongside a router test. Pytest 650 / 0 / 32 warnings (no regression vs Phase 18 BASELINE floor 632 / 32 / 0 datetime.utcnow). ROADMAP success criteria #3 (parse-nl shipped, mirrors nlcron/skill_router), #4 (hard-validation + None on hallucination), #5 backend (metrics endpoint exposed) satisfied; ROADMAP success criterion #1 + #2 are Plan 21-03 (frontend) territory.
- Phase 22 plan front-matter MUST cite SQL columns or temporal-JOIN derivation source for body_ms / subagent_ms / tool_ms before any UI work begins (Pitfall 10 acceptance criterion).
- Phase 23 closes the milestone — audit hooks (full pytest + vitest + playwright green; `cmc doctor` clean; REQUIREMENTS.md traceability 13/13 or honest 12/13 + descope) belong in the final phase plan.
- v1.2 verifiers (Phase 19+) MUST read `.planning/phases/18-polish-carry-forward-cleanup/BASELINE.md` at phase-close time and apply the embedded per-suite verifier rules (pytest >= 566, vitest >= 293, playwright >= 7, `warnings_datetime_utcnow > 0` → fail).

### Blockers/Concerns

None blocking roadmap → planning. Risk register:

- **SKLP-11 feasibility unknown until Phase 22 spike.** Roadmap accommodates "spike-only, descope to v1.3" outcome; Phase 23 has no hard dependency on SKLP-11.
- **NL grammar edge cases (ALRT-14).** Unit ambiguity, implicit metrics, nested conditions, time-window vs duration confusion — all need explicit system-prompt handling. MEDIUM confidence until tested against real prompts in Phase 21.
- ~~**KNOWN_METRICS sync drift.** Phase 21 plan must lock either `GET /api/alerts/metrics` dynamic endpoint or CI sync test before merging.~~ **Resolved 2026-05-07 by Plan 21-03 — BOTH paths shipped: `useAlertMetrics()` runtime fetch from `GET /api/alerts/metrics` (deploy-stable cache) AND `backend/tests/test_alerts_metrics_sync.py` static-time drift guard (regex over `.tsx` text, comment-filtered, asserts set equality with `_SCOPE_EXTRACTORS.keys()`).**

Two operational human-verify items still carry forward (non-blocking, auto-discharging):

- Apply Alembic migrations 0002 and 0003 to live `data/cmc.db` — auto-applies on next `cmc start` via `lifespan.py:98-100`. `0003_project_key` (Phase 19 Plan 01) will backfill existing sessions on first boot post-merge.
- Phase 14 visual checkpoint per Plan 14-05 (visual rendering on `/activity` TopSkills, `/skills` 3 panels, `/skills/$name` detail) — operator-driven dashboard navigation.

## Deferred Items

| Category | Item | Status | Deferred At |
|----------|------|--------|-------------|
| Skills (v1.3+) | SKLP-12 percentile-split overhead breakdown (only if SKLP-11 ships) | v1.3 candidate | 2026-05-05 |
| Skills (v1.3+) | SKLP-13 heatmap toggle on per-project breakdown | v1.3 candidate | 2026-05-05 |
| Cost (v1.3+) | ANLY-08 confidence band on monthly forecast | v1.3 candidate | 2026-05-05 |
| Cost (v1.3+) | ANLY-09 per-project cost budgets with alert integration | v1.3 candidate | 2026-05-05 |
| Alerts (v1.3+) | ALRT-15 predictive alerts (forecast × anomaly combination) | v1.3 candidate | 2026-05-05 |
| Alerts (v1.3+) | ALRT-16 NL queries beyond AlertRule schema (NL2SQL) | v1.3 candidate | 2026-05-05 |
| Compare (v1.3+) | CMPR-08 sessions-table right-click "compare with previous" | v1.3 candidate | 2026-05-05 |
| Compare (v1.3+) | CMPR-09 per-skill cost delta in compare | v1.3 candidate | 2026-05-05 |
| Platform | PLAT-01 (Linux/systemd) | v2 | 2026-04-28 (carried from v1.0) |
| Automation | AUTO-01..03 (NL schedules beyond cron, auto-retry, task dependencies) | v2 | 2026-04-28 (carried from v1.0) |

## Performance Metrics

**v1.0 baseline:** 47 plans, 4 days (2026-04-25 → 2026-04-28), ~39,800 LOC.
**v1.1:** 28 plans, 4 days (2026-05-02 → 2026-05-05), +81,397 / -13,435 lines vs v1.0, ~56,232 LOC at close.
**v1.2:** Phases 18–23 defined (6 phases, 13 requirements). **Phase 18 complete (5/5 plans, 2026-05-05)**: Plan 01 ~10 min (cmc.core.time helper + 5 unit tests), Plan 02 ~42 min (atomic 22-site sweep `datetime.utcnow` → `now_utc`, commit c3d792f), Plan 03 ~3 min (`vi.spyOn(Date, 'now')` pin in SchedulesCard.test.tsx), Plan 04 ~5 min (Playwright strict-mode disambiguation + e2e/README.md), Plan 05 ~9 min (BASELINE.md phase-exit artifact). Final phase-18 baseline: backend pytest 566 passed / 0 failed / 32 warnings / 0 datetime.utcnow lines; frontend vitest 293 passed / 66 files; Playwright 7 passed / 1 skipped (alerts steady-state) / 0 failed. Pytest deprecation-warning delta ~1429 → 0. Net-zero dependency change across the phase. POLI-06/POLI-07/POLI-08 all green; BASELINE.md (`.planning/phases/18-polish-carry-forward-cleanup/BASELINE.md`) is the canonical reference for v1.2 phases 19+ verifiers. **Phase 19 complete (4/4 plans, 2026-05-06)**: Plan 01 ~8 min (commits 53fe578 helper + 95bd1df migration/wiring): `cmc.core.project_key.compute_project_key` helper (11 unit tests), Alembic migration `0003_project_key` (sessions.project_key VARCHAR(12) NOT NULL DEFAULT '' + indexed + Python-loop backfill via realpath), sessions model field, scheduler.py + repository.py wiring; pytest 579 passed / 0 failed / 32 warnings / 0 datetime.utcnow (+13 vs baseline), ruff clean. Plan 02 ~16 min (commits b6d73a7 schemas + 056141b endpoint+7tests): `SkillProjectRow` + `SkillProjectsResponse` DTOs (path-leakage-resistant by enumeration), `GET /api/skills/{name}/projects` endpoint with two-CTE rollup (`_PROJECTS_PERCENTILE_SQL` window-CTE adapted from `_LATENCY_SQL` + `_PROJECTS_TOKEN_SQL` per-project token sums), 7 tests including the LOAD-BEARING `test_skill_projects_no_path_leakage` programmatic key+value scan; pytest 586 passed / 0 failed / 32 warnings / 0 datetime.utcnow (+7 vs 19-01, +20 vs Phase 18 baseline), ruff clean. Plan 03 ~23 min (commits ee662cb DeltaPill schema + ea0d1cb handlers/CTEs + 68aeb5c 12 tests): `DeltaPill` Pydantic primitive (curr/prev/delta/delta_pct/direction; delta_pct=None when prev=0), `SkillUsageRow.usage_delta` + `.badges`, `SkillCostResponse.cost_delta`, module constants `_DELTA_WINDOW_DAYS=7` / `_BADGE_NEW_DAYS=7` / `_BADGE_DORMANT_DAYS=30` / `_BADGE_COLDSTART_DAYS=14`, `_build_delta_pill` + `_derive_badges` + `_compute_cost_delta` helpers, `_USAGE_DELTA_BADGE_SQL_PORTABLE` (skills_seen UNION + LEFT JOIN trio for SQLite <3.39 compatibility) + four cost-delta SQL fragments (curr/prev × request/session) with LITERAL `datetime('now', '-7 days')` / `'-14 days'` bounds (Pitfall 2 enforced at the SQL level — `?range=` cannot bind to delta windows), 12 tests covering delta math (4) + badge boundaries (3) + cold-start suppression (1) + DST spring-forward (1, LOAD-BEARING for ROADMAP success criterion #5; combines Python helper UTC-arithmetic assertion + structural grep guard asserting source contains NO `'localtime'` modifier on any 7d/14d/30d window; adversarial-mutation verified RED→GREEN) + cost_delta on growth+empty (2) + default-pill invariant (1); pytest 598 passed / 0 failed / 32 warnings / 0 datetime.utcnow (+12 vs 19-02, +32 vs Phase 18 baseline), ruff + pyright clean. Plan 04 ~19 min (commits 2333b46 DeltaPill primitive + api/queries hook + 5092e51 SkillProjectsTable + mount + b729ecc TopSkills/SkillCostCard/SkillsRegistry wiring + Playwright e2e): `frontend/src/components/ui/DeltaPill.tsx` pure presentation primitive (↑/↓/· + abs + pct, integer | currency formats; '—' for null deltaPct) with 7 vitest cases, `SkillProjectsTable.tsx` sortable per-project rollup panel with section-level data-testid (`skills-detail-projects-table`), `useSkillProjects` + `qk.skillProjects` + `api.skillProjects` + `fetchSkillProjects` (60s/45s cadence), `SkillProjectsResponse` + `SkillProjectRow` + `DeltaPill` TS types mirroring backend, TopSkills DeltaPill + new/dormant Badges (top-skills-delta-pill / top-skills-new-badge / top-skills-dormant-badge), SkillCostCard DeltaPill in Total cost KpiTile (skill-cost-card-delta-pill, format=currency), SkillsRegistry badges merged from useSkillUsage(14d, 200) by skill_name (skills-registry-new-badge / skills-registry-dormant-badge), `frontend/tests/e2e/skills-detail.spec.ts` load-bearing path-leakage assertion (regex on getByTestId textContent) with adversarial-mutation verification done unit-side; existing test fixtures patched with `_flatPill` constants to satisfy new required schema fields (TopSkills + SkillCostCard + SkillLatencyTable tests); vitest 293 → 306 (+13: 7 DeltaPill + 6 SkillProjectsTable); playwright 7 passed / 2 skipped (alerts steady-state + new skills-detail when no skills seeded) / 0 failed; backend pytest unchanged at 598 / 0 / 32; tsc clean. ROADMAP success criteria #1, #3, #4 satisfied user-side; #5 satisfied server-side in Plan 19-03.
**Phase 20 (in flight, 3/4 plans, 2026-05-06):** Plan 20-01 ~21 min (commits 96dbc9e RED tests + 17e162f GREEN SQL refactor + 3b33b2d path-leakage guard): `_BREAKDOWN_BY_PROJECT_SQL` refactored from `COALESCE(s.cwd, '<unknown>') AS key` + `GROUP BY COALESCE(s.cwd, '<unknown>')` → `s.project_key AS key` + `WHERE s.project_key != ''` + `GROUP BY s.project_key`; 11-line comment block citing Phase 19 SKLP-08 invariant + 20-RESEARCH.md Pitfall 1; 2 new tests in `test_cost_router.py` (`test_breakdown_project_groups_by_project_key` exercising 12-char hex regex + `compute_project_key` equality, `test_breakdown_project_excludes_empty_key` exercising the empty-key WHERE filter); 1 net-new structural test file `tests/test_cost_no_path_leakage.py` (114 lines, adversarial-mutation-verified RED→GREEN by reverting SQL to cwd-rollup, observing `row['key']='/tmp/super/secret/leakage/path' starts with '/'`, restoring, confirming GREEN); banned-key set `{cwd, path, display_path, cwd_path}` + banned-fragment set `('/tmp/super', '/secret/leakage', 'leakage/path')` is the locked sniffer pattern for any future per-project endpoint; pytest 598 → 601 passed (+3 = exactly the count this plan claimed) / 0 failed / 32 warnings / 0 datetime.utcnow warnings (Phase 18 BASELINE preserved); ruff + pyright clean. Plan 20-02 ~23 min (commits 01b25a1 RED unit tests + 10e0757 GREEN forecast module + 54f922b RED schema/endpoint tests + 2765f07 GREEN endpoint): new `cmc/cost/__init__.py` + `cmc/cost/forecast.py` (pure-stdlib Decimal OLS — `decimal_ols(xs, ys)` closed-form least squares with defensive zero-variance guard returning `(Decimal(0), mean(ys))`, `days_elapsed_in_month(today)` = `today.day - 1` (D-01), `days_in_month(today)` via `calendar.monthrange` leap-year correct, `project_month_total(slope, intercept, days)` trapezoidal sum with NO clamp on negative projections (D-03)); module docstring forbids `getcontext()` calls (Decimal context inherited from `cmc.pricing` — verified by grep gate); `CostForecastResponse` schema (8 fields: rates_as_of, days_elapsed, days_in_month, baseline_days, month_to_date_usd, projected_month_total_usd, insufficient_data, partial_month_bias) with Pydantic v2 default Decimal-as-JSON-string; `GET /api/cost/forecast` endpoint with two-phase computation (MTD always; 14d baseline only when `days_elapsed >= 7` per D-02 today excluded); `_FORECAST_BASELINE_SQL` (day, model GROUP BY across `[today-14, today-1]`) + `_FORECAST_MTD_SQL` (day, model GROUP BY across `[month_start, today]`); 26 unit tests in `test_cost_forecast.py` (decimal_ols known/zero-variance/negative/single/empty/length-mismatch + 9-case days_elapsed parametric + 7-case leap-year parametric + 4 project_month_total cases) + 5 integration tests in `test_cost_router.py` (decimal-string serialization Anti-Pattern guard + insufficient_data threshold day-3 + flags-clear day-8 + adversarial outlier-isolation locking D-02 with 999_999_999-token today against flat 14d baseline + baseline_days==14 invariant) using `pytest_freezer.freezer` fixture (freezegun-based, patches `datetime.now(UTC)` globally so `cmc.core.time.now_utc` is transparently mocked); pytest 601 → 632 passed (+31 = exactly the count this plan claimed: 26 unit + 5 integration) / 0 failed / 32 warnings / 0 datetime.utcnow warnings (Phase 18 BASELINE preserved); ruff `--select UP` clean; auto-fixed deviations: model-name typo `claude-opus-4-5` → `claude-opus-4-7` in test seed helper (lookup-miss silently zeroed cost), I001 import-sort on test file, RUF003 ambiguous-multiplication-sign in comment.

Plan 20-03 ~8 min (commits f90ec21 feat — types + api fetchers + qk + hooks; 1fc13e1 feat — CostForecastCard + CostByProjectCard panels + 9 vitest cases; 96ea120 feat — /cost route + NavBar Cost link + routeTree codegen + NavBar test): TS types `CostForecastResponse` + `CostBreakdownResponse` + `CostBreakdownRow` + `CostRange` + `BreakdownDim` (Decimal fields as `string`); `api.costForecast()` + `api.costBreakdown(dim, range)` typed fetchers with `fetchCostForecast` / `fetchCostBreakdown` standalone re-exports; `qk.costForecast() = ['cost-forecast']` + `qk.costBreakdown(dim, range) = ['cost-breakdown', dim, range]` keyed by BOTH dim AND range (Phase 19 hotfix lesson — STATE.md L121 cache-key discipline); `useCostForecast()` + `useCostBreakdown()` hooks at 60s/45s daily-aggregate cadence (panels NEVER inline `refetchInterval`); `CostForecastCard.tsx` panel (ANLY-06) with section-level testid `cost-forecast-card` + branch-specific testids (`cost-forecast-card-projected` / `-bias-banner` / `-insufficient-message` / `-mtd`); bias banner wired to `data.partial_month_bias` flag NEVER `data.days_elapsed < 7` re-derivation (Pitfall 7 adversarial test: `days_elapsed: 10, partial_month_bias: true` → banner shows); MTD KpiTile ALWAYS rendered; Decimal-as-JSON-string template-literal display pinned by `expect(projected.textContent).toBe('$125.4321')` (full 4-decimal preservation); `CostByProjectCard.tsx` panel (ANLY-07) with sortable Project/Tokens/Cost columns, 7d/30d `RangeToggle` (`persistKey='cost-by-project'`), 4-decimal `$0.xxxx` cost format mirroring `SkillProjectsTable.fmtCost`, default sort `cost desc`; runtime-DOM path-leakage guard via `container.textContent` regex `/\/[A-Za-z][\w/.-]+/` (mirrors Phase 19 SKLP-08 dual-guard pattern); new `/cost` top-level route (TanStack Router file-route, sibling of `/skills`/`/activity`/`/alerts`); NavBar `routes` extended with `{ to: '/cost', label: 'Cost' }` between Skills and Alerts (5 links total); `routeTree.gen.ts` regenerated by @tanstack/router-plugin/vite on first `tsc --noEmit`; `NavBar.test.tsx` updated to bootstrap /cost+/alerts in in-memory router with 5-link count + Cost href assertion; surface area pin in `queries.test.ts` 32 → 34 callable exports + cost-key-discipline `it()` block. vitest 306 → 316 (+10 = 5 ForecastCard + 4 ByProjectCard + 1 cost-key qk discipline) / 0 failed; tsc clean; backend pytest unaffected (frontend-only changes; pre-existing Pydantic `NoDecode` collection error confirmed pre-existing via stash-pop verification — not a Plan 20-03 regression). 3 atomic commits, no REFACTOR. Phase 18 BASELINE.md vitest floor (>= 293) preserved at 316.

**Phase 21 (3/3 plans complete, 2026-05-07):** Plan 21-01 (commit c2a7793 feat — `_resolve_alpha` helper inside `evaluate_anomaly`, sliding=`1/N` ewma=`2/(N+1)`; `WindowKind` Literal at API boundary; `AlertRuleCreate` / `AlertRulePatch` validators reject unknown window_kind + enforce min_samples >= window_n; 5 sliding-window detector tests + AST `test_only_one_anomaly_detector` guard mirror of `test_no_tasks_import` precedent; pytest 644 → 650 +6); Plan 21-02 (commits dfeb6fa feat — `cmc/alerts/nl_parser.py` with lazy `from anthropic import AsyncAnthropic` inside `parse_alert_nl`, `_build_system_prompt()` from `sorted(_SCOPE_EXTRACTORS.keys())`, returns `None` on hallucination; 3 schemas appended to `schemas/alerts.py`; `POST /api/alerts/parse-nl` 503 collapsed-failure-mode + `GET /api/alerts/metrics`; ef2a3d7 test — 8 parser unit tests via `_patched_import` shim + 4 router tests via `monkeypatch.setattr` bind-replace + log-warning assertions via direct `MagicMock` patch sidestepping FastAPI lifespan caplog clearing; pytest 650 / 0 / 32 warnings); Plan 21-03 ~25 min (commits b902661 feat — `alertsParseNl` + `alertMetrics` typed client fns + 3 types in `lib/api.ts`; `useParseAlertNl` mutation mirroring `useParseNlCron` + `useAlertMetrics` query (`staleTime: Infinity`, `gcTime: Infinity`, `refetchOnWindowFocus: false`) + `qk.alertMetrics` key in `lib/queries.ts`; `KNOWN_METRICS` → `FALLBACK_KNOWN_METRICS` rename in `AlertRuleForm.tsx` + runtime `useMemo(() => useAlertMetrics().data?.metrics ?? FALLBACK_KNOWN_METRICS, ...)`; new `AlertNlInput` sub-component verbatim-mirroring `NLCronInput` at `ScheduleComposer.tsx:452-498`; new local state `parsedRule` / `parsedDescription` / `previewOpen` / `manualDisabled` driving `AlertDialog` preview-and-confirm modal at the bottom of the article; on Save, `useCreateAlertRule.mutate(parsedRule)` fires DIRECTLY — Pitfall 5 single-source: NOT merged with manual draft; on parse 503, inline `role="alert"` "Could not parse — please rephrase or use the manual form below." message — does NOT echo the raw 503 body literal; manual fields disabled while `previewOpen || createMutation.isPending`; 379a673 test — 4 new vitest cases under `'AlertRuleForm — NL authoring (ALRT-14)'` + `installRoutedFetch(opts)` URL+method router helper covering `/api/alerts/parse-nl` + `/api/alerts/metrics` + `/api/alerts/rules` POST; parse → preview → save asserts captured POST body has `kind='anomaly'` while manual default is `'threshold'` — locks the Pitfall 5 contract by type-mismatch detection; parse 503 asserts inline message + modal NOT in DOM + POST `/api/alerts/rules` was never called (PITFALLS lockout); manual-disabled-while-preview test asserts re-enable on Cancel; useAlertMetrics-driven `<select>` test asserts the 3 canonical metric values render; new `backend/tests/test_alerts_metrics_sync.py` cross-language drift guard reads `frontend/src/components/panels/AlertRuleForm.tsx` as text via `Path(__file__).resolve().parents[2]`, captures `FALLBACK_KNOWN_METRICS` (or `KNOWN_METRICS` — forward-portable) array literal body, **filters out single-line `//`-commented entries before regex-extracting `value:` strings (Rule 1 fix during execution: without the filter, commented-out metrics still match because the value: '...' substring survives the comment marker, defeating the entire purpose of the guard)**, then asserts `set() == _SCOPE_EXTRACTORS.keys()` with both-sets diagnostic on failure; negative-test re-verified post-fix: comment-out a metric → FAILS with `Only in backend: ['skill_p95_latency_ms']` diagnostic, revert → PASSES); Vitest 316 → 320 (+4) / 0 failed; pytest 650 / 1 pre-existing flaky (`test_emergency_stop.py::test_estop02_validate_pid_is_claude_positive` — passes in isolation, environment-dependent PID lookup; logged in `.planning/phases/21-alert-anomaly-depth-nl-authoring/deferred-items.md`); tsc + ruff + pyright clean. ALRT-13 + ALRT-14 user-shippable end-to-end. Phase 21 awaits verifier sweep before close.

Plan 20-04 ~7 min (commit 0ad412a test — `frontend/tests/e2e/cost-dashboard.spec.ts` 4 tests + `frontend/tests/e2e/README.md` skips inventory): Playwright e2e ships under `test.describe('ANLY-06/07: /cost dashboard panels')`: (1) `opens /cost and mounts both panels` — section testids `cost-forecast-card` + `cost-by-project-card` visible (dev-DB-state-independent); (2) `NavBar Cost link navigates from / to /cost` — clicks `getByRole('link', { name: 'Cost' })` from /, asserts URL becomes /cost AND both card testids mount (dev-DB-state-independent); (3) `cost-by-project-card-table has no path-leakage` — LOAD-BEARING; preflights `/api/cost/breakdown?dim=project&range=7d` via Playwright's `request` fixture, skips with documented reason if `rows.length === 0`, otherwise scans `getByTestId('cost-by-project-card-table').textContent()` against regex `/\/[A-Za-z][\w/.-]+/` (NOT-MATCH) + defensive `cwd` / `display_path` substring guards; mirrors `skills-detail.spec.ts` verbatim — one regex, two surfaces; (4) `7d→30d toggle fires a /api/cost/breakdown?range=30d request` — same preflight skip discipline; arms `page.waitForRequest` matcher BEFORE clicking `30d` button; matcher requires BOTH `range=30d` AND `dim=project` to avoid false-positive on the forecast endpoint. Adversarial-mutation verification performed locally (one-shot, NOT committed): edited `_BREAKDOWN_BY_PROJECT_SQL` to `COALESCE(s.cwd, '<unknown>') AS key` + `GROUP BY COALESCE(s.cwd, '<unknown>')`, killed uvicorn:8765 so Playwright spawned fresh, re-ran spec → Test 3 FAILED (RED) showing actual cwd-shaped strings (`/Users/patrykattc/work/git/PatrykQuantumNomad…`) in rendered table textContent; restored SQL, re-ran → Test 3 PASSED (GREEN). Bonus signal: on the very first run, Test 3 caught a real environment regression (the running uvicorn:8765 had started 1:47PM, 54 min before Plan 20-01's 14:41 commit, so was serving pre-refactor SQL via `reuseExistingServer=true`); killing the stale process let Playwright spawn fresh and all 4 tests passed. README extended under "Known Steady-State Skips" with cost-dashboard.spec.ts entry (mirroring skills-detail.spec.ts framing), added the previously-implied skills-detail.spec.ts entry for completeness, and documented Phase 20 close baseline (passed >= 8 / skipped 1-3 / failed 0; current dev DB observed at 11 passed / 2 skipped). Steady-state Playwright baseline shifts from Phase 19's 7 passed / 2 skipped → 11 passed / 2 skipped on the seeded dev DB; vitest 316/0 unchanged; pytest 632/0 unchanged; tsc clean. Path-leakage prohibition now enforced at FOUR defense layers (SQL filter + structural pytest + vitest container regex + Playwright real-DOM regex). 1 atomic commit. Phase 18 BASELINE.md preserved across all suites.

**Cumulative:** 75 plans across 17 phases (11 v1.0 + 6 v1.1) over 8 calendar days of active development pre-v1.2.

**Phase 22 Plan 01 (1 plan, 2026-05-07):** Plan 22-01 (skill-latency-overhead-feasibility-spike, SKLP-11 gate) ~3 min (commit 07abcfa docs(22-01) — SQL-only feasibility spike against live `data/cmc.db` resolved **NO — descope SKLP-11 to v1.3**). Probe 0 (preamble): SQLite 3.51.0; otel_events skill_activated total = 1 row; tools census Agent=743/64-pending, Skill=16/0-pending. Probe 1 (CT-1 coverage): `total_events=1, with_duration_ms=0` for the only group `spike-test-skill` — STRUCTURAL FAILURE (the diagnosis named in Plan §Action under CT-1). Counter-evidence: direct `json_each`-over-`body.record.attributes` enumeration confirms `duration_ms` key is absent from the only existing skill_activated row (Phase 12 spike artifact, 2026-05-02). Probes 2 (CT-3) + 3 (CT-4) recorded N/A per failure-isolation rule. SPIKE-FINDINGS.md (195 lines) committed at 07abcfa with regex-pinned `## Outcome: NO — descope SKLP-11 to v1.3` banner, mandatory sections (Preamble, Threshold Definitions, Probes 1/2/3, Negative Finding, Anti-Fake-Decomposition Reaffirmation, Next Steps), `MIN_LATENCY_SAMPLES = 30` cited verbatim with SOT pointer to `cmc/api/routes/skills.py:606`, no forbidden tokens (TBD/placeholder/best-effort/approximate decomposition/`ratio guess` — paraphrased to "fractional-of-total estimation"). Plan 22-02 NOT written this round per 22-RESEARCH.md §Architecture Patterns Option 1 (sequential branching). Test baseline unchanged from Phase 21 (pytest 650 / vitest 320 / 1 pre-existing flaky). Zero source code touched. User must re-invoke `/gsd-plan-phase 22` to author Plan 22-02-descope.

## Session Continuity

Last session: 2026-05-07T14:34:53Z
Stopped at: Phase 22 Plan 01 complete — SQL feasibility spike resolved NO (SKLP-11 descopes to v1.3); SPIKE-FINDINGS.md committed at 07abcfa; SUMMARY at .planning/phases/22-skill-latency-overhead-spike-gated/22-01-SUMMARY.md. User must re-invoke /gsd-plan-phase 22 to author Plan 02-descope (REQUIREMENTS.md flip).
Resume file: None

---

*v1.0 shipped 2026-04-28 — see `.planning/milestones/v1.0-ROADMAP.md` for full phase history.*
*v1.1 shipped 2026-05-05 — see `.planning/milestones/v1.1-ROADMAP.md` for full phase history.*
*v1.2 active — see `.planning/ROADMAP.md` Phase Details section for current milestone scope.*
