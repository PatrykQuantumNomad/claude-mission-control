---
milestone: v1.3
audited: 2026-05-17
agent: gsd-integration-checker
disposition: PASSED
scores:
  wiring: 6/7  # 1 WARNING — REQUIREMENTS.md dep-count text mismatch (6 actual vs 3 named)
  e2e_flows: 4/5  # 1 documented Accepted Exception — TIME-04 DeltaPill on /cost
---

# v1.3 Surface Redesign — Cross-Phase Integration Report

Verifies the WIRING between phases and the E2E flows that no single phase owns. Each individual phase already passed its own goal-backward verification at close. This report covers the seams.

## Wiring Claims (6/7 verified)

### 1. Saved-view auto-capture pipeline (state_json opaque round-trip) — WIRED

`SaveViewDialog.tsx` git history shows only two commits — both from Phase 25 Plan 06 (`007c47e`, `9beb70d`). Zero Phase 26/27/28 commits. Pitfall 3 lock holds.

- `SaveViewDialog.tsx:52` — `const search = useRouterState({ select: (s) => s.location.search })`
- `SaveViewDialog.tsx:78` — `state_json: search as Record<string, unknown>`

Schema is opaque (`Record<string, unknown>` frontend, `dict[str, Any]` backend). All six v1.3 search params (`time_from`, `time_to`, `compare_panels` from Phase 26; `hidden_panels`, `panel_order`, `split_sizes` from Phase 28) round-trip via the URL search blob.

Round-trip tested: `v13-saved-views.spec.ts:447–528` (Phase 26 time params); `v13-layout.spec.ts:529–629` (Phase 28 layout params).

### 2. AppShellHeader single mount point for all v1.3 chrome — WIRED

`frontend/src/components/shell/AppShellHeader.tsx` mounts (left to right):

1. `EmergencyStopBanner`
2. `<TimePicker />` — Phase 26, line 51
3. `<RefreshDropdown />` — Phase 26, line 52
4. `SavedViewMenu` + `UnsavedPip` inside `saved-view-chrome` div — Phase 25, lines 57–59
5. `cmdk-trigger` button — lines 60–66
6. `<DensityToggle />` — Phase 24, line 67
7. `<ThemeToggle />` — line 68

Phase 24 `save-view-button` placeholder removed; comment on line 18 documents the swap. No `save-view-button` testid in component body.

### 3. Two range-bridge hooks coexist without duplication — WIRED

- `frontend/src/lib/time/useRouteRange.ts:36–43` — Phase 26, takes `routeDefault: Range`, returns `Range` via `rangeToVocab()`. Used by 21 Phase 26 panels on `/` + `/activity`.
- `frontend/src/lib/time/useRouteRangeVocab.ts:27–42` — Phase 27, generic over vocab type `V`, takes snapper function. Used by `/skills`, `/skills/$name`, `/cost`, `/alerts` panels via `snapToSkillRange` / `snapToCostRange` / `snapToAlertRange`.

Both read `time_from`/`time_to` from identical URL contract — no drift. Phase 27 adds `coerceToAbsolute()` that Phase 26 didn't need.

### 4. PANEL_REGISTRY route coverage matches actual route file panel mounts — WIRED

| Route | Route file panel count | Registry panel count | Match |
|-------|------------------------|----------------------|-------|
| `/` | 15 | 15 | ✓ |
| `/activity` | 6 | 6 | ✓ |
| `/cost` | 2 | 2 | ✓ |
| `/skills` | 10 | 10 | ✓ |
| `/alerts` | 3 | 3 | ✓ |

`/skills/$name` excluded (Accepted Exception #1 — single-column-stack). `/sessions/compare` has single registry entry (`session-compare`) — split-pane only, no PanelHeaderMenu.

### 5. TDBT-01 project_key wire shape preserved through Phase 28 — WIRED

`SessionCompareView.tsx` line 691 wraps content in `ResizablePanelGroup`. Data query (`useSessionCompare(a, b)` at line 743) returns `{a, b}` with `project_key: string` intact (`api.ts:182`).

`CommandPalette.tsx:379–380` reads `data.a.project_key` → passes as `scopeProjectKey` (line 721) → `ComparePicker` filter at line 765 (`row.project_key === scopeProjectKey`). Zero `scopeCwd` in production source.

### 6. Runtime dep audit — WARNING (documentation mismatch, no functional break)

`REQUIREMENTS.md` line 12 names 3 baseline deps. Actual additions across v1.3 (via `git log -- frontend/package.json`):

| Phase | Commit | Deps |
|-------|--------|------|
| 24 | `396c092` | `@radix-ui/react-popover@^1.1.15`, `@radix-ui/react-dropdown-menu@^2.1.16` |
| 26 | `6df78d9` | `sonner@^2.0.7`, `react-day-picker@^10.0.0`, `date-fns@^4.1.0` |
| 28 | `5e26a5c` | `react-resizable-panels@4.11.0` (exact pin) |

**Total: 6 new runtime deps** (not 3 as REQUIREMENTS.md states). Phase 26 additions are fully documented in `26-01-SUMMARY.md` `tech-stack.added` and operator-justified (sonner ~9KB toast lib; react-day-picker for dual-month calendar; date-fns for DST-correct date math). Zero Python deps added — that half of the constraint is satisfied.

The closing note in REQUIREMENTS.md ("single new runtime dep this milestone react-resizable-panels@4.11.0") refers to Phase 28 only, not the full v1.3 milestone — the wording is misleading.

### 7. Single Alembic migration `0004_saved_views` — WIRED

`backend/migrations/versions/` shows `0001_initial.py`, `0002_v1_1_alerts_and_skills.py`, `0003_project_key.py` (pre-v1.3), `0004_saved_views.py` (Phase 25 only v1.3 migration). No additional migrations in Phases 24/26/27/28.

---

## End-to-End Flows (4/5 complete; 1 with accepted exception)

### Flow A — Saved view full round-trip with layout customization — COMPLETE

| Hop | File:Line |
|-----|-----------|
| `PanelHeaderMenu.Hide` → `setHidden(panelId, true)` | `PanelHeaderMenu.tsx:89`, `useLayoutState.ts:155–174` |
| URL gains `?hidden_panels=<id>` | `useLayoutState.ts:161–172` |
| `SaveViewDialog.onSubmit` captures `location.search` | `SaveViewDialog.tsx:52, 78` |
| `POST /api/views` stores `state_json` opaque | `queries.ts:266–274` |
| `SavedViewMenu` Open → navigate to route + search | `SavedViewMenu.tsx:126–130` |
| `LoadedViewContext.setLoadedView` fires → `UnsavedPip` correct | `SavedViewMenu.tsx:130` |
| Round-trip tested | `v13-layout.spec.ts:529–629` |

### Flow B — Time picker → drives layout-customizable panel re-query — COMPLETE with documented gap

All URL hops wired. Single documented gap: TIME-04 DeltaPill column on `/cost` not rendered because `/api/cost/breakdown` returns rolled-up totals without day axis. Documented as Phase 27 Accepted Exception #2 — operator-acknowledged 2026-05-16. URL contract for `compare_panels` honored on `/cost`; saved-view round-trip works.

### Flow C — Cmd+K cross-cutting access — COMPLETE

All 6 groups mount in locked order in `CommandPalette.tsx`: Recents (521), Saved Views (574), Pages (599), Time range (634), Density (676), Actions (689).

Shared URL-write codepath: `CommandPalette.tsx:420–432` `applyTimeRange` and `TimePicker.tsx:54–64` `applyRange` use identical TanStack Router navigate pattern (`navigate({to: location.pathname, search: prev => ({...prev, time_from, time_to})})`). Not parallel implementations — two call sites of the same API.

### Flow D — Compare picker uses authoritative project_key (TDBT-01) — COMPLETE

| Hop | File:Line |
|-----|-----------|
| Backend returns `project_key` in session list | `sessions.py:243` |
| Frontend type carries `project_key` | `api.ts:118` |
| CommandPalette reads `data.a.project_key` | `CommandPalette.tsx:379–380` |
| Passed as `scopeProjectKey` | `CommandPalette.tsx:721` |
| ComparePicker filter | `CommandPalette.tsx:765` |
| Zero `scopeCwd` in production | grep returns only test-file comment |

### Flow E — AlertRuleForm dynamic vocab + NL retry (TDBT-02 + TDBT-03) — COMPLETE

TDBT-02: `useAlertMetrics` import at `AlertRuleForm.tsx:55`; fetches `/api/alerts/metrics` returning `sorted(_SCOPE_EXTRACTORS.keys())` at `alerts.py:430`. `FALLBACK_KNOWN_METRICS` absent (0 grep hits). Drift guard: `test_alerts_metrics_contract.py` (runtime contract), `test_alerts_metrics_sync.py` deleted.

TDBT-03: `role="alert"` div at `AlertRuleForm.tsx:273`; honest non-specific copy at 274–277; Retry button with `data-testid="alert-nl-retry"` at 282; `hadError` latch at 211, 219 keeps error block mounted across retry pending window.

---

## Anti-Pattern Check

Spot-checked 20+ key files modified in v1.3. Results:

- **TODO / FIXME / XXX:** Zero in production source. Scanned `AppShellHeader.tsx`, `SaveViewDialog.tsx`, `panelRegistry.ts`, `useLayoutState.ts`, `useRouteRangeVocab.ts`, `useRouteRange.ts`, `CompareToggle.tsx`, `TimePicker.tsx`, `CommandPalette.tsx`, `AlertRuleForm.tsx`, `CostByProjectCard.tsx`, `SessionCompareView.tsx`, and the entire `frontend/src/components/savedviews/` + `frontend/src/components/time/` directories.
- **stub:** One occurrence in `CostForecastCard.tsx:56` — code comment describing a network-stub path in an existing helper. Not a deferred implementation.
- **placeholder:** Three in `AlertRuleForm.tsx` — two are legitimate HTML `placeholder=""` attributes; one is a JSX comment describing loading-window UX.

No unresolved stubs or active placeholders in v1.3 production source.

---

## Final Disposition: PASSED

The v1.3 cross-phase wiring is sound. Critical integration seams hold: SaveViewDialog opaque-capture pipeline round-trips all six v1.3 search params without schema enumeration (confirmed by git history + source). AppShellHeader mounts all four v1.3 chrome components with Phase 24 placeholders removed. The two range-bridge hooks coexist without duplication and share the same URL contract. PANEL_REGISTRY parity holds across all five in-scope routes. Phase 28's CompareBody refactor did not regress TDBT-01's wire shape. All five E2E flows complete; the single gap (TIME-04 DeltaPill on `/cost`) is a pre-acknowledged Accepted Exception, not a wiring break. The one WARNING (REQUIREMENTS.md dep-count text vs 6 actual deps) is documentation inaccuracy — all Phase 26 additions are operator-justified and documented in Phase 26 SUMMARYs; zero Python deps added across the milestone.
