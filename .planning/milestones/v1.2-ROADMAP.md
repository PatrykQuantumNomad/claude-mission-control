# Milestone v1.2: Depth & Polish

**Status:** ✅ SHIPPED 2026-05-09
**Phases:** 18–23 (6 phases)
**Total Plans:** 22
**Requirements:** 12 active complete + 1 honestly deferred to v1.3 (SKLP-11)

## Overview

Closed v1.1's carried debt with a green CI baseline (centralized `cmc/core/time.py` naive-UTC helper across 22 sites, deterministic `vi.spyOn(Date, 'now')` for time-boundary tests, Playwright strict-mode `data-testid` convention), then deepened every v1.1 lane: added per-project skill breakdown / 7d-vs-prev-7d delta pills / new+dormant badges via a normalized `project_key` (sha1[:12] of `realpath(cwd.rstrip('/'))`) shipping as Alembic migration `0003_project_key`; shipped a Decimal-only OLS monthly cost forecast and per-project cost card on a new `/cost` route; extended the alert engine's `evaluate_anomaly` with a `params_json.window_kind` discriminator (sliding window joins EWMA inside the same function, no parallel detector) and a Haiku-backed `POST /api/alerts/parse-nl` natural-language rule authoring path that hard-validates against `_SCOPE_EXTRACTORS.keys()` (returns `None` on hallucination — no fallback rule); a feasibility-gated SKLP-11 spike that resolved NO and was honestly descoped to v1.3; and finally extended `/sessions/compare` with per-skill p95 latency deltas plus a Cmd+K "compare with previous session" entry-point backed by `GET /api/sessions/{sid}/previous`. All milestone-close gates green: backend pytest 661/0/0 (vs Phase 18 baseline 566; 0 datetime.utcnow warnings preserved), frontend vitest 326/0/0, Playwright 13 passed / 2 skipped (both dev-DB-state-dependent and documented). One Alembic migration (`0003_project_key`) and one new top-level route (`/cost`) carry the schema/UI deltas.

## Phases

### Phase 18: Polish & Carry-Forward Cleanup

**Goal:** Discharge accumulated v1.1 carried debt so every subsequent v1.2 phase runs against a green CI baseline with no false-signal noise from time-of-day flakes, deprecated stdlib calls, or Playwright strict-mode collisions.
**Depends on:** Nothing (first phase of v1.2; v1.1 shipped at `af6d308`)
**Requirements:** POLI-06, POLI-07, POLI-08
**Plans:** 5/5
**Completed:** 2026-05-05

Plans:

- [x] 18-01: `cmc/core/time.py` with `now_utc()` + colocated `UTCDatetime` Annotated alias; helper-first commit per D-Sweep style; 9 import sites preserved via re-export from `cmc.api.schemas.common`
- [x] 18-02: Mechanical sweep of all 22 `datetime.utcnow` call sites (20 Field defaults + 2 inline calls); atomic bisect-friendly commit `c3d792f`; dual verify gate (`ruff --select UP` + `git grep` zero) drops ~1429 deprecation warnings to 0
- [x] 18-03: SchedulesCard.test.tsx > stale row migrated to `vi.spyOn(Date, 'now')` (NOT `vi.useFakeTimers` — narrowest blast radius); test-factory sentinel pattern locked in to prevent calendar-aging fixtures
- [x] 18-04: `data-testid="schedule-composer-name"` on source component (NOT test wrapper); `feature-component-element` kebab-case path-style convention documented in `frontend/tests/e2e/README.md`; only the actual collision was decorated (anti-pattern: pre-decoration)
- [x] 18-05: Phase-exit `BASELINE.md` artifact (pytest 566 / vitest 293 / Playwright 7+1-skipped + warning deltas) for Phase 19+ verifier comparison; verifier rules embedded as prose-with-bounds inside the same file (single source of truth)

**Details:** BASELINE.md lives in the phase directory (not `.planning/` root) so future "polish v2" phases write their own baselines without mutating Phase 18's frozen one. Warning-delta is load-bearing: tracked both `total_warnings (32)` and `datetime.utcnow`-specific warnings (0). Even ad-hoc dev-DB inspection scripts respect the POLI-06 ban (use `cmc.core.time.now_utc`, not `datetime.utcnow`).

### Phase 19: Skills Per-Project, Deltas & Badges

**Goal:** User can drill into any skill on `/skills/$name` and see *where* it runs (per-project breakdown), *how its trajectory is changing* (period-over-period deltas), and *whether it's freshly active or going dormant* (badges) — backed by a normalized `project_key` that prevents cwd cardinality blowup and path leakage.
**Depends on:** Phase 18 (green CI baseline; no flakes obscuring new test signal)
**Requirements:** SKLP-08, SKLP-09, SKLP-10
**Plans:** 4/4
**Completed:** 2026-05-06

Plans:

- [x] 19-01: Alembic migration `0003_project_key` (sessions.project_key VARCHAR(12) NOT NULL DEFAULT '', indexed, Python-loop backfill via `realpath`); `cmc.core.project_key.compute_project_key` helper (sha1[:12]); scheduler.py + repository.py wiring so new/re-synced sessions get keyed
- [x] 19-02: SKLP-08 `GET /api/skills/{name}/projects` endpoint returning `SkillProjectRow[]` (project_key, count, p50/p95, cost_usd, cost_attribution, low_sample); structural no-path-leakage test pins response shape to project_key only
- [x] 19-03: SKLP-09 prev-period CTE (7d-vs-prev-7d) extends `/skills/usage` and `/skills/{name}/cost`; SKLP-10 new/dormant badges via MIN/MAX(ts) with cold-start suppression (skills <14 days old never tagged dormant); DST spring-forward unit test crossing the boundary
- [x] 19-04: DeltaPill primitive + SkillProjectsTable panel mount on `/skills/$name`; badges on TopSkills + SkillsRegistry via `useSkillUsage` merge; Playwright `skills-detail.spec.ts` with runtime-DOM path-leakage regex guard

**Details:** Migration 0003 INLINES the sha1[:12] backfill logic instead of importing `compute_project_key` — defensive against future helper refactors (Alembic migrations must remain runnable against historical revisions). Empty-string sentinel for `compute_project_key(None)` and `compute_project_key('')` (column is NOT NULL; `''` is the natural "no canonical project" marker — queries exclude `WHERE project_key != ''`). `_SESSION_MUTABLE_COLS` includes `project_key` so cwd corrections after first sync re-key correctly (Pitfall 9).

### Phase 20: Cost Forecast & Per-Project Card

**Goal:** User can see *where the month is heading* (monthly cost forecast) and *which projects are driving spend* (per-project cost card) on the cost dashboard, using read-time analytics consistent with the v1.1 "tokens stored, $ computed at read time" invariant.
**Depends on:** Phase 19 (consumes migration `0003_project_key` for ANLY-07; sequencing avoids duplicate migration ownership)
**Requirements:** ANLY-06, ANLY-07
**Plans:** 4/4
**Completed:** 2026-05-06

Plans:

- [x] 20-01: Refactor `_BREAKDOWN_BY_PROJECT_SQL` to GROUP BY `s.project_key` + `WHERE s.project_key != ''`; structural path-leakage test mirrors Phase 19 SKLP-08
- [x] 20-02: `cmc/cost/forecast.py` Decimal-only OLS module + `GET /api/cost/forecast` endpoint with `CostForecastResponse`; `insufficient_data` when `days_elapsed < 7`; baseline excludes today; partial-month bias banner during week 1
- [x] 20-03: NEW `/cost` top-level route + `CostForecastCard` + `CostByProjectCard` (sortable DataTable with 7d/30d toggle); NavBar Cost link; bias banner driven by server `partial_month_bias` flag
- [x] 20-04: Playwright `cost-dashboard.spec.ts` (4 tests); fourth-defense-layer adversarial-mutation-verified path-leakage regex on real DOM `cost-by-project-card-table` textContent

**Details:** No dollar values stored at any point — cost figures computed at read time via `cmc.pricing.compute_cost`, preserving the v1.1 self-correcting historical-totals property. Phase 13 endpoint `/api/cost/breakdown?dim=project` was reused for the per-project card (UI-only ANLY-07 addition; no new endpoint). `/cost` is the only new top-level route in v1.2 (deliberate; v1.2's slogan is "depth, not breadth").

### Phase 21: Alert Anomaly Depth & NL Authoring

**Goal:** User can author richer alert rules — *both* by configuring a sliding-window rolling-mean ± stddev anomaly detector (alongside the existing EWMA z-score one) *and* by typing rules in natural language ("alert me when haiku skill p95 exceeds 5s for 10 minutes") — without the alert engine ever shipping a fallback rule on Haiku hallucination or a parallel detector function.
**Depends on:** Phase 18 (green CI baseline; ALRT lifecycle tests need clean signal). Independent of Phases 19/20.
**Requirements:** ALRT-13, ALRT-14
**Plans:** 3/3
**Completed:** 2026-05-07

Plans:

- [x] 21-01: `_resolve_alpha` helper inside `evaluate_anomaly` (sliding=`1/N`, ewma=`2/(N+1)`); `params_json.window_kind` validator + `min_samples >= window_n` coupling on `AlertRuleCreate`/`AlertRulePatch`; AST-based static-import test pinning the single-detector invariant (no third `kind` value, no parallel detector function)
- [x] 21-02: `cmc/alerts/nl_parser.py` (lazy `AsyncAnthropic`, `_SCOPE_EXTRACTORS.keys()` injected verbatim into system prompt, `None` on hallucination); `POST /api/alerts/parse-nl` (503 collapse) + `GET /api/alerts/metrics` (dynamic vocabulary endpoint); parser unit tests + router tests
- [x] 21-03: `useParseAlertNl` + `useAlertMetrics` hooks; NL input + `AlertDialog` preview modal in `AlertRuleForm`; FALLBACK_KNOWN_METRICS sourced via React Query; backend `test_alerts_metrics_sync.py` regex drift guard with single-line `//`-comment filter

**Details:** ALRT-13 ships as a `params_json.window_kind: "ewma" | "sliding"` discriminator INSIDE `evaluate_anomaly` — no third `kind` value, no parallel detector function, no second dispatch branch (verified by AST static-import test). Sliding-window detector reuses the shipped Welford variance recurrence verbatim with a warmup-boundary `PENDING_FIRE` guard. ALRT-14 hard-validates parser output via `is_known_metric()` and returns `None` on hallucination (no "best-guess" save path, no fallback rule); UI surfaces an honest "could not parse" message instead.

### Phase 22: Skill Latency Overhead (spike-gated)

**Goal:** Feasibility-gated delivery of SKLP-11 — open with a mandatory data-availability spike against `tools` temporal JOIN vs `skill_activated.duration_ms`; if the derivation is reliable, ship the body/subagent/tool stacked-bar breakdown; if not, document the negative finding and descope SKLP-11 to v1.3 cleanly without blocking Phase 23.
**Depends on:** Phase 19 (reuses CTE patterns and `/skills/$name` panel scaffolding)
**Requirements:** SKLP-11 (descoped)
**Plans:** 2/2
**Completed:** 2026-05-08 (descoped)

Plans:

- [x] 22-01: SQL spike against live `data/cmc.db` (CT-1 coverage / CT-3 negative-residual / CT-4 subagent containment); `22-01-SPIKE-FINDINGS.md` committed with binary outcome banner `## Outcome: NO — descope SKLP-11 to v1.3`
- [x] 22-02: NO branch only — flip SKLP-11 to `Deferred to v1.3` in REQUIREMENTS.md, anchored on SPIKE-FINDINGS.md negative finding; closes Phase 22 descope deliverable without blocking Phase 23

**Details:** Phase 22 is the v1.2 "honest deferral" lane — the spike-gated decision tree was set up explicitly so a NO outcome would NOT block Phase 23. No fake decomposition (ratio guesswork, fabricated event types, etc.) shipped. SKLP-11 carries forward to v1.3 with the negative-finding artifact intact for re-evaluation when the upstream OTEL data availability changes.

### Phase 23: Compare Depth & Milestone Close

**Goal:** Close the v1.2 milestone by deepening the v1.1 compare lane — user sees per-skill latency delta in `/sessions/compare` *and* can jump from any session view to compare-with-previous via Cmd+K — then run the milestone-close audit (full test suite green, REQUIREMENTS.md traceability, archive-ready ROADMAP).
**Depends on:** Phase 22 (optional reuse of overhead-derivation work if SKLP-11 had shipped; runs cleanly under the descope path)
**Requirements:** CMPR-06, CMPR-07
**Plans:** 4/4
**Completed:** 2026-05-09

Plans:

- [x] 23-01: Backend — extend `_build_compare_side` with per-side `skill_latencies` dict + `low_sample_a/b` flags on `/api/sessions/compare` (CMPR-06); new `GET /api/sessions/{sid}/previous` resolver with project_key + ended_at ordering, 404-as-empty-state (CMPR-07); SQL budget invariant pin preserves CMPR-04 9-SQL-per-request envelope
- [x] 23-02: Frontend — render per-skill p95 latency section in `SessionCompareView` with delta suppression on low-sample (CMPR-06); Cmd+K "Compare with previous session" action gated by previous-session existence + project-scoped picker (CMPR-07); new `ActiveSessionContext` for cross-Sheet active-session signal
- [x] 23-03: Playwright — extend `frontend/tests/e2e/sessions-compare.spec.ts` with TEST-23-CMPR-06 (per-skill latency section mounts; delta suppressed on low-sample) + TEST-23-CMPR-07 (Cmd+K previous-session gating + navigation); preflight-driven branch annotations on per-session 200/404 split
- [x] 23-04: Milestone close — full validation gates green (backend pytest 661/0/0, frontend vitest 326/0/0, Playwright 13/0/2-skipped, cmc doctor clean) against Phase 18 BASELINE.md; REQUIREMENTS.md traceability backfilled with shipped commit references for CMPR-06/07 + ALRT-13; v1.2 archive snapshot

**Details:** CMPR-04 9-SQL-per-request budget AND 200-with-flag over-cap fallback both still hold under CMPR-06 (verified by per-request SQL counter assertions). CMPR-05 tabular-only invariant preserved (no diff library, no raw message rendering — DevTools Sources scan: zero matches against `diff|jsdiff|react-diff`). Cmd+K action is conditionally visible only when a previous session exists (404-as-empty-state branch); existing self-compare guard prevents `a=b` URLs. ActiveSessionContext signal lives in React Context (not a route parameter) so both session-detail Sheets (LiveSessionsCard + SkillRunsTable) can opt-in without inventing routes for them.

---

## Milestone Summary

**Decimal Phases:** None — all 6 phases were planned base phases (no urgent insertions, no audit gap-closures).

**Honest Deferral:** SKLP-11 (per-skill body/subagent/tool latency overhead breakdown) descoped to v1.3 via Phase 22's spike-gated NO outcome. The descope was budgeted into the milestone plan from day one (Phase 22 opened with a mandatory data-availability spike), so Phase 23 unblocked on schedule with zero rework. v1.2 ships 12/12 active requirements complete + 1 honestly deferred — total 13.

**Key Decisions:**

- **`cmc/core/time.py` is the canonical home for naive-UTC time concerns** — `now_utc()` returns `datetime.now(UTC).replace(tzinfo=None)`; `UTCDatetime` PlainSerializer is colocated; `cmc.api.schemas.common` re-exports `UTCDatetime` so 8 existing schema importers keep working without an 8-file cosmetic sweep
- **Two-commit migration enforced for POLI-06** — Plan 18-01 created the helper; Plan 18-02 owned the 22-site mechanical sweep as a single atomic bisect-friendly commit (`c3d792f`)
- **`vi.spyOn(Date, 'now')` is the locked clock-pin mechanism for boundary-threshold tests** (NOT `vi.useFakeTimers` — narrowest blast radius); test factories MUST default time-dependent fields to a sentinel (`null` for "never run"), never a hard-coded ISO string
- **`data-testid` lives on the source React component, not test-only wrappers**; `feature-component-element` kebab-case path-style is the locked convention; decorate only when strict mode collides — pre-decoration is anti-pattern
- **Phase-exit BASELINE.md lives in the phase directory** (not `.planning/` root); verifier rules embedded as prose-with-bounds (`passed >= 566 → pass`, `warnings_datetime_utcnow > 0 → fail`, `total_warnings > 132 → warn`); single source of truth for both counts and thresholds
- **`project_key = sha1(realpath(cwd.rstrip('/')))[:12]`** is the canonical project-identity primitive; raw `cwd` PITFALLS-locked out (cardinality blowup + path leakage); migration 0003 INLINES the backfill logic instead of importing the helper (defensive against future helper refactors)
- **Empty-string sentinel for `compute_project_key(None|'')`** — column is NOT NULL; `''` is the natural "no canonical project" marker; queries exclude `WHERE project_key != ''`
- **Decimal-only OLS for cost forecasting** — `cmc/cost/forecast.py` reuses stdlib only; `insufficient_data` when `days_elapsed < 7`; baseline excludes today; partial-month bias banner during week 1
- **`/cost` is the only new top-level route in v1.2** — every other feature extends an existing page (per CONTEXT lock: "v1.2 is depth, not breadth")
- **ALRT-13 ships as a `params_json.window_kind` discriminator inside `evaluate_anomaly`** — no third `kind` value, no parallel detector function, no second dispatch branch; AST-based static-import test pins the single-detector invariant
- **ALRT-14 returns `None` on hallucination** — no fallback rule, no "best-guess" save path; UI surfaces honest "could not parse"; parser hard-validates against `_SCOPE_EXTRACTORS.keys()` via `is_known_metric()`
- **NL alert vocabulary served dynamically** via `GET /api/alerts/metrics` + frontend `useAlertMetrics` hook with `FALLBACK_KNOWN_METRICS` constant; backend cross-language drift guard (`test_alerts_metrics_sync.py`) catches future divergence
- **Spike-gated phases must commit a binary YES/NO outcome banner** before downstream work proceeds — Phase 22's `22-01-SPIKE-FINDINGS.md` resolved NO and the descope plan was authored in advance
- **CMPR-06 preserves CMPR-04's 9-SQL-per-request budget** — extension via single rollup SQL per side, not N+1; per-request SQL counter assertion in tests; over-cap fallback still HTTP 200 with empty `tool_counts={}`
- **`ActiveSessionContext` lives in React Context, not a route parameter** — both session-detail Sheets (LiveSessionsCard + SkillRunsTable) opt-in without inventing routes for them; URL-driven activeSessionId source is `/sessions/compare?a=<sid>` per D-10
- **Project-scoped compare picker uses `cwd` as the project-identity proxy** because the wire APIs do NOT expose `project_key` on sessions list / sessionCompare side endpoints today (documented inline in `CommandPalette.tsx`)
- **Stable testid anchors over user-facing copy** — Phase 23 surfaces use `session-compare-skill-latency-section`, `cmdk-compare-with-previous`, etc.; locked behavior names overlap with sibling commands under regex matching
- **Strict-mode-safe locator anchoring with character-unique substrings (U+2026 ellipsis)** — when a Phase X feature adds a Cmd+K command alongside an existing one with overlapping copy, regex-based getByRole locators in older e2e specs may break; mitigation: anchor regexes with characters the new label cannot match

**Issues Resolved:**

- ~1429 `datetime.utcnow` deprecation warnings → 0 across 22 call sites in a single bisect-friendly commit (`c3d792f`); ruff `--select UP` clean; `git grep` zero (Phase 18-02)
- `SchedulesCard.test.tsx > stale row` time-of-day-dependent flake (Phase 7 origin) — fixed via `vi.spyOn(Date, 'now')` + sentinel-default test factories (Phase 18-03)
- `schedule-composer.spec.ts` strict-mode collision with Phase 14 firehose `Filter skill name` — fixed via `data-testid="schedule-composer-name"` on source component (Phase 18-04)
- Stale long-running dev backends miss new endpoints — when a `/api/...` route returns HTML doctype with status 200, the SPA catch-all is serving the request because the backend was started before the route shipped. Documented in Plan 23-03 SUMMARY; mitigation: cycle backend (or let Playwright's `webServer reuse=true` boot a fresh one)
- ALRT-13 traceability gap — REQUIREMENTS.md marker not flipped at Phase 21 close; backfilled at Phase 23 milestone close (Plan 23-04)
- v1.2 traceability double-counted (`v1.2 requirements: 13 total` + `12 total` duplicate line) — collapsed to single accurate line (Plan 23-04)

**Issues Deferred:**

- SKLP-11 — per-skill body/subagent/tool latency overhead breakdown — descoped to v1.3 (Phase 22 spike negative finding documented in `22-01-SPIKE-FINDINGS.md`); upstream OTEL data availability change is the unblock condition
- SKLP-12 — SKLP-11 percentile-split breakdown (p50 / p95 / p99 per overhead category) — depends on SKLP-11 lift in v1.3
- SKLP-13 — heatmap toggle on per-project skill breakdown — alternative view, not v1.2 differentiator
- ANLY-08 — confidence band on monthly cost forecast (residual-stddev-derived ± range) — v1.3+ enhancement
- ANLY-09 — per-project cost budgets with alert integration — bridges cost lane and alerts lane; v1.3+
- ALRT-15 — predictive alerts (forecast × anomaly combination) — tabled until false-positive UX validated
- ALRT-16 — NL queries beyond AlertRule schema (NL2SQL) — its own milestone if ever
- CMPR-08 — sessions-table right-click "compare with previous" entry point — LOW differentiator vs Cmd+K
- CMPR-09 — per-skill cost delta — depends on per-skill cost rollup not yet wired

**Technical Debt Incurred:**

- Phase 23 picker uses `cwd` as the project-identity proxy because the wire APIs do NOT expose `project_key` on sessions list / sessionCompare side endpoints today — documented inline in `CommandPalette.tsx`; v1.3 should expose `project_key` on those wire shapes for picker correctness in edge cases (cwd realpath differences without sha1 collision)
- KNOWN_METRICS frontend constant must stay in sync with backend `_SCOPE_EXTRACTORS` keys — sync warning in module docstring; v1.2 ships `useAlertMetrics` hook + `test_alerts_metrics_sync.py` regex guard, but the constant still exists as a fallback path
- Phase 21-03 frontend NL input couples to a 503 collapse on `POST /api/alerts/parse-nl` (Anthropic credentials missing or service unreachable); UI surfaces the honest error, but no graceful retry/queue UX
- Two pre-existing Playwright skips at v1.2 close (`alerts.spec.ts:40 TEST-05a`, `skills-detail.spec.ts:25 SKLP-08/09/10`) — both dev-DB-state-dependent, both skip via documented `skip-with-reason` pattern; current dev DB lacks recent failed_task + lacks seeded skill row; not regressions, but the v1.3 baseline should re-record after seed refresh

---

_For current project status, see .planning/ROADMAP.md_
