---
milestone: v1.2
audited: 2026-05-09T10:00:00Z
status: passed
scores:
  requirements: 12/12 (active) + 1/1 honestly deferred (SKLP-11 → v1.3)
  phases: 6/6
  integration: 12/12
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 18-polish-carry-forward-cleanup
    items:
      - "Info: 3 `_utcnow_naive()` local helpers in `cmc/dispatcher/alerts.py:73`, `cmc/api/routes/alerts.py:77`, `tests/test_doctor.py:20` — duplicate `now_utc()` logic but use `datetime.now(UTC).replace(tzinfo=None)` (NOT deprecated `datetime.utcnow`). No POLI-06 violation; stylistic redundancy only."
  - phase: 19-skills-per-project-deltas-badges
    items:
      - "Info: `'localtime'` modifier in pre-existing sparkline SQL (`_USAGE_TOP_SQL`, `_COST_TREND_*` at skills.py:444, 752, 773) — display-only day bucketing, NOT threshold arithmetic. DST test confirms badge classification is unaffected."
  - phase: 21-alert-anomaly-depth-nl-authoring
    items:
      - "Override accepted (locked Q3): warmup-boundary guard returns `AlertSignal.INSUFFICIENT` instead of `PENDING_FIRE`. Documented as semantically correct — INSUFFICIENT is the right signal for 'no baseline yet'; the validator coupling `min_samples >= window_n` is the warmup-boundary mechanism."
      - "Cross-phase type-safety note: `AlertRuleParseResponse.rule: AlertRuleCreate` is non-nullable in `frontend/src/lib/api.ts:754`. Backend never returns `{rule: null}` (raises 503 instead, per REQUIREMENTS.md PITFALLS-lock + nl_parser.py docstring), so the type is correct today. A `rule: AlertRuleCreate | null` type with explicit null-check would offer compile-time defense against a future regression of the no-fallback contract."
  - phase: 22-skill-latency-overhead-spike-gated
    items:
      - "Info: `TO_BE_UPDATED_BY_SUMMARY` placeholder in `22-01-SPIKE-FINDINGS.md` Preamble metadata field (line 26). The SUMMARY recorded the commit (`07abcfa`) but did not write it back into SPIKE-FINDINGS.md. Cosmetic; commit SHA is verifiable from `git log`."
  - phase: 23-compare-depth-milestone-close
    items:
      - "Doc inconsistency: REQUIREMENTS.md line 30 describes `/sessions/{sid}/previous` as 'most-recent same-cwd session'. ROADMAP success criteria (authoritative contract) correctly says 'same-`project_key` session' and the implementation uses `project_key`. Code is correct; only the requirements wording lags."
      - "Accepted deviation (D-12 honored): frontend compare picker filters candidates by `cwd` equality as a project-identity proxy because the wire APIs (`SessionListItemFull`, `SessionCompareSide`) don't expose `project_key`. The backend `/previous` endpoint correctly uses `project_key` for authoritative resolution. Intentional, documented in `CommandPalette.tsx` header + 23-02 SUMMARY."
      - "Steady-state Playwright skips: `alerts.spec.ts:40 TEST-05a` (no recently-failed task in dev DB) and `skills-detail.spec.ts:25 SKLP-08/09/10` (no seeded skill row). Both dev-DB-state-dependent and pre-existing; not Phase 23 regressions."
---

# v1.2 Depth & Polish — Milestone Audit

**Milestone:** v1.2 Depth & Polish
**Audited:** 2026-05-09T10:00:00Z
**Status:** passed
**Goal:** Differentiate the four v1.1 lanes (skills / cost / alerts / compare) from baseline observability into a polished, opinionated tool — and discharge accumulated polish/test-flake debt in a dedicated cleanup phase.

---

## Summary

All 6 phases (18–23) shipped against the milestone definition of done. 12/12 active requirements satisfied; 1 requirement (SKLP-11) honestly deferred to v1.3 per the Phase 22 spike negative finding (ROADMAP SC#3 explicitly contemplates this branch). 12/12 cross-phase integration seams wired and 5/5 end-to-end user flows complete. Tech debt is minor and consists of info-level cosmetic redundancies, one accepted override, two intentional design deviations, and one minor REQUIREMENTS.md wording lag — none of which block milestone close.

---

## Requirements Coverage

### Active (12/12 satisfied)

| Req | Description | Phase | Status |
|-----|-------------|-------|--------|
| POLI-06 | Replace `datetime.utcnow` deprecation with `cmc/core/time.py::now_utc` across 22 sites | 18 | ✓ Complete |
| POLI-07 | Stabilize `SchedulesCard.test.tsx > stale row` time-of-day flake (`vi.spyOn(Date, 'now')`) | 18 | ✓ Complete |
| POLI-08 | Disambiguate `schedule-composer.spec.ts` strict-mode collision via `data-testid` convention | 18 | ✓ Complete |
| SKLP-08 | Per-project skill breakdown on `/skills/$name` (`GET /api/skills/{name}/projects`, sortable table, project_key normalization) | 19 | ✓ Complete |
| SKLP-09 | 7d-vs-prev-7d delta pills on TopSkills, SkillCostCard, per-skill detail page | 19 | ✓ Complete |
| SKLP-10 | "new this week" / "dormant" badges with cold-start suppression + DST-safe UTC windowing | 19 | ✓ Complete |
| ANLY-06 | Monthly cost forecast (Decimal-only OLS, 14d baseline, insufficient_data + partial_month_bias guards) | 20 | ✓ Complete |
| ANLY-07 | Per-project cost card (uses existing `/api/cost/breakdown?dim=project`, project_key-keyed, 4 path-leakage defense layers) | 20 | ✓ Complete |
| ALRT-13 | Sliding-window anomaly detector via `params_json.window_kind` discriminator inside `evaluate_anomaly` | 21 | ✓ Complete |
| ALRT-14 | NL alert authoring via `POST /api/alerts/parse-nl` with hard-validation, no fallback rule on hallucination | 21 | ✓ Complete |
| CMPR-06 | Per-skill latency delta in `/sessions/compare` with `low_sample_a/b` flags at <30 events | 23 | ✓ Complete |
| CMPR-07 | Cmd+K "compare with previous session" via `GET /api/sessions/{sid}/previous` + `ActiveSessionContext` | 23 | ✓ Complete |

### Honestly Deferred (1)

| Req | Description | Phase | Status |
|-----|-------------|-------|--------|
| SKLP-11 | Per-skill latency overhead breakdown (body / subagent / tool stacked bar) | 22 | Deferred to v1.3 |

**Deferral rationale:** Phase 22 spike (`22-01-SPIKE-FINDINGS.md`, commit `07abcfa`) resolved **NO** with verbatim sqlite3 evidence. CT-1 coverage probe failed: `duration_ms` is structurally absent from the data needed for the body/subagent/tool decomposition. ROADMAP SC#3 explicitly contemplates this branch ("If spike fails: SKLP-11 is marked descoped, the SPIKE plan's negative-finding document anchors the descope decision, and Phase 23 begins on schedule with no blocking dependency"). REQUIREMENTS.md line 41 + traceability table line 97 record this correctly. Phase 23 was unaffected.

---

## Phase Verification Results (6/6)

| Phase | Status | Score | Verified | Notes |
|-------|--------|-------|----------|-------|
| 18 — Polish & Carry-Forward Cleanup | passed | 4/4 | 2026-05-05T21:30:00Z | Initial verification |
| 19 — Skills Per-Project, Deltas & Badges | passed | 5/5 | 2026-05-06T09:30:00Z | Operator-approved 2026-05-06T10:30:00Z after 2 hotfixes (limit-cap 422 + spurious 404 on events-only skills) |
| 20 — Cost Forecast & Per-Project Card | passed | 4/4 | 2026-05-06 | Initial verification |
| 21 — Alert Anomaly Depth & NL Authoring | passed | 5/5 | 2026-05-07T08:30:00Z | 1 override accepted (INSUFFICIENT vs PENDING_FIRE — locked Q3 decision) |
| 22 — Skill Latency Overhead (spike-gated) | passed | 3/3 | 2026-05-08T17:30:00Z | NO branch — descope deliverable executed; SC#2 N/A |
| 23 — Compare Depth & Milestone Close | passed | 4/4 | 2026-05-09T09:00:00Z | Initial verification; 661 backend pytest + 326 vitest + 13 Playwright passed |

---

## Cross-Phase Integration (12/12 wired)

| Seam | From → To | Mechanism | Status |
|------|-----------|-----------|--------|
| Time hygiene | Phase 18 → all later phases | `cmc/core/time.py::now_utc` adopted across new model files; `git grep datetime.utcnow` returns 0 | WIRED |
| project_key column ownership | Phase 19 → Phase 20 | Migration `0003_project_key` is single owner; Phase 20 only `SELECT/WHERE/GROUP BY` references | WIRED |
| project_key in compare picker | Phase 19 → Phase 23 | `GET /api/sessions/{sid}/previous` filters `SessionModel.project_key == current.project_key` (indexed) | WIRED |
| Backend ↔ frontend metric sync | Phase 21 backend ↔ frontend | `_SCOPE_EXTRACTORS` == `FALLBACK_KNOWN_METRICS` enforced by `test_alerts_metrics_sync.py` drift guard + runtime `GET /api/alerts/metrics` | WIRED |
| ActiveSessionContext provider tree | Phase 23 internal | `AppShell` wraps `<ActiveSessionProvider>` outermost; `LiveSessionsCard` + `SkillRunsTable` mirror, `CommandPalette` reads | WIRED |
| `GET /api/skills/{name}/projects` | Phase 19 | Endpoint registered + `api.skillProjects` + `useSkillProjects` hook + `SkillProjectsTable` mounted | WIRED |
| `GET /api/cost/forecast` | Phase 20 | Endpoint registered + `api.costForecast` + `useCostForecast` hook + `CostForecastCard` mounted | WIRED |
| `POST /api/alerts/parse-nl` | Phase 21 | Endpoint registered + `api.parseAlertNl` + `useParseAlertNl` mutation + `AlertNlInput` wired | WIRED |
| `GET /api/alerts/metrics` | Phase 21 | Endpoint registered + `api.alertMetrics` + `useAlertMetrics` query + `AlertRuleForm` consumes | WIRED |
| `GET /api/sessions/{sid}/previous` | Phase 23 | Endpoint registered + `api.sessionPrevious` + `useSessionPrevious` (404 → null) + CommandPalette gate | WIRED |
| `/cost` route registration | Phase 20 | Present in `routeTree.gen.ts`; NavBar links via `{ to: '/cost', label: 'Cost' }` | WIRED |
| Pricing-as-tokens invariant | v1.1 → v1.2 | No `_usd`/`_dollars` columns added in any v1.2 migration; all cost computed via `compute_cost` at read time | WIRED |

---

## End-to-End Flows (5/5 complete)

| Flow | Steps | Status |
|------|-------|--------|
| A — Skills depth | `/skills` → TopSkills DeltaPill + badges → click skill → `/skills/$name` → SkillCostCard DeltaPill + SkillProjectsTable sortable | COMPLETE |
| B — Cost dashboard | `/cost` (NavBar) → CostForecastCard (insufficient_data + partial_month_bias server-flag-driven) + CostByProjectCard (7d/30d toggle, no path leakage) | COMPLETE |
| C — NL alert authoring | `/alerts` → AlertNlInput → `POST /api/alerts/parse-nl` → preview modal (manual fields disabled) → save via `POST /api/alerts/rules`; on hallucination, 503 → "could not parse" surfaced inline | COMPLETE |
| D — Sliding-window alert | Manual form → `kind=anomaly` + `params_json.window_kind="sliding"` → validator enforces `min_samples >= window_n` → detector dispatches via `_resolve_alpha` | COMPLETE |
| E — Session compare | LiveSessionsCard/SkillRunsTable open → `setActiveSessionId` → Cmd+K → "Compare with previous" gated on `useSessionPrevious` → `/sessions/compare?a=&b=` → SkillLatencySection with `low_sample_a/b` suppression | COMPLETE |

---

## Test Suite Status (at milestone close)

| Suite | Result | Source |
|-------|--------|--------|
| Backend pytest | 661 passed / 0 failed / 32 warnings | Phase 23 verification (re-run during audit) |
| Frontend vitest | 326 passed (70 files) | Phase 23 verification |
| Playwright e2e | 13 passed / 0 failed / 2 skipped (steady-state, dev-DB-state-dependent) | Phase 23 VALIDATION.md |
| `cmc doctor` | 7 ✓ / 0 ✗ / 1 ⚠ (port-perms warning is psutil-needs-root, pre-existing) | Phase 23 VALIDATION.md |
| Baseline comparison vs Phase 18 BASELINE.md | pytest +95 (566 → 661); vitest +33 (293 → 326); Playwright +6 (7 → 13) | Phase 18 + 23 |
| `git grep datetime.utcnow -- backend/` | 0 matches | POLI-06 invariant held end-to-end |

---

## Tech Debt Summary

All tech debt observed during audit is non-blocking. See YAML frontmatter `tech_debt` field for full list. Categories:

- **Info-level cosmetic** (4 items): 3 `_utcnow_naive()` local helpers using non-deprecated patterns; `'localtime'` modifier on pre-existing sparkline SQL; `TO_BE_UPDATED_BY_SUMMARY` cosmetic placeholder in spike findings preamble.
- **Accepted overrides** (1 item): Phase 21 `INSUFFICIENT` vs `PENDING_FIRE` — locked Q3 decision, semantically correct.
- **Intentional design deviations** (1 item): Phase 23 frontend picker uses `cwd` proxy because wire APIs don't expose `project_key`; backend `/previous` correctly uses `project_key`.
- **Documentation lag** (1 item): REQUIREMENTS.md line 30 still says "same-cwd" while code uses `project_key` — code is correct; minor wording fix candidate for next milestone open.
- **Type-safety strengthening opportunity** (1 item): `AlertRuleParseResponse.rule` non-nullable type is correct today (backend raises 503, never returns `{rule: null}`) but offers no compile-time guard against a future regression of the no-fallback contract.

---

## Conclusion

**Status: passed.** v1.2 milestone definition of done is met. All 12 active requirements ship; SKLP-11 is honestly deferred to v1.3 per ROADMAP SC#3. Cross-phase seams hold; E2E flows complete. Tech debt is minor and tracked. Ready to proceed to milestone close.

---

_Audited: 2026-05-09T10:00:00Z_
_Auditor: Claude (gsd-audit-milestone)_
