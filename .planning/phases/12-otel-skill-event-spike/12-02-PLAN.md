---
phase: 12-otel-skill-event-spike
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - .planning/research/SPIKE.md
autonomous: true
requirements: [SPIK-01]
must_haves:
  truths:
    - "User can read SPIKE.md and find a verbatim sqlite3 LIMIT 50 dump of `event_name LIKE '%skill%'` rows from real Claude Code data (success criterion #1)"
    - "User can identify the literal attribute key for skill name (one of `skill_name` / `skill.name` / `name`) from a quoted query result block — NOT from prose paraphrase (success criterion #2)"
    - "User can find an explicit lock stating whether `duration_ms` is present on `claude_code.skill_activated` events, with the Q5 query result quoted as evidence (success criterion #3a)"
    - "User can find an explicit lock stating that the cache TTL split (`ephemeral_5m_input_tokens` / `ephemeral_1h_input_tokens`) lives in the JSONL `message.usage.cache_creation` block and NOT in the OTEL `api_request` event, with both Q6 (NULL on OTEL) and Q7 (present on JSONL) outputs cited (success criterion #3b)"
    - "User can rely on the spike doc as the single reference cited by Phases 13-17 — every locked finding has a stable anchor heading (e.g., `#### LOCK-1: Event name`) and a bracket-tag reference convention (e.g., `[LOCK-1]`) so downstream plans can deep-link (success criterion #4)"
    - "User can find an explicit lock for the literal session-correlation attribute key (`session.id` dotted, NOT `session_id` underscore) — coverage scope item #1 from CONTEXT.md"
    - "User can find an explicit lock for multi-skill turn batching: whether one assistant turn that fires N skills produces N separate `skill_activated` events or 1 batched event (or, if Wave 1 produced only 1 skill invocation, a TENTATIVE lock with explicit confidence note) — coverage scope item #2"
    - "User can find an explicit lock for error/cancel/failure status attribute presence on skill events (or its absence, with Q11 cited) — coverage scope item #3"
    - "User can find an explicit lock for token attribution: whether token counts are inline on `skill_activated` events OR require joining to adjacent `api_request` events via `request_id` ↔ `requestId` — coverage scope item #4"
    - "User can find a `Pitfalls & Latent v1.0 Bugs` section flagging (a) `cmc/api/routes/observability.py:535` flat json_extract bug returning NULL for 1,251 tool_decision rows, and (b) `cmc/api/routes/ingest.py:103` reading `session_id` (underscore) instead of `session.id` (dotted) leaving all 6,392 rows with NULL session_id column — both flagged as Phase 13 followups, NOT fixed in this phase"
    - "User can find a Provenance / Confidence Stamp on every lock: `[VERIFIED: <citation to query result block>]` for HIGH-confidence locks; `[CITED: <STACK.md / Context7 docs reference>]` for MEDIUM; `[ASSUMED]` for low — every lock heading shows its provenance"
    - "User can find a Changelog block at the end documenting the initial author date (2026-05-02), the service.version of record, and an append-only re-run policy"
  artifacts:
    - path: ".planning/research/SPIKE.md"
      provides: "The single canonical reference for OTEL skill event shape, cited by Phases 13-17 plans"
      contains: "#### LOCK-1: Event name"
      contains_alt: "## Appendix A"
      min_locks: 9
  key_links:
    - from: ".planning/research/SPIKE.md body locks"
      to: ".planning/research/SPIKE.md raw appendix"
      via: "Anchor heading deep-links and inline pretty-printed body excerpts that quote the verbatim raw appendix output"
      pattern: "see Appendix A|#lock-"
    - from: ".planning/research/SPIKE.md Pitfalls section"
      to: "cmc/api/routes/observability.py:535 and cmc/api/routes/ingest.py:103"
      via: "File:line references flagging the v1.0 latent bugs as Phase 13 followups (documented only — NOT fixed in Phase 12)"
      pattern: "observability\\.py:535|ingest\\.py:103"
    - from: ".planning/research/SPIKE.md cache TTL lock"
      to: "Phase 13 cost engine ANLY-01"
      via: "Explicit cross-reference noting that Phase 13's compute_cost(... cache_create_5m, cache_create_1h ...) signature must read from JSONL `message.usage.cache_creation`, not OTEL `api_request`"
      pattern: "ANLY-01|Phase 13|cache_create_5m"
---

<objective>
Compose `.planning/research/SPIKE.md` from the raw query output captured by Plan 01 — author HIGH-confidence (or TENTATIVE if Wave 1 produced no event) locks for every CONTEXT.md coverage scope item plus every ROADMAP success criterion. Every lock is anchored at a stable heading (`#### LOCK-N: <name>`), cites a verbatim query block in Appendix A as evidence, and carries a provenance tag (`[VERIFIED]` / `[CITED]` / `[ASSUMED]`).

Purpose: SPIKE.md is the ONE document Phases 13-17 cite when locking attribute keys, ingest column names, JOIN keys, and cost-math input sources. STATE.md flags this as a P0 hard gate. Without a lockable reference, every downstream phase risks paraphrasing Anthropic docs and shipping bugs.

Output: A complete `.planning/research/SPIKE.md` with: header + provenance metadata; an `## Executive summary` block; 9+ locked findings (`#### LOCK-1` through `#### LOCK-9+`) each with anchor heading, evidence pointer, and provenance tag; a `## Pitfalls & Latent v1.0 Bugs` section flagging the two known ingest bugs as Phase 13 followups; a `## Cross-references for downstream phases` table mapping each lock to the consuming phase/requirement; the `## Appendix A` raw capture from Plan 01 preserved verbatim; and a `## Changelog` block enabling append-only re-runs.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-otel-skill-event-spike/12-CONTEXT.md
@.planning/phases/12-otel-skill-event-spike/12-RESEARCH.md
@.planning/phases/12-otel-skill-event-spike/12-01-SUMMARY.md
@.planning/research/SPIKE.md

<interfaces>
<!-- Locked structural decisions from CONTEXT.md and the SPIKE.md structure recommendations in RESEARCH.md §"Open Questions". -->
<!-- Plan 02 must follow these — they ARE the lock format. -->

Citation convention (RESEARCH.md "Open Questions" Q2 — locked here as Plan 02 implementation):
- Anchor headings: `#### LOCK-1: Event name`, `#### LOCK-2: Skill name attribute key`, etc.
  - These create stable URI fragments: `SPIKE.md#lock-1`, `SPIKE.md#lock-2-skill-name-attribute-key`
- In-text bracket tags: `[LOCK-1]`, `[LOCK-2]` — used in prose paragraphs to point at locks
- Provenance tags on every lock heading line (or first line under the heading):
  - `[VERIFIED: see Appendix A — Q1]` — HIGH confidence, evidence in raw appendix
  - `[CITED: STACK.md §1 → Context7 /ericbuess/claude-code-docs]` — MEDIUM, no live verification this session
  - `[ASSUMED]` — LOW, no source — should be rare and explicitly justified

Lifecycle (RESEARCH.md "Open Questions" Q3 — locked here):
- Append-only with a `## Changelog` block at the bottom
- Phase 12 writes the initial set of locks with date `2026-05-02` and `service.version: <value>` stamp
- Future re-runs (Phase 17 doctor work, version drift) APPEND a dated section; never silently overwrite locks
- Superseded locks are explicitly marked, never deleted

Required lock set (must include AT LEAST these 9 — derive more if Wave 1 produced rich evidence):
- LOCK-1: Event name (`skill_activated` bare vs `claude_code.skill_activated` prefixed) — addresses success criterion #2 head-on, Pitfall 3
- LOCK-2: Skill name attribute key (`skill_name` vs `skill.name` vs `name`) — success criterion #2
- LOCK-3: `duration_ms` presence/absence on `skill_activated` events — success criterion #3a
- LOCK-4: Cache TTL split surface (JSONL `message.usage.cache_creation` vs OTEL `api_request`) — success criterion #3b
- LOCK-5: Session correlation attribute key (`session.id` dotted vs `session_id` underscore) — coverage scope #1
- LOCK-6: Project correlation attribute key — coverage scope #1
- LOCK-7: Multi-skill turn batching (N events vs 1 batched event) — coverage scope #2
- LOCK-8: Error / cancel / failure status attribute on skill events — coverage scope #3
- LOCK-9: Token attribution (inline on skill event vs join to api_request via request_id ↔ requestId) — coverage scope #4

Optional locks if evidence supports (do NOT pad if data absent):
- LOCK-10: Service version of record (Pitfall 6)
- LOCK-11: Plugin / marketplace skill attributes (only if `OTEL_LOG_TOOL_DETAILS=1` flowed through and produced visible attrs)
- LOCK-12: claude_code.skill_activated body.stringValue full prefixed name vs event_name column bare name (Pitfall 3 dual-surface lock)

Pitfalls & latent v1.0 bugs to document (NOT to fix — Phase 13's scope):
- BUG-A: `cmc/api/routes/observability.py:535` uses `json_extract(body, '$.tool_name')` — flat extraction. Returns NULL for all 1,251 `tool_decision` rows in production [VERIFIED 2026-05-02]. Should use json_each pattern. Phase 13 ingest plan should fix.
- BUG-B: `cmc/api/routes/ingest.py:103` reads attribute key `session_id` (underscore). Claude Code 2.1.116 emits `session.id` (dotted). Result: `otel_events.session_id` column is NULL for all 6,392 production rows [VERIFIED 2026-05-02]. Phase 13's `attrs_skill_name`-column work must include fixing this read.

Cross-reference table format:
| Lock | Phase | Requirement | Consuming artifact |
|------|-------|-------------|-------------------|
| LOCK-2 (skill name attr key) | Phase 13 | INGST-11 | `cmc/ingest/otel_attrs.py` extractor for `attrs_skill_name` indexed column |
| LOCK-4 (cache TTL surface) | Phase 13 | ANLY-01 | `cmc/pricing.py::compute_cost(..., cache_create_5m, cache_create_1h)` reads from JSONL, not OTEL |
| LOCK-5 (session attr key) | Phase 14 | SKIL-04..07 | per-session rollups must use json_each extraction, not the broken column |
| ... | ... | ... | ... |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author SPIKE.md header, executive summary, all 9+ locked findings, pitfalls, cross-references, and changelog (preserving Plan 01's raw appendix verbatim)</name>
  <files>.planning/research/SPIKE.md</files>
  <action>
    Read the current `.planning/research/SPIKE.md` (Plan 01 left it with placeholder header + populated `## Appendix A — Raw Capture Output`). Read `.planning/phases/12-otel-skill-event-spike/12-01-SUMMARY.md` for the Wave 0/Wave 1 outcome details. Read each `### Qn` block in Appendix A and use those literal outputs as the `[VERIFIED]` evidence for every lock you author.

    Replace the placeholder header at the top of `.planning/research/SPIKE.md` with the following structure (preserving the existing Appendix A section UNTOUCHED — append-only, no edits to verbatim raw output). The body order is REPLACE → INSERT → KEEP-AS-IS. Concretely:

    ```
    # OTEL Skill Event Spike — Phase 12

    **Authored:** <date from 12-01-SUMMARY.md or `date -u +%Y-%m-%d`>
    **Service version of record:** <value of LOCK-10 — typically `claude-code 2.1.116`>
    **Confidence:** <HIGH if Wave 1 produced events; TENTATIVE if Wave 1 produced no event; mixed if some locks verified some cited>
    **Lifecycle:** Append-only. Future re-runs add a dated section under `## Changelog`; existing locks are never silently overwritten.

    > This document is the SINGLE canonical reference for OTEL skill-event shape. Phases 13-17 plans MUST cite locks here (e.g., `[SPIKE.md#lock-2]`) rather than paraphrase Anthropic docs. STATE.md flags this as a P0 hard gate.

    ## Executive summary

    | Question | Locked answer | Confidence | Anchor |
    |----------|---------------|------------|--------|
    | Event name (bare) | `skill_activated` (or whatever Q1/re-run shows) | HIGH/TENTATIVE | [LOCK-1] |
    | Skill name attribute key | `skill_name` OR `skill.name` OR `name` (whichever Q4 showed non-null) | HIGH/TENTATIVE | [LOCK-2] |
    | duration_ms present? | yes/no, with sample count | HIGH/TENTATIVE | [LOCK-3] |
    | Cache TTL split surface | JSONL `message.usage.cache_creation` (NOT OTEL api_request) | HIGH | [LOCK-4] |
    | Session correlation key | `session.id` (dotted) | HIGH | [LOCK-5] |
    | Project correlation key | <whatever Q3/Q9 surfaced; or `n/a — no project_* attribute observed`> | HIGH/TENTATIVE | [LOCK-6] |
    | Multi-skill turn batching | <one of: N separate events / 1 batched event / insufficient sample> | HIGH/TENTATIVE | [LOCK-7] |
    | Error/cancel/failure status attribute | <attr key from Q11 if any; "absent — Phase 14 SkillLatencyTable error rate must be derived from adjacent tool_result events" if Q11 empty> | HIGH/TENTATIVE | [LOCK-8] |
    | Token attribution location | OTEL `api_request` event (inline `cache_creation_tokens`, `input_tokens`, `output_tokens`) joined via `request_id` ↔ JSONL `requestId` | HIGH | [LOCK-9] |

    ## Locked findings

    #### LOCK-1: Event name [VERIFIED: see Appendix A — Q1] (or [CITED: STACK.md §1] if Wave 1 produced no event)

    **Bare `event_name` column value:** `skill_activated`
    **Full `body.record.body.stringValue` value:** `claude_code.skill_activated`

    Evidence:
    ```
    <quote 5-10 verbatim lines from Appendix A — Q1 output>
    ```

    Why both surfaces: ingest at `cmc/api/routes/ingest.py:102` reads `event.name` attribute (bare) into the column; the OTLP record itself carries the prefixed form at `body.record.body.stringValue`. Phase 13 INGST-11 plans must filter on the BARE form (Pitfall 3).

    #### LOCK-2: Skill name attribute key [VERIFIED: see Appendix A — Q4]

    **Literal key:** `<whichever of skill_name/skill.name/name was non-null in Q4>`

    Evidence:
    ```
    <quote the Q4 result row(s) showing which column was non-null>
    ```

    Phase 13's `otel_events.attrs_skill_name` indexed column reads from THIS attribute key via the json_each pattern. The column NAME (`attrs_skill_name`) is independent of the source attribute key (`<literal key>`); Phase 13 plan can keep underscore in the column name to match `attrs_mcp_*` convention regardless of which key form is observed.

    #### LOCK-3: `duration_ms` presence on skill_activated [VERIFIED: see Appendix A — Q5]

    **Result:** `<X of Y skill events have duration_ms attribute>` OR `<absent — 0 of Y events>`

    Evidence:
    ```
    <quote Q5 output: total | have_duration_ms columns>
    ```

    If absent: Phase 14 `SkillLatencyTable` (SKLP-05) must derive latency from `(skill_completed.ts - skill_activated.ts)` if both events exist, OR from adjacent `tool_result` event durations within the same `session.id` window. This is a non-trivial branch that Phase 14 plan must address.

    #### LOCK-4: Cache TTL split surface [VERIFIED: see Appendix A — Q6 (OTEL surface) AND Q7 (JSONL surface)]

    **The split lives in:** JSONL `~/.claude/projects/<hash>/<session>.jsonl` → assistant row → `message.usage.cache_creation.ephemeral_5m_input_tokens` / `ephemeral_1h_input_tokens`
    **The split is NOT in:** OTEL `api_request` event (carries only flat `cache_creation_tokens`)

    Evidence (OTEL — confirmed NULL for ephemeral_5m and ephemeral_1h):
    ```
    <quote Q6 output>
    ```

    Evidence (JSONL — confirmed both keys present):
    ```
    <quote Q7 output — the json.dumps(usage, indent=2) block>
    ```

    Correlation key OTEL → JSONL: `OTEL api_request.request_id` (e.g. `req_011CaeDNYmM8khosYcF2jkq7`) ↔ JSONL `requestId` field on the assistant row. NEVER timestamp-proximity (Pitfall 7).

    Cross-reference: Phase 13 `cmc/pricing.py::compute_cost(model, input, output, cache_read, cache_create_5m, cache_create_1h)` (ANLY-01) MUST read `cache_create_5m` and `cache_create_1h` from JSONL via this correlation. The OTEL surface alone is insufficient for the locked Phase 13 success criterion #1.

    #### LOCK-5: Session correlation attribute key [VERIFIED: see Appendix A — Q3, Q9, Q10]

    **Literal attribute key:** `session.id` (DOTTED)
    **NOT:** `session_id` (underscore — only present in ancient smoke-fixture rows)

    Evidence:
    ```
    <quote Q9 output showing session.id frequency>
    ```

    The `otel_events.session_id` indexed column is NULL for all 6,392 production rows because `cmc/api/routes/ingest.py:103` reads the wrong key (Pitfall 4 / BUG-B below). Phase 13 plans MUST either fix the ingest read or join via the body attribute path.

    #### LOCK-6: Project correlation attribute key [VERIFIED: see Appendix A — Q3]

    **Literal attribute key:** `<from Q3 result — likely "project.id" or similar; if no project-* key surfaced, state "absent — project ID is not emitted on skill_activated; Phase 14 must derive project from session.id → sessions table join">`

    Evidence:
    ```
    <quote Q3 output rows with key LIKE '%project%'>
    ```

    #### LOCK-7: Multi-skill turn batching [VERIFIED or TENTATIVE depending on sample]

    **Observation:** `<from Q10 output — count of skill events per session.id within tight ts window>`

    If Wave 1 only produced 1 skill event, this lock is TENTATIVE: `[CITED: STACK.md §1 — Context7 docs imply N separate events per skill, 1 event per skill activation. Sample size: 1.]`

    Phase 14 timeline (SKLP-06) and cost-attribution math depend on this answer.

    #### LOCK-8: Error / cancel / failure status attribute [VERIFIED: see Appendix A — Q11]

    **Result:** `<from Q11: list of attribute keys matching error/status/outcome/cancel/success patterns; if empty, state "no error/status attribute observed on skill_activated events">`

    If absent: Phase 14 `SkillLatencyTable` error rate column (SKLP-05) cannot read error directly off `skill_activated`. Plan must derive error state from adjacent `tool_result` events with `is_error=true` within the same `session.id` + adjacent `request_id` window.

    #### LOCK-9: Token attribution location [VERIFIED: see Appendix A — Q12 + Q13]

    **Tokens are NOT inline on `skill_activated` events** (Q12 shows zero token-pattern attribute keys on skill events).
    **Tokens ARE on the adjacent `api_request` event** carrying flat `input_tokens`, `output_tokens`, `cache_read_tokens`, `cache_creation_tokens`, plus a precomputed `cost_usd` (e.g. `"0.32559375"` — informational; Phase 13 recomputes).
    **Cache TTL split:** JSONL only (see [LOCK-4]).

    Cross-reference: Phase 13 `cmc/cost/engine.py` and Phase 14 `SkillCostCard` (SKLP-02) MUST join `skill_activated.session.id` + `request_id` to the adjacent `api_request` event for token totals, then to JSONL for the TTL split.

    #### LOCK-10 (optional): Service version of record [VERIFIED: see Appendix A — Q8]

    **Version:** `claude-code 2.1.116` (6,459 of 6,461 rows; 2 ancient rows have NULL version from smoke-fixture era)

    Locks above are valid for this version. Future minor versions trigger a re-run; Phase 17 doctor (POLI-01) warns when `service.version` drifts.

    ## Pitfalls & latent v1.0 bugs (Phase 13 followups — NOT fixed in this phase)

    ### BUG-A: `cmc/api/routes/observability.py:535` flat json_extract returns NULL silently

    The `_EDIT_DECISIONS_OTEL_SQL` query uses `json_extract(body, '$.tool_name')`. Because OTEL attributes are an array of `{key, value}` pairs (not a flat dict), this returns NULL for all 1,251 `tool_decision` rows in production [VERIFIED: `SELECT json_extract(body, '$.tool_name'), COUNT(*) FROM otel_events WHERE event_name = 'tool_decision' GROUP BY 1` returned `|1286` (NULL/count) on 2026-05-02].

    Correct pattern (and what Phase 13 should adopt for ALL OTEL attribute reads):
    ```sql
    SELECT (SELECT json_extract(value,'$.value.stringValue')
            FROM json_each(json_extract(body,'$.record.attributes'))
            WHERE json_extract(value,'$.key') = 'tool_name') AS tool_name
    FROM otel_events WHERE event_name = 'tool_decision';
    ```

    **Phase 13 followup:** Audit every `json_extract(body, '$.<atom>')` in `cmc/api/routes/*.py` and `cmc/mcp/*.py`; fix or migrate to indexed columns.

    ### BUG-B: `cmc/api/routes/ingest.py:103` reads `session_id` (underscore) instead of `session.id` (dotted)

    Result: `otel_events.session_id` column is NULL for all 6,392 production rows [VERIFIED 2026-05-02]. The session id IS in the body (key `session.id`) on every row, but it never makes it into the indexed FK column.

    **Phase 13 followup:** Update the ingest read to try `session.id` first, fall back to `session_id` for backward compat with smoke fixtures. This is on the critical path for INGST-11 (the same migration adds `attrs_skill_name`); doing both reads in one ingest pass keeps the schema change atomic.

    ## Cross-references for downstream phases

    | Lock | Consumed by | Requirement | Action |
    |------|-------------|-------------|--------|
    | LOCK-1 (event name) | Phase 13 | INGST-11 | Filter `event_name = 'skill_activated'` (bare) when extracting `attrs_skill_name` |
    | LOCK-2 (skill name attr key) | Phase 13 | INGST-11 | Read attribute key `<literal>` via json_each into `attrs_skill_name` column |
    | LOCK-3 (duration_ms) | Phase 14 | SKLP-05 | If absent, derive latency from adjacent tool_result events |
    | LOCK-4 (cache TTL surface) | Phase 13 | ANLY-01 | `compute_cost(..., cache_create_5m, cache_create_1h)` reads from JSONL, joined via `request_id`/`requestId` |
    | LOCK-5 (session attr key) | Phase 13 | INGST-11 | Fix `cmc/api/routes/ingest.py:103` to read `session.id` (BUG-B) |
    | LOCK-6 (project attr key) | Phase 14 | SKIL-04, SKLP-02 | Project rollups via `<literal key or session→project join>` |
    | LOCK-7 (multi-skill batching) | Phase 14 | SKLP-06 | Timeline rendering — handle batched vs separate events accordingly |
    | LOCK-8 (error/status attr) | Phase 14 | SKLP-05 | Error rate column derives from `<source per lock>` |
    | LOCK-9 (token attribution) | Phase 13/14 | ANLY-04, SKLP-02 | JOIN `skill_activated` + adjacent `api_request` on session.id + request_id |
    | BUG-A (json_extract bug) | Phase 13 | INGST-11 (collateral) | Fix observability.py:535 in same plan that adds attrs_skill_name |
    | BUG-B (session_id NULL) | Phase 13 | INGST-11 | Fix ingest.py:103 in same Alembic migration |

    ## Re-run instructions

    To verify these locks remain valid for a future Claude Code version:

    1. From repo root: `sqlite3 data/cmc.db "SELECT json_extract(body,'\$.scope.version') AS v, COUNT(*) FROM otel_events GROUP BY 1 ORDER BY 2 DESC;"` — confirm version of record
    2. Re-run all 13 queries from Appendix A
    3. Diff outputs against the verbatim blocks below
    4. If any lock changes (attribute keys, presence/absence): append a new dated section under `## Changelog` with the new evidence; mark the old lock as `[SUPERSEDED]`; do NOT delete history

    ## Appendix A — Raw Capture Output

    <PLAN 01 PRESERVED VERBATIM — DO NOT EDIT>

    ## Changelog

    ### 2026-05-02 — Initial author (Phase 12)
    - service.version of record: `claude-code 2.1.116`
    - Locks LOCK-1 through LOCK-10 written from Wave 0 + Wave 1 capture
    - BUG-A and BUG-B documented as Phase 13 followups
    - Cross-references map locks to Phases 13-17 requirements
    ```

    Implementation guidance:
    - For each lock, the `[VERIFIED]` / `[CITED]` / `[ASSUMED]` provenance tag MUST match the actual evidence available. If Wave 1 produced no event, downgrade LOCK-1, LOCK-2, LOCK-3, LOCK-7, LOCK-8 to `[CITED: STACK.md §1 → Context7 /ericbuess/claude-code-docs]` with explicit "TENTATIVE — no live verification this run" notes. LOCK-4, LOCK-5, LOCK-6, LOCK-9, LOCK-10 are VERIFIABLE from the existing 6,392 rows even without skill events, so they should remain `[VERIFIED]` regardless of Wave 1 outcome.
    - Quote evidence from Appendix A by referencing the heading anchor (e.g., "see [Appendix A — Q4](#q4--skill_name-vs-skillname-vs-name-probe)") AND by inline-quoting 3-10 lines of verbatim output. The inline quote is the readable body; the appendix is the raw forensic record. Per CONTEXT.md "raw appendix + readable body" decision.
    - Keep the lock list to ≥9 entries. Add LOCK-11/LOCK-12 if Q3/Q11/Q13 surfaced rich attributes (e.g., plugin, marketplace, tool_input on skill events) — let evidence drive the count, do NOT pad.
    - The Cross-references table is the contract every downstream phase reads. Be SPECIFIC about which file:line / function signature / column / endpoint each lock feeds.
    - Do NOT rewrite Appendix A. The verbatim raw block is the audit trail; touching it breaks STATE.md's P0 hard gate.

    Author the doc using the Write tool (replace the placeholder header) — the existing Appendix A section content must be preserved EXACTLY. The simplest safe approach: read the current SPIKE.md, identify the exact line where `## Appendix A` begins, replace everything BEFORE that line with the new header + executive summary + locks + pitfalls + cross-references + re-run instructions, and append `## Changelog` after the appendix ends.
  </action>
  <verify>
    <automated>test -f .planning/research/SPIKE.md && grep -c '^#### LOCK-' .planning/research/SPIKE.md | awk '$1 >= 9 {exit 0} {exit 1}' && grep -q '^## Pitfalls' .planning/research/SPIKE.md && grep -q '^## Cross-references' .planning/research/SPIKE.md && grep -q '^## Changelog' .planning/research/SPIKE.md && grep -q '^## Appendix A' .planning/research/SPIKE.md && grep -q -E 'BUG-A|observability\.py:535' .planning/research/SPIKE.md && grep -q -E 'BUG-B|ingest\.py:103' .planning/research/SPIKE.md && grep -E -c '\[(VERIFIED|CITED|ASSUMED)' .planning/research/SPIKE.md | awk '$1 >= 9 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    `.planning/research/SPIKE.md` is a complete, structurally-valid markdown document with: header + provenance metadata; executive summary table mapping every success criterion to a LOCK anchor; ≥9 LOCK headings each with provenance tag and evidence quote; pitfalls section flagging BUG-A and BUG-B as Phase 13 followups (with file:line precision); cross-reference table mapping every lock to a downstream phase + requirement + action; Appendix A preserved verbatim from Plan 01; changelog block stamping initial author date and service.version of record. All four ROADMAP success criteria are satisfied: SQL output present (#1), skill name attribute key locked (#2), duration_ms + cache TTL split locked (#3), single canonical reference structure for downstream phases (#4). All four CONTEXT.md coverage scope items are addressed (LOCK-5/6 session+project, LOCK-7 multi-skill batching, LOCK-8 error status, LOCK-9 token attribution).
  </done>
</task>

</tasks>

<verification>
Final SPIKE.md must satisfy ALL of these grep invariants (the machine-checkable surface from RESEARCH.md §"Validation Architecture"):

```bash
test -f .planning/research/SPIKE.md
grep -c '^#### LOCK-' .planning/research/SPIKE.md             # ≥ 9
grep -E -c '\[(VERIFIED|CITED|ASSUMED)' .planning/research/SPIKE.md  # ≥ 9 (one provenance tag per lock minimum)
grep -q '^## Pitfalls' .planning/research/SPIKE.md
grep -q '^## Cross-references' .planning/research/SPIKE.md
grep -q '^## Changelog' .planning/research/SPIKE.md
grep -q '^## Appendix A' .planning/research/SPIKE.md
grep -q -E 'observability\.py:535|BUG-A' .planning/research/SPIKE.md
grep -q -E 'ingest\.py:103|BUG-B' .planning/research/SPIKE.md
grep -q 'sqlite3 data/cmc.db' .planning/research/SPIKE.md     # at least one literal command in the doc
grep -q 'cache_creation\.ephemeral_5m_input_tokens' .planning/research/SPIKE.md  # JSONL TTL key locked
grep -q 'cache_creation\.ephemeral_1h_input_tokens' .planning/research/SPIKE.md
grep -q -E 'session\.id' .planning/research/SPIKE.md          # dotted form locked (LOCK-5)
grep -q 'service.version' .planning/research/SPIKE.md         # version-of-record stamp
grep -q '2.1.116' .planning/research/SPIKE.md                  # current Claude Code version
```

Manual review (cannot be automated):
- A reader unfamiliar with the codebase can identify, from the executive summary alone, the literal skill name attribute key, whether duration_ms is present, and where the cache TTL split lives — without reading any other file.
- Every lock's `[VERIFIED]` claim points to a real anchor in Appendix A (no broken links, no fabricated evidence).
- Cross-references table cites SPECIFIC file paths or endpoint paths for downstream consumers (not "Phase 13 will use this" — must say "`cmc/pricing.py::compute_cost` reads cache_create_5m from this lock").
</verification>

<success_criteria>
1. **ROADMAP success criterion #1 (verbatim SQL output):** Appendix A preserves Plan 01's raw capture verbatim; Q1 output is quoted both in Appendix A and inline as evidence under LOCK-1.
2. **ROADMAP success criterion #2 (skill name attribute key identifiable):** LOCK-2 names the literal key (one of `skill_name`, `skill.name`, `name`) with the Q4 result block as evidence.
3. **ROADMAP success criterion #3 (duration_ms + cache TTL split):** LOCK-3 states duration_ms presence/absence with sample count; LOCK-4 names the JSONL surface as the cache TTL location and pins the `cache_creation.ephemeral_5m_input_tokens` / `ephemeral_1h_input_tokens` keys.
4. **ROADMAP success criterion #4 (single canonical reference):** Cross-references table maps every lock to a downstream phase + requirement + action; the document uses anchor-heading IDs Phases 13-17 can deep-link to.
5. **CONTEXT.md coverage scope (4 items):** LOCK-5 + LOCK-6 (session/project keys), LOCK-7 (multi-skill batching), LOCK-8 (error/cancel/status), LOCK-9 (token attribution) all present.
6. **CONTEXT.md "raw appendix + readable body" decision:** The locks section uses pretty-printed JSON inline (3-10 line excerpts); Appendix A holds the verbatim raw output untouched.
7. **STATE.md P0 hard gate:** Every lock's prose claim traces to either a `[VERIFIED]` Appendix A reference or a `[CITED]` STACK.md / Context7 reference. Zero `[ASSUMED]` locks unless explicitly justified.
8. **Phase 13 unblocked:** A reader of SPIKE.md can author Phase 13's `attrs_skill_name` ingest extraction, `compute_cost` cache-TTL handling, and the BUG-A/BUG-B fixes WITHOUT consulting Anthropic docs or guessing.
</success_criteria>

<output>
After completion, create `.planning/phases/12-otel-skill-event-spike/12-02-SUMMARY.md` documenting:
- Final lock count (e.g., "9 locks: LOCK-1 through LOCK-9, all VERIFIED" or "10 locks: 7 VERIFIED, 3 TENTATIVE/CITED — Wave 1 produced no event")
- The literal answer to each ROADMAP success criterion (1-4) — copy the lock value, not the question
- The two BUG flags surfaced for Phase 13
- Confirmation that Appendix A was preserved verbatim from Plan 01
- A pointer line: "Phase 13 plan can now begin — cite SPIKE.md#lock-2, #lock-4, #lock-5, #lock-9 for INGST-11/ANLY-01."
</output>
</content>
</invoke>