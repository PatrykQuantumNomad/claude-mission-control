---
phase: 22-skill-latency-overhead-spike-gated
plan: 02
type: execute
wave: 2
depends_on:
  - "22-01"
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - SKLP-11
must_haves:
  truths:
    - "22-01-SPIKE-FINDINGS.md outcome is `## Outcome: NO — descope SKLP-11 to v1.3` and is cited as the anchor for this descope"
    - "REQUIREMENTS.md marks SKLP-11 as Deferred to v1.3 and moves it from `## v1.2 Requirements > ### Skills Polish` to `## Future Requirements (deferred) > ### Skills (v1.3+)`"
    - "REQUIREMENTS.md Traceability row for SKLP-11 is updated from Pending to `Deferred to v1.3` and includes a pointer to the Phase 22 negative finding (SPIKE-FINDINGS.md)"
    - "REQUIREMENTS.md v1.2 totals are updated to reflect SKLP-11 is no longer a v1.2 requirement (13 → 12) and Phase distribution counts reflect Phase 22 has 0 v1.2 requirements"
    - "SKLP-12 wording in Future Requirements no longer claims 'only if SKLP-11 ships' (since SKLP-11 is deferred); it remains deferred to v1.3 without the conditional"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Source of truth requirement status and traceability for v1.2/v1.3"
      contains: "SKLP-11"
      min_lines: 100
---

<objective>
Close Phase 22's NO branch cleanly by recording the already-verified spike outcome as an explicit requirements descope: update `.planning/REQUIREMENTS.md` so SKLP-11 is marked **Deferred to v1.3**, moved to the v1.3 Skills section, and traceability totals reflect the change. This is the remaining deliverable required by ROADMAP Phase 22 Success Criterion #3 (and the "descope decision committed" clause of Success Criterion #1).
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md to defer SKLP-11 to v1.3 and update traceability totals</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md (Phase 22 Success Criteria)
    - .planning/phases/22-skill-latency-overhead-spike-gated/22-01-SPIKE-FINDINGS.md (negative finding anchor)
  </read_first>
  <action>
Pre-flight (MUST pass; this plan is valid only under the NO branch):

- Confirm the spike outcome banner is exactly:
  `## Outcome: NO — descope SKLP-11 to v1.3`
  in `.planning/phases/22-skill-latency-overhead-spike-gated/22-01-SPIKE-FINDINGS.md`.
  If the banner is `YES`, STOP — do not modify REQUIREMENTS.md.

Edit `.planning/REQUIREMENTS.md` to reflect the Phase 22 spike outcome:

1. **Move SKLP-11 out of v1.2 Skills Polish:**
   - Remove the SKLP-11 bullet from `## v1.2 Requirements > ### Skills Polish`.
   - Add a new bullet under `## Future Requirements (deferred) > ### Skills (v1.3+)`:
     - `- **SKLP-11**: User can see per-skill latency overhead breakdown (body / subagent / tool stacked bar) — Deferred to v1.3 (Phase 22 spike negative finding; see .planning/phases/22-skill-latency-overhead-spike-gated/22-01-SPIKE-FINDINGS.md)`

2. **Update the v1.2 requirement count:**
   - Change the line that reads `13 requirements across ...` to `12 requirements across ...`.

3. **Update the Traceability table:**
   - Change the SKLP-11 row from:
     - `| SKLP-11 | Phase 22 | Pending (spike-gated; descopes to v1.3 if feasibility fails) |`
   - To:
     - `| SKLP-11 | Phase 22 | Deferred to v1.3 (Phase 22 spike negative finding — see 22-01-SPIKE-FINDINGS.md) |`

4. **Update the Phase distribution section:**
   - Adjust totals so v1.2 totals reflect 12 requirements.
   - Update the Phase 22 line from `1 requirement (SKLP-11)` to `0 requirements (descoped; SKLP-11 deferred to v1.3)`.

5. **De-conditionalize SKLP-12 wording (v1.3+ Skills):**
   - Update SKLP-12 from:
     - `- **SKLP-12**: ... — only if SKLP-11 ships`
   - To remove the conditional while keeping it deferred, e.g.:
     - `- **SKLP-12**: SKLP-11 percentile-split breakdown (p50 / p95 / p99 per overhead category)`

6. **Update ROADMAP.md progress row for Phase 22 (docs-only, NO branch):**
   - In `.planning/ROADMAP.md` Progress table, update Phase 22 from `0/1 | Planned (spike)` to `2/2 | Complete (descoped)` and include a parenthetical reference to `22-01-SPIKE-FINDINGS.md`.

7. Commit the docs changes:
   ```bash
   gsd-sdk query commit "docs(22-02): descope SKLP-11 to v1.3 (Phase 22 NO outcome)" --files .planning/REQUIREMENTS.md .planning/ROADMAP.md
   ```

Do not change any other requirement statuses.
  </action>
  <verify>
    <automated>
      set -euo pipefail
      grep -q '^## Outcome: NO — descope SKLP-11 to v1.3$' .planning/phases/22-skill-latency-overhead-spike-gated/22-01-SPIKE-FINDINGS.md
      test -f .planning/REQUIREMENTS.md
      # SKLP-11 moved to v1.3+ section and removed from Skills Polish block
      awk 'BEGIN{in_polish=0} /^### Skills Polish/{in_polish=1} /^### / && $0 !~ /^### Skills Polish/{if(in_polish==1) in_polish=0} {if(in_polish==1) print}' .planning/REQUIREMENTS.md | grep -q "SKLP-11" && exit 1 || true
      grep -n "### Skills (v1.3+)" -n .planning/REQUIREMENTS.md >/dev/null
      awk 'BEGIN{in_v13=0} /^### Skills \\(v1\\.3\\+\\)/{in_v13=1} /^### / && $0 !~ /^### Skills \\(v1\\.3\\+\\)/{if(in_v13==1) in_v13=0} {if(in_v13==1) print}' .planning/REQUIREMENTS.md | grep -q "SKLP-11"
      # Traceability updated
      grep -E "^\\| SKLP-11 \\| Phase 22 \\| Deferred to v1\\.3" .planning/REQUIREMENTS.md
      grep -q "22-01-SPIKE-FINDINGS.md" .planning/REQUIREMENTS.md
      # Counts updated
      grep -q "^12 requirements" .planning/REQUIREMENTS.md
      grep -qE '^- v1\\.2 requirements: 12 total' .planning/REQUIREMENTS.md
      grep -qE '^- Mapped to phases: 12' .planning/REQUIREMENTS.md
      grep -q "^\\- Phase 22 \\(Skill Latency Overhead, spike-gated\\): 0 requirements" .planning/REQUIREMENTS.md
      # SKLP-12 no longer conditional on shipping
      grep -n "\\*\\*SKLP-12\\*\\*" .planning/REQUIREMENTS.md | grep -qi "only if SKLP-11 ships" && exit 1 || true
      # ROADMAP progress row updated
      grep -E '^\\| 22\\.[^|]*\\| v1\\.2 \\| 2/2 \\| Complete \\(descoped\\)' .planning/ROADMAP.md
      grep -q '22-01-SPIKE-FINDINGS.md' .planning/ROADMAP.md
      # Commit recorded
      git log -1 --pretty=%s -- .planning/REQUIREMENTS.md .planning/ROADMAP.md | grep -qE '^docs\\(22-02\\):'
    </automated>
  </verify>
  <done>SKLP-11 is explicitly marked `Deferred to v1.3` in `.planning/REQUIREMENTS.md`, moved from the v1.2 Skills Polish list to Future Requirements (v1.3+ Skills), and the traceability row + v1.2 totals reflect the descope. The update references `22-01-SPIKE-FINDINGS.md` as the negative-finding anchor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer edits → `.planning/REQUIREMENTS.md` | Docs-only change; no runtime surface |
| SPIKE-FINDINGS.md → requirement status | Descope justification is anchored to an already-committed negative finding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | Tampering | Requirement status flip without evidence | mitigate | The update MUST reference `.planning/phases/22-skill-latency-overhead-spike-gated/22-01-SPIKE-FINDINGS.md` (Outcome + Negative Finding) as the source of truth. |
| T-22-06 | Repudiation | Ambiguous wording about deferral | mitigate | Use explicit phrase `Deferred to v1.3` in both the SKLP-11 bullet and traceability row so it is grep-verifiable. |
</threat_model>

<verification>
1. Confirm SKLP-11 is removed from v1.2 Skills Polish and added to v1.3+ Skills.
2. Confirm traceability row explicitly reads `Deferred to v1.3` and references the Phase 22 spike artifact.
3. Confirm counts (v1.2 total + phase distribution) are consistent with SKLP-11 no longer in v1.2.
</verification>

<success_criteria>
1. `.planning/REQUIREMENTS.md` marks SKLP-11 as `Deferred to v1.3` and links to `22-01-SPIKE-FINDINGS.md`.
2. `.planning/REQUIREMENTS.md` v1.2 totals and phase distribution reflect SKLP-11 is no longer a v1.2 requirement.
3. Traceability table records SKLP-11 as `Deferred to v1.3` for Phase 22.
</success_criteria>

<output>
After completion, create `.planning/phases/22-skill-latency-overhead-spike-gated/22-02-SUMMARY.md` capturing:

- The spike branch gate: `Outcome: NO — descope SKLP-11 to v1.3` (quote the banner line)
- REQUIREMENTS.md before/after: SKLP-11 moved from v1.2 Skills Polish → Future Requirements (v1.3+ Skills)
- Traceability row before/after for SKLP-11
- v1.2 count delta (13 → 12) and Phase 22 distribution (1 → 0)
- ROADMAP.md Progress row updated to `2/2 | Complete (descoped)` (with link)
- Commit SHA for the docs(22-02) commit
</output>

