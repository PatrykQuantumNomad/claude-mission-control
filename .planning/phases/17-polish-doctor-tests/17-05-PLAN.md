---
phase: 17-polish-doctor-tests
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - backend/.env.example
autonomous: true

must_haves:
  truths:
    - "User reading README.md sees the v1.1 panels (Skills / Alerts / Sessions Compare) documented with what they show, where to find them, and how to use them."
    - "User reading README.md sees the pricing seed workflow (`cmc pricing seed`) and OTEL spike findings (link to .planning/research/SPIKE.md) documented."
    - "User reading backend/.env.example sees a new `OTEL_LOG_TOOL_DETAILS` comment block explaining what unsetting it does to plugin/marketplace skill attrs (matches POLI-01 doctor warning)."
  artifacts:
    - path: README.md
      provides: "v1.1 panels, pricing seed workflow, OTEL spike summary"
      contains: "Alerts"
    - path: backend/.env.example
      provides: "OTEL_LOG_TOOL_DETAILS comment + v1.1 env hints"
      contains: "OTEL_LOG_TOOL_DETAILS"
  key_links:
    - from: "README.md (v1.1 panels section)"
      to: "/alerts route + AlertEventsList + AlertRulesList components"
      via: "panel descriptions reference UI route paths"
      pattern: "/alerts"
    - from: "README.md (pricing section)"
      to: "`cmc pricing seed` CLI + backend/cmc/pricing.py + .planning/research/SPIKE.md"
      via: "command + spike-findings link"
      pattern: "pricing seed"
    - from: "backend/.env.example (OTEL section)"
      to: "_check_otel_log_tool_details in cmc/cli/doctor.py:617"
      via: "comment explains the doctor warning"
      pattern: "OTEL_LOG_TOOL_DETAILS"
---

<objective>
Close POLI-05 by updating the de-facto in-repo documentation surface (README.md + backend/.env.example) for v1.1.

**Researcher's interpretation finding (baked in):** `build-your-own-dashboard-guide.html` is referenced in `.planning/PROJECT.md:94` and `REQUIREMENTS.md:72` as a "companion guide" but does NOT exist in this repo file tree (verified via `find`). The README.md (452 lines) and `backend/.env.example` (108 lines) are the in-repo docs surface. POLI-05 is interpreted as updating those — the companion HTML guide is maintained externally by the user.

This interpretation matches researcher recommendation (option (a) in Open Question 1). It is BAKED INTO this plan with a tagged note in REQUIREMENTS.md so the verifier sees the interpretation explicitly. If the user wanted the HTML guide authored from scratch in-repo, this plan would not close POLI-05 — but per researcher Pitfall 7, the phase requirement language ("User can read updated guide") matches the in-repo update interpretation, and the HTML's absence in the repo at HEAD makes (b) infeasible without scope creep.

Topics to add to README:
1. **v1.1 panels:** `/alerts` (rules list + composer + events history), `/sessions/compare?a=&b=` (two-up KPI + chart diff), Skills page enhancements (cost/latency cards if not already documented).
2. **Pricing seed workflow:** `cmc pricing seed` command, pricing.json source, the doctor.py warning at >30 days.
3. **OTEL spike findings:** Link to `.planning/research/SPIKE.md` summarizing what was learned about plugin/marketplace skill attrs and the `OTEL_LOG_TOOL_DETAILS` env var dependency.

Topics to add to .env.example:
1. **OTEL_LOG_TOOL_DETAILS comment block** — the env var the doctor.py warning at line 617 enforces. Comment should explain that unsetting it masks plugin/marketplace skill attrs in OpenTelemetry traces.
2. **Sanity check on existing v1.1 env vars** — make sure any new vars added in phases 12-16 (alerts, skills, compare) have current explanation lines.

Output:
- README.md diff with v1.1 panels + pricing + OTEL sections (~80 line diff per researcher estimate).
- backend/.env.example updated comment block for OTEL_LOG_TOOL_DETAILS (~10 line diff).
- POLI-05 status flip + companion-guide-out-of-repo note in REQUIREMENTS.md is performed by `17-06-PLAN.md` (single-writer wave 2 plan) — this plan's verification artifact IS the doc diff.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/17-polish-doctor-tests/17-RESEARCH.md
@.planning/research/SPIKE.md
@README.md
@backend/.env.example
@backend/cmc/cli/doctor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md with v1.1 panels + pricing seed + OTEL spike sections</name>
  <files>README.md</files>
  <action>
    Read the current `README.md` (452 lines) end-to-end first to understand its existing section structure (e.g., Quickstart, Architecture, Panels, Configuration, Doctor, etc.). Then add or update sections to cover v1.1:

    **A. Panels section** — find the existing panels/UI overview (likely near the top in a "What's in the dashboard" section). Add or extend descriptions for:

    - **`/alerts`**: Threshold and anomaly alert rules. List shows enabled/disabled toggle + delete. Composer is a 2-tab (Threshold / Anomaly) form mapping the Pydantic v2 discriminated union. Events history shows fired_at / rule / scope / status (Firing/Cleared) / value with a 4-tier range toggle (1d/7d/14d/30d). Acknowledgement is via Telegram callback (POST /api/alerts/_ack — no in-UI button in v1.1). Reference the relevant route file: `frontend/src/routes/alerts.tsx`.

    - **`/sessions/compare?a=&b=`**: Two-up session diff. URL is deep-linkable; both query params are validated as UUIDs (invalid values are stripped to undefined). Two entry points: SessionsTable per-row Compare button + Cmd+K context-aware "Compare with…" action that opens a ComparePicker Sheet. Shows KPI strips (cost / tokens / tool calls / duration / model) per side, plus skill-set diff and tool-counts diff. Falls back to summary-only when either session has >500 tool calls (CMPR-04 cap). All diffs are tabular — no character-level diff library.

    - **Skills page enhancements** (Phase 13/14 carry-forward) — confirm the existing README mentions them; if not, add a brief description.

    **B. Pricing seed workflow** — find the existing Pricing or Cost section (or add one near Configuration). Document:

    - `cmc pricing seed` reads `pricing.json` and upserts pricing rows with `seed_hash` so doctor can detect drift.
    - `cmc doctor` warns when pricing rows are >30 days old (POLI-01).
    - Reference `backend/cmc/pricing.py` and `backend/pricing.json` if they exist (verify with the filesystem).
    - Note: pricing.json is a project file the user maintains; the source-of-truth is OpenAI/Anthropic public pricing pages.

    **C. OTEL spike findings** — find the existing Observability or OTEL section. Add a paragraph:

    - The Phase 12 spike (see `.planning/research/SPIKE.md`) found that plugin and marketplace-installed skill activation events emit `tool_name` but NOT a `skill_name` attribute by default. Setting `OTEL_LOG_TOOL_DETAILS=1` in the environment is required for the dashboard to attribute skill activations correctly.
    - `cmc doctor` warns when this env var is unset (POLI-01) — the warning is informational, not a hard failure, because plain CLI / non-plugin workflows do not depend on it.

    **D. Doctor command** — confirm there's a "Doctor" section. If existing, no change needed. If missing, add 5-10 lines listing the 14 checks (cite `cmc doctor --help` for the canonical list).

    **Style guidelines:**
    - Match the existing README's tone (declarative, code-first, command-first). Don't introduce marketing copy.
    - Use existing heading levels (`##`, `###`) consistently.
    - Where adding code blocks, use `bash` fencing for shell commands and `text` for output.
    - Cross-link between sections using `[Pricing](#pricing)` style anchors.
    - Mention the companion guide ONCE at the top of the relevant section: "An external companion guide (build-your-own-dashboard-guide.html) covers a subset of these topics from a different angle and is maintained outside this repo." This avoids confusion about where the HTML guide lives.

    **What NOT to do:**
    - Do NOT create or modify `build-your-own-dashboard-guide.html` (it's not in this repo).
    - Do NOT add a CHANGELOG.md or new `docs/` directory — POLI-05 scope is README + .env.example.
    - Do NOT rewrite sections that don't need v1.1 changes — surgical edits only.
    - Do NOT include emojis (per project convention).

    Use `Read` to load README.md first, identify section boundaries, then `Edit` for each section update. Aim for the researcher's ~80-line diff target.
  </action>
  <verify>
    grep -E "^##|^###" README.md | head -30  # confirm section structure intact
    grep -c "/alerts\|/sessions/compare\|pricing seed\|OTEL_LOG_TOOL_DETAILS" README.md  # ≥4 mentions
  </verify>
  <done>README.md contains documented v1.1 panels (/alerts, /sessions/compare), pricing seed workflow, OTEL spike summary linking to SPIKE.md, and a single mention of the externally-maintained companion guide. Section structure intact (existing headings preserved).</done>
</task>

<task type="auto">
  <name>Task 2: Update backend/.env.example with OTEL_LOG_TOOL_DETAILS comment block</name>
  <files>backend/.env.example</files>
  <action>
    Read the current `backend/.env.example` (108 lines) end-to-end. Find the OpenTelemetry / OTEL section (or, if none exists, add one near the bottom before any "Optional" trailing section).

    Add or refine a comment block for `OTEL_LOG_TOOL_DETAILS`:

    ```
    # OTEL_LOG_TOOL_DETAILS — set to "1" to emit detailed tool-call attributes
    # (skill_name, tool_input snippets) on OpenTelemetry spans. REQUIRED for the
    # dashboard to attribute plugin/marketplace skill activations correctly: when
    # unset, plugin skill activation events emit tool_name but NOT skill_name,
    # which makes Skills page attribution fall back to "<unknown>" for those
    # rows. `cmc doctor` (check #14) warns if this is unset.
    #
    # See .planning/research/SPIKE.md for the Phase 12 OTEL spike findings.
    OTEL_LOG_TOOL_DETAILS=1
    ```

    If the env var is already present in .env.example, REPLACE the existing comment block with the version above (don't double up).

    Also do a quick scan for any v1.1 env vars added across phases 12-16 (alerts, skills, compare) and confirm each has at least a 1-sentence comment. If any are missing, add a brief comment line. Likely candidates: alert rate limits / cooldown defaults, dispatcher cadence overrides, telegram chat_id splits. If none are missing, no further changes needed.

    **What NOT to do:**
    - Do NOT add new env vars that don't exist in HEAD code. Only document existing ones.
    - Do NOT change default values; only adjust comments.
  </action>
  <verify>grep -A2 "OTEL_LOG_TOOL_DETAILS" backend/.env.example</verify>
  <done>OTEL_LOG_TOOL_DETAILS comment block present, references the doctor check + the SPIKE.md link. File still parses as valid env-shell syntax (no broken VAR=value lines).</done>
</task>

</tasks>

<verification>
1. `git diff README.md backend/.env.example` — diffs are surgical (section additions/refinements, not whole-file rewrites).
2. `grep -E "/alerts|/sessions/compare|pricing seed|OTEL_LOG_TOOL_DETAILS|companion guide" README.md` — at least 5 matches.
3. `grep "OTEL_LOG_TOOL_DETAILS" backend/.env.example` — present with comment.
4. README.md still renders cleanly when previewed (visual check optional — no broken markdown).
5. REQUIREMENTS.md status flip + companion-guide note for POLI-05 is performed by `17-06-PLAN.md` (single-writer wave 2 plan).
</verification>

<success_criteria>
- README.md updated with v1.1 panels (Alerts + Compare) + pricing seed + OTEL spike sections, all linking back to canonical sources (SPIKE.md, doctor check IDs).
- backend/.env.example has clear OTEL_LOG_TOOL_DETAILS comment block matching the doctor warning's intent.
- No new files created (POLI-05 scope is in-repo doc updates only).
- No code changes (purely doc updates).
- REQUIREMENTS.md status flip deferred to 17-06.
</success_criteria>

<output>
After completion, create `.planning/phases/17-polish-doctor-tests/17-05-SUMMARY.md` documenting:
- The README.md sections updated (with line ranges before/after).
- The .env.example OTEL comment block delta.
- Confirmation that the companion HTML guide remains out-of-repo per researcher's interpretation, baked in as the explicit POLI-05 sub-bullet note.
- The diff size in lines vs the researcher's ~80-line README estimate.
</output>
