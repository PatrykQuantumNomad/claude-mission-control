---
phase: 03-read-only-apis
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/cmc/app/lifespan.py
  - backend/cmc/api/schemas/__init__.py
  - backend/cmc/api/schemas/common.py
  - backend/cmc/api/schemas/system.py
  - backend/cmc/api/schemas/sessions.py
  - backend/cmc/api/schemas/observability.py
  - backend/cmc/api/schemas/mcp.py
  - backend/cmc/api/schemas/skills.py
  - backend/cmc/api/sse.py
  - backend/cmc/api/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_phase3_system.py
  - backend/tests/test_phase3_sessions.py
  - backend/tests/test_phase3_observability.py
  - backend/tests/test_phase3_mcp.py
  - backend/tests/test_phase3_skills.py
autonomous: true
requirements:
  - SAPI-01
must_haves:
  truths:
    - "psutil is installed and importable in the venv (psutil.Process().memory_info().rss > 0)"
    - "PyYAML availability documented (verified 6.0.3 already transitive — no install needed)"
    - "app.state.boot_time is a UTC-aware datetime set during lifespan startup"
    - "All 7 Pydantic v2 response-schema modules importable; common.py exports RangeWindow, Pagination, ErrorResponse"
    - "Five Phase-3 test files exist with seeded_app + client fixtures importable from conftest"
    - "Existing Phase 1 + Phase 2 test suites stay green (61/61 pre-existing tests)"
  artifacts:
    - path: backend/pyproject.toml
      provides: "psutil==7.2.2 added to runtime dependencies"
      contains: "psutil==7.2.2"
    - path: backend/cmc/api/schemas/__init__.py
      provides: "schemas package marker"
    - path: backend/cmc/api/schemas/common.py
      provides: "RangeWindow, PaginationParams, ErrorResponse"
      exports: ["RangeWindow", "PaginationParams", "ErrorResponse"]
    - path: backend/cmc/api/schemas/system.py
      provides: "SystemHealthResponse, DaemonAge, SystemStateResponse, AttentionResponse, FirehoseEvent"
    - path: backend/cmc/api/schemas/sessions.py
      provides: "SessionListItem, SessionListResponse, SessionDetailsResponse, ToolTimelineEntry, LiveSessionItem, LiveSessionState, FollowUpMessageRequest, TodaySummaryResponse"
    - path: backend/cmc/api/schemas/observability.py
      provides: "TokenUsageDailyRow, TokenUsageResponse, CacheTrendRow, CacheResponse, OutcomeDailyRow, OutcomesResponse, ToolLatencyRow, ToolLatencyResponse, HookActivityRow, HookActivityResponse, ProjectRollupRow, ProjectRollupResponse, AgentFanoutRow, AgentFanoutResponse, EditDecisionRow, EditDecisionsResponse, ProductivityResponse, PressureResponse"
    - path: backend/cmc/api/schemas/mcp.py
      provides: "McpServerRow, McpServerListResponse, McpToolRow, McpToolsResponse, McpSyncResponse, McpMeasureResponse"
    - path: backend/cmc/api/schemas/skills.py
      provides: "SkillRow, SkillListResponse, SkillSyncResponse, SkillAutonomyPatch, SkillAutonomyResponse"
    - path: backend/cmc/api/sse.py
      provides: "tail_otel_events async generator helper"
      exports: ["tail_otel_events"]
    - path: backend/cmc/app/lifespan.py
      provides: "app.state.boot_time set during startup"
      contains: "boot_time"
    - path: backend/tests/conftest.py
      provides: "seeded_app, client, make_session_row, make_otel_event, make_token_usage_bucket, make_tool_call fixtures"
      contains: "seeded_app"
    - path: backend/tests/test_phase3_system.py
      provides: "Phase 3 system router test file (placeholder + module docstring declaring per-router convention)"
    - path: backend/tests/test_phase3_sessions.py
      provides: "Phase 3 sessions router test file"
    - path: backend/tests/test_phase3_observability.py
      provides: "Phase 3 observability router test file"
    - path: backend/tests/test_phase3_mcp.py
      provides: "Phase 3 MCP router test file"
    - path: backend/tests/test_phase3_skills.py
      provides: "Phase 3 skills router test file"
  key_links:
    - from: backend/cmc/app/lifespan.py
      to: backend/cmc/api/routes/system.py (Wave 1 consumer)
      via: app.state.boot_time
      pattern: "app\\.state\\.boot_time"
    - from: backend/tests/conftest.py
      to: All Phase 3 test files
      via: seeded_app + client fixtures (per-router files import these)
      pattern: "seeded_app|@pytest\\.fixture"
    - from: backend/cmc/api/schemas/common.py
      to: All other schemas/*.py modules
      via: RangeWindow + ErrorResponse re-used
      pattern: "from cmc\\.api\\.schemas\\.common import"
---

<objective>
Phase 3 foundation. Lay the shared rails Wave 1's four router plans (03-02..03-05) all
depend on: install psutil, set `app.state.boot_time` in lifespan, create the seven
Pydantic v2 response-schema modules with all DTOs Wave 1 will need, create the shared
`tail_otel_events` SSE helper, and seed five empty per-router test files plus extended
conftest fixtures (`seeded_app`, `client`, factories) so Wave 1 plans can append tests
without re-bootstrapping infrastructure.

Purpose: Eliminate per-router foundation churn. Wave 1 plans consume DTOs, fixtures,
and the SSE helper as already-defined contracts; they do not invent shapes or
bootstrap a test app from scratch. This is the "interfaces-first" task ordering for
all of Phase 3.

Output:
- backend/pyproject.toml with psutil==7.2.2 in runtime deps (re-`pip install -e .` run)
- backend/cmc/app/lifespan.py with `app.state.boot_time = datetime.now(timezone.utc)`
  set during startup (BEFORE alembic upgrade so it's set even if upgrade is fast)
- backend/cmc/api/__init__.py + backend/cmc/api/schemas/__init__.py — package markers
- 6 schema modules under cmc/api/schemas/ (common, system, sessions, observability, mcp, skills)
- backend/cmc/api/sse.py — shared `tail_otel_events` async generator
- backend/tests/conftest.py — extended with Phase 3 fixtures
- 5 empty test files under backend/tests/ (test_phase3_system.py etc.) — module
  docstring declares per-router convention; one placeholder test in each that imports
  the `client` fixture and asserts it builds (proves conftest wiring is sound)
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-read-only-apis/03-RESEARCH.md
@.planning/phases/01-foundation-database/01-01-SCHEMA.md
@backend/pyproject.toml
@backend/cmc/app/lifespan.py
@backend/cmc/app/factory.py
@backend/cmc/db/__init__.py
@backend/cmc/db/session.py
@backend/cmc/api/routes/__init__.py
@backend/cmc/api/routes/health.py
@backend/cmc/api/routes/sync.py
@backend/tests/conftest.py
@backend/tests/test_phase2_ingest.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/cmc/db/__init__.py — already exported:
```python
from cmc.db.base import SQLModel
from cmc.db.engine import create_engine_for_settings
from cmc.db.session import get_session, make_sessionmaker
```

From backend/cmc/db/session.py:
```python
async def get_session(request: Request) -> AsyncIterator[AsyncSession]:
    """FastAPI dependency: yields a fresh AsyncSession per request."""
```

From backend/cmc/db/models/sessions.py — Session model has these fields used by
SESS-01..07 / OBSV-01..10:
session_id (PK), started_at, ended_at, synced_at, jsonl_mtime, jsonl_path, cwd,
project_hash, model, source, outcome, tokens_input, tokens_output,
tokens_cache_read, tokens_cache_create, tool_call_count, message_count,
error_message.

From backend/cmc/db/models/otel_events.py — OtelEvent has:
id, ts, event_name, session_id, body (JSON), attrs_mcp_server, attrs_mcp_tool,
received_at.

From backend/cmc/db/models/system_state.py — SystemState has:
key (PK), value (str), value_json (dict|JSON), updated_at.

From backend/cmc/app/lifespan.py (current):
- `app.state.settings`, `app.state.engine`, `app.state.sessions`, `app.state.sync_task`
  all set during startup. Wave 0 ADDS `app.state.boot_time` to this set.

From backend/tests/conftest.py — current Phase 1+2 fixtures available:
- `clean_env`, `tmp_db_path`, `test_settings`, `tmp_static_dir`,
  `test_settings_with_static`, `fake_jsonl_dir`, `golden_jsonl_session`,
  `otlp_log_payload`, `otlp_metric_payload`.

The Phase 2 test file uses an inline `_bootstrap_app(test_settings)` helper at
backend/tests/test_phase2_ingest.py:447 — Wave 0 promotes a hardened version of
this helper into conftest as the `seeded_app` fixture so all five Phase 3 test
files share it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1a: Install psutil dep, wire boot_time into lifespan, create 6 schema modules + api package marker</name>
  <files>backend/pyproject.toml, backend/cmc/app/lifespan.py, backend/cmc/api/__init__.py, backend/cmc/api/schemas/__init__.py, backend/cmc/api/schemas/common.py, backend/cmc/api/schemas/system.py, backend/cmc/api/schemas/sessions.py, backend/cmc/api/schemas/observability.py, backend/cmc/api/schemas/mcp.py, backend/cmc/api/schemas/skills.py</files>
  <behavior>
    - Test smoke (added inline as a plain `python -c` import probe in <verify>):
      `from cmc.api.schemas.system import SystemHealthResponse, DaemonAge, SystemStateResponse, AttentionResponse, FirehoseEvent` succeeds.
    - All other 5 schema module imports succeed via the same probe.
    - psutil sanity: `import psutil; assert psutil.Process().memory_info().rss > 0`.
    - Phase 1 + Phase 2 existing tests still pass (61 tests stay green).
    Note: Per-router test files do NOT exist yet at the end of this task — they
    are created in Task 1b. So pytest is run only against existing Phase 1+2
    files in this task's <automated> verify (psutil + boot_time live in lifespan
    so an existing-test rerun proves nothing regressed).
  </behavior>
  <action>
    Step A — Add psutil dep:
      Edit backend/pyproject.toml: in `[project].dependencies` (line ~6-17), append
      `"psutil==7.2.2",` (production dep, NOT dev — SAPI-02 uses it at runtime per
      research §3 and Open Question 4).
      Then run from repo root:
        `/Users/patrykattc/work/git/claude-mission-control/backend/.venv/bin/python -m pip install -e backend`
      Verify: `/Users/patrykattc/work/git/claude-mission-control/backend/.venv/bin/python -c "import psutil; print(psutil.__version__)"`
      MUST print `7.2.2`.
      DO NOT add pyyaml — verified already present (6.0.3) per research Wave-0 check.

    Step B — Wire boot_time into lifespan:
      Edit backend/cmc/app/lifespan.py. Add `from datetime import datetime, timezone`
      at the top. INSIDE the `lifespan(app)` function, immediately after `settings =
      app.state.settings` (around line 44), add:
        `app.state.boot_time = datetime.now(timezone.utc)`
      Add an inline comment: `# Phase 3 SAPI-02 reads this for uptime calc; set
      BEFORE alembic upgrade so it's the moment the process started serving.`
      Per research Open Question 10 — set in lifespan startup, not per-call.
      Do NOT touch any other line of lifespan.py.

    Step C — Create cmc/api package marker if missing:
      Verify backend/cmc/api/__init__.py exists. If absent, create empty file. (It
      almost certainly exists already since Phase 2 added cmc.api.routes; the empty
      __init__.py is conventional.)

    Step D — Create schemas package:
      Create backend/cmc/api/schemas/__init__.py (empty file with module docstring:
      `"""Pydantic v2 response models — distinct from cmc.db.models (storage shape).
      Per RESEARCH §Pattern 1: from_attributes=True so model_validate(orm_row) works.
      """`).

    Step E — Create cmc/api/schemas/common.py:
      Define:
        ```python
        from enum import Enum
        from pydantic import BaseModel, ConfigDict, Field
        from typing import Optional, Any

        class RangeWindow(str, Enum):
            today = "today"
            d7 = "7d"
            d30 = "30d"
            all = "all"

        class PaginationParams(BaseModel):
            limit: int = Field(50, ge=1, le=200)
            offset: int = Field(0, ge=0)

        class ErrorResponse(BaseModel):
            error: str
            detail: Optional[str] = None

        # Shared base config: enables ORM-row -> response-model conversion.
        class ORMBase(BaseModel):
            model_config = ConfigDict(from_attributes=True)
        ```
      Note Pitfall 6: Pydantic v2 needs `from_attributes=True` (not `orm_mode`).

    Step F — Create cmc/api/schemas/system.py with these DTOs (Pydantic v2; ORMBase
    where input is an ORM row, plain BaseModel otherwise):
        - DaemonAge: key (str), last_tick_at (Optional[str]), age_seconds (Optional[int])
        - SystemHealthResponse: status (Literal["ok","degraded"]), uptime_seconds (int),
          memory_rss_mb (float), last_otel_event_age_seconds (Optional[int]),
          daemon_ages (list[DaemonAge]), tzname (str)
        - SystemStateResponse: items (dict[str, Any]) — whitelisted KV keys; missing
          keys absent
        - AttentionItem: kind (str), severity (Literal["info","warning","error"]),
          count (int), detail (Optional[str])
        - AttentionResponse: items (list[AttentionItem]), pending_decisions (int),
          failed_tasks (int), stale_dispatcher_seconds (Optional[int]),
          stuck_sessions (int)  # explicit zero-fields for Phase-4-deferred per Pitfall 7
        - FirehoseEvent: id (int), ts (str ISO), event_name (str),
          session_id (Optional[str]), attrs_mcp_server (Optional[str]),
          attrs_mcp_tool (Optional[str])  # used by SSE message data payload

    Step G — Create cmc/api/schemas/sessions.py with these DTOs:
        - SessionListItem(ORMBase): session_id, started_at (datetime),
          ended_at (Optional[datetime]), cwd (Optional[str]), model (Optional[str]),
          source (Optional[str]), outcome (Optional[str]),
          tokens_input (int), tokens_output (int), tokens_cache_read (int),
          tokens_cache_create (int), tool_call_count (int), message_count (int)
        - SessionListResponse: items (list[SessionListItem]), total (int),
          limit (int), offset (int)
        - ToolTimelineEntry(ORMBase): tool_use_id, tool_name,
          started_at, ended_at (Optional), duration_ms (Optional[int]),
          status (str), input_summary (Optional[str]),
          mcp_server_name (Optional[str]), mcp_tool_name (Optional[str]),
          decision (Optional[str])
        - SessionDetailsResponse: session (SessionListItem), tools (list[ToolTimelineEntry])
        - LiveSessionItem(ORMBase): session_id, started_at, last_activity_at
          (Optional[datetime]), state (Optional[str]),
          current_tool (Optional[str]), model (Optional[str])
        - LiveSessionState(ORMBase): session_id, last_activity_at, state,
          current_message (Optional[str]), current_tool (Optional[str]),
          pid (Optional[int]), updated_at (datetime)
        - FollowUpMessageRequest: message (str, min_length=1, max_length=10000)
        - FollowUpMessageResponse: queued (bool), session_id (str), queue_path (str)
        - TodaySummaryResponse: date (str), sessions_count (int),
          tokens_input_total (int), tokens_output_total (int),
          tokens_cache_read_total (int), tokens_cache_create_total (int),
          tool_call_count (int), error_count (int)

    Step H — Create cmc/api/schemas/observability.py with these DTOs:
        - TokenUsageDailyRow: day (str YYYY-MM-DD), model (str), source (str),
          tokens_input (int), tokens_output (int), tokens_cache_read (int),
          tokens_cache_create (int), sessions_count (int)
        - TokenUsageResponse: items (list[TokenUsageDailyRow]), range (RangeWindow)
        - CacheTrendRow: day (str), hit_rate (float), billable_tokens (int),
          low_sample (bool)  # low_sample = billable_tokens < 10_000
        - CacheResponse: hit_rate (float), trend (list[CacheTrendRow]),
          range (RangeWindow), low_sample (bool)
        - OutcomeDailyRow: day (str), errored (int), rate_limited (int),
          truncated (int), unfinished (int), ok (int), total (int)
        - OutcomesResponse: items (list[OutcomeDailyRow]), range (RangeWindow)
        - ToolLatencyRow: tool_name (str), call_count (int), p50_ms (Optional[int]),
          p95_ms (Optional[int]), max_ms (Optional[int]), error_rate (float)
        - ToolLatencyResponse: items (list[ToolLatencyRow]), range (RangeWindow)
        - HookActivityRow: day (str), hook_name (str), fires (int),
          paired_duration_ms_p50 (Optional[int])
        - HookActivityResponse: items (list[HookActivityRow]), range (RangeWindow),
          total_fires (int)
        - ProjectRollupRow: cwd (str), display_path (str), sessions (int),
          tokens_effective (int), tool_calls (int), pct_of_total (float)
        - ProjectRollupResponse: items (list[ProjectRollupRow]), range (RangeWindow)
        - AgentFanoutRow: session_id (str), title (Optional[str]),
          agent_calls (int), started_at (datetime)
        - AgentFanoutResponse: items (list[AgentFanoutRow]), range (RangeWindow)
        - EditDecisionRow: tool_name (str), accepted (int), rejected (int),
          accept_rate (float), low_sample (bool)
        - EditDecisionsResponse: items (list[EditDecisionRow]), range (RangeWindow)
        - ProductivityResponse: commits (int), pull_requests (int),
          lines_added (int), lines_removed (int), range (RangeWindow)
        - ApiErrorEntry: ts (datetime), session_id (Optional[str]), message (str)
        - PressureResponse: api_retries_exhausted (int), compaction_count (int),
          recent_api_errors (list[ApiErrorEntry])

    Step I — Create cmc/api/schemas/mcp.py with these DTOs:
        - McpServerRow(ORMBase): server_name, call_count (int), error_count (int),
          latency_p50_ms (Optional[float]), latency_p95_ms (Optional[float]),
          latency_max_ms (Optional[float]), source_priority (str),
          computed_at (datetime)
        - McpServerListResponse: items (list[McpServerRow])
        - McpToolRow(ORMBase): server_name, tool_name (str), call_count (int),
          error_count (int), latency_p50_ms (Optional[float]),
          latency_p95_ms (Optional[float]), latency_max_ms (Optional[float]),
          source_priority (str), schema_size_bytes (Optional[int])
        - McpToolsResponse: server_name (str), items (list[McpToolRow])
        - McpSyncResponse: status (Literal["ok","conflict"]),
          servers (int), tools (int),
          source_counts (dict[str, int]),  # {"tool_decision":n, "tools":n, "otel":n}
          duration_ms (int)
        - McpMeasureResponse: status (str), servers_measured (int),
          duration_ms (int)

    Step J — Create cmc/api/schemas/skills.py with these DTOs:
        - SkillRow(ORMBase): name (str), environment (str),
          user_invocable (bool), autonomy (str), description (Optional[str]),
          path (str), updated_at (datetime)
        - SkillListResponse: items (list[SkillRow])
        - SkillSyncResponse: status (str), found (int), upserted (int),
          unchanged (int), errors (int), duration_ms (int)
        - SkillAutonomyPatch: autonomy (Literal["auto","review","manual"])
        - SkillAutonomyResponse(ORMBase): name (str), autonomy (str),
          updated_at (datetime)

    Per Pitfall 6 reminder: use `model_config = ConfigDict(from_attributes=True)` on
    every schema that will accept ORM rows (the ORMBase mixin handles this).

    Atomic commit: `feat(03-01): deps + lifespan boot_time + schema modules (Task 1a)`
  </action>
  <verify>
    <automated>/Users/patrykattc/work/git/claude-mission-control/backend/.venv/bin/python -c "import psutil; assert psutil.__version__ == '7.2.2'; from cmc.api.schemas.common import RangeWindow, PaginationParams, ErrorResponse, ORMBase; from cmc.api.schemas.system import SystemHealthResponse, DaemonAge, SystemStateResponse, AttentionItem, AttentionResponse, FirehoseEvent; from cmc.api.schemas.sessions import SessionListItem, SessionListResponse, ToolTimelineEntry, SessionDetailsResponse, LiveSessionItem, LiveSessionState, FollowUpMessageRequest, FollowUpMessageResponse, TodaySummaryResponse; from cmc.api.schemas.observability import TokenUsageDailyRow, TokenUsageResponse, CacheTrendRow, CacheResponse, OutcomeDailyRow, OutcomesResponse, ToolLatencyRow, ToolLatencyResponse, HookActivityRow, HookActivityResponse, ProjectRollupRow, ProjectRollupResponse, AgentFanoutRow, AgentFanoutResponse, EditDecisionRow, EditDecisionsResponse, ProductivityResponse, ApiErrorEntry, PressureResponse; from cmc.api.schemas.mcp import McpServerRow, McpServerListResponse, McpToolRow, McpToolsResponse, McpSyncResponse, McpMeasureResponse; from cmc.api.schemas.skills import SkillRow, SkillListResponse, SkillSyncResponse, SkillAutonomyPatch, SkillAutonomyResponse; print('OK')" && cd /Users/patrykattc/work/git/claude-mission-control && /Users/patrykattc/work/git/claude-mission-control/backend/.venv/bin/python -m pytest backend/tests/ -q --ignore=backend/tests/test_phase3_system.py --ignore=backend/tests/test_phase3_sessions.py --ignore=backend/tests/test_phase3_observability.py --ignore=backend/tests/test_phase3_mcp.py --ignore=backend/tests/test_phase3_skills.py</automated>
  </verify>
  <done>
    `python -c "import psutil; print(psutil.__version__)"` prints `7.2.2`.
    `grep -n "boot_time" backend/cmc/app/lifespan.py` shows the assignment line.
    All schema imports succeed via the inline import probe above.
    Phase 1 + Phase 2 existing tests stay green (61/61) — Phase 3 test files
    don't exist yet (created in Task 1b).
    Atomic commit landed: `feat(03-01): deps + lifespan boot_time + schema modules (Task 1a)`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1b: Create shared SSE helper + 5 per-router test scaffold files</name>
  <files>backend/cmc/api/sse.py, backend/tests/test_phase3_system.py, backend/tests/test_phase3_sessions.py, backend/tests/test_phase3_observability.py, backend/tests/test_phase3_mcp.py, backend/tests/test_phase3_skills.py</files>
  <behavior>
    - Test 1 (in test_phase3_system.py): `from cmc.api.schemas.system import SystemHealthResponse, DaemonAge, SystemStateResponse, AttentionResponse, FirehoseEvent` succeeds.
    - Test 2 (in test_phase3_sessions.py): all DTOs from cmc.api.schemas.sessions importable.
    - Test 3 (in test_phase3_observability.py): all DTOs from cmc.api.schemas.observability importable.
    - Test 4 (in test_phase3_mcp.py): all DTOs from cmc.api.schemas.mcp importable.
    - Test 5 (in test_phase3_skills.py): all DTOs from cmc.api.schemas.skills importable.
    - Test 6 (psutil sanity, in test_phase3_system.py): `import psutil; assert psutil.Process().memory_info().rss > 0`.
    - Test 7 (sse helper, in test_phase3_system.py): `from cmc.api.sse import tail_otel_events` is callable.
    - Phase 1 + Phase 2 existing tests still pass.
  </behavior>
  <action>
    Step A — Create cmc/api/sse.py — shared SSE generator helper:
        ```python
        """Shared SSE helpers for SAPI-05 firehose and SESS-05 live stream.

        Per RESEARCH §3 + Pitfall 1 (memory leak from un-disposed generators) and
        Pitfall 3 (cursor lifetime in long-running SSE generator):
          - Always check `await request.is_disconnected()` each loop iteration.
          - Exhaust query results to a list before sleeping (no held cursors).
          - Cap loop duration (60 minutes) — clients reconnect.
        """
        from __future__ import annotations
        import asyncio
        import json
        from datetime import datetime, timezone
        from typing import AsyncIterator, Optional

        from fastapi import Request
        from sqlalchemy import select
        from sqlalchemy.ext.asyncio import AsyncSession

        from cmc.db.models.otel_events import OtelEvent

        SSE_POLL_INTERVAL_S = 1.0
        SSE_MAX_DURATION_S = 60 * 60  # 1 hour cap (Pitfall 1)
        SSE_BATCH_LIMIT = 100

        async def tail_otel_events(
            request: Request,
            db: AsyncSession,
            *,
            since_id: Optional[int] = None,
            event_name: Optional[str] = None,
        ) -> AsyncIterator[dict]:
            """Yield SSE-formatted dicts ({event, data, id}) for new otel_events.

            Generator exits cleanly when:
              - request.is_disconnected() (client closed)
              - SSE_MAX_DURATION_S reached (client should reconnect)
            """
            last_id = since_id or 0
            if last_id == 0:
                # default: start at MAX(id) - 100, so reconnects don't replay history
                from sqlalchemy import func
                row = await db.execute(select(func.coalesce(func.max(OtelEvent.id), 0)))
                last_id = max(0, (row.scalar_one() or 0) - 100)
            start = datetime.now(timezone.utc)
            while True:
                if await request.is_disconnected():
                    return
                if (datetime.now(timezone.utc) - start).total_seconds() > SSE_MAX_DURATION_S:
                    return
                stmt = select(OtelEvent).where(OtelEvent.id > last_id)
                if event_name:
                    stmt = stmt.where(OtelEvent.event_name == event_name)
                stmt = stmt.order_by(OtelEvent.id.asc()).limit(SSE_BATCH_LIMIT)
                rows = (await db.execute(stmt)).scalars().all()
                for row in rows:
                    last_id = row.id
                    yield {
                        "event": "otel",
                        "id": str(row.id),
                        "data": json.dumps({
                            "id": row.id,
                            "ts": row.ts.isoformat() if row.ts else None,
                            "event_name": row.event_name,
                            "session_id": row.session_id,
                            "attrs_mcp_server": row.attrs_mcp_server,
                            "attrs_mcp_tool": row.attrs_mcp_tool,
                        }, separators=(",", ":")),
                    }
                await asyncio.sleep(SSE_POLL_INTERVAL_S)
        ```

    Step B — Create five test files:
      For each router (system, sessions, observability, mcp, skills), write
      backend/tests/test_phase3_<router>.py with:
        - module docstring stating the per-router convention (cite Phase 2's 1156-
          line monolith as the rationale; declare that all SAPI-* tests live in
          test_phase3_system.py, all SESS-* in test_phase3_sessions.py, etc., per
          research Open Question 6 / A10)
        - exactly the imports the smoke tests in <behavior> need
        - the smoke test asserting all DTOs from the matching schemas/<router>.py
          import successfully
        - test_phase3_system.py additionally has the psutil and tail_otel_events
          smoke tests (Tests 6 + 7 in <behavior>)

    Atomic commit: `feat(03-01): SSE helper + per-router test scaffolds (Task 1b)`
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/claude-mission-control && /Users/patrykattc/work/git/claude-mission-control/backend/.venv/bin/python -m pytest backend/tests/test_phase3_system.py backend/tests/test_phase3_sessions.py backend/tests/test_phase3_observability.py backend/tests/test_phase3_mcp.py backend/tests/test_phase3_skills.py -q</automated>
  </verify>
  <done>
    pytest reports exactly the 7 smoke tests from <behavior> pass (5 schema-import
    smokes + psutil sanity + tail_otel_events callable).
    `python -c "from cmc.api.sse import tail_otel_events"` succeeds.
    Atomic commit landed: `feat(03-01): SSE helper + per-router test scaffolds (Task 1b)`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend conftest.py with Phase 3 shared fixtures (seeded_app, client, factories)</name>
  <files>backend/tests/conftest.py</files>
  <action>
    Append to backend/tests/conftest.py — DO NOT modify or remove the existing Phase
    1+2 fixtures. Per research Test Framework section the new fixtures are:

      1. `seeded_app(test_settings)` — async fixture that builds a FastAPI app with
         lifespan-bootstrapped engine + sessionmaker, mirroring the existing
         _bootstrap_app helper at backend/tests/test_phase2_ingest.py:447 BUT
         additionally:
           a. yields a `(app, lifespan_cm)` tuple, OR an entered context — pick the
              former so callers can `async with cm: ...`.
           b. ALSO redirects jsonl_root to a tmp nonexistent path if it's the
              default ~/.claude/projects (Pitfall: don't ingest user data in tests).
           c. Sets `app.state.boot_time = datetime.now(timezone.utc) - timedelta(seconds=42)`
              if the lifespan didn't already (defensive — Wave 1 shouldn't depend on
              this defensive write, but it's free insurance).

      2. `async client(seeded_app)` — wraps seeded_app in an httpx AsyncClient
         (httpx>=0.28 is already a dev dep). Use httpx.ASGITransport(app=app) so
         the lifespan is properly entered. Yields a httpx.AsyncClient.

      3. `make_session_row(session_id="sess-1", started_at=None, ended_at=None,
           cwd="/Users/test/proj", model="claude-opus-4-7", source="claude-code",
           outcome=None, tokens_input=0, tokens_output=0, tokens_cache_read=0,
           tokens_cache_create=0, tool_call_count=0, message_count=0,
           error_message=None)` — pure factory function (NOT a fixture; declared as
         a regular module-level helper) returning a dict suitable for direct insert
         OR for constructing a Session ORM instance.

      4. `make_otel_event(id=None, ts=None, event_name="claude_code.tool_result",
           session_id=None, body=None, attrs_mcp_server=None, attrs_mcp_tool=None)`
         — same shape, returns dict.

      5. `make_token_usage_bucket(day=None, model="claude-opus-4-7",
           source="claude-code", tokens_input=1000, tokens_output=500,
           tokens_cache_read=0, tokens_cache_create=0, sessions_count=1)`.

      6. `make_tool_call(tool_use_id="tu-1", session_id="sess-1",
           tool_name="Bash", started_at=None, ended_at=None, duration_ms=None,
           status="ok", error_message=None, input_summary=None,
           mcp_server_name=None, mcp_tool_name=None, decision=None)`.

    Imports to add at the top of the new section (do NOT touch existing imports):
      ```python
      from datetime import datetime, timedelta, timezone
      import httpx
      import pytest_asyncio
      ```

    Pattern reference: copy the `_bootstrap_app` body from
    backend/tests/test_phase2_ingest.py:447 verbatim (it already handles
    jsonl_root override + lifespan startup); promote it to conftest. Use
    `pytest_asyncio.fixture` for async fixtures (decorator imported above) so
    Phase 1+2 sync fixtures are unaffected.

    The `client` fixture MUST use ASGITransport so lifespan startup runs:
      ```python
      @pytest_asyncio.fixture
      async def client(seeded_app):
          app, cm = seeded_app
          async with cm:
              transport = httpx.ASGITransport(app=app)
              async with httpx.AsyncClient(transport=transport, base_url="http://testserver") as ac:
                  yield ac
      ```

    Pitfall 4 awareness: factory functions hard-code timezone-aware UTC datetimes
    when defaults are needed. NEVER use `datetime.utcnow()` (deprecated path).

    Add 1-2 trivial smoke tests in test_phase3_system.py at the bottom that verify:
      - `seeded_app` yields a 2-tuple
      - `client` is a httpx.AsyncClient instance and `await client.get("/api/health")`
        returns 200 (proves lifespan started, alembic ran, Phase 1 router still
        works through the new fixture).
  </action>
  <verify>
    <automated>cd /Users/patrykattc/work/git/claude-mission-control && /Users/patrykattc/work/git/claude-mission-control/backend/.venv/bin/python -m pytest backend/tests/ -q</automated>
  </verify>
  <done>
    Full backend/tests/ pytest suite is green: Phase 1 (25 tests) + Phase 2 (36
    tests) + Phase 3 Wave 0 — TOTAL ≥ 70 tests passing, 0 failures.
    Wave 0 smoke tests deliver: 7 from Task 1b (5 schema imports + psutil + sse helper)
    + 2 from this task (seeded_app shape + client.get /api/health 200) = 9 Wave 0
    smoke tests. Existing Phase 1+2 totals unchanged.
    The `seeded_app` and `client` fixtures importable from conftest by every
    Phase 3 test file. `await client.get("/api/health")` returns 200 in the test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 has no new HTTP endpoints — purely scaffolding (deps, schemas, fixtures, lifespan attribute) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T (Tampering) | psutil dep on PyPI | accept | Pinned to exact version `psutil==7.2.2` per research §3; no SCA tooling required at this scale; macOS-only project + localhost-only network bind already mitigate supply-chain blast radius |
| T-03-01-02 | I (Information Disclosure) | Pydantic v2 response schemas | mitigate | All response DTOs are NEW Pydantic models distinct from SQLModel — never expose internal storage column names. ORMBase mixin enforces explicit field selection (per RESEARCH §Pattern 1 + Anti-Patterns). |
| T-03-01-03 | D (DoS) | tail_otel_events SSE generator | mitigate | Max duration 60min (SSE_MAX_DURATION_S), 1s poll interval, request.is_disconnected() check each iteration, batch limit 100 — directly mitigates Pitfall 1 (memory leak from un-disposed generators). |
| T-03-01-04 | T (Tampering) | tests bootstrapping the real ~/.claude/projects | mitigate | seeded_app fixture redirects jsonl_root to a nonexistent tmp path when it would default to user data, mirroring the Phase 2 _bootstrap_app helper (BLOCKER-3-style protection). |
</threat_model>

<verification>
Phase 3 Wave 0 success means:

  1. `/Users/patrykattc/work/git/claude-mission-control/backend/.venv/bin/python -m pytest backend/tests/ -q` reports ALL tests pass
     (Phase 1 + Phase 2 + Phase 3 Wave 0 ≈ 70 tests).
  2. `python -c "from cmc.api.schemas.system import SystemHealthResponse;
     from cmc.api.schemas.sessions import SessionListResponse;
     from cmc.api.schemas.observability import ToolLatencyResponse;
     from cmc.api.schemas.mcp import McpToolsResponse;
     from cmc.api.schemas.skills import SkillListResponse;
     from cmc.api.sse import tail_otel_events;
     print('OK')"` prints OK from the venv.
  3. `grep -n "boot_time" backend/cmc/app/lifespan.py` finds the assignment line.
  4. `grep -n "psutil==7.2.2" backend/pyproject.toml` finds the dep line.
  5. `python -c "import psutil; print(psutil.__version__)"` prints `7.2.2`.
  6. Calling `await client.get("/api/health")` from a Phase 3 test returns 200
     (proves seeded_app + ASGI transport + lifespan integration).
</verification>

<success_criteria>
- psutil 7.2.2 installed and pinned in pyproject.toml runtime deps
- backend/cmc/app/lifespan.py sets `app.state.boot_time` during startup
- 6 schemas modules exist (common, system, sessions, observability, mcp, skills) and
  all exports listed in `must_haves.artifacts` are importable
- backend/cmc/api/sse.py exports `tail_otel_events` async generator
- backend/tests/conftest.py exports `seeded_app`, `client`, and 4 factory functions
- 5 test files exist (test_phase3_system.py / sessions / observability / mcp / skills)
  with module docstrings declaring per-router convention
- Full backend/tests/ pytest suite green (Phase 1 + Phase 2 + Phase 3 Wave 0)
- No edits to backend/cmc/api/routes/__init__.py, backend/cmc/app/factory.py,
  backend/cmc/db/, backend/cmc/ingest/, backend/cmc/config/, or migrations/
  (Wave 1 plans add their routers; this plan establishes the rails only)
</success_criteria>

<output>
After completion, create `.planning/phases/03-read-only-apis/03-01-SUMMARY.md`
documenting:
  - psutil version installed, pyproject.toml diff
  - lifespan.py boot_time hook (line + context)
  - DTO inventory with one-line description of each (so Wave 1 plans can grep
    the SUMMARY for the right import)
  - tail_otel_events contract (since_id, event_name kwargs; cap behavior)
  - conftest fixtures with signatures (seeded_app, client, factories)
  - Open questions answered IN this plan: A1, A3, Q3 (whitelist), Q5 (yaml verified),
    Q6 (per-router files), Q7 (read-time outcome), Q8 (queue path), Q10 (lifespan boot_time)
  - Entry contracts for Wave 1 plans 03-02..03-05 (where to plug routers in,
    what fixtures to use, how to compose tests)
</output>
