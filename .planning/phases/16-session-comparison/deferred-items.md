- **2026-05-05 (16-02 execution)** — `frontend/src/components/panels/__tests__/SchedulesCard.test.tsx > SchedulesCard > stale row (last_run_at > 48h ago) gets cmc-schedules-row--stale class` fails on clean main-branch tree (verified via `git stash && pnpm vitest run` on `ed9a0fb`). The test computes `last_run_at` relative to current wall-clock and asserts the >48h boundary inverts `cmc-schedules-row--stale` — likely time-of-day-dependent (assertion is `freshRow?.classList.contains('cmc-schedules-row--stale')).toBe(false)` returning true). NOT caused by Phase 16 changes. Out of scope for 16-02 (Rule 3 scope boundary). Track for a future Phase 17 polish or dedicated fix.
