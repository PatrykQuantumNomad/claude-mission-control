---
phase: 23-compare-depth-milestone-close
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - frontend/src/lib/api.ts
  - frontend/src/lib/queries.ts
  - frontend/src/components/panels/SessionCompareView.tsx
  - frontend/src/components/ui/CommandPalette.tsx
  - frontend/src/components/shell/ActiveSessionContext.tsx
  - frontend/src/components/shell/AppShell.tsx
  - frontend/src/routes/sessions_.compare.tsx
  - frontend/src/components/panels/SessionsTable.tsx
  - frontend/src/components/panels/LiveSessionsCard.tsx
  - frontend/src/components/panels/SkillRunsTable.tsx
  - frontend/src/components/panels/__tests__/SessionCompareView.test.tsx
  - frontend/src/components/ui/__tests__/CommandPalette.test.tsx
autonomous: true
requirements:
  - CMPR-06
  - CMPR-07
user_setup: []

must_haves:
  truths:
    - "On /sessions/compare, the UI shows per-skill p95 latency per side and suppresses deltas when either low_sample_a or low_sample_b is true (per D-15..D-17)."
    - "Cmd+K shows 'Compare with previous session' only on session detail views and only when a previous session exists (per D-07..D-09)."
    - "From /sessions/compare, Cmd+K treats search param `a` as current and navigates to compare-with-previous-of-a (per D-10)."
    - "When choosing session B via picker flows, candidates are scoped to side A’s project_key when available (per D-11..D-13)."
  artifacts:
    - path: frontend/src/components/panels/SessionCompareView.tsx
      provides: "Per-skill latency section rendering + delta suppression"
    - path: frontend/src/components/ui/CommandPalette.tsx
      provides: "Compare-with-previous command + gating behavior"
    - path: frontend/src/lib/api.ts
      provides: "sessionsPrevious() client for GET /api/sessions/{sid}/previous"
    - path: frontend/src/lib/queries.ts
      provides: "useSessionPrevious() hook treating 404 as non-error visibility gate"
  key_links:
    - from: frontend/src/components/ui/CommandPalette.tsx
      to: /api/sessions/{sid}/previous
      via: "useSessionPrevious query/mutation"
      pattern: "sessions.*previous"
    - from: frontend/src/components/ui/CommandPalette.tsx
      to: /sessions/compare
      via: "navigate with a and b search params"
      pattern: "/sessions/compare\\?a="
    - from: frontend/src/components/panels/SessionCompareView.tsx
      to: "compare response side.skill_latencies"
      via: "render section with per-skill rows"
      pattern: "skill_latencies"
---

<objective>
Implement the CMPR-06 compare UI depth (per-skill p95 latency section with low-sample delta suppression) and the CMPR-07 Cmd+K “Compare with previous session” action (gated by existence), plus compare-picker scoping by `project_key`.

Purpose: Make compare faster to interpret (latency deltas) and easier to access (Cmd+K previous-session), while preserving existing validateSearch hygiene and scoping rules.
Output: Frontend compare view updates, new `/previous` client + hook, command palette action, and unit tests.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-compare-depth-milestone-close/23-CONTEXT.md
@.planning/phases/23-compare-depth-milestone-close/23-RESEARCH.md
@.planning/phases/23-compare-depth-milestone-close/23-VALIDATION.md

Canonical refs (per 23-CONTEXT.md):
@frontend/src/routes/sessions_.compare.tsx
@frontend/src/components/panels/SessionCompareView.tsx
@frontend/src/components/ui/CommandPalette.tsx
@frontend/src/components/panels/SessionsTable.tsx
@frontend/src/components/panels/LiveSessionsCard.tsx
@frontend/src/components/panels/SkillRunsTable.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 23-02-01: Render per-skill p95 latency section in compare view (CMPR-06)</name>
  <read_first>
    - .planning/phases/23-compare-depth-milestone-close/23-CONTEXT.md (D-15..D-18)
    - frontend/src/components/panels/SessionCompareView.tsx (existing compare rendering composition)
    - frontend/src/routes/sessions_.compare.tsx (validateSearch behavior for a/b)
    - frontend/src/components/panels/__tests__/SessionCompareView.test.tsx (existing test harness and mock patterns)
  </read_first>
  <files>
    frontend/src/components/panels/SessionCompareView.tsx
    frontend/src/components/panels/__tests__/SessionCompareView.test.tsx
  </files>
  <action>
    Implement D-15..D-17 in the UI:
    - Read `skill_latencies` per side from the compare payload (one mapping per side).
    - Render a new section in `SessionCompareView` showing per-skill p95 latency for A and B.
    - If both sides are NOT low-sample, compute and display a delta column (B - A) in ms and as a directional indicator (no diff library; tabular-only per CMPR-05).
    - If either `low_sample_a` OR `low_sample_b` is true, suppress the delta calculation/display (per D-17) but still render raw values + a visible low-sample badge/message in the section header.

    Over-cap behavior (D-18): ensure this section still renders when `over_cap=true` (it should, because backend still includes skill_latencies). Do not conditionally hide this section based on over-cap.

    Tests:
    - Add a unit test where both low_sample flags are false and assert delta column appears with correct sign.
    - Add a unit test where `low_sample_a=true` and assert delta column is suppressed (e.g., renders '—') and a low-sample indicator is shown.
    - Add a unit test for `over_cap=true` that still renders the skill latency section when skill_latencies is present.
  </action>
  <acceptance_criteria>
    - Compare view displays per-skill p95 latency for both sides.
    - Delta is shown only when both sides are not low-sample.
    - Over-cap compare still shows the section.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm test -- SessionCompareView</automated>
  </verify>
  <done>
    `SessionCompareView` includes a stable, tabular per-skill latency section that respects low-sample suppression and over-cap semantics.
  </done>
</task>

<task type="auto">
  <name>Task 23-02-02: Add Cmd+K “Compare with previous session” (CMPR-07) + compare scoping improvements</name>
  <read_first>
    - .planning/phases/23-compare-depth-milestone-close/23-CONTEXT.md (D-07..D-14)
    - frontend/src/components/ui/CommandPalette.tsx (existing compare action + picker patterns)
    - frontend/src/components/panels/SessionsTable.tsx (existing compare entrypoint)
    - frontend/src/components/panels/LiveSessionsCard.tsx and frontend/src/components/panels/SkillRunsTable.tsx (session detail Sheet context; how selection is represented)
    - frontend/src/routes/sessions_.compare.tsx (search param sanitation; D-14)
    - frontend/src/components/ui/__tests__/CommandPalette.test.tsx (existing testing patterns)
  </read_first>
  <files>
    frontend/src/lib/api.ts
    frontend/src/lib/queries.ts
    frontend/src/components/ui/CommandPalette.tsx
    frontend/src/components/shell/ActiveSessionContext.tsx
    frontend/src/components/shell/AppShell.tsx
    frontend/src/routes/sessions_.compare.tsx
    frontend/src/components/panels/SessionsTable.tsx
    frontend/src/components/panels/LiveSessionsCard.tsx
    frontend/src/components/panels/SkillRunsTable.tsx
    frontend/src/components/ui/__tests__/CommandPalette.test.tsx
  </files>
  <action>
    Implement D-07..D-10 Cmd+K behavior and D-11..D-13 scoping behavior without introducing new dependencies.

    1) Backend client + hook:
    - Add `api.sessionsPrevious(sessionId)` (or similarly named) to `frontend/src/lib/api.ts` calling `GET /api/sessions/{sid}/previous` and returning `{session_id}`.
    - Add a TanStack Query hook in `frontend/src/lib/queries.ts` (e.g., `useSessionPrevious(sessionId | null)`) that:
      - is disabled when sessionId is null/undefined
      - treats a 404 `{error:"no previous session"}` as a non-error empty state (per D-04 + D-09 and Research Pattern 2)
      - treats other errors as real errors

    2) “Current session id” context so Cmd+K knows it’s on a session detail view:
    - Create `frontend/src/components/shell/ActiveSessionContext.tsx` exporting:
      - `<ActiveSessionProvider>` (stores `activeSessionId: string | null` + setter)
      - `useActiveSession()` hook (returns `{ activeSessionId, setActiveSessionId }`)
    - Wrap `CommandPalette` + `<main>` in this provider in `frontend/src/components/shell/AppShell.tsx` so the palette can read the active session id regardless of the current route.
    - Wire the value from the session-detail Sheets in BOTH places where the “Session details” Sheet exists:
      - `frontend/src/components/panels/LiveSessionsCard.tsx`: when a row is clicked and the Sheet opens, call `setActiveSessionId(sid)`; when Sheet closes (`onOpenChange` false), call `setActiveSessionId(null)`.
      - `frontend/src/components/panels/SkillRunsTable.tsx`: same behavior keyed off `openSid`.
    - Per D-07, the action must only be available on session detail views; use presence of `activeSessionId` as the gate.
    - Concrete done check for this sub-piece: opening either “Session details” Sheet sets `activeSessionId` to the opened sid; closing the Sheet clears it back to null.

    3) Add the Cmd+K action:
    - In `CommandPalette.tsx`, add a new command “Compare with previous session”.
    - Visibility rule (D-09): hide unless `useSessionPrevious(activeSessionId)` indicates a previous exists (200).
    - Action (D-08): navigate directly to `/sessions/compare?a=<current>&b=<previous>`.
    - Special-case D-10: when user is already on `/sessions/compare` and `a` exists, treat `a` as current and set `b` to previous-of-`a`. This implies CommandPalette needs access to current route/search params (or the compare route can pass it in via context).

    4) Compare picker scoping:
    - Implement D-11..D-13: when choosing session B via the existing picker/helper flow, scope candidates to side A’s `project_key` when available; if side A has empty/unknown project_key, do not scope (global list).
    - Keep D-14: invalid UUIDs for a/b are stripped/sanitized, not hard-failed.

    5) Unit tests:
    - Add a test ensuring the new action is NOT visible when there is no active session id.
    - Add a test ensuring 404 from /previous hides the action without showing an error UI.
    - Add a test ensuring when previous exists, selecting the action navigates to `/sessions/compare` with correct `a` and `b` params.
  </action>
  <acceptance_criteria>
    - Cmd+K action appears only on session detail views and only when a previous session exists.
    - On /sessions/compare, Cmd+K uses `a` as current to compute previous-of-a and navigates accordingly.
    - Compare picker candidate scoping uses project_key when available and falls back to global when not.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm test -- CommandPalette</automated>
  </verify>
  <done>
    Cmd+K provides a reliable “compare with previous” jump that is gated by existence and matches locked decisions, and compare picker scoping is project_key-consistent.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| UI→API | Frontend calls `/api/sessions/{sid}/previous` and `/api/sessions/compare` with user-derived ids |
| Route search→UI logic | `validateSearch` sanitation must prevent invalid UUIDs from creating error loops |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-23-04 | T (Tampering) | compare URL params `a`/`b` | mitigate | Continue stripping invalid UUIDs in `validateSearch` (D-14); never pass unsanitized ids into API calls. |
| T-23-05 | D (DoS) | command palette fetching previous on every keystroke | mitigate | Make `useSessionPrevious` disabled unless session id is present; cache with sensible staleTime; do not refetch on every palette open if not needed. |
| T-23-06 | I (Info disclosure) | previous-session endpoint 404 handling | accept | Treat 404 as empty-state; no sensitive info is exposed beyond existence of prior sessions within localhost-only app assumptions. |
</threat_model>

<verification>
- Frontend unit tests for compare view and command palette pass per Nyquist sampling map.
</verification>

<success_criteria>
- Compare page renders skill latency section with correct delta suppression logic.
- Cmd+K action is context-aware, hidden when no previous, and navigates correctly.
- Compare picker scoping aligns with project_key rules.
</success_criteria>

<output>
After completion, create `.planning/phases/23-compare-depth-milestone-close/23-02-SUMMARY.md`
</output>

