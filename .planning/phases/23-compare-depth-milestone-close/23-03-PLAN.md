---
phase: 23-compare-depth-milestone-close
plan: 03
type: execute
wave: 3
depends_on:
  - 23-02
files_modified:
  - frontend/tests/e2e/sessions-compare.spec.ts
autonomous: true
requirements:
  - CMPR-06
  - CMPR-07
user_setup: []

must_haves:
  truths:
    - "E2E compare flow still works and shows the per-skill latency section when data is available (CMPR-06)."
    - "Cmd+K compare-with-previous works from a session detail view when a previous session exists and is hidden otherwise (CMPR-07)."
  artifacts:
    - path: frontend/tests/e2e/sessions-compare.spec.ts
      provides: "Playwright coverage for per-skill latency section and Cmd+K previous-session compare"
  key_links:
    - from: "Cmd+K action"
      to: "/api/sessions/{sid}/previous"
      via: "request triggered by command palette (or preflight)"
      pattern: "/api/sessions/.*/previous"
    - from: "/sessions/compare"
      to: "/api/sessions/compare"
      via: "compare page load"
      pattern: "/api/sessions/compare"
---

<objective>
Extend Playwright coverage for Phase 23: ensure the compare page renders the per-skill latency section (with low-sample behavior) and Cmd+K “Compare with previous session” navigates correctly when available.

Purpose: Lock the integration behavior across real routing + command palette UX and prevent regressions as compare grows.
Output: Updated `sessions-compare.spec.ts` with Phase 23 assertions and state-aware conditional skip where needed.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-compare-depth-milestone-close/23-CONTEXT.md
@.planning/phases/23-compare-depth-milestone-close/23-VALIDATION.md
@frontend/tests/e2e/sessions-compare.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 23-03-01: Extend Playwright compare spec for skill latencies + Cmd+K previous-session</name>
  <read_first>
    - .planning/phases/23-compare-depth-milestone-close/23-CONTEXT.md (D-07..D-10, D-15..D-18)
    - frontend/tests/e2e/sessions-compare.spec.ts (existing compare navigation patterns and any preflight/skip logic)
  </read_first>
  <files>frontend/tests/e2e/sessions-compare.spec.ts</files>
  <action>
    Update the existing compare e2e spec to cover both CMPR-06 and CMPR-07 in a state-aware manner.

    CMPR-06 assertions:
    - Add an assertion that the per-skill latency section mounts on `/sessions/compare` (use a stable testid if introduced in the UI; otherwise use role/text anchors that are unlikely to change).
    - If the dev DB has no suitable skill latency data for the compared sessions, do not false-fail: add a backend preflight request (Playwright `request` fixture) to `/api/sessions/compare?a=<...>&b=<...>` and skip the section-content assertions if `skill_latencies` is empty for both sides. Still assert the compare page mounts.
    - Add an assertion that when low-sample flag(s) are true, the delta column is suppressed (e.g., '—'). If the compared response has both low-sample flags false, assert a delta value appears.

    CMPR-07 assertions:
    - From a session detail view (per D-07), open Cmd+K and confirm the “Compare with previous session” command is visible only when `/api/sessions/{sid}/previous` returns 200.
    - If `/previous` returns 404 `{error:"no previous session"}`, assert the command is hidden (and no error UI appears).
    - If it exists, select it and assert navigation to `/sessions/compare?a=<current>&b=<previous>`.

    Keep the suite stable:
    - Prefer a single API preflight and `test.skip()` with an actionable reason if the environment has no sessions suitable to exercise the previous-session path.
    - Do not introduce new test-only helpers outside this spec.
  </action>
  <acceptance_criteria>
    - Spec covers presence of the per-skill latency section and delta suppression behavior without false-failing on empty dev DBs.
    - Spec covers Cmd+K previous-session compare flow and the hidden-on-404 rule.
  </acceptance_criteria>
  <verify>
    <automated>cd frontend && pnpm test:e2e frontend/tests/e2e/sessions-compare.spec.ts</automated>
  </verify>
  <done>
    Phase 23’s compare-depth behaviors are covered by Playwright and run green in steady-state environments.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| Browser automation→App | E2E test interacts via UI and API preflight |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-23-07 | R (Repudiation) | flaky e2e assertions | mitigate | Use preflight + conditional skip for data-dependent assertions; assert only stable anchors for mount checks. |
</threat_model>

<verification>
- Run the e2e spec per Nyquist contract `23-VALIDATION.md`.
</verification>

<success_criteria>
- Playwright covers CMPR-06 and CMPR-07 end-to-end with stable selectors and clear skips for empty-state environments.
</success_criteria>

<output>
After completion, create `.planning/phases/23-compare-depth-milestone-close/23-03-SUMMARY.md`
</output>

