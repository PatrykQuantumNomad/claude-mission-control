---
phase: 23-compare-depth-milestone-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmc/api/routes/sessions.py
  - backend/cmc/api/schemas/sessions.py
  - backend/tests/test_sessions_router.py
autonomous: true
requirements:
  - CMPR-06
  - CMPR-07
user_setup: []

must_haves:
  truths:
    - "GET /api/sessions/{sid}/previous returns {session_id} for the most-recent prior ended session with the same project_key, else 404 {error:'no previous session'} (per D-01..D-06)."
    - "GET /api/sessions/compare?a=&b= includes per-side per-skill p95 latency in skill_latencies and exposes low_sample_a/low_sample_b flags with threshold 30 (per D-15..D-17)."
    - "Compare over-cap responses still include skill_latencies; only heavy tool-counts work is skipped (per D-18)."
  artifacts:
    - path: backend/cmc/api/routes/sessions.py
      provides: "/api/sessions/{sid}/previous endpoint + compare-side skill_latencies rollup"
    - path: backend/cmc/api/schemas/sessions.py
      provides: "SessionCompareSide extensions (skill_latencies + low_sample flag(s))"
    - path: backend/tests/test_sessions_router.py
      provides: "Backend tests covering CMPR-06 + CMPR-07 contracts"
  key_links:
    - from: backend/cmc/api/routes/sessions.py
      to: backend/cmc/api/schemas/sessions.py
      via: "SessionCompareResponse serialization"
      pattern: "SessionCompare"
    - from: backend/cmc/api/routes/sessions.py
      to: "SQLite otel_events body.record.attributes duration_ms"
      via: "combined skills+latency rollup SQL per side"
      pattern: "duration_ms"
    - from: backend/cmc/api/routes/sessions.py
      to: "sessions.project_key + ended_at ordering"
      via: "previous session resolver query"
      pattern: "project_key"
---

<objective>
Add CMPR-06 + CMPR-07 backend contracts: extend the compare payload with per-skill p95 latency per side (with low-sample gating) and implement the locked “previous session” resolver endpoint.

Purpose: Deepen compare without blowing the CMPR-04 SQL budget, and centralize previous-session semantics server-side to prevent frontend drift.
Output: Updated `/api/sessions/compare` response + new `GET /api/sessions/{sid}/previous` endpoint + backend tests.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-compare-depth-milestone-close/23-CONTEXT.md
@.planning/phases/23-compare-depth-milestone-close/23-RESEARCH.md
@.planning/phases/23-compare-depth-milestone-close/23-VALIDATION.md

Canonical refs (per 23-CONTEXT.md):
@backend/cmc/api/routes/sessions.py
@backend/cmc/api/schemas/sessions.py
@backend/cmc/db/models/sessions.py
@backend/tests/test_sessions_router.py
</context>

<tasks>

<task type="auto">
  <name>Task 23-01-01: Add per-skill p95 latency to compare sides (CMPR-06)</name>
  <read_first>
    - backend/cmc/api/routes/sessions.py (compare endpoint, `_build_compare_side`, over-cap handling, CMPR-04 SQL budget notes)
    - backend/cmc/api/schemas/sessions.py (SessionCompareResponse/Side DTOs)
    - backend/cmc/api/routes/skills.py (`_LATENCY_SQL` percentile + duration_ms extraction pattern to mirror)
    - backend/tests/test_sessions_router.py (existing compare tests, including over-cap assertions)
  </read_first>
  <files>
    backend/cmc/api/routes/sessions.py
    backend/cmc/api/schemas/sessions.py
    backend/tests/test_sessions_router.py
  </files>
  <action>
    Implement locked decisions D-15..D-18 by extending the compare response schema and compare-side build with an unambiguous response shape:
    - **Top-level on the compare response** (per D-16): `low_sample_a: bool` and `low_sample_b: bool`, where each is computed from that side’s duration sample count with threshold **30** (`sample_count < 30` → true; `>= 30` → false).
    - **Per-side on each compare side** (per D-15): `skill_latencies: { [skill_name: string]: number }` representing per-skill **p95 latency in ms** for that side.

    Critical contract pinning (per D-15 + D-16):
    - `low_sample_a` / `low_sample_b` MUST NOT be nested under the side objects; they MUST be top-level fields on the compare response.
    - `skill_latencies` MUST be nested under each side (A and B), not a top-level dict.
    - Delta suppression (D-17) is a frontend behavior, but backend must expose top-level `low_sample_*` flags so the UI can suppress deltas deterministically.

    Query-budget constraint (CMPR-04): do NOT add extra per-side SQL beyond what already exists for skills listing.
    Replace the existing per-side “skills used list” SQL with a combined rollup that returns both:
    - distinct `skill_name` set used in the session
    - per-skill p95 latency, extracted from OTEL event JSON (`duration_ms` in `otel_events.body.record.attributes`)
    - total duration sample_count used to compute `low_sample_a` / `low_sample_b` (sample_count < 30)

    Over-cap behavior (D-18): ensure that when `over_cap=true`, the handler still computes and returns `skill_latencies` and low-sample flags; the only work skipped under over-cap is the existing heavy tool-counts query.

    Update `backend/cmc/api/schemas/sessions.py` so the schema matches this contract:
    - Add `skill_latencies` to the per-side schema (SessionCompareSide or equivalent).
    - Add `low_sample_a` and `low_sample_b` to the top-level compare response schema (SessionCompareResponse or equivalent).

    Update `backend/tests/test_sessions_router.py`:
    - Assert response-shape invariants (D-15/D-16):
      - `low_sample_a` and `low_sample_b` are present at the TOP LEVEL and are booleans.
      - `skill_latencies` exists under each side and is a dict; assert it is NOT present at the top level.
    - Add boundary tests for low-sample threshold (D-16):
      - When a side has **29** duration samples → corresponding `low_sample_*` is true.
      - When a side has **30** duration samples → corresponding `low_sample_*` is false.
    - Add explicit test that over-cap responses still include per-side `skill_latencies` AND top-level low-sample flags (per D-18).
    - Strengthen CMPR-04 budget preservation: extend existing SQL counter assertions (or add a new assertion) proving that the compare request remains within the expected SQL query budget after replacing the skills query with the combined skills+latency rollup; ensure the over-cap path does not increase query count beyond the current over-cap behavior.
  </action>
  <acceptance_criteria>
    - `/api/sessions/compare` response shape is pinned:
      - top-level `low_sample_a` / `low_sample_b` booleans
      - per-side `skill_latencies` dicts
      - `skill_latencies` is not top-level.
    - Over-cap response keeps `skill_latencies` populated (only tool-counts are empty/omitted per existing behavior).
    - Low-sample threshold is exactly 30 and is pinned by a boundary test.
    - Compare SQL counter assertions still pass, explicitly covering the skill-latency addition (CMPR-04 budget preserved).
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/test_sessions_router.py -k "compare and (skill_latencies or over_cap or low_sample or sql)" -x</automated>
  </verify>
  <done>
    Compare endpoint returns per-skill p95 latency per side with low-sample gating metadata, without increasing compare SQL query count beyond the existing budgeted structure.
  </done>
</task>

<task type="auto">
  <name>Task 23-01-02: Implement GET /api/sessions/{sid}/previous resolver (CMPR-07)</name>
  <read_first>
    - .planning/phases/23-compare-depth-milestone-close/23-CONTEXT.md (D-01..D-06 contract)
    - backend/cmc/api/routes/sessions.py (UUID guard patterns, error shape conventions)
    - backend/cmc/db/models/sessions.py (project_key, ended_at, started_at)
    - backend/tests/test_sessions_router.py (router test harness patterns)
  </read_first>
  <files>
    backend/cmc/api/routes/sessions.py
    backend/tests/test_sessions_router.py
  </files>
  <action>
    Add a new endpoint `GET /api/sessions/{sid}/previous` that implements D-01..D-06 exactly:
    - D-01: same project is same `project_key` only (do not use cwd)
    - D-02: previous is most recent by `ended_at` strictly less than the current session's `ended_at`; ignore sessions with `ended_at IS NULL`
    - D-03: tie-breaker: highest `started_at` when multiple candidates tie on ended_at
    - D-04: if none, return 404 with `{error: "no previous session"}`
    - D-05: no other filters
    - D-06: response is ID-only `{session_id: <uuid>}`

    Ensure the current session is validated:
    - If the current session id does not exist, return 404 with the existing router error shape for missing session (do NOT reuse "no previous session").
    - If the current session has `ended_at IS NULL`, treat as having no previous for the purpose of D-02 (return 404 `{error:"no previous session"}`) because ordering is undefined when current ended_at is null.
    - If the current session has `project_key == ''` (empty sentinel), treat as having no previous by D-01 (return 404 `{error:"no previous session"}`) because “same project_key” cannot match meaningfully.

    Add backend tests in `backend/tests/test_sessions_router.py`:
    - Happy path: create three sessions with same project_key, ended_at times; ensure previous chooses correct one strictly less than current ended_at.
    - Ignore ended_at NULL candidates (both as potential previous and as current).
    - Tie-breaker: two candidates with same ended_at, different started_at; pick higher started_at.
    - No previous -> 404 body exact `{error:"no previous session"}`.
    - Different project_key sessions must be ignored even if ended_at ordering would match.
  </action>
  <acceptance_criteria>
    - Endpoint implements D-01..D-06 exactly, including response shape and 404 body.
    - Endpoint never selects an active session (ended_at NULL) as previous.
    - Tie-breaker behavior is pinned by a deterministic unit test.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/test_sessions_router.py -k "previous" -x</automated>
  </verify>
  <done>
    `GET /api/sessions/{sid}/previous` is fully specified and green under pytest with deterministic ordering behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| client→API | Untrusted path/query params (`sid`, `a`, `b`) enter backend routing |
| API→DB | Backend composes SQL/ORM queries against SQLite; must avoid injection and excessive queries |
| OTEL JSON→API response | Untrusted event payloads contribute to derived metrics (duration_ms extraction) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-23-01 | T (Tampering) | session id inputs (`sid`, `a`, `b`) | mitigate | Keep strict UUID validation/stripping patterns; never interpolate ids into SQL strings without parameters. |
| T-23-02 | I (Info disclosure) | compare payload (skill_latencies) | mitigate | Only emit aggregated per-skill p95 + flags; do not expose raw event JSON or any filesystem-shaped fields; extend tests to ensure no extra keys. |
| T-23-03 | D (DoS) | compare query budget | mitigate | Replace (not add) per-side skills query; keep over-cap behavior skipping only tool-counts; add tests asserting presence under over-cap. |
</threat_model>

<verification>
- Backend compare and previous-session tests green per Nyquist sampling map in `23-VALIDATION.md`.
</verification>

<success_criteria>
- Backend endpoints implement locked decisions (D-01..D-06, D-15..D-18) with regression tests.
- Over-cap compare still includes skill_latencies.
</success_criteria>

<output>
After completion, create `.planning/phases/23-compare-depth-milestone-close/23-01-SUMMARY.md`
</output>

