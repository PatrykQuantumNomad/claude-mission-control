---
phase: 23-compare-depth-milestone-close
plan: 04
type: execute
wave: 4
depends_on:
  - 23-03
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/milestones/v1.2-ROADMAP.md
autonomous: true
requirements:
  - CMPR-06
  - CMPR-07
user_setup: []

must_haves:
  truths:
    - "All suites are green at milestone close: backend pytest, frontend vitest, and Playwright e2e."
    - "REQUIREMENTS.md marks CMPR-06 and CMPR-07 as complete with traceability notes."
    - "ROADMAP is archive-ready and v1.2 is captured in milestones as v1.2-ROADMAP.md."
  artifacts:
    - path: .planning/milestones/v1.2-ROADMAP.md
      provides: "Frozen v1.2 roadmap snapshot for archive"
    - path: .planning/REQUIREMENTS.md
      provides: "Traceability updated to reflect CMPR-06/07 completion"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/milestones/v1.2-ROADMAP.md"
      via: "snapshot copy at milestone close"
      pattern: "v1.2-ROADMAP.md"
---

<objective>
Close the v1.2 milestone: run the full validation gates, update traceability, and archive the roadmap snapshot.

Purpose: Make v1.2 “Depth & Polish” shippable and self-contained for future onboarding and audits.
Output: Green test gates, updated REQUIREMENTS/ROADMAP, and `.planning/milestones/v1.2-ROADMAP.md`.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-compare-depth-milestone-close/23-VALIDATION.md

Baseline policy reference:
@.planning/phases/18-polish-carry-forward-cleanup/BASELINE.md
</context>

<tasks>

<task type="auto">
  <name>Task 23-04-01: Run milestone-close validation gates</name>
  <read_first>
    - .planning/phases/23-compare-depth-milestone-close/23-VALIDATION.md (sampling + full-suite commands)
    - .planning/phases/18-polish-carry-forward-cleanup/BASELINE.md (baseline floors and warning rules)
  </read_first>
  <files>
    .planning/phases/23-compare-depth-milestone-close/23-VALIDATION.md
    .planning/phases/18-polish-carry-forward-cleanup/BASELINE.md
    .planning/phases/23-compare-depth-milestone-close/23-04-SUMMARY.md
  </files>
  <action>
    Run the full suite gates (no watch mode, no partial runs) and ensure results satisfy the baseline rules recorded in Phase 18 BASELINE.md:
    - Backend: run the Phase 23 quick gate repeatedly during execution; at close, run full backend suite as required by repo scripts (at minimum ensure `tests/test_sessions_router.py` is green).
    - Frontend: run `pnpm test` (vitest) and `pnpm test:e2e frontend/tests/e2e/sessions-compare.spec.ts`.
    - If `cmc doctor` is available as a CLI command in this repo, run it and ensure it returns clean (ROADMAP Phase 23 success criterion #3).

    Nyquist close-out: once all Wave 0 items in `23-VALIDATION.md` are satisfied (no `MISSING` automated verifications remain and the per-task verification map is actionable), update `23-VALIDATION.md` frontmatter to:
    - `nyquist_compliant: true`
    - `wave_0_complete: true`

    Record any baseline deltas in the plan SUMMARY as part of phase close (but do not create additional artifacts beyond the required SUMMARY).
  </action>
  <acceptance_criteria>
    - Backend pytest relevant suites are green; no new datetime.utcnow warnings introduced.
    - Frontend vitest and Playwright e2e are green for the session compare spec.
    - If `cmc doctor` exists, it runs clean.
    - `23-VALIDATION.md` frontmatter is flipped to `nyquist_compliant: true` and `wave_0_complete: true` once Wave 0 requirements are satisfied.
  </acceptance_criteria>
  <verify>
    <automated>cd backend && pytest tests/test_sessions_router.py -x -q</automated>
    <automated>cd frontend && pnpm test && pnpm test:e2e frontend/tests/e2e/sessions-compare.spec.ts</automated>
    <automated>if command -v cmc >/dev/null 2>&1; then cmc doctor; fi</automated>
  </verify>
  <done>
    Validation gates are green and meet Phase 18 baseline policy.
  </done>
</task>

<task type="auto">
  <name>Task 23-04-02: Update traceability + archive v1.2 roadmap snapshot</name>
  <read_first>
    - .planning/REQUIREMENTS.md (traceability table + status conventions)
    - .planning/ROADMAP.md (Phase 23 details + milestone framing)
  </read_first>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/milestones/v1.2-ROADMAP.md
  </files>
  <action>
    Perform milestone close bookkeeping:
    - Update `.planning/REQUIREMENTS.md`:
      - Mark `CMPR-06` and `CMPR-07` complete (checkbox and traceability row) with concise references to the Phase 23 plans and/or key commits created during execution.
      - Ensure the v1.2 traceability section is consistent (counts, mapped rows). Do not edit unrelated deferred items.
    - Update `.planning/ROADMAP.md` Phase 23:
      - Set `**Plans:** 4 plans` and list the plan filenames + one-line objectives (matching the plans created here).
      - Mark Phase 23 as complete once executed and verified (the executor will do this at the end).
    - Create `.planning/milestones/v1.2-ROADMAP.md` as an archive snapshot:
      - Copy the current `.planning/ROADMAP.md` content as-is at milestone close.
      - Ensure the milestone header reflects v1.2 shipped/closed state consistent with the repo’s prior milestone snapshot style.
  </action>
  <acceptance_criteria>
    - REQUIREMENTS.md shows CMPR-06 and CMPR-07 complete and traceability is internally consistent.
    - ROADMAP Phase 23 section references the 4 plans created for Phase 23.
    - v1.2 roadmap snapshot exists at `.planning/milestones/v1.2-ROADMAP.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/milestones/v1.2-ROADMAP.md</automated>
    <automated>rg -n "\\*\\*Plans:\\*\\*\\s*4\\s+plans" .planning/ROADMAP.md</automated>
    <automated>rg -n "23-01-PLAN\\.md|23-02-PLAN\\.md|23-03-PLAN\\.md|23-04-PLAN\\.md" .planning/ROADMAP.md</automated>
    <automated>rg -n "CMPR-06|CMPR-07" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    Milestone close bookkeeping is complete and the roadmap snapshot is archived.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---------|-------------|
| CI-like checks→repo state | Test gates reflect current working tree; must avoid false green from stale servers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|----------|----------|-----------|-------------|-----------------|
| T-23-08 | R (Repudiation) | milestone close artifacts | mitigate | Archive ROADMAP snapshot and update REQUIREMENTS traceability so future readers can verify what shipped. |
| T-23-09 | D (DoS) | long-running full suites | accept | This is final-phase closure; running suites is required to ensure ship-readiness. |
</threat_model>

<verification>
- Phase close requires green suites and updated traceability per ROADMAP Phase 23 success criterion #3.
</verification>

<success_criteria>
- CMPR-06 and CMPR-07 delivered and recorded as complete; v1.2 milestone is archive-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/23-compare-depth-milestone-close/23-04-SUMMARY.md`
</output>

