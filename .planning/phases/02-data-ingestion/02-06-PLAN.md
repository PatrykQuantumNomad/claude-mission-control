---
phase: 02-data-ingestion
plan: 06
type: execute
wave: 5
depends_on: ["02-05"]
files_modified:
  - .planning/phases/02-data-ingestion/02-06-SMOKE.md
autonomous: false
requirements:
  - INGST-01
  - INGST-04
  - INGST-07
  - INGST-09
  - INGST-10
tags:
  - smoke-test
  - integration
  - phase-2
  - checkpoint

must_haves:
  truths:
    - "Real `uvicorn --app-dir backend cmc.app:create_app --factory --host 127.0.0.1 --port 8765` boot from repo root produces an `ingest.boot_sync` log line in stdout within ~5s"
    - "After boot, querying SQLite directly confirms `sessions` table has rows from real `~/.claude/projects/` JSONL files (count > 0 if user has Claude Code history)"
    - "POSTing the recorded sample OTLP/HTTP JSON to `/v1/logs` returns 200 with `{}` and inserts a row in `otel_events`"
    - "POSTing the recorded sample OTLP/HTTP JSON to `/v1/metrics` returns 200 with `{}` and inserts rows in `otel_metrics` (one per dataPoint)"
    - "POSTing to `/api/sync` returns the summary dict and triggers an immediate re-scrape (verifiable by reading the new `synced_at` value on a sessions row)"
    - "Server shuts down cleanly with Ctrl+C — no zombie task warnings, no `RuntimeError`s about disposed engines, no 'Task was destroyed but it is pending' warnings"
    - "After shutdown, `data/cmc.db` (and WAL siblings) remain at the repo root (not under `backend/data/`) — Phase 1 BLOCKER 1 regression check"
  artifacts:
    - path: ".planning/phases/02-data-ingestion/02-06-SMOKE.md"
      provides: "Recorded smoke-test transcript for Phase 2 (analogous to 01-VERIFICATION.md). Includes: command output, curl outputs, sample sqlite queries, MCP attribute spot-check."
      min_lines: 30
  key_links:
    - from: ".planning/phases/02-data-ingestion/02-06-SMOKE.md"
      to: "Phase 2 ROADMAP success criteria 1-5"
      via: "Each success criterion gets a recorded verification step"
      pattern: "INGST-"
---

<objective>
Validate Phase 2 end-to-end against a real Claude Code installation: boot the server, watch the boot-time sync ingest real `~/.claude/projects/` data, exercise the OTLP endpoints with a recorded sample payload, trigger a manual sync, and confirm clean shutdown.

Purpose: Automated tests prove each component works in isolation; this checkpoint proves the integrated system works in the user's actual environment. It's the analog of Phase 1's browser-smoke checkpoint, scoped to Phase 2's data-flow guarantees.

Output: A recorded smoke transcript (`02-06-SMOKE.md`) capturing command outputs, sqlite query results, and OTLP curl exchanges. The user-facing checkpoint task confirms each of the five Phase 2 ROADMAP success criteria observed working with real data.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-data-ingestion/02-RESEARCH.md
@.planning/phases/02-data-ingestion/02-05-SUMMARY.md
@.planning/phases/01-foundation-database/01-07-PLAN.md

<interfaces>
The canonical run command (pinned by Phase 1):
  `uvicorn --app-dir backend cmc.app:create_app --factory --host 127.0.0.1 --port 8765`

The user's `~/.claude/projects/` directory is verified to contain real session JSONLs (research §1 inspected three of them on this exact machine). Phase 2's boot sync will pick them up.

Sample OTLP payloads are pre-built — write them to disk in this plan and curl them.

After this checkpoint passes, Phase 2 is ready for the verifier (`/gsd:verify-work` or just done if not used).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prepare two sample OTLP payload files for the smoke test</name>
  <files>.planning/phases/02-data-ingestion/02-06-SMOKE.md</files>
  <action>
Create `.planning/phases/02-data-ingestion/02-06-SMOKE.md` as the checkpoint scratchpad. The executor will record commands and outputs into this file as the manual smoke runs.

Initial structure:

```markdown
# Phase 2 Smoke Test (Plan 02-06)

This document records the end-to-end verification of Phase 2 against real
Claude Code data + recorded OTLP samples.

Run from REPO ROOT.

## Sample OTLP/HTTP JSON payloads

The two files below are used in Steps 3 and 4. Save them to /tmp/ before running curl.

### /tmp/otlp_logs_sample.json
```json
{
  "resourceLogs": [{
    "resource": {"attributes": [
      {"key": "service.name", "value": {"stringValue": "claude-code"}}
    ]},
    "scopeLogs": [{
      "scope": {"name": "com.anthropic.claude_code.events"},
      "logRecords": [
        {
          "timeUnixNano": "1745601281385000000",
          "body": {"stringValue": "tool_result"},
          "attributes": [
            {"key": "event.name", "value": {"stringValue": "claude_code.tool_result"}},
            {"key": "session_id", "value": {"stringValue": "smoke-1"}},
            {"key": "tool_name", "value": {"stringValue": "Bash"}},
            {"key": "duration_ms", "value": {"intValue": "1234"}}
          ]
        },
        {
          "timeUnixNano": "1745601282385000000",
          "body": {"stringValue": "tool_result"},
          "attributes": [
            {"key": "event.name", "value": {"stringValue": "claude_code.tool_result"}},
            {"key": "session_id", "value": {"stringValue": "smoke-1"}},
            {"key": "tool_name", "value": {"stringValue": "mcp__myserver__do_thing"}}
          ]
        }
      ]
    }]
  }]
}
```

### /tmp/otlp_metrics_sample.json
```json
{
  "resourceMetrics": [{
    "resource": {"attributes": [
      {"key": "service.name", "value": {"stringValue": "claude-code"}}
    ]},
    "scopeMetrics": [{
      "scope": {"name": "com.anthropic.claude_code.metrics"},
      "metrics": [
        {
          "name": "claude_code.token.usage",
          "unit": "tokens",
          "sum": {
            "aggregationTemporality": 2,
            "isMonotonic": true,
            "dataPoints": [{
              "timeUnixNano": "1745601281385000000",
              "asInt": "47855",
              "attributes": [
                {"key": "type", "value": {"stringValue": "input"}}
              ]
            }]
          }
        },
        {
          "name": "claude_code.session.count",
          "gauge": {
            "dataPoints": [{
              "timeUnixNano": "1745601281385000000",
              "asInt": "3"
            }]
          }
        }
      ]
    }]
  }]
}
```

## Smoke transcript

(executor fills below as the smoke runs)

### Step 1 — Boot
```
$ uvicorn --app-dir backend cmc.app:create_app --factory --host 127.0.0.1 --port 8765
... (paste stdout including the `ingest.boot_sync` line)
```

### Step 2 — Real data ingestion check
```
$ sqlite3 data/cmc.db "SELECT count(*), max(synced_at) FROM sessions;"
... (count should be > 0; synced_at should be recent)
```

### Step 3 — POST /v1/logs
```
$ curl -s -X POST http://127.0.0.1:8765/v1/logs \
    -H 'Content-Type: application/json' \
    -d @/tmp/otlp_logs_sample.json
... (expect: {})

$ sqlite3 data/cmc.db "SELECT event_name, attrs_mcp_server, attrs_mcp_tool FROM otel_events ORDER BY id DESC LIMIT 2;"
... (expect: 2 rows; one with attrs_mcp_server='myserver' attrs_mcp_tool='do_thing')
```

### Step 4 — POST /v1/metrics
```
$ curl -s -X POST http://127.0.0.1:8765/v1/metrics \
    -H 'Content-Type: application/json' \
    -d @/tmp/otlp_metrics_sample.json
... (expect: {})

$ sqlite3 data/cmc.db "SELECT metric_name, kind, value FROM otel_metrics ORDER BY id DESC LIMIT 3;"
... (expect: counter row + gauge row)
```

### Step 5 — POST /api/sync
```
$ curl -s -X POST http://127.0.0.1:8765/api/sync
... (expect: {"status":"ok","files_seen":N,...})
```

### Step 6 — Clean shutdown
- Press Ctrl+C in the uvicorn terminal.
- Confirm: no `Task was destroyed but it is pending` warnings.
- Confirm: no `RuntimeError` from disposed engines.

### Step 7 — Path resolution regression
```
$ ls -la data/cmc.db data/cmc.db-wal data/cmc.db-shm
$ ls -la backend/data/cmc.db 2>/dev/null && echo "REGRESSION" || echo "OK"
... (expect: WAL siblings present at repo root; no backend/data/cmc.db)
```
```

The executor fills in the actual outputs in the next task; this task just creates the scaffold + sample payload files documented in the markdown.
  </action>
  <verify>
    <automated>test -f /Users/patrykattc/work/git/claude-mission-control/.planning/phases/02-data-ingestion/02-06-SMOKE.md</automated>
  </verify>
  <done>
- `.planning/phases/02-data-ingestion/02-06-SMOKE.md` scaffold exists with embedded sample OTLP payloads ready to copy to /tmp/.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual smoke — boot the server, exercise OTLP endpoints, /api/sync, verify all 5 Phase 2 success criteria with real data</name>
  <what-built>
Phase 2 implementation is complete:
- JSONL parser (Plan 02-02)
- OTLP /v1/logs and /v1/metrics endpoints (Plan 02-03)
- Repository upserts + scheduler with periodic loop (Plan 02-04)
- Lifespan boot-sync + manual /api/sync route (Plan 02-05)

Automated tests cover each component in isolation. This checkpoint exists because
Phase 2's success criteria require seeing real data flow — the user's actual
~/.claude/projects/ JSONL files becoming rows in `sessions` + `tools` + `token_usage`
+ `otel_events` + `otel_metrics` tables.
  </what-built>
  <how-to-verify>
1. **From the REPO ROOT**, copy the two sample payloads from `02-06-SMOKE.md` to `/tmp/`:
   ```bash
   # Open the SMOKE.md, copy each fenced JSON block to the corresponding /tmp/ file:
   #   /tmp/otlp_logs_sample.json
   #   /tmp/otlp_metrics_sample.json
   ```

2. **Boot the server** using the CANONICAL command:
   ```bash
   # From repo root:
   uvicorn --app-dir backend cmc.app:create_app --factory --host 127.0.0.1 --port 8765
   ```
   Watch stdout for an `ingest.boot_sync` log line within ~5 seconds. Record the line
   in `02-06-SMOKE.md` Step 1.

3. **Verify real-data ingestion** (in a second terminal, from repo root):
   ```bash
   sqlite3 data/cmc.db "SELECT count(*), max(synced_at) FROM sessions;"
   ```
   - Expected: `count > 0` (you have real Claude Code sessions on disk)
   - `synced_at` is within the last few minutes
   - Record the output in `02-06-SMOKE.md` Step 2

4. **POST /v1/logs**:
   ```bash
   curl -s -X POST http://127.0.0.1:8765/v1/logs \
       -H 'Content-Type: application/json' \
       -d @/tmp/otlp_logs_sample.json
   ```
   - Expected response: `{}` (empty JSON, status 200)
   - Then: `sqlite3 data/cmc.db "SELECT event_name, attrs_mcp_server, attrs_mcp_tool FROM otel_events ORDER BY id DESC LIMIT 2;"`
   - Expected: 2 rows. The MCP record has `attrs_mcp_server="myserver"` and `attrs_mcp_tool="do_thing"`.
   - Record both outputs in `02-06-SMOKE.md` Step 3

5. **POST /v1/metrics**:
   ```bash
   curl -s -X POST http://127.0.0.1:8765/v1/metrics \
       -H 'Content-Type: application/json' \
       -d @/tmp/otlp_metrics_sample.json
   ```
   - Expected response: `{}`
   - Then: `sqlite3 data/cmc.db "SELECT metric_name, kind, value FROM otel_metrics ORDER BY id DESC LIMIT 3;"`
   - Expected: counter row (`claude_code.token.usage`, value=47855) + gauge row (`claude_code.session.count`, value=3)
   - Record both outputs in `02-06-SMOKE.md` Step 4

6. **POST /api/sync** (manual sync trigger):
   ```bash
   curl -s -X POST http://127.0.0.1:8765/api/sync
   ```
   - Expected: `{"status":"ok","files_seen":N,"files_updated":M,"errors":0,"duration_ms":D}` with sensible values
   - Then verify the sync actually ran: `sqlite3 data/cmc.db "SELECT max(synced_at) FROM sessions;"` should show a timestamp newer than the boot-sync timestamp from Step 2
   - Record outputs in `02-06-SMOKE.md` Step 5

7. **Clean shutdown**: In the uvicorn terminal, press Ctrl+C. Watch the stderr/stdout for:
   - NO `Task was destroyed but it is pending` warnings
   - NO `RuntimeError` from a disposed engine being accessed
   - Final output: clean exit
   Record what you saw (or didn't see) in `02-06-SMOKE.md` Step 6.

8. **Path resolution regression check**:
   ```bash
   ls -la data/cmc.db data/cmc.db-wal data/cmc.db-shm
   ls -la backend/data/cmc.db 2>/dev/null && echo "REGRESSION" || echo "OK"
   ```
   - Expected: WAL siblings present at repo-root `data/`; no `backend/data/cmc.db`
   - Record in `02-06-SMOKE.md` Step 7

If ALL of the above pass, reply "approved" — Phase 2 is complete.

If anything fails, describe what failed and what you saw vs. expected.
The orchestrator will route the fix through `/gsd:plan-phase 2 --gaps` planning.
  </how-to-verify>
  <resume-signal>Reply "approved" if all seven steps pass, or describe the failure.</resume-signal>
</task>

</tasks>

<verification>
- All seven smoke steps documented and recorded in `02-06-SMOKE.md`.
- Real `~/.claude/projects/` data ingested by boot sync (verified via sqlite count).
- OTLP /v1/logs and /v1/metrics return 200 + persist rows; MCP attributes extracted.
- POST /api/sync triggers a re-sync (verified by `synced_at` advancing).
- Clean Ctrl+C shutdown (no zombie warnings, no engine errors).
- DB still lives at repo-root `data/` — Phase 1 BLOCKER 1 not regressed.
</verification>

<success_criteria>
Phase 2 ROADMAP success criteria (all 5) hold against real data:
1. ✅ Sessions from ~/.claude/projects/ in sessions table with correct token totals (Step 2 + 5)
2. ✅ /v1/logs stores events with mcp_server/tool extracted (Step 4)
3. ✅ /v1/metrics stores metrics (Step 5)
4. ✅ Corrupted JSONL lines skipped without crashing (covered by Plan 02-02 + 02-04 tests; survives Step 1 boot if any real file has a partial last line)
5. ✅ POST /api/sync triggers re-scrape and returns success (Step 6)

Phase 2 ready for verifier or roadmap close-out.
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-ingestion/02-06-SUMMARY.md` summarizing:
- Smoke transcript filename: `02-06-SMOKE.md`
- All 5 ROADMAP success criteria verified against real data
- Total Phase 2 test count (after Plan 02-05): ~50 tests passing
- Phase 2 closure status: ready for `/gsd:verify-work` or roadmap update
- Note any deferred items surfaced during smoke (e.g., if `OTEL_LOG_TOOL_DETAILS` was off so MCP fallback path was the only one exercised on real data)
</output>
</content>
</invoke>