---
phase: 27-per-route-adoption-ii-skills-cost-alerts-tech-debt
plan: 09
type: execute
wave: 7
depends_on:
  - "27-03"
  - "27-04"
  - "27-05"
  - "27-06"
  - "27-07"
  - "27-08"
files_modified:
  - frontend/tests/e2e/v13-tail-routes.spec.ts
  - frontend/tests/e2e/v13-a11y.spec.ts
  - frontend/tests/e2e/v13-portal-containment.spec.ts
  - frontend/tests/e2e/v13-truncation.spec.ts
  - frontend/tests/e2e/v13-time-picker.spec.ts
  - frontend/tests/e2e/v13-visual-capture.spec.ts
  - frontend/tests/e2e/alerts.spec.ts
  - frontend/tests/e2e/cost-dashboard.spec.ts
  - frontend/tests/e2e/skills-detail.spec.ts
  - .planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-VISUAL-CHECK.md
autonomous: false

must_haves:
  truths:
    - "v13-tail-routes.spec.ts ships covering SC#1 (skills detail bounded + 4-panel + truncation + global picker re-anchor)"
    - "v13-tail-routes.spec.ts covers SC#2 (/cost picker re-query + CompareToggle round-trip + project truncation)"
    - "v13-tail-routes.spec.ts covers SC#3 (compare picker uses project_key — symlink + realpath edge cases mocked)"
    - "v13-tail-routes.spec.ts covers SC#4 (AlertRuleForm loads metrics from useAlertMetrics; FALLBACK_KNOWN_METRICS grep returns 0)"
    - "v13-tail-routes.spec.ts covers SC#5 (NL composer 503 → honest copy + Retry button visible; Retry re-fires mutation)"
    - "v13-a11y.spec.ts extended for /skills, /skills/$name, /cost, /alerts chrome — 0 NEW Phase-27-attributable axe violations"
    - "v13-portal-containment.spec.ts extended for any new Sheet/Popover on tail routes (likely none; CompareToggle dropdown if applicable)"
    - "v13-truncation.spec.ts extended for long skill name on /skills/$name + long project_key on /cost"
    - "v13-time-picker.spec.ts extended with re-anchor probes on 4 tail-end routes"
    - "v13-visual-capture.spec.ts adds 24 NEW Phase 27 surface PNGs (4 routes × 3 densities × 2 themes) — total visual matrix becomes 96 + 24 = 120 PNGs"
    - "Lighthouse 9/9 PASS preserved (no perf regression — Phase 27 is consumer-only, zero new deps)"
    - "ResponsiveContainer count UNCHANGED at the Phase 24 lock baseline (DeltaPill column from 27-05 is HTML not recharts)"
    - "Backend pytest baseline updated for Plan 27-02 (+3 project_key tests) and Plan 27-07 (-N sync test + 2 contract tests) — exact delta captured in VISUAL-CHECK.md evidence"
    - "Operator verdict captured in 27-VISUAL-CHECK.md (PASS / CONDITIONAL / FAIL) — checkpoint:human-verify task"
    - "27-VISUAL-CHECK.md pre-positions an Accepted Exception for CostForecastCard MTD-only contract — SC#2's 're-query' literal is satisfied as 're-render' for that card (no range parameter on the MTD endpoint per cost.tsx:56-58 + Phase 19/20 design)"
  artifacts:
    - path: ".planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-VISUAL-CHECK.md"
      provides: "Operator-driven visual review scaffold + automated evidence + operator-signed verdict (PASS/CONDITIONAL/FAIL)"
      contains: "Operator verdict"
    - path: "frontend/tests/e2e/v13-tail-routes.spec.ts"
      provides: "SC#1-#5 e2e integration coverage for Phase 27"
      contains: "TDBT-01"
  key_links:
    - from: "27-VISUAL-CHECK.md"
      to: "ROADMAP.md Phase 27 success criteria 1-5"
      via: "Operator-signed mapping table — each criterion to its evidence source"
      pattern: "Success Criterion"
---

<objective>
Phase 27 close gate. Author the Playwright e2e + axe + Lighthouse + visual-capture cascade that proves all 5 ROADMAP success criteria + all 3 REQ-IDs (TDBT-01, TDBT-02, TDBT-03) ship green. Final artifact: `27-VISUAL-CHECK.md` with operator-signed PASS verdict (or CONDITIONAL / FAIL with documented Accepted Exceptions).

Purpose: Follows the POLI-09 pattern established Phase 24 plan 07 + Phase 25 plan 11 + Phase 26 plan 09 (same evidence-assembly + operator-checkpoint structure). The pattern is FORMAL — copy the precedent, adapt to Phase 27 surfaces.

This plan is the ONLY plan in Phase 27 with `autonomous: false` (operator visual checkpoint at the end). Tasks 1-2 are autonomous (cascade authoring + automated runs); Task 3 is the operator checkpoint.

Output: 1 NEW v13-tail-routes.spec.ts + 5 spec extensions + visual-capture matrix expansion (24 NEW PNGs) + 1 NEW 27-VISUAL-CHECK.md (operator verdict file).
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-RESEARCH.md

# Precedent close-gate evidence files (FOLLOW THIS PATTERN)
@.planning/phases/24-shell-density-containment-primitives/24-VISUAL-CHECK.md
@.planning/phases/25-saved-views-backend-frontend/25-VISUAL-CHECK.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-VISUAL-CHECK.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-09-PLAN.md

# Existing e2e spec families to extend
@frontend/tests/e2e/v13-a11y.spec.ts
@frontend/tests/e2e/v13-portal-containment.spec.ts
@frontend/tests/e2e/v13-truncation.spec.ts
@frontend/tests/e2e/v13-time-picker.spec.ts
@frontend/tests/e2e/v13-visual-capture.spec.ts
@frontend/tests/e2e/alerts.spec.ts
@frontend/tests/e2e/cost-dashboard.spec.ts
@frontend/tests/e2e/skills-detail.spec.ts
@frontend/playwright.config.ts

# Lighthouse config
@frontend/lighthouserc.json

# All Phase 27 plan SUMMARYs (after Plans 01-08 ship)
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-01-SUMMARY.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-02-SUMMARY.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-03-SUMMARY.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-04-SUMMARY.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-05-SUMMARY.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-06-SUMMARY.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-07-SUMMARY.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v13-tail-routes.spec.ts (SC#1-5 e2e) + extend 5 existing v13-* spec families for Phase 27 surfaces</name>
  <files>frontend/tests/e2e/v13-tail-routes.spec.ts, frontend/tests/e2e/v13-a11y.spec.ts, frontend/tests/e2e/v13-portal-containment.spec.ts, frontend/tests/e2e/v13-truncation.spec.ts, frontend/tests/e2e/v13-time-picker.spec.ts, frontend/tests/e2e/alerts.spec.ts, frontend/tests/e2e/cost-dashboard.spec.ts, frontend/tests/e2e/skills-detail.spec.ts</files>
  <action>
**CREATE `frontend/tests/e2e/v13-tail-routes.spec.ts`** — NEW Playwright spec. Covers SC#1-#5 + TDBT-01..03 e2e integration. Mirror the structure of `v13-time-picker.spec.ts` (Phase 26 Plan 09) — `test.describe('Phase 27 / tail routes + tech debt', ...)` with one `test()` per success criterion + a few targeted edge cases.

Required test cases:

```
test('SC#1: /skills/$name long skill name renders bounded with TruncatedCell + tooltip', async ({ page }) => { ... })
test('SC#1: /skills/$name 4-panel set is bounded (SkillProjectsTable + SkillRunsTable + SkillLatencySnapshot + SkillTimeline)', async ({ page }) => { ... })
test('SC#1: /skills/$name global TimePicker re-anchors panels (set time_from=now-7d → snapToSkillRange = 14d → network capture)', async ({ page }) => { ... })
test('SC#1: /skills/$name density toggle flips data-density attribute + panels re-spaced without React re-render', async ({ page }) => { ... })
test('SC#1: /skills/$name route-local ?range= preserved alongside global picker (URL contract append-only)', async ({ page }) => { ... })
test('SC#1: /skills/$name global picker WINS WHEN PRESENT over route-local ?range= (LOCKED OPERATOR DECISION 2)', async ({ page }) => { ... })

test('SC#2: /cost both panels render bounded; root section has cmc-page--bounded', async ({ page }) => { ... })
test('SC#2: /cost set TimePicker to last 30 days → URL gains ?time_from=now-30d&time_to=now → CostByProjectCard refetches with range=30d', async ({ page }) => { ... })
test('SC#2: /cost saved view round-trips time_from/time_to', async ({ page }) => { ... })
test('SC#2: /cost CompareToggle round-trip — toggle on cost-by-project → URL gains compare_panels=cost-by-project', async ({ page }) => { ... })
test('SC#2: /cost CostByProjectCard project column truncates cleanly via TruncatedCell', async ({ page }) => { ... })

test('SC#3 / TDBT-01: GET /api/sessions response includes project_key (12-char hex on every row)', async ({ request }) => { ... })
test('SC#3 / TDBT-01: GET /api/sessions/compare response includes a.project_key + b.project_key', async ({ request }) => { ... })
test('SC#3 / TDBT-01: compare picker filters candidates by project_key, not cwd (mock: same cwd + different project_key → excluded)', async ({ page }) => { ... })
test('SC#3 / TDBT-01: compare picker description does NOT leak the 12-char project_key hex', async ({ page }) => { ... })

test('SC#4 / TDBT-02: /alerts AlertRuleForm metric vocabulary loads from useAlertMetrics (network capture)', async ({ page }) => { ... })
test('SC#4 / TDBT-02: AlertRuleForm <select> renders disabled with "Loading metric vocabulary…" during fetch (throttled network)', async ({ page }) => { ... })
test('SC#4 / TDBT-02: FALLBACK_KNOWN_METRICS source grep returns 0 (build-time invariant)', async () => {
  // Use fs.readFile to assert grep on AlertRuleForm.tsx source
})
test('SC#4 / TDBT-02: backend test_alerts_metrics_contract.py replaces test_alerts_metrics_sync.py (file-existence invariants)', async () => {
  // Use fs.exists to assert sync.py absent + contract.py present
})

test('SC#5 / TDBT-03: NL composer 503 (mock parse-nl as 503) → honest non-specific copy + Retry button visible', async ({ page }) => { ... })
test('SC#5 / TDBT-03: NL composer copy does NOT contain "credentials missing" / "Anthropic" / "API key" (honors V11 + LOCKED OPERATOR DECISION 3)', async ({ page }) => { ... })
test('SC#5 / TDBT-03: Click Retry → useParseAlertNl mutation re-fires (network capture: 2 POSTs same payload)', async ({ page }) => { ... })
test('SC#5 / TDBT-03: Retry button disabled while m.isPending (DoS guard)', async ({ page }) => { ... })
test('SC#5 / TDBT-03: Manual ThresholdForm / AnomalyForm remain usable after 503 (Phase 21 Pitfall 5 invariant)', async ({ page }) => { ... })
```

Use Playwright's `page.route('**/api/alerts/parse-nl', ...)` to mock 503 responses for SC#5 tests. Use `await request.get(...)` for backend API integration tests.

**EXTEND `v13-a11y.spec.ts`** — add new axe scan section for Phase 27 chrome:
- Open `/skills` + run axe.analyze() — 0 NEW Phase-27-attributable violations
- Open `/skills/$name` + run axe.analyze() — same
- Open `/cost` + run axe.analyze() — same
- Open `/alerts` + run axe.analyze() — same
- Open AlertNlInput 503 state (mocked) + run axe.analyze() — focus + role + aria all clean
- Confirm Phase 26 a11y deferrals from RESEARCH Pitfall 6 (`.cmc-heatmap-cell`, `.cmc-otel-feed`, `.cmc-sessions-table-header__label`, Range filter `<select>`) are EITHER fixed (if any plan opportunistically lifted them) OR still on the Accepted Exceptions ledger (carried forward into Phase 28+ or future polish)

Use the Phase 25/26 `PHASE_N_NET_CLASS_MARKERS` inversion-filter pattern: add `PHASE_27_NET_CLASS_MARKERS` for any new Phase 27 chrome classes (`.cmc-alert-nl__error*` from Plan 27-08, `.cmc-cost-by-project__delta-col` from Plan 27-05).

**EXTEND `v13-portal-containment.spec.ts`** — Phase 27 likely adds no new portal surfaces (CompareToggle is a button, not a portal). If any new Sheet/Popover landed during execution (verify), assert it walks no ancestor with `transform: !none`.

**EXTEND `v13-truncation.spec.ts`** — add:
- `/skills/tdd-coverage-author-with-fanout` (synthetic long name) → page header `TruncatedCell` truncates with tooltip on hover (Phase 24 truncation primitive pattern)
- `/cost` long `project_key` row in CostByProjectCard → `TruncatedCell` wraps + tooltip visible on hover

**EXTEND `v13-time-picker.spec.ts`** — add re-anchor probes on the 4 tail-end routes (mirror the Phase 26 Plan 09 pattern that probed /, /activity, /sessions/compare):
- `/skills` set picker to last 14 days → SkillCostCardForTopSkill refetches (snap to '14d')
- `/skills/$name` set picker to last 30 days → all 4 panels refetch
- `/cost` set picker to last 7 days → CostByProjectCard refetches with range='7d'
- `/alerts` set picker to last 1 day → AlertEventsList refetches with range='1d'

**EXTEND `cost-dashboard.spec.ts`, `alerts.spec.ts`, `skills-detail.spec.ts`** — these are existing route-specific specs from prior phases. Add bounded check + global picker re-anchor + (for cost) CompareToggle round-trip. Use the same pattern Phase 26 Plan 09 used to extend route-specific specs.

Run the full e2e suite: `pnpm --filter cmc-frontend test:e2e`. Capture any flake — re-run flaky specs once; if persistent flake, document as Accepted Exception with rationale in the VISUAL-CHECK.md.

**Lighthouse CI run:** `cd frontend && npx lhci autorun` (existing config). Assertions must stay 9/9 PASS. Phase 27 ships ZERO new runtime deps so no perf regression is expected.
  </action>
  <verify>
    <automated>cd frontend && pnpm test:e2e --reporter=line && pnpm tsc --noEmit && pnpm lint && cd ../backend && uv run pytest tests/test_url_contract.py tests/test_alerts_metrics_contract.py tests/test_sessions_router.py -v</automated>
  </verify>
  <done>
- `v13-tail-routes.spec.ts` ships with ≥ 23 test cases mapped 1:1 to SC#1-#5 + TDBT-01/02/03
- 5 existing v13-* spec families + 3 route-specific spec files extended for Phase 27 surfaces
- All Playwright tests green; flakes documented + retried
- Lighthouse 9/9 PASS preserved
- backend pytest delta documented (likely 686 → 688 or 689 with Plan 27-02 + 27-07 net effect)
- ResponsiveContainer count = Phase 24 lock baseline (DeltaPill column is HTML)
- testid registry contains every NEW testid used in the new specs (Plans 27-04..08 registered them all; this plan only consumes)
- `pnpm tsc --noEmit` clean; `pnpm lint --max-warnings 0` clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend v13-visual-capture.spec.ts with 24 NEW Phase 27 surface PNGs (4 routes × 3 densities × 2 themes) + author 27-VISUAL-CHECK.md scaffold</name>
  <files>frontend/tests/e2e/v13-visual-capture.spec.ts, .planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-VISUAL-CHECK.md</files>
  <action>
**Step 1 — extend `frontend/tests/e2e/v13-visual-capture.spec.ts`** with new visual-capture rows for the Phase 27 surfaces. Mirror Phase 25 plan 11 / Phase 26 plan 09 pattern: surfaces × densities × themes.

Phase 27 surfaces to capture (4 routes — each in the BOUNDED + adopted state):
1. **skills-index-bounded** — `/skills` with `cmc-page--bounded` modifier; all panels bounded
2. **skills-detail-bounded** — `/skills/$name` for a long-name skill (e.g. `tdd-coverage-author-with-fanout`); 4 panels bounded; TruncatedCell on header
3. **cost-bounded** — `/cost` with both panels bounded; project column truncates
4. **alerts-bounded** — `/alerts` with all 3 panels bounded; AlertRuleForm card-bounded
5. (Optional supplemental) **cost-compare-active** — `/cost?compare_panels=cost-by-project` showing the DeltaPill column
6. (Optional supplemental) **alerts-nl-503** — `/alerts` with mocked 503 showing the honest copy + Retry button

For each surface, iterate `density × theme` (3 × 2 = 6 PNGs per surface). With 4 core surfaces, that's 4 × 6 = 24 PNGs. With 2 supplemental surfaces, that's 36. Recommend 24 NEW PNGs (4 core surfaces) for parity with Phase 26's 30 PNG addition; the supplemental compare-active + 503 surfaces are extra evidence already captured by Task 1's e2e tests.

Path: `.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/visual-check/<surface>-<density>-<theme>.png`

EXAMPLE shape (extend existing v13-visual-capture.spec.ts):

```typescript
const PHASE_27_TAIL_MATRIX = [
  { name: 'skills-index-bounded', setup: async (page) => {
      await page.goto('/skills');
      await page.waitForSelector('[data-testid="cmc-page-bounded"]');  // adjust testid as registered
  }},
  { name: 'skills-detail-bounded', setup: async (page) => {
      await page.goto('/skills/tdd-coverage-author-with-fanout');
      await page.waitForSelector('[data-testid="skill-detail-header"]');
  }},
  { name: 'cost-bounded', setup: async (page) => {
      await page.goto('/cost');
      await page.waitForSelector('[data-testid="cost-by-project-card"]');
  }},
  { name: 'alerts-bounded', setup: async (page) => {
      await page.goto('/alerts');
      await page.waitForSelector('[data-testid="alert-rules-list"]');
  }},
];

for (const surface of PHASE_27_TAIL_MATRIX) {
  for (const density of ['compact', 'comfortable', 'cozy']) {
    for (const theme of ['light', 'dark']) {
      test(`Phase 27 chrome capture: ${surface.name} × ${density} × ${theme}`, async ({ page }) => {
        await page.evaluate(([d, t]) => {
          document.documentElement.setAttribute('data-density', d as string);
          document.documentElement.setAttribute('data-theme', t as string);
          window.localStorage.setItem('cmc.density', d as string);
          window.localStorage.setItem('cmc.theme', t as string);
        }, [density, theme]);
        await surface.setup(page);
        await page.screenshot({
          path: `.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/visual-check/${surface.name}-${density}-${theme}.png`,
          fullPage: false,
        });
      });
    }
  }
}
```

(Adjust testid selectors to match what each route actually exposes — read the registered testids in `docs/testid-registry.md` and each route file at execution time.)

Run `pnpm --filter cmc-frontend test:e2e --reporter=line v13-visual-capture` — generates 24 NEW PNGs. Visual matrix total: 96 (Phase 26 close) + 24 = 120 PNGs.

**Step 2 — author `27-VISUAL-CHECK.md` scaffold** following the precedent format (read 24/25/26 VISUAL-CHECK.md files for structure):

```markdown
# Phase 27 — Visual Check + Operator Verdict

**Phase:** 27 — Per-Route Adoption II (Skills/Cost/Alerts) + Tech Debt
**Sealed:** 2026-MM-DD
**Status:** PENDING OPERATOR VERDICT

---

## ROADMAP Success Criteria Mapping

| SC | Criterion | Evidence | Verdict |
|----|-----------|----------|---------|
| SC#1 | /skills/$name 4-panel bounded + density + global picker re-anchor | v13-tail-routes.spec.ts tests 1-6 + visual-capture skills-detail-bounded × 6 | ⬜ pending |
| SC#2 | /cost 7d/30d picker + re-query + TIME-04 + project truncation | v13-tail-routes.spec.ts tests 7-11 + visual-capture cost-bounded × 6 | ⬜ pending |
| SC#3 | Compare picker uses project_key (TDBT-01) | v13-tail-routes.spec.ts tests 12-15 + backend test_sessions_router.py +3 | ⬜ pending |
| SC#4 | AlertRuleForm metric vocab from useAlertMetrics (TDBT-02) | v13-tail-routes.spec.ts tests 16-19 + backend test_alerts_metrics_contract.py +2 | ⬜ pending |
| SC#5 | NL composer 503 retry/queue UX (TDBT-03) | v13-tail-routes.spec.ts tests 20-24 + AlertRuleForm.test.tsx +5 | ⬜ pending |

## REQ-ID Closure

| REQ-ID | Description | Plan(s) | Verdict |
|--------|-------------|---------|---------|
| TDBT-01 | project_key wire exposure + ComparePicker switch | 27-02 + 27-03 | ⬜ pending |
| TDBT-02 | FALLBACK_KNOWN_METRICS removal + contract test | 27-07 | ⬜ pending |
| TDBT-03 | NL composer retry UX + honest copy | 27-08 | ⬜ pending |

## Automated Evidence Summary

| Gate | Phase 26 baseline | Phase 27 measured | Delta |
|------|-------------------|-------------------|-------|
| Backend pytest | 686 / 0 / 0 | TBD / 0 / 0 | +N / -1 (sync.py deleted) / +2 (contract.py) / +3 (sessions_router project_key) |
| Frontend vitest | 610 / 0 / 0 | TBD / 0 / 0 | + ≥ 25 (Plans 27-01..08) |
| Playwright e2e | 207 (203 pass + 4 forward-compat skip) | TBD | + ≥ 23 (v13-tail-routes.spec.ts) + ≥ N extensions |
| Lighthouse | 9/9 PASS | 9/9 PASS | 0 regressions |
| axe | 39/39 PASS | TBD/TBD PASS | +5 (4 route + 1 NL-error scans) |
| Portal containment | 6/6 PASS | 6/6 PASS | 0 (no new portals) |
| URL contract | 2/2 PASS | 2/2 PASS | 0 |
| Visual capture PNG total | 96 | 120 | +24 (4 tail routes × 3 densities × 2 themes) |
| ResponsiveContainer count | Phase 24 lock | UNCHANGED | 0 |

## Accepted Exceptions

**Pre-positioned exception (carried into operator review for confirmation):**

- **CostForecastCard MTD-only — SC#2 "re-query" satisfied as "re-render":** The `/api/cost/forecast` endpoint is month-to-date with NO range parameter (per `frontend/src/routes/cost.tsx:56-58` and Phase 19/20 design). Plan 27-05 correctly opts CostForecastCard OUT of the `useRouteRangeVocab` bridge. When the operator changes the global time picker on `/cost`, CostForecastCard produces a React re-render (URL change re-runs the route component) but does NOT issue a new API call. This satisfies SC#2's "panels re-query on picker change" contract per the existing architecture; achieving a literal "re-query" for the MTD card would require a backend range extension which is OUT OF SCOPE for Phase 27 (tracked as a Phase 28+ candidate). Operator: confirm acceptance or escalate to gap closure.

(any additional exceptions surfaced during operator review are appended here)

## Operator Verdict

**Verdict:** ⬜ PASS / ⬜ CONDITIONAL / ⬜ FAIL
**Signed by:** _________________________ (operator name + date)
**Notes:** (any conditions or follow-ups)
```

Save to `.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-VISUAL-CHECK.md`.
  </action>
  <verify>
    <automated>cd frontend && pnpm test:e2e --reporter=line v13-visual-capture && ls .planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/visual-check/*.png | wc -l</automated>
  </verify>
  <done>
- v13-visual-capture.spec.ts generates 24 NEW Phase 27 PNGs (4 tail routes × 3 densities × 2 themes)
- 27-VISUAL-CHECK.md scaffold authored with all automated evidence populated
- Verdict field is PENDING (operator signs in Task 3)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator visual review + sign 27-VISUAL-CHECK.md verdict</name>
  <what-built>
- Phase 27 full close-gate cascade: 9 plans (27-01..27-08 + this 27-09), 5 SCs, 3 REQ-IDs
- 24 NEW visual-capture PNGs for the 4 tail-end routes
- 27-VISUAL-CHECK.md scaffold with automated evidence pre-populated; operator verdict pending
- All gates documented in the evidence table (backend pytest delta, vitest, Playwright, Lighthouse, axe, URL contract, ResponsiveContainer count)
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-VISUAL-CHECK.md` end-to-end
2. Spot-check 24 NEW PNGs in `.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/visual-check/` — confirm:
   - Each tail route renders bounded at every density × theme
   - Long skill name on `/skills/$name` truncates cleanly
   - `/cost` project column renders without overflow
   - AlertRuleForm card has the cmc-card--bounded modifier (right-aligned `<select>` or similar visible bounded affordance)
3. Manually verify SC#1-5 in the browser:
   - SC#1: Navigate to `/skills/tdd-coverage-author-with-fanout` (or any long-name skill) → header truncates; 4 panels bounded; set global TimePicker to "Last 7 days" → URL gains time_from/time_to; panels refetch
   - SC#2: Navigate to `/cost` → both panels bounded; toggle CompareToggle on CostByProjectCard → URL gains compare_panels; DeltaPill column appears (or accepted-exception documented if data shape didn't support it)
   - SC#3: Open Cmd+K → trigger compare picker → verify picker filters by project_key not cwd; verify description copy doesn't leak the 12-char hex
   - SC#4: Open `/alerts` → AlertRuleForm `<select>` renders loaded metrics (or "Loading…" placeholder during throttled fetch); inspect source: `grep -c FALLBACK_KNOWN_METRICS frontend/src/components/panels/AlertRuleForm.tsx` returns 0
   - SC#5: Unset `ANTHROPIC_API_KEY`, restart backend, navigate to `/alerts`, type a description in AlertNlInput, click Parse → honest copy + Retry button visible (NOT silent error); click Retry → mutation re-fires
4. Sign verdict in `27-VISUAL-CHECK.md`: PASS / CONDITIONAL (with documented Accepted Exceptions) / FAIL (with reasons + gap closure plan needed)
5. If CONDITIONAL, populate "Accepted Exceptions" section with rationale + follow-up plan name
6. If FAIL, file gap-closure ticket → re-spawn `/gsd:plan-phase 27 --gaps` to author closure plans
  </how-to-verify>
  <resume-signal>Type "approved — verdict PASS" (or CONDITIONAL / FAIL with details) to mark Phase 27 closed</resume-signal>
</task>

</tasks>

<verification>
**Consolidated SC + REQ-ID verdict map for Phase 27 close:**

| SC | Verified by |
|----|-------------|
| SC#1 (skills/$name 4-panel bounded + density + global picker) | v13-tail-routes tests 1-6 + visual-capture skills-detail-bounded × 6 |
| SC#2 (/cost picker + TIME-04 overlay + truncation) | v13-tail-routes tests 7-11 + visual-capture cost-bounded × 6 |
| SC#3 (compare picker uses project_key — TDBT-01) | v13-tail-routes tests 12-15 + backend test_sessions_router.py +3 |
| SC#4 (AlertRuleForm useAlertMetrics-only — TDBT-02) | v13-tail-routes tests 16-19 + backend test_alerts_metrics_contract.py +2 |
| SC#5 (NL composer 503 retry UX — TDBT-03) | v13-tail-routes tests 20-24 + AlertRuleForm.test.tsx +5 |

| REQ-ID | Plan(s) | Verified |
|--------|---------|----------|
| TDBT-01 | 27-02 (backend) + 27-03 (frontend) | SC#3 + backend pytest |
| TDBT-02 | 27-07 | SC#4 + backend test_alerts_metrics_contract.py |
| TDBT-03 | 27-08 | SC#5 + AlertRuleForm.test.tsx |

- 24 NEW visual-capture PNGs accepted at operator review
- Backend pytest baseline rolled forward (delta tracked in VISUAL-CHECK.md)
- Frontend vitest baseline rolled forward
- Playwright e2e expanded (v13-tail-routes.spec.ts + 5 spec extensions)
- Lighthouse 9/9 PASS preserved (no perf regression — Phase 27 introduces ZERO new deps)
- ResponsiveContainer count UNCHANGED (Phase 24 lock preserved)
- v1.3 milestone advances 3/5 → 4/5 phases complete after operator PASS
</verification>

<success_criteria>
1. v13-tail-routes.spec.ts ships with ≥ 23 cases (1:1 SC/REQ-ID coverage)
2. 24 NEW visual-capture PNGs land in `.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/visual-check/`
3. 27-VISUAL-CHECK.md authored with operator-signed PASS verdict
4. All v1.3 invariants preserved (URL contract append-only, ResponsiveContainer count, density on :root, no React Context bridges, testid registry, axe baseline)
5. v1.3 milestone advances 3/5 → 4/5 phases complete
6. Phase 27 closes; Phase 28 (Layout Customization) can begin
</success_criteria>

<output>
After completion, create `.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-09-SUMMARY.md` per `summary.md` template + the operator-signed 27-VISUAL-CHECK.md.
</output>
</content>
</invoke>