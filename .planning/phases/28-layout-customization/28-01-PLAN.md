---
phase: 28
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/lib/layout/__tests__/useLayoutState.test.ts
  - frontend/src/lib/__tests__/panelRegistry.test.ts
  - frontend/src/lib/__tests__/searchSchemas.test.ts
  - frontend/src/components/ui/__tests__/PanelHeaderMenu.test.tsx
  - frontend/src/components/ui/__tests__/DraggablePanelWrap.test.tsx
  - frontend/src/components/ui/__tests__/ResizablePanelGroup.test.tsx
  - frontend/tests/e2e/v13-layout.spec.ts
  - docs/url-contract.md
  - docs/testid-registry.md
autonomous: true
requirements:
  - LAYO-01
  - LAYO-02
  - LAYO-03
  - LAYO-04

must_haves:
  truths:
    - "Every downstream Wave 1-3 task has a placeholder test file to extend (no scavenger hunt for where tests live)"
    - "docs/url-contract.md documents the three new APPEND-ONLY search params (hidden_panels, panel_order, split_sizes) on every Phase-28 in-scope route"
    - "docs/testid-registry.md lists the 5 new dynamic testid families Phase 28 introduces (panel-header-menu / panel-hide / panel-drag-grip / panel-reset-layout / resize-handle)"
    - "Backend pytest tests/test_url_contract.py still 2/2 PASS after doc append (route ↔ doc bidirectional consistency preserved)"
  artifacts:
    - path: frontend/src/lib/layout/__tests__/useLayoutState.test.ts
      provides: "Skeleton vitest with describe blocks for isHidden/orderedPanels/splitSizes/reset"
      min_lines: 25
    - path: frontend/src/lib/__tests__/searchSchemas.test.ts
      provides: "Skeleton vitest extending existing file with describe blocks for asHiddenPanels/asPanelOrder/asSplitSizes"
      min_lines: 20
    - path: frontend/src/components/ui/__tests__/PanelHeaderMenu.test.tsx
      provides: "Skeleton vitest for menu render + hide + reset"
      min_lines: 20
    - path: frontend/src/components/ui/__tests__/DraggablePanelWrap.test.tsx
      provides: "Skeleton vitest for dragstart + keyboard space-to-grab"
      min_lines: 20
    - path: frontend/src/components/ui/__tests__/ResizablePanelGroup.test.tsx
      provides: "Skeleton vitest for URL round-trip + double-click reset"
      min_lines: 20
    - path: frontend/tests/e2e/v13-layout.spec.ts
      provides: "Playwright suite skeleton with test.skip placeholders for all 4 LAYO-* user journeys"
      min_lines: 40
      contains: "test.describe('Phase 28"
    - path: docs/url-contract.md
      provides: "Phase 28 effects section with 3 new search params per route"
      contains: "Phase 28 effects on URL contract"
    - path: docs/testid-registry.md
      provides: "5 new testid families registered"
      contains: "panel-header-menu-"
  key_links:
    - from: "frontend/tests/e2e/v13-layout.spec.ts"
      to: "docs/testid-registry.md"
      via: "test selectors match registered testid families"
      pattern: "panel-header-menu-|panel-hide-|panel-drag-grip-|panel-reset-layout-|resize-handle-"
    - from: "docs/url-contract.md"
      to: "frontend/src/routes/{index,activity,cost,skills,alerts,sessions_.compare}.tsx"
      via: "every documented route file exists; every route file is documented (bidirectional)"
      pattern: "hidden_panels|panel_order|split_sizes"
---

<objective>
Stand up the test scaffolding and doc extensions that every Wave 1-3 plan in Phase 28 will extend. This is the Nyquist-rule gate: each Wave 1-3 task verifies via a pre-existing automated command, so the files must exist before downstream waves run. Doc updates land here too so the URL contract pytest stays green from Wave 1 onward (instead of breaking mid-phase).

Purpose: Eliminate the "where does this test go?" decision-cost for every downstream executor. Pre-register every testid Phase 28 will introduce so ESLint `cmc/testid-registry-only` does not block downstream commits. Append the URL contract entries so `tests/test_url_contract.py` stays 2/2 PASS while routes are progressively wired.

Output:
- 6 new/extended test files (5 vitest skeletons + 1 Playwright spec skeleton)
- `docs/url-contract.md` Phase 28 effects section
- `docs/testid-registry.md` 5 new testid families registered
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-layout-customization/28-RESEARCH.md
@docs/url-contract.md
@docs/testid-registry.md
@frontend/tests/e2e/v13-tail-routes.spec.ts
@backend/tests/test_url_contract.py
@frontend/src/lib/searchSchemas.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author vitest skeletons + Playwright spec skeleton</name>
  <files>frontend/src/lib/layout/__tests__/useLayoutState.test.ts, frontend/src/lib/__tests__/panelRegistry.test.ts, frontend/src/lib/__tests__/searchSchemas.test.ts, frontend/src/components/ui/__tests__/PanelHeaderMenu.test.tsx, frontend/src/components/ui/__tests__/DraggablePanelWrap.test.tsx, frontend/src/components/ui/__tests__/ResizablePanelGroup.test.tsx, frontend/tests/e2e/v13-layout.spec.ts</files>
  <action>
Create six placeholder test files so downstream Wave 1-3 executors have a known landing site for assertions. Each file imports vitest's `describe`/`it`/`expect` (or Playwright's `test` for the e2e spec) and contains the test scaffold described below. Use `it.todo(...)` for vitest specs to keep the file green at commit time but visibly enumerate the work; use `test.skip(...)` for Playwright. Critical: tests MUST be skeleton-green so `pnpm test --run` continues to pass — do NOT write failing tests here.

`frontend/src/lib/__tests__/searchSchemas.test.ts` already exists per Phase 26/27. EXTEND it (append-only) with a new `describe('Phase 28 layout validators', ...)` block containing three `describe` children — `asHiddenPanels`, `asPanelOrder`, `asSplitSizes` — each with 3 `it.todo` placeholders covering: valid CSV input, empty-string input (must return undefined), malformed input (must return undefined). Do NOT import the validators yet — Wave 1 implements them.

`frontend/src/lib/layout/__tests__/useLayoutState.test.ts` NEW. Vitest skeleton with `describe('useLayoutState', ...)` containing `it.todo` placeholders for: isHidden returns false by default, isHidden returns true after setHidden(id, true), orderedPanels respects panel_order, splitSizes parses groupId, reset clears hidden_panels/panel_order/split_sizes, reset preserves time_from/time_to/compare_panels/range/a/b (Pitfall 11 + LAYO-04 SC#3), unknown panel ids filtered via PANEL_REGISTRY (Pitfall 7).

`frontend/src/lib/__tests__/panelRegistry.test.ts` NEW. Vitest skeleton with `describe('PANEL_REGISTRY', ...)` containing `it.todo` placeholders for: isValidPanelId returns true for registered id per route, isValidPanelId returns false for unknown id, registry shape lock (one assertion per in-scope route asserting the route key exists and has ≥1 entry). Use `it.todo` so file is green.

`frontend/src/components/ui/__tests__/PanelHeaderMenu.test.tsx` NEW. Skeleton with `describe('PanelHeaderMenu', ...)` containing `it.todo` for: renders settings trigger with correct testid, opens menu on click, Hide item fires setHidden(panelId, true), Reset Layout item fires reset() and writes URL without hidden_panels/panel_order/split_sizes.

`frontend/src/components/ui/__tests__/DraggablePanelWrap.test.tsx` NEW. Skeleton with `describe('DraggablePanelWrap', ...)` containing `it.todo` for: mouse dragstart writes panelId to dataTransfer, keyboard Space toggles aria-pressed (grab mode), ArrowDown in grab mode calls onReorder with index+1, cross-column drop is ignored.

`frontend/src/components/ui/__tests__/ResizablePanelGroup.test.tsx` NEW. Skeleton with `describe('ResizablePanelGroup', ...)` containing `it.todo` for: reads split_sizes from URL on mount, writes URL on onLayoutChanged (NOT on onLayoutChange — Pitfall 1), double-click Separator removes split_sizes from URL.

`frontend/tests/e2e/v13-layout.spec.ts` NEW. Playwright spec skeleton mirroring `v13-tail-routes.spec.ts` structure. Single `test.describe('Phase 28 — Layout Customization', () => { ... })` block. Inside, group tests by REQ ID using nested describes:
- `test.describe('LAYO-01 hide-and-persist', ...)` — 5 `test.skip` placeholders, one per in-scope route (`/`, `/activity`, `/cost`, `/skills`, `/alerts`) with stub bodies that read like "hide System Pressure → URL has hidden_panels=system-pressure → reload → panel still hidden". Wave 2 will unskip + implement.
- `test.describe('LAYO-02 reorder', ...)` — 2 `test.skip` for mouse-drag (on `/cost`) + 2 `test.skip` for keyboard reorder (Tab to grip + Space + ArrowDown + Enter).
- `test.describe('LAYO-03 split-pane', ...)` — 3 `test.skip`: resize persists, refresh preserves, double-click resets to 50/50 (no `split_sizes` in URL).
- `test.describe('LAYO-04 reset', ...)` — 2 `test.skip`: per-panel menu reset clears the three layout keys; SavedViewMenu Reset Layout clears the same keys; both preserve `time_from`/`time_to`/`compare_panels` (Pitfall non-layout-preservation).
- `test.describe('Round-trip with saved view', ...)` — 1 `test.skip` for hide → save view → navigate away → load view → assert panel hidden + URL has `hidden_panels=...`.
- `test.describe('Perf — ResponsiveContainer DOM identity', ...)` — 1 `test.skip` for chart `<svg>` element identity preserved across split-pane drag on `/sessions/compare`.

Each `test.skip` body MUST include the intended `data-testid` selectors (using the families registered in Task 3 below) and a `// TODO Wave N` comment naming the implementing plan number. This makes the spec a living spec — Wave 2/3/4/5 executors flip `test.skip` → `test` and implement.

Do NOT import any source modules that do not yet exist (PanelHeaderMenu, useLayoutState, etc.) — the skeletons MUST run green via `pnpm test --run` and `pnpm playwright test --list v13-layout.spec.ts` at commit time. Use the `it.todo` and `test.skip` mechanisms to keep the files green pre-implementation.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run -- src/lib/__tests__/searchSchemas.test.ts src/lib/layout/__tests__/useLayoutState.test.ts src/lib/__tests__/panelRegistry.test.ts src/components/ui/__tests__/PanelHeaderMenu.test.tsx src/components/ui/__tests__/DraggablePanelWrap.test.tsx src/components/ui/__tests__/ResizablePanelGroup.test.tsx && cd frontend && pnpm exec playwright test --list tests/e2e/v13-layout.spec.ts 2>&1 | grep -c "test.skip\|skipped"</automated>
  </verify>
  <done>All 6 vitest skeleton files exist and pass green (all `it.todo`); Playwright spec lists at least 15 skipped tests; `pnpm test --run` shows no new failures vs Phase 27 close baseline.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend docs/url-contract.md with Phase 28 effects section</name>
  <files>docs/url-contract.md</files>
  <action>
Append a new section to `docs/url-contract.md` titled "## Phase 28 effects on URL contract" mirroring the existing "Phase 26 effects" and "Phase 27 effects" section format (already in the file). The section MUST enumerate three new APPEND-ONLY optional search params per Phase 28 in-scope route. Per RESEARCH.md §2 + §5:

- `hidden_panels?: string` (CSV of registered panel ids, regex `^[a-z0-9_-]+(?:,[a-z0-9_-]+)*$`) — added on: `/`, `/activity`, `/cost`, `/skills`, `/alerts`. Default `undefined`. NEVER an empty-string default (Pitfall 2 — DefaultViewLoader bare-URL gate).
- `panel_order?: string` (CSV groups, regex `^[a-z0-9_-]+:[a-z0-9_-]+(?:,[a-z0-9_-]+)*(?:;[a-z0-9_-]+:[a-z0-9_-]+(?:,[a-z0-9_-]+)*)*$`) — added on: `/`, `/activity`, `/cost`, `/skills`, `/alerts`. Default `undefined`.
- `split_sizes?: string` (CSV groups of percentages 0-100, regex `^[a-z0-9_-]+:\d{1,3}(?:,\d{1,3})+(?:;[a-z0-9_-]+:\d{1,3}(?:,\d{1,3})+)*$`) — added on: `/sessions/compare` only in v1. Default `undefined`.

Then update the per-route table — find the existing rows for each in-scope route and APPEND a "Phase 28 / LAYO-01..04 APPENDS hidden_panels, panel_order" (and for `/sessions/compare`: "Phase 28 / LAYO-03 APPENDS split_sizes") tag in the validateSearch shape cell. Do NOT remove or rename existing search params — append-only invariant (Phase 25 Pitfall 3).

Document the locked panel-id append-only vocabulary invariant in a new "## Locked invariants (Phase 28)" subsection: "Panel ids registered in `frontend/src/lib/layout/panelRegistry.ts` are append-only — once a panel id ships, it cannot be renamed or repurposed (Pitfall 9). New panels add entries; deleted panels leave their id retired (registry entry removed; saved views referencing the id resolve to no-op via PANEL_REGISTRY membership filter)."

Document `/skills/$name` as explicitly OUT OF SCOPE for Phase 28 with rationale: single-column stack, no grid to reorder, no meaningful split-pane (per RESEARCH.md §5 + Question 11 Pitfall 12). Future-phase candidate.

Critical: do NOT touch unrelated routes' rows. The append is additive; existing test_url_contract.py reads route filename ↔ doc table consistency only — adding rows is invisible to it, but renaming/removing rows breaks the test. Verify locally before committing.
  </action>
  <verify>
    <automated>cd backend && uv run pytest tests/test_url_contract.py -v 2>&1 | tail -10 | grep -E "passed|failed"</automated>
  </verify>
  <done>`docs/url-contract.md` contains a "Phase 28 effects on URL contract" section and a "Locked invariants (Phase 28)" subsection; backend `tests/test_url_contract.py` 2/2 PASS; per-route rows in the routes table mention the new params for the 5 in-scope routes and `/sessions/compare`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend docs/testid-registry.md with Phase 28 testid families</name>
  <files>docs/testid-registry.md</files>
  <action>
Append a new section to `docs/testid-registry.md` titled "## Phase 28 — Layout Customization" mirroring the existing per-phase format (the file already organizes testids by phase — verify the exact heading shape before writing).

Register 5 new dynamic testid families. For each, document: testid pattern, where it mounts, which interaction triggers it, and which Phase 28 plan ships it. Dynamic ids use `{panelId}` / `{route}` / `{groupId}` placeholders consistent with existing dynamic-testid entries (e.g. Phase 26's `cmdk-time-range-{value}`, Phase 27's `recents-route-{slug}`).

1. `panel-header-menu-{panelId}` — DropdownMenu trigger button inside `PanelHeaderMenu` (PanelCard trailing slot). Click → opens the show-hide+reset menu. Ships Plan 28-03.
2. `panel-hide-{panelId}` — DropdownMenu.Item "Hide this panel". Click → calls `setHidden(panelId, true)`. Ships Plan 28-03.
3. `panel-drag-grip-{panelId}` — `<button>` inside `DraggablePanelWrap` exposing the GripVertical icon. Mouse drag = native HTML5 dnd source; Space/Enter = enter grab mode; ArrowUp/Down = reorder; Esc = cancel; Enter/Space (second press) = commit. Ships Plan 28-04.
4. `panel-reset-layout-{route}` — DropdownMenu.Item "Reset layout" mounted in BOTH `PanelHeaderMenu` (per-panel) AND `SavedViewMenu` chrome (escape hatch when all panels hidden, per RESEARCH.md §7 — A2 assumption). Route slug uses the same vocabulary as SidebarNavLink slug (e.g. `home`, `cost`, `sessions-compare`). Ships Plan 28-02 (SavedViewMenu) + Plan 28-03 (per-panel).
5. `resize-handle-{groupId}` — `Separator` from react-resizable-panels mounted between Panels in `ResizablePanelGroup`. groupId matches the URL `split_sizes` group key (e.g. `compare` for `/sessions/compare`). Drag = pointer-driven resize; double-click = reset to defaultSize. Ships Plan 28-05.

Also register one EXACT-MATCH testid: `panel-grid-{columnId}` — placed on the `<div role="list" data-column-id={columnId}>` grid container holding draggable children (Plan 28-04). columnId vocabulary: lowercase ASCII per panelRegistry conventions.

Critical: every Phase 28 testid mounted in source MUST appear in this registry — ESLint `cmc/testid-registry-only` blocks unknown testids per POLI-14. Wave 1-4 executors will reference this file via `@docs/testid-registry.md` context.

Document the locked dynamic-vs-exact behaviour: dynamic testids use literal `{panelId}` / `{route}` / `{groupId}` placeholders in the registry; the ESLint rule normalizes by stripping the `-{...}` suffix during lookup.

**Pre-flight check for the verify command (confirmed 2026-05-16 by planner — operator may re-run if registry rule changes):**

Run `grep -n "loadRegistry\|readFileSync" frontend/eslint-rules/testid-registry-only.cjs` and confirm the rule sources its allowlist from `docs/testid-registry.md` at module load time (it does — `loadRegistry()` calls `fs.readFileSync(.../docs/testid-registry.md)` and parses bullet lines). Because the rule reads the doc dynamically, adding new bullet entries to `docs/testid-registry.md` (Task 3 above) is sufficient — no separate ESLint config file edit is required. If a future refactor moves the allowlist into a hardcoded constant inside the rule source, this Task 3 must additionally update that hardcoded list and add the rule file to `files_modified` — the planner-verified state as of 2026-05-16 is that no hardcoded list exists, so the verify command below is meaningful as written.

Note: the verify command runs the ESLint rule across all source files. At this point in Phase 28 Wave 0, NO source files yet mount the new testids (those land in Plans 28-03/28-04/28-05). So the rule's pass on this commit only proves "adding the registry entries did not break the rule's load" — the meaningful test of the rule's coverage happens in Plans 28-03/28-04/28-05 when source consumes the testids. This is acceptable for Wave 0 since the alternative (lazy registration) would force Plan 28-03 to do both source mounts AND registry updates in one commit, increasing risk.
  </action>
  <verify>
    <automated>cd frontend && pnpm exec eslint --no-warn-ignored --rule '{"cmc/testid-registry-only":"error"}' 'src/**/*.{ts,tsx}' 2>&1 | tail -5</automated>
  </verify>
  <done>`docs/testid-registry.md` has a Phase 28 section listing 5 dynamic + 1 exact testid family; `pnpm exec eslint` rule pass (no new violations from registry edit); registry file format matches existing per-phase sections; every entry names the plan that ships it.</done>
</task>

</tasks>

<verification>
- `cd frontend && pnpm test --run` → green (no new failures from skeletons)
- `cd frontend && pnpm exec playwright test --list tests/e2e/v13-layout.spec.ts` → lists ≥15 tests, all currently `.skip`
- `cd backend && uv run pytest tests/test_url_contract.py -v` → 2/2 PASS
- `cd frontend && pnpm exec eslint 'src/**/*.{ts,tsx}'` → no `cmc/testid-registry-only` violations
- Source-grep `grep -c "Phase 28" docs/url-contract.md` → ≥2 (effects section + locked invariants)
- Source-grep `grep -c "panel-header-menu-\|panel-hide-\|panel-drag-grip-\|panel-reset-layout-\|resize-handle-" docs/testid-registry.md` → 5
</verification>

<success_criteria>
- 6 new/extended test files exist, all green
- `docs/url-contract.md` documents the 3 new search params on every in-scope route + locked invariants
- `docs/testid-registry.md` lists every Phase-28 testid family that downstream plans will mount
- Backend URL contract pytest 2/2 PASS preserved
- No ESLint violations from the registry edit
- Phase 28 downstream waves can extend these files without creating them
</success_criteria>

<output>
After completion, create `.planning/phases/28-layout-customization/28-01-SUMMARY.md` per the standard SUMMARY template — list every file modified, every testid registered (with full names), every URL contract entry added, and any deviations from this plan.
</output>