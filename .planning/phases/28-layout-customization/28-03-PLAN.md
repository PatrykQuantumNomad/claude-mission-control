---
phase: 28
plan: 03
type: execute
wave: 2
depends_on:
  - 28-02
files_modified:
  - frontend/src/components/ui/PanelHeaderMenu.tsx
  - frontend/src/components/ui/__tests__/PanelHeaderMenu.test.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/routes/activity.tsx
  - frontend/src/routes/cost.tsx
  - frontend/src/routes/skills.tsx
  - frontend/src/routes/alerts.tsx
  - frontend/tests/e2e/v13-layout.spec.ts
autonomous: true
requirements:
  - LAYO-01
  - LAYO-04

must_haves:
  truths:
    - "User opens the PanelHeaderMenu DropdownMenu on any panel of /, /activity, /cost, /skills, /alerts, picks Hide this panel — that panel disappears, URL gains ?hidden_panels=<panel-id>"
    - "User picks Reset layout in the panel menu — URL drops hidden_panels + panel_order + split_sizes; time_from/time_to/compare_panels/range/a/b are preserved (LAYO-04 SC#3)"
    - "Hidden state round-trips via SaveViewDialog: hide panel → Save view → navigate away → load view → URL has hidden_panels=... → panel stays hidden (no SaveViewDialog edits — auto-capture)"
    - "validateSearch on /, /activity, /cost, /skills, /alerts is APPEND-ONLY extended with hidden_panels (Pitfall 2 — defaults to undefined, NEVER empty string)"
    - "When hidden_panels is set, the route renders ONLY the visible-set panels (render-time filter via useLayoutState.isHidden + isValidPanelId)"
  artifacts:
    - path: frontend/src/components/ui/PanelHeaderMenu.tsx
      provides: "Radix DropdownMenu trigger + Hide + Reset Layout items"
      min_lines: 60
      contains: "DropdownMenu"
    - path: frontend/src/routes/index.tsx
      provides: "validateSearch extended with hidden_panels; panel children filtered by useLayoutState.isHidden"
      contains: "asHiddenPanels"
    - path: frontend/src/routes/activity.tsx
      provides: "Same APPEND + filter"
      contains: "asHiddenPanels"
    - path: frontend/src/routes/cost.tsx
      provides: "Same APPEND + filter"
      contains: "asHiddenPanels"
    - path: frontend/src/routes/skills.tsx
      provides: "Same APPEND + filter"
      contains: "asHiddenPanels"
    - path: frontend/src/routes/alerts.tsx
      provides: "Same APPEND + filter"
      contains: "asHiddenPanels"
  key_links:
    - from: "frontend/src/components/ui/PanelHeaderMenu.tsx"
      to: "frontend/src/lib/layout/useLayoutState.ts"
      via: "consumes setHidden + reset"
      pattern: "useLayoutState"
    - from: "frontend/src/routes/index.tsx"
      to: "frontend/src/lib/searchSchemas.ts"
      via: "validateSearch invokes asHiddenPanels on raw.hidden_panels"
      pattern: "asHiddenPanels"
    - from: "frontend/src/routes/index.tsx"
      to: "frontend/src/lib/layout/panelRegistry.ts"
      via: "every PanelCard mount passes panelId from registry"
      pattern: "panelId="
---

<objective>
Ship LAYO-01 and the per-panel half of LAYO-04 on all 5 in-scope routes. After this plan, the operator can open the chrome menu on any panel and hide it; URL updates; saved views round-trip the hidden state; Reset Layout (per-panel) restores defaults without touching unrelated URL params.

Purpose: LAYO-01 is the most-visible Phase 28 surface — the first thing the operator sees when Phase 28 ships. The corresponding LAYO-04 escape hatch ships in the same plan so the user can always recover from a customization mistake.

Output:
- New `PanelHeaderMenu` component
- All 5 in-scope routes (`/`, `/activity`, `/cost`, `/skills`, `/alerts`) extended with APPEND-ONLY validateSearch for `hidden_panels`, panelId props on every PanelCard, and render-time filtering
- Playwright spec hide-and-persist tests unskipped + passing
- Round-trip saved-view test unskipped + passing
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-layout-customization/28-RESEARCH.md
@.planning/phases/28-layout-customization/28-02-SUMMARY.md
@frontend/src/lib/layout/panelRegistry.ts
@frontend/src/lib/layout/useLayoutState.ts
@frontend/src/lib/searchSchemas.ts
@frontend/src/components/ui/PanelCard.tsx
@frontend/src/routes/index.tsx
@frontend/src/components/savedviews/SaveViewDialog.tsx
@frontend/tests/e2e/v13-saved-views.spec.ts
@docs/testid-registry.md

<interfaces>
<!-- Contracts produced by Plan 28-02 (already shipped before this plan executes): -->

From frontend/src/lib/layout/useLayoutState.ts:
```typescript
export function useLayoutState(route: string): {
  isHidden: (panelId: string) => boolean
  setHidden: (panelId: string, hide: boolean) => void
  reset: () => void
  // ... orderedPanels/setOrder/splitSizes/setSplit consumed by Plans 28-04 and 28-05
}
```

From frontend/src/lib/layout/panelRegistry.ts:
```typescript
export const PANEL_REGISTRY: Record<string, PanelDescriptor[]>
export function isValidPanelId(route: string, panelId: string): boolean
export function normalizeRouteId(pathname: string): string
```

From frontend/src/lib/searchSchemas.ts:
```typescript
export function asHiddenPanels(v: unknown): string | undefined  // NEW from Plan 28-02
```

From frontend/src/components/ui/PanelCard.tsx (after Plan 28-02):
```typescript
interface PanelCardProps {
  // existing props ...
  panelId?: string             // emits data-panel-id
  headerMenu?: ReactNode       // trailing chrome slot
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build PanelHeaderMenu component</name>
  <files>frontend/src/components/ui/PanelHeaderMenu.tsx, frontend/src/components/ui/__tests__/PanelHeaderMenu.test.tsx</files>
  <behavior>
    - Renders a Settings-icon `<button>` with data-testid="panel-header-menu-{panelId}" and aria-label="Customize {label}"
    - Clicking the trigger opens a DropdownMenu via Radix portal
    - Menu has two items: "Hide this panel" (EyeOff icon, data-testid="panel-hide-{panelId}") + "Reset layout" (RotateCcw icon, data-testid="panel-reset-layout-{routeSlug}") with a Separator between
    - Clicking Hide calls useLayoutState(route).setHidden(panelId, true) — verified via mock
    - Clicking Reset calls useLayoutState(route).reset() AND emits sonner toast.success('Layout reset')
    - Portal mounts to document.body (Radix default — Phase 24 CONT-02 invariant)
    - No transform on the trigger button hover state (Phase 24 Pitfall 2 — transform-bearing ancestor breaks Portal)
    - Trigger button is keyboard-accessible (Tab-reachable, Enter/Space opens menu — Radix default)
    - axe-core clean (no aria violations on trigger + menu)
  </behavior>
  <action>
Create `frontend/src/components/ui/PanelHeaderMenu.tsx` NEW per RESEARCH.md "Code Examples — Show/hide DropdownMenu in panel header" pattern. The component takes `{ panelId, label }` props and internally derives `route` via `useRouterState({ select: s => normalizeRouteId(s.location.pathname) })`.

Imports:
- `* as DropdownMenu` from `@radix-ui/react-dropdown-menu`
- `EyeOff, RotateCcw, Settings` from `lucide-react`
- `useLayoutState` + `normalizeRouteId` from `frontend/src/lib/layout/...`
- `useRouterState` from `@tanstack/react-router`
- `toast` from `sonner`

Structure:
```
<DropdownMenu.Root>
  <DropdownMenu.Trigger asChild>
    <button class="cmc-icon-button" aria-label={...} data-testid={panel-header-menu-{panelId}}>
      <Settings size={14} aria-hidden />
    </button>
  </DropdownMenu.Trigger>
  <DropdownMenu.Portal>
    <DropdownMenu.Content class="cmc-dropdown" sideOffset={6} align="end">
      <DropdownMenu.Item onSelect={() => setHidden(panelId, true)} data-testid={panel-hide-{panelId}}>
        <EyeOff size={14} aria-hidden /> Hide this panel
      </DropdownMenu.Item>
      <DropdownMenu.Separator class="cmc-dropdown__separator" />
      <DropdownMenu.Item onSelect={handleReset} data-testid={panel-reset-layout-{routeSlug}}>
        <RotateCcw size={14} aria-hidden /> Reset layout
      </DropdownMenu.Item>
    </DropdownMenu.Content>
  </DropdownMenu.Portal>
</DropdownMenu.Root>
```

Where `handleReset = () => { reset(); toast.success('Layout reset'); }`.

CSS (no new file — extend existing `frontend/src/styles.css` cmc-dropdown / cmc-icon-button rules if Phase 24 already established them — verify via grep `grep -n "cmc-dropdown\|cmc-icon-button" frontend/src/styles.css`. If absent, add minimal rules using density-token vars from `:root` (Pitfall 1 — never scope tokens to a subtree). Use `--cmc-z-dropdown` for stacking (Phase 24 z-index ladder lock; no raw z-index per CONT-05 / `cmc/no-raw-z-index` ESLint rule).

Verify trigger button has NO `transform:` rule in any hover/focus/active CSS (Phase 24 Pitfall 2). If the existing `cmc-icon-button` class has a transform, override with `transform: none` on `.cmc-icon-button[data-radix-collection-item]` or use a Phase-28-scoped class.

UNSKIP the `it.todo` placeholders in `frontend/src/components/ui/__tests__/PanelHeaderMenu.test.tsx` and implement the 4 assertions described in the behavior block. Use `@testing-library/react` + `@testing-library/user-event` for interaction. Mock `@tanstack/react-router` (`useRouterState` returns `{ pathname: '/' }`) + mock `useLayoutState` to return spy functions + mock `sonner` (`vi.mock('sonner', () => ({ toast: { success: vi.fn() }}))`).

TDD: RED commit asserts the 4 behaviors against an empty component → fail → GREEN commit implements PanelHeaderMenu → pass.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run -- src/components/ui/__tests__/PanelHeaderMenu.test.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>PanelHeaderMenu renders with correct testids; clicking Hide calls setHidden; clicking Reset calls reset() + toast; vitest green; pnpm tsc clean; no transform on trigger.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2a: Wire LAYO-01 on / and /cost (validateSearch + panelId + filter)</name>
  <files>frontend/src/routes/index.tsx, frontend/src/routes/cost.tsx, frontend/tests/e2e/v13-layout.spec.ts</files>
  <action>
For each of the 2 routes (`/`, `/cost`), perform 3 surgical edits in this exact order. Do each route end-to-end (validateSearch → panelId props → render-time filter → Playwright unskip) before moving on to the next; commit between routes if helpful for bisect.

Edit 1 — APPEND-ONLY validateSearch extension. In the route's `validateSearch` function, ADD ONE LINE: `hidden_panels: asHiddenPanels(raw.hidden_panels),`. Place it adjacent to existing `compare_panels` for symmetry. Import `asHiddenPanels` from `frontend/src/lib/searchSchemas.ts` (already exported by Plan 28-02). Default `undefined` — NEVER per-route fallback (Pitfall 2). Do NOT touch any other validateSearch field — append-only invariant.

Edit 2 — Add `panelId={...}` prop to every `<PanelCard ...>` and `<BoundedPanelCard ...>` mount within the route. Use the canonical id from `PANEL_REGISTRY[<route>]` produced by Plan 28-02 (verify the SUMMARY from 28-02 enumerates the ids — if missing, walk the route's render JSX and emit one panelId per card using kebab-case slug of the user-visible title). Each id MUST appear in PANEL_REGISTRY (vitest panelRegistry.test.ts proves coverage). Also pass `headerMenu={<PanelHeaderMenu panelId={...} label={...} />}` to wire Task-1's component into the trailing chrome slot. Import PanelHeaderMenu from `../components/ui/PanelHeaderMenu`.

Edit 3 — Add render-time filter. Inside the route component, after the existing route hooks (queries, etc.), call `const { isHidden } = useLayoutState(normalizeRouteId(pathname))` where `pathname` comes from existing `useRouterState({ select: s => s.location.pathname })` or `useMatchRoute()` — pick the lowest-disruption hook (verify by reading existing route shape). For each PanelCard JSX subtree, wrap with `{!isHidden('<panel-id>') && <PanelCard panelId="<panel-id>" ... />}` OR (preferred) filter a render array before mapping: build an array of `{ panelId, render: () => <PanelCard ... /> }` objects, filter by `!isHidden(panelId)`, then map. Use the same pattern across both routes for consistency — Task 2b will reuse the pattern for the remaining 3 routes.

Per-route notes:

`/` (routes/index.tsx): Most panels — 11 entries per RESEARCH.md §5 (kpi-row, system-pressure, attention-bar, live-sessions, token-usage, cache-efficiency, session-outcomes, agent-fanout, tool-usage, otel-panel, cmd-grid OR equivalent depending on current shape — verify via grep). Filter applies to `.cmc-card-grid` children + any chrome rows (e.g. KpiStrip) that the operator should be able to hide. If an entry in PANEL_REGISTRY belongs to columnId='top', filter that row's render explicitly.

`/cost` (routes/cost.tsx): 2 entries — cost-forecast, cost-by-project. Trivial filter.

Test extension (Playwright): Unskip the LAYO-01 hide-and-persist tests for `/` and `/cost` only in `frontend/tests/e2e/v13-layout.spec.ts` (2 tests total in this task). Test body shape per route:
1. Navigate to route with no layout params
2. Locate `[data-testid="panel-header-menu-<panel-id>"]` → click
3. Locate `[data-testid="panel-hide-<panel-id>"]` → click
4. Assert `[data-panel-id="<panel-id>"]` is NOT visible in the DOM
5. Assert URL contains `?hidden_panels=<panel-id>` (use page.url())
6. Reload page → assert step 4 + 5 still hold

CRITICAL — `SaveViewDialog.tsx` is NOT touched (auto-capture invariant per Pitfall 3 + RESEARCH.md "Don't Hand-Roll"). Verify via `git diff --stat frontend/src/components/savedviews/SaveViewDialog.tsx` → must show no changes.

The round-trip-with-saved-view Playwright test stays `test.skip` here; Task 2b unskips it after all 5 routes are wired (round-trip exercise needs at least one wired route plus navigation to a different route).
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run 2>&1 | tail -5 && cd frontend && pnpm exec playwright test tests/e2e/v13-layout.spec.ts -g "hide-and-persist.*/cost\b|hide-and-persist.*/$" 2>&1 | tail -5 && cd backend && uv run pytest tests/test_url_contract.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>`/` and `/cost` have hidden_panels in validateSearch (APPEND-ONLY); every PanelCard on those 2 routes has panelId + headerMenu; route filter hides panels per useLayoutState.isHidden; 2 Playwright hide-and-persist tests green; URL contract pytest 2/2 PASS; SaveViewDialog.tsx unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2b: Wire LAYO-01 on /activity, /skills, /alerts + round-trip Playwright</name>
  <files>frontend/src/routes/activity.tsx, frontend/src/routes/skills.tsx, frontend/src/routes/alerts.tsx, frontend/tests/e2e/v13-layout.spec.ts</files>
  <action>
For each of the 3 remaining in-scope routes (`/activity`, `/skills`, `/alerts`), repeat the exact same 3-edit pattern Task 2a established. Reuse the per-route render-filter pattern (build a `{panelId, render}[]` array, filter via `!isHidden(panelId)`, then map) verbatim — consistency across the 5 routes is a Phase-28 lint check.

Per-route notes:

`/activity` (routes/activity.tsx): 6 entries per RESEARCH.md §5 (activity-heatmap, charts-strip, sessions-table at the top + 3 grid panels). Heatmap + ChartsStrip are above the grid — filter individually. For columnId='top' entries the filter still applies (the operator can hide chrome rows).

`/skills` (routes/skills.tsx): 8 entries per RESEARCH.md §5. Use the canonical list from 28-02 SUMMARY.

`/alerts` (routes/alerts.tsx): 3 entries — alert-rules-list, alert-rule-form, alert-events-list. Filter applies to all three even though alert-events-list is full-width below.

CRITICAL — for `/sessions/compare` (NOT in scope for this plan — Plan 28-05 handles it): do NOT touch `frontend/src/routes/sessions_.compare.tsx`. The single-panel compare view does not host LAYO-01 per RESEARCH.md §5.

Test extension (Playwright): Unskip the LAYO-01 hide-and-persist tests for `/activity`, `/skills`, `/alerts` in `frontend/tests/e2e/v13-layout.spec.ts` (3 more tests). Same test-body shape as Task 2a (6-step navigate-click-assert-reload pattern).

Also unskip the "round-trip with saved view" test (lives in the same spec file under the round-trip describe block):
1. Hide a panel on `/` (Task 2a wired this route)
2. Open SaveViewDialog → save view as "Compact home"
3. Navigate to `/cost` (Task 2a wired this route)
4. Open SavedViewMenu → select "Compact home"
5. Assert URL has `?hidden_panels=...` AND target panel hidden

`SaveViewDialog.tsx` is NOT touched in this plan — auto-capture invariant (Pitfall 3 + RESEARCH.md "Don't Hand-Roll"). Verify no diff via `git diff --stat frontend/src/components/savedviews/SaveViewDialog.tsx` → must show no changes.

ESLint testid registry: every new dynamic testid `panel-header-menu-<id>`, `panel-hide-<id>`, `panel-reset-layout-<route>` is pre-registered by Plan 28-01 — no new registry edits needed.
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run 2>&1 | tail -5 && cd frontend && pnpm exec playwright test tests/e2e/v13-layout.spec.ts -g "hide-and-persist|Round-trip" 2>&1 | tail -5 && cd backend && uv run pytest tests/test_url_contract.py -v 2>&1 | tail -5</automated>
  </verify>
  <done>`/activity`, `/skills`, `/alerts` have hidden_panels in validateSearch (APPEND-ONLY); every PanelCard on those 3 routes has panelId + headerMenu; ≥6 Playwright tests green (5 hide-and-persist + 1 round-trip); URL contract pytest 2/2 PASS; SaveViewDialog.tsx unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → React app | hidden_panels CSV crosses validateSearch on every route mount |
| Panel id → PANEL_REGISTRY | unknown ids from stale saved views resolve to no-op (Pitfall 7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05 | Input Validation (V5) | hidden_panels via validateSearch | mitigate | asHiddenPanels regex anchor + undefined default (already shipped by Plan 28-02) |
| T-28-06 | Spoofing | sonner toast hijack | accept | sonner is portal-mounted DOM only; no XSS vector in plain-text "Layout reset" string |
| T-28-07 | Information Disclosure | panel-id leak in URL | accept | Panel ids are kebab-case slugs of user-visible labels; no PII or secret content; local single-user (REQUIREMENTS.md macOS-only) |
</threat_model>

<verification>
- `cd frontend && pnpm test --run` → green
- `cd frontend && pnpm exec playwright test tests/e2e/v13-layout.spec.ts -g "hide-and-persist|Round-trip"` → ≥6 tests pass (5 routes + 1 round-trip)
- `cd backend && uv run pytest tests/test_url_contract.py -v` → 2/2 PASS
- `cd frontend && pnpm tsc --noEmit` → no errors
- `cd frontend && pnpm exec eslint 'src/**/*.{ts,tsx}'` → no `cmc/testid-registry-only` or `cmc/no-raw-z-index` violations
- Source-grep `grep -c "asHiddenPanels" frontend/src/routes/{index,activity,cost,skills,alerts}.tsx` → 5
- Source-grep `grep -c "panelId=" frontend/src/routes/{index,activity,cost,skills,alerts}.tsx` → matches PANEL_REGISTRY entry count per route
- No diff: `git diff --stat frontend/src/components/savedviews/SaveViewDialog.tsx` → empty (Pitfall 3 lock)
- ResponsiveContainer count probe: `grep -rE "^[[:space:]]*<ResponsiveContainer" frontend/src/components/panels/*.tsx | wc -l` → 8 (Phase 24/26/27 lock — this plan adds no charts)
</verification>

<success_criteria>
- LAYO-01 satisfied: every in-scope route has a PanelHeaderMenu on every panel; Hide hides the panel; URL persists; saved view round-trips
- LAYO-04 partially satisfied (per-panel half): Reset Layout in the panel menu clears the 3 layout keys; preserves non-layout params (full LAYO-04 requires SavedViewMenu reset — already shipped by Plan 28-02)
- validateSearch APPEND-ONLY contract honored on all 5 routes
- No SaveViewDialog edits (Pitfall 3 auto-capture lock)
- No ResponsiveContainer drift (Phase 24/26/27 lock = 8)
- Round-trip via state_json works without server-side schema changes (Pitfall 5 — backend opacity preserved)
</success_criteria>

<output>
Create `.planning/phases/28-layout-customization/28-03-SUMMARY.md` listing every route's PanelCard mount sites updated, the final canonical panel-id list per route, any deviations from PANEL_REGISTRY (which Plan 28-04 + 28-05 will inherit), and screenshots/evidence that hide-and-persist + round-trip work.
</output>