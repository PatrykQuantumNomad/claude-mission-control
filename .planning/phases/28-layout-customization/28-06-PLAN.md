---
phase: 28
plan: 06
type: execute
wave: 5
depends_on:
  - 28-03
  - 28-04
  - 28-05
files_modified:
  - frontend/tests/e2e/v13-visual-capture.spec.ts
  - frontend/tests/e2e/v13-a11y.spec.ts
  - .planning/phases/28-layout-customization/28-VISUAL-CHECK.md
autonomous: false
requirements:
  - LAYO-01
  - LAYO-02
  - LAYO-03
  - LAYO-04

must_haves:
  truths:
    - "Visual capture matrix extended with exactly 18 NEW PNGs covering 3 surfaces × 3 densities × 2 themes (Pitfall 10 cap)"
    - "Backend pytest, frontend vitest, Playwright e2e all green vs Phase 27 baseline + Phase 28 deltas"
    - "Bundle size delta ≤ 15kB gzipped vs Phase 27 baseline (Phase 28 ships 1 small runtime dep — react-resizable-panels@4.11.0; verified via `pnpm exec vite build && du -sh frontend/dist`)"
    - "axe-core blocking violations = 0 net new (Phase 28 introduces no new chrome that fails AA contrast or aria roles)"
    - "Portal containment 7/7 PASS (Phase 27 baseline — Phase 28 adds PanelHeaderMenu Portal which counts as +1 → 8/8 expected)"
    - "ResponsiveContainer count = 8 (locked since Phase 24 — no new charts in Phase 28)"
    - "URL contract pytest 2/2 PASS"
    - "Operator verdict signed PASS in `28-VISUAL-CHECK.md` via live walkthrough (RESEARCH.md §8 contract)"
    - "v1.3 milestone closes 5/5 phases complete, 45/45 active requirements satisfied"
  artifacts:
    - path: .planning/phases/28-layout-customization/28-VISUAL-CHECK.md
      provides: "Operator-signed verdict, all 9 required sections per RESEARCH.md §8"
      contains: "Operator verdict"
      min_lines: 100
    - path: frontend/tests/e2e/v13-visual-capture.spec.ts
      provides: "18 new visual capture PNG generators for 3 Phase-28 surfaces"
      contains: "Phase 28"
  key_links:
    - from: ".planning/phases/28-layout-customization/28-VISUAL-CHECK.md"
      to: "LAYO-01..04 + Success Criteria 1-5"
      via: "REQ-ID Closure table + Success Criteria mapping table"
      pattern: "LAYO-01|LAYO-02|LAYO-03|LAYO-04|SC#1|SC#2|SC#3|SC#4|SC#5"
    - from: "frontend/tests/e2e/v13-visual-capture.spec.ts"
      to: ".planning/phases/28-layout-customization/visual-check/"
      via: "Playwright writes PNGs to the visual-check directory"
      pattern: "screenshot.*path.*28-layout"
---

<objective>
Close Phase 28 — and therefore v1.3 — via the formalized phase-close pattern: extend visual capture by exactly 18 PNGs (Pitfall 10 cap), confirm every automated gate is green, and capture an operator-signed verdict in `28-VISUAL-CHECK.md`. This plan ships NO new features; it gates the milestone close.

Purpose: This plan exists because POLI-09 mandates a formal operator visual checkpoint per phase, because RESEARCH.md §8 provides the canonical `28-VISUAL-CHECK.md` template, and because v1.3 cannot ship 5/5 without this verdict. The operator's live walkthrough (Chrome DevTools MCP against http://localhost:5173) is the gate.

Output:
- `v13-visual-capture.spec.ts` extended with 18 new PNG-generating tests (3 surfaces × 3 densities × 2 themes)
- `v13-a11y.spec.ts` extended with the layout-customized URL variants
- `.planning/phases/28-layout-customization/28-VISUAL-CHECK.md` authored with all 9 required sections, awaiting operator verdict
- Operator runs walkthrough, signs verdict, marks v1.3 milestone closed
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/28-layout-customization/28-RESEARCH.md
@.planning/phases/28-layout-customization/28-03-SUMMARY.md
@.planning/phases/28-layout-customization/28-04-SUMMARY.md
@.planning/phases/28-layout-customization/28-05-SUMMARY.md
@.planning/phases/27-per-route-adoption-ii-skills-cost-alerts-tech-debt/27-VISUAL-CHECK.md
@frontend/tests/e2e/v13-visual-capture.spec.ts
@frontend/tests/e2e/v13-a11y.spec.ts
@frontend/tests/e2e/v13-portal-containment.spec.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend v13-visual-capture.spec.ts with 18 new PNG cells</name>
  <files>frontend/tests/e2e/v13-visual-capture.spec.ts</files>
  <action>
Extend the existing visual capture spec with EXACTLY 18 new PNGs (Pitfall 10 cap — adding more breaks the 138-total-PNG budget; adding fewer leaves coverage gaps). The matrix is 3 surfaces × 3 densities × 2 themes:

Surfaces (3):
1. `layout-default` — bare URL on `/` (no layout overrides) → `/?range=7d` (or matching baseline URL the existing spec uses)
2. `layout-customized` — `/?hidden_panels=token-usage&panel_order=main:cache-efficiency,session-outcomes` → both LAYO-01 + LAYO-02 visible in concert
3. `compare-resized` — `/sessions/compare?a=<a>&b=<b>&split_sizes=compare:70,30` → LAYO-03 visible

Densities (3): `compact`, `comfortable`, `cozy` — apply via the same density-cookie / localStorage mechanism existing tests use (verify by reading the existing spec; the established pattern is `await page.goto(...); await page.evaluate(d => localStorage.setItem('cmc.density', d), 'compact'); await page.reload();` or equivalent — match exactly).

Themes (2): `light`, `dark` — apply via the same theme mechanism (existing tests already cycle themes).

Capture pattern (extend the existing `test.describe.parallel` block — locate it by searching for "v13-visual-capture" header comments):
```
for (const surface of ['layout-default', 'layout-customized', 'compare-resized']) {
  for (const density of ['compact', 'comfortable', 'cozy']) {
    for (const theme of ['light', 'dark']) {
      test(`Phase 28 — ${surface} × ${density} × ${theme}`, async ({ page }) => {
        await page.goto(URL_FOR_SURFACE[surface])
        await page.evaluate(([d, t]) => {
          localStorage.setItem('cmc.density', d)
          localStorage.setItem('cmc.theme', t)
        }, [density, theme])
        await page.reload()
        await page.waitForLoadState('domcontentloaded')
        await page.waitForTimeout(200)  // settle any layout shift
        await page.screenshot({
          path: `.planning/phases/28-layout-customization/visual-check/${surface}-${density}-${theme}.png`,
          fullPage: true,
        })
      })
    }
  }
}
```

For `compare-resized`, the session ids `a` and `b` come from the same fixture helper Plan 28-05 used. If the dev DB lacks 2 sessions, env-skip the 6 compare-resized cells with a documented Accepted Exception (mirroring Plan 27-09's env-skip handling).

Critical: do NOT touch existing PNG-generating tests in this file (Pitfall 10 — Phase 27 ships 120 PNGs; Phase 28 adds exactly 18 to bring v1.3 close-gate total to 138). Verify by counting before/after:
```
grep -c "page.screenshot" frontend/tests/e2e/v13-visual-capture.spec.ts
```
Expected: existing count + 18 = (Phase 27 close baseline) + 18.

Create the output directory if it does not yet exist (explicit step — not optional; Playwright `page.screenshot({ path: ... })` will throw if the parent directory is missing):
```
mkdir -p .planning/phases/28-layout-customization/visual-check/
```
Note: this directory is implicitly part of `files_modified` (the SUMMARY at end-of-plan lists all 18 PNGs under this path); committing the directory is handled by git auto-detecting the new files.

Run the spec to generate PNGs:
```
cd frontend && pnpm exec playwright test tests/e2e/v13-visual-capture.spec.ts -g "Phase 28"
```

Verify post-run:
```
ls .planning/phases/28-layout-customization/visual-check/*.png | wc -l
```
Must return 18 (or 12 if 6 compare-resized cells are env-skipped — document in 28-VISUAL-CHECK.md Accepted Exceptions section).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test tests/e2e/v13-visual-capture.spec.ts -g "Phase 28" --reporter=line 2>&1 | tail -5 && ls /Users/patrykattc/work/git/claude-mission-control/.planning/phases/28-layout-customization/visual-check/*.png 2>/dev/null | wc -l</automated>
  </verify>
  <done>18 PNGs (or documented env-skip subset) generated under `.planning/phases/28-layout-customization/visual-check/`; v13-visual-capture.spec.ts has exactly 18 new test cases tagged "Phase 28"; no existing PNG cells modified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend v13-a11y.spec.ts with layout-customized URL variants</name>
  <files>frontend/tests/e2e/v13-a11y.spec.ts</files>
  <action>
Extend the existing a11y spec with axe-core scans for 3 layout-customized URL variants on the 5 in-scope routes. Pattern mirrors how Phase 27 added "tail-routes axe scans":

For each route in `['/', '/activity', '/cost', '/skills', '/alerts']`, add one scan with both layout params:
```
test(`a11y — Phase 28 layout-customized ${route}`, async ({ page }) => {
  // Use any valid hidden_panels + panel_order combo for the route — use the first panel registered for the route
  await page.goto(`${route}?hidden_panels=<first-panel-id>&panel_order=main:<csv-of-registry-order>`)
  await page.waitForLoadState('domcontentloaded')
  const results = await new AxeBuilder({ page }).analyze()
  // Filter against the existing PHASE_NET_CLASS_MARKERS inversion (Phase 25/27 pattern)
  const blockingViolations = results.violations.filter(v => v.impact === 'critical' || v.impact === 'serious')
  expect(blockingViolations).toEqual([])
})
```

Plus one scan for the split-pane variant:
```
test(`a11y — Phase 28 split-pane resized /sessions/compare`, async ({ page }) => {
  const { a, b } = await getCompareSessionIds(page)
  await page.goto(`/sessions/compare?a=${a}&b=${b}&split_sizes=compare:70,30`)
  // Same axe + filter pattern
})
```

Plus one scan for the drag grip:
```
test(`a11y — Phase 28 drag grip aria attributes`, async ({ page }) => {
  await page.goto('/cost')
  const grip = page.locator('[data-testid^="panel-drag-grip-"]').first()
  expect(await grip.getAttribute('aria-label')).toMatch(/^Reorder /)
  expect(await grip.getAttribute('aria-pressed')).toBe('false')
  await grip.focus()
  await page.keyboard.press('Space')
  expect(await grip.getAttribute('aria-pressed')).toBe('true')
  // aria-live region present
  await expect(page.locator('[role="status"][aria-live="polite"]').first()).toBeVisible()
})
```

Total new a11y tests: 5 (routes) + 1 (split-pane) + 1 (drag grip) = 7 new tests. Existing v1.2-baseline-carry-over accepted exceptions in the spec are PRESERVED — do NOT touch them (Pitfall 7 Phase 25 carry-overs).
  </action>
  <verify>
    <automated>cd frontend && pnpm exec playwright test tests/e2e/v13-a11y.spec.ts -g "Phase 28" 2>&1 | tail -5</automated>
  </verify>
  <done>7 new a11y tests green; no new blocking axe violations; aria attributes on drag grip verified; aria-live region present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author 28-VISUAL-CHECK.md with all 9 required sections</name>
  <files>.planning/phases/28-layout-customization/28-VISUAL-CHECK.md</files>
  <action>
Author `28-VISUAL-CHECK.md` using the 27-VISUAL-CHECK.md as the canonical template (RESEARCH.md §8 enumerates the 9 required sections — match exactly).

All 9 required sections, in order:

**1. Front matter / preamble:** Operator name (Patryk Golabek), date capture run, date verdict signed (LEAVE BLANK pending operator), phase number/name (28 / Layout Customization), plan that produced evidence (28-06), status (PROVISIONAL pending operator → PASS after sign-off).

**2. Capture commands:** the exact invocations that produce the evidence:
```
cd backend && uv run pytest tests/
cd frontend && pnpm test --run
cd frontend && pnpm exec playwright test
cd frontend && pnpm exec playwright test tests/e2e/v13-visual-capture.spec.ts -g "Phase 28"
cd frontend && pnpm exec playwright test tests/e2e/v13-a11y.spec.ts -g "Phase 28"
cd frontend && pnpm exec playwright test tests/e2e/v13-portal-containment.spec.ts
cd backend && uv run pytest tests/test_url_contract.py -v
```

**3. Captured PNGs directory + count:** `.planning/phases/28-layout-customization/visual-check/` — 18 PNGs (or env-skip-adjusted count). Combined v1.3 visual capture total at close: 120 + 18 = 138 (or env-skip-adjusted).

**4. ROADMAP Success Criteria Mapping table (5 rows — one per SC#1..SC#5):**

| SC# | Criterion (verbatim from ROADMAP) | Status | Evidence |
|-----|-----------------------------------|--------|----------|
| 1 | Hide System Pressure → URL persist → reload re-hides | PASS | v13-layout.spec.ts hide-and-persist tests; round-trip with saved view test |
| 2 | Drag divider on /sessions/compare → refresh persists → double-click resets | PASS | v13-layout.spec.ts split-pane tests |
| 3 | Reorder on /cost → persists into saved view → reset-to-default clears overrides | PASS | v13-layout.spec.ts reorder + reset tests |
| 4 | react-resizable-panels@4.11.0 is the only new runtime dep; zero chart re-mounts; ResponsiveContainer count unchanged; axe clean | PASS | Bundle delta from build; DOM-identity probe; ResponsiveContainer grep = 8; axe scan |
| 5 | Backend pytest + frontend vitest + Playwright e2e all green; URL contract 2/2; v1.3 milestone close gate met | PASS | Test invocations above |

**5. REQ-ID Closure table (4 rows — one per LAYO-01..04):**

| REQ ID | Description | Plan(s) | Status |
|--------|-------------|---------|--------|
| LAYO-01 | Per-route panel show/hide menu; persists into state_json | 28-03 | Complete |
| LAYO-02 | 1D drag-reorder within columns; persists into state_json | 28-04 | Complete |
| LAYO-03 | Split-pane resize via react-resizable-panels@4.11.0; double-click reset | 28-05 | Complete |
| LAYO-04 | Reset-to-default in both per-panel header + SavedViewMenu | 28-02 (SavedViewMenu half) + 28-03 (per-panel half) | Complete |

**6. Automated Evidence Summary table:**

| Metric | Phase 27 close baseline | Phase 28 close measured | Delta | Status |
|--------|------------------------|-------------------------|-------|--------|
| Backend pytest | 690/0/0 | (measure live) | (compute) | (PASS expected) |
| Frontend vitest | 662/0/0 | (measure live — estimated +35 per RESEARCH.md §10) | (compute) | (PASS expected) |
| Playwright e2e | ~243 tests | (measure live — estimated +18 LAYO + +7 a11y + 18 visual = ~286) | (compute) | (PASS expected) |
| Bundle size (frontend/dist gzipped) | Phase 27 baseline (measure once via `pnpm exec vite build && du -sh frontend/dist`) | (re-measure post-install of react-resizable-panels@4.11.0) | ≤ 15kB | (PASS expected — react-resizable-panels minified+gzipped ~12kB per npm registry; if delta > 15kB, investigate before signing off) |
| axe blocking violations | 0 Phase-27-attributable | (measure live with PHASE_28_NET_CLASS_MARKERS inversion) | (compute) | (PASS expected) |
| Portal containment | 7/7 | 8/8 expected (PanelHeaderMenu Portal adds +1 — verify via existing spec) | (compute) | (PASS expected) |
| URL contract pytest | 2/2 PASS | (re-run) | (compute) | (PASS expected) |
| Visual capture PNG total | 120 | 138 (or env-skip-adjusted) | +18 | (PASS expected) |
| ResponsiveContainer count | 8 | (re-grep) | 0 | (PASS expected — locked) |

**7. Visual capture verdict table (18 rows, one per surface × density × theme cell):**

| PNG | Surface | Density | Theme | Operator verdict |
|-----|---------|---------|-------|------------------|
| layout-default-compact-light.png | / (default) | compact | light | (operator) |
| ... (17 more rows) ... | ... | ... | ... | (operator) |

**8. Accepted Exceptions:**

Items to enumerate (pre-list with operator-acknowledgment placeholders):
- `/skills/$name` show/hide deferred to v1.4 — single-column stack provides marginal value (RESEARCH.md §5 + Question 11 Pitfall 12). Per Phase 27 close pattern, deferrals are operator-acknowledged here, not silenced.
- Any env-skipped visual capture cells (if dev DB lacks 2 sessions for compare-resized cells) — note the skip count + the reason; mirror Plan 27-09's env-skip pattern.
- LAYO-05 (full 2D grid via react-grid-layout) — DEFERRED per REQUIREMENTS.md to v1.4+; blocked by Issue #2045.
- CMPR-10 (3+ way compare) — DEFERRED per REQUIREMENTS.md, OUT OF SCOPE for v1.3.
- Any verification-discovered fixes (mirror Phase 26 e838135 / Phase 27 d76a95b precedent) — list as a numbered Accepted Exception if the operator walkthrough surfaces a Rule-1 self-heal.

**9. Operator verdict line:** Format verbatim per 27-VISUAL-CHECK.md:
```
**Operator verdict:** {PASS|FAIL} — signed {YYYY-MM-DD} by Patryk Golabek via live Chrome DevTools MCP walkthrough against http://localhost:5173 + backend on :8001.
```

LEAVE BLANK on initial write — the operator fills this in during Task 4.

Also include a brief "v1.3 milestone close" paragraph at the bottom: "Phase 28 close advances v1.3 from 4/5 phases to 5/5 phases; 41/45 → 45/45 active requirements; v1.3 milestone shipped — all LAYO-01..04 satisfied with the single agreed dep (react-resizable-panels@4.11.0) and APPEND-ONLY URL contract preserved across all v1.3 phases."
  </action>
  <verify>
    <automated>test -f .planning/phases/28-layout-customization/28-VISUAL-CHECK.md && grep -c "^##" .planning/phases/28-layout-customization/28-VISUAL-CHECK.md</automated>
  </verify>
  <done>28-VISUAL-CHECK.md has all 9 required sections (≥9 `## ` headings); REQ-ID Closure table lists LAYO-01..04 with their closing plan numbers; Operator verdict line present but blank (pending Task 4); ResponsiveContainer count and ALL gate metrics filled with measured values, not placeholders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Operator live walkthrough + verdict signing</name>
  <what-built>Phase 28 ships all 4 LAYO requirements (show/hide + reorder + split-pane + reset). Tasks 1-3 of this plan produced 18 visual capture PNGs + 7 a11y tests + an unsigned 28-VISUAL-CHECK.md. This checkpoint asks the operator to verify behavior via live Chrome DevTools MCP walkthrough and sign the verdict, mirroring Phase 27's close-gate pattern.</what-built>
  <how-to-verify>
Boot the dev stack:
```
cd backend && uv run uvicorn app.main:app --reload --port 8001 &
cd frontend && pnpm dev &
```

Open http://localhost:5173 in Chrome (or Chrome DevTools MCP-controlled session).

LAYO-01 — Show/hide:
1. Navigate to `/` — verify panel header settings menus visible
2. Open a panel's menu → click "Hide this panel" → verify panel disappears AND URL gains `?hidden_panels=<id>`
3. Repeat on `/activity`, `/cost`, `/skills`, `/alerts` (one panel each)

LAYO-04 — Reset (per-panel):
4. With a panel hidden on `/`, open ANOTHER visible panel's menu → click "Reset layout" → verify hidden panel reappears + sonner toast "Layout reset" + URL drops layout params + time_from/time_to preserved (set range via the Time Picker first to verify preservation)

LAYO-04 — Reset (SavedViewMenu escape hatch):
5. Hide every panel on `/cost` (only 2 panels — should be trivial to hide both)
6. Without a panel header to access, open SavedViewMenu from AppShellHeader → click "Reset layout" → verify all panels reappear + toast

LAYO-02 — Drag-reorder:
7. On `/cost`, drag the panel grip on cost-by-project up above cost-forecast → verify visual reorder + URL gains panel_order
8. Reload — verify order persists
9. Keyboard test: Tab to a grip, press Space (aria-pressed=true, screen-reader-equivalent announcement should appear in DevTools as a status role with new text), ArrowDown, Enter → verify reorder

LAYO-03 — Split-pane:
10. Navigate to `/sessions/compare?a=<a>&b=<b>` (pick two valid session ids from `/api/sessions`)
11. Drag the Separator between the two sides → verify URL gains `split_sizes=compare:<a>,<b>` on release
12. Reload → verify dragged ratio persists
13. Double-click the Separator → verify 50/50 reset + URL drops `split_sizes` entirely

Round-trip via saved view:
14. Hide a panel on `/`, save view as "Compact home" via SaveViewDialog
15. Navigate to `/cost`, open SavedViewMenu → select "Compact home" → verify URL has `hidden_panels=...` + the panel is hidden

Final verification:
16. Visual capture: scan the 18 PNGs in `.planning/phases/28-layout-customization/visual-check/` — verify each cell matches its surface × density × theme label; no clipped panels, no broken layouts
17. Open 28-VISUAL-CHECK.md and complete:
    - Fill the Visual capture verdict table (column 5 — operator's per-cell PASS/FAIL/EXCEPTION)
    - Fill the Operator verdict line at section 9: replace the blank with `**Operator verdict:** PASS — signed YYYY-MM-DD by Patryk Golabek via live Chrome DevTools MCP walkthrough against http://localhost:5173 + backend on :8001.` (replace YYYY-MM-DD with today's date)
    - If any LAYO behavior failed in steps 1-16: do NOT sign PASS; instead, return to the planner via "blocked: <failure>" so a gap-closure plan can run.

If any verification-discovered fix is needed (mirror Phase 26 e838135 / Phase 27 d76a95b precedent), apply Rule-1 self-heal in this checkpoint scope, document as a numbered Accepted Exception in section 8, and commit the fix as part of the close-gate evidence.
  </how-to-verify>
  <resume-signal>Type "approved" after signing 28-VISUAL-CHECK.md verdict line as PASS, or "blocked: &lt;reason&gt;" if any step failed (gap-closure plan needed).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visual capture matrix → operator | Operator verdict is the gate; no machine sign-off |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-15 | Repudiation | Operator verdict | mitigate | Signature line includes name + date + verification mechanism (Chrome DevTools MCP walkthrough URL + backend URL) — auditable per Phase 24-27 pattern |
| T-28-16 | Information Disclosure | PNGs in `.planning/phases/28-layout-customization/visual-check/` | accept | Local single-user; visual capture is operator-curated; no PII in panel content (panel labels are kebab-case slugs of UI titles) |
</threat_model>

<verification>
- `cd backend && uv run pytest` → green
- `cd frontend && pnpm test --run` → green
- `cd frontend && pnpm exec playwright test` → all green
- `cd frontend && pnpm exec playwright test tests/e2e/v13-visual-capture.spec.ts -g "Phase 28"` → 18 cells green (or env-skip-adjusted; document)
- `cd frontend && pnpm exec playwright test tests/e2e/v13-a11y.spec.ts -g "Phase 28"` → 7 new a11y tests green
- `cd frontend && pnpm exec playwright test tests/e2e/v13-portal-containment.spec.ts` → 8/8 PASS (Phase 27's 7/7 + PanelHeaderMenu Portal +1)
- `cd backend && uv run pytest tests/test_url_contract.py -v` → 2/2 PASS
- `grep -rE "^[[:space:]]*<ResponsiveContainer" frontend/src/components/panels/*.tsx | wc -l` → 8 (locked)
- `ls .planning/phases/28-layout-customization/visual-check/*.png | wc -l` → 18 (or documented env-skip)
- `grep -c "Operator verdict" .planning/phases/28-layout-customization/28-VISUAL-CHECK.md` → ≥1
- `grep "Operator verdict.*PASS.*signed" .planning/phases/28-layout-customization/28-VISUAL-CHECK.md` → 1 (post-operator)
</verification>

<success_criteria>
- v13-visual-capture.spec.ts ships exactly 18 new Phase-28 PNG cells
- v13-a11y.spec.ts ships 7 new Phase-28 a11y scans
- 28-VISUAL-CHECK.md has all 9 required sections + operator-signed PASS verdict
- All v1.3 close-gate metrics green per RESEARCH.md §8 contract
- v1.3 milestone advances from 4/5 phases → 5/5; 41/45 → 45/45 active requirements
</success_criteria>

<output>
Create `.planning/phases/28-layout-customization/28-06-SUMMARY.md` documenting: final pytest/vitest/Playwright counts (filled into Section 6 of 28-VISUAL-CHECK.md), final PNG total (138 or env-skip-adjusted), any verification-discovered fixes the operator surfaced and the Rule-1 self-heal commit hash, and the operator's signed verdict date. After this SUMMARY ships, mark v1.3 milestone complete in `.planning/ROADMAP.md` (5/5 phases, 45/45 requirements).
</output>
