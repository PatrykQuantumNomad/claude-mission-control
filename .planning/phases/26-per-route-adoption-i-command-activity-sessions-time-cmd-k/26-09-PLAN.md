---
phase: 26-per-route-adoption-i-command-activity-sessions-time-cmd-k
plan: 09
type: execute
wave: 5
depends_on:
  - "26-04"
  - "26-05"
  - "26-06"
  - "26-07"
  - "26-08"
files_modified:
  - frontend/tests/e2e/v13-time-picker.spec.ts
  - frontend/tests/e2e/v13-a11y.spec.ts
  - frontend/tests/e2e/v13-portal-containment.spec.ts
  - frontend/tests/e2e/v13-saved-views.spec.ts
  - frontend/tests/e2e/command-palette.spec.ts
  - frontend/tests/e2e/v13-visual-capture.spec.ts
  - frontend/tests/e2e/v13-sidebar.spec.ts
  - .planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-VISUAL-CHECK.md
autonomous: false

must_haves:
  truths:
    - "v13-time-picker.spec.ts ships covering TIME-01..05 (preset apply, copy/paste, brush-zoom, refresh, reset-zoom)"
    - "v13-a11y.spec.ts axe scan extended to cover TimePicker popover + RefreshDropdown + Compare overlay + Recently Visited section — 0 NEW Phase-26-attributable violations"
    - "v13-portal-containment.spec.ts extended to assert TimePicker popover + sonner Toaster portal-bounded at every density × viewport ≥ 1024px"
    - "v13-saved-views.spec.ts extended: saved view containing compare_panels + time_from + time_to round-trips through state_json"
    - "command-palette.spec.ts extended to cover Recents / Time range / Density groups (6 group JSX order verified)"
    - "v13-visual-capture.spec.ts adds 5+ NEW Phase 26 chrome surfaces (time-picker-open / refresh-dropdown-open / compare-toggle-active / cmdk-with-recents / sidebar-with-recently-visited) × 3 densities × 2 themes"
    - "Lighthouse 9/9 PASS preserved (no perf regression)"
    - "ResponsiveContainer count = 26 at phase close"
    - "Backend pytest 686/0/0 preserved (no backend changes this phase)"
    - "Operator verdict captured in 26-VISUAL-CHECK.md (PASS / CONDITIONAL / FAIL)"
  artifacts:
    - path: ".planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-VISUAL-CHECK.md"
      provides: "Operator-driven visual review scaffold + automated evidence + operator-signed verdict"
      contains: "Operator verdict"
    - path: "frontend/tests/e2e/v13-time-picker.spec.ts"
      provides: "TIME-01..05 e2e coverage"
      contains: "time-picker-trigger"
  key_links:
    - from: "26-VISUAL-CHECK.md"
      to: "ROADMAP.md Phase 26 success criteria 1-5"
      via: "operator-signed mapping table — each criterion to its evidence source"
      pattern: "Success Criterion"
---

<objective>
Phase 26 close gate. Author the Playwright e2e + axe + Lighthouse + visual-capture cascade that proves all 5 ROADMAP success criteria + all 9 requirement IDs (SHEL-05, TIME-01..05, CMDK-02..04) ship green. Final artifact: `26-VISUAL-CHECK.md` with operator-signed PASS verdict (or CONDITIONAL / FAIL with documented Accepted Exceptions).

Purpose: Follows the POLI-09 pattern established Phase 24 plan 07 + Phase 25 plan 11 (same evidence-assembly + operator-checkpoint structure). The pattern is now FORMAL — copy the precedent, adapt to Phase 26 surfaces.

This plan is the ONLY plan in Phase 26 with `autonomous: false` (operator visual checkpoint at the end). Tasks 1-2 are autonomous (cascade authoring + automated runs); Task 3 is the operator checkpoint.

Output: 7 e2e spec files extended (1 NEW v13-time-picker.spec.ts + 6 extensions), 1 NEW 26-VISUAL-CHECK.md (operator verdict file).
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-CONTEXT.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-RESEARCH.md

# Precedent close-gate evidence files (FOLLOW THIS PATTERN)
@.planning/phases/24-shell-density-containment-primitives/24-VISUAL-CHECK.md
@.planning/phases/25-saved-views-backend-frontend/25-VISUAL-CHECK.md

# Existing e2e spec families to extend
@frontend/tests/e2e/v13-a11y.spec.ts
@frontend/tests/e2e/v13-portal-containment.spec.ts
@frontend/tests/e2e/v13-saved-views.spec.ts
@frontend/tests/e2e/v13-visual-capture.spec.ts
@frontend/tests/e2e/v13-sidebar.spec.ts
@frontend/tests/e2e/command-palette.spec.ts
@frontend/playwright.config.ts

# Lighthouse config
@frontend/lighthouserc.json

# All Phase 26 plan SUMMARYs (after Plans 01-08 ship)
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-01-SUMMARY.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-02-SUMMARY.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-03-SUMMARY.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-04-SUMMARY.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-05-SUMMARY.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-06-SUMMARY.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-07-SUMMARY.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author v13-time-picker.spec.ts + extend the 5 existing v13-* spec families</name>
  <files>frontend/tests/e2e/v13-time-picker.spec.ts, frontend/tests/e2e/v13-a11y.spec.ts, frontend/tests/e2e/v13-portal-containment.spec.ts, frontend/tests/e2e/v13-saved-views.spec.ts, frontend/tests/e2e/v13-sidebar.spec.ts, frontend/tests/e2e/command-palette.spec.ts</files>
  <action>
**CREATE `frontend/tests/e2e/v13-time-picker.spec.ts`** — NEW Playwright spec. Covers TIME-01..05 + CMDK-02..04 e2e integration. Mirror the structure of `v13-saved-views.spec.ts` (Phase 25 plan 11) — `test.describe('Phase 26 / TIME / CMDK', ...)` with one `test()` per success criterion + a few targeted edge cases.

Required test cases:

```
test('TIME-01: opens time picker popover; clicking Last 7 days writes URL ?time_from=now-7d&time_to=now', async ({ page }) => { ... })
test('TIME-01: custom range calendar applies absolute ISO time_from / time_to', async ({ page }) => { ... })
test('TIME-01: refresh dropdown selects 30s → cmc.autoRefresh.interval localStorage gets "30s"', async ({ page }) => { ... })
test('TIME-01: refresh pause indicator shows when URL has absolute time_from', async ({ page }) => { ... })
test('TIME-02: panel-sync — set TimePicker to last 7 days on / → TokenUsageCard, CacheEfficiencyCard, ProductivityCard re-query via rangeToVocab bridge (Range vocab = 7d)', async ({ page }) => { ... })
test('TIME-02: panel-sync — set picker on /activity → ChartsStrip/UnifiedFailures/TopSkills re-anchor; OtelPanel (live SSE) is opt-out and stays uninterrupted', async ({ page }) => { ... })
test('TIME-03: Cmd+Shift+C copies serialized range; Cmd+Shift+V applies pasted range (toast fired)', async ({ page }) => { ... })
test('TIME-03: paste between routes — copy on /activity → navigate to / → paste → / URL has the same time_from/time_to', async ({ page }) => { ... })
test('CMDK-03 / TIME-03: copy/paste via Cmd+K commands (clipboard-permission scenarios)', async ({ page }) => { ... })
test('TIME-04: CompareToggle round-trip — toggle on token-usage → URL gains compare_panels=token-usage → reload preserves', async ({ page }) => { ... })
test('TIME-05: brush-zoom on /activity ChartsStrip writes absolute time_from/time_to to URL', async ({ page }) => { ... })
test('TIME-05: ResetZoomButton appears when URL has absolute time_from; click clears it', async ({ page }) => { ... })
test('TIME-05 + auto-refresh interaction: brush-zoom commits absolute → auto-refresh shows "paused"', async ({ page }) => { ... })
test('CMDK-02: density command in Cmd+K flips <html data-density> attribute + persists', async ({ page }) => { ... })
test('CMDK-03: time range "Last 7 days" command in Cmd+K writes URL', async ({ page }) => { ... })
test('CMDK-04: Recents group shows last 5 routes from cmc.recents.routes; selecting one navigates', async ({ page }) => { ... })
test('SHEL-05: sidebar Recently Visited shows top 3 routes (current filtered); navigating updates the list', async ({ page }) => { ... })
test('Cmd+K group order: Recents → Saved Views → Pages → Time range → Density → Actions (per Pitfall 10 lock)', async ({ page }) => { ... })
```

Use Playwright's `page.evaluate(() => window.localStorage.getItem('cmc.recents.routes'))` to assert localStorage writes. Use `page.evaluate(() => navigator.clipboard.writeText(...))` to seed clipboard for paste tests (note: requires `--use-fake-ui-for-media-stream` or `permissions: ['clipboard-read', 'clipboard-write']` in playwright config — verify Phase 25's playwright.config.ts already grants these; if not, extend it).

**EXTEND `v13-a11y.spec.ts`** — add a new axe scan section for Phase 26 chrome:
- Open TimePicker popover → axe.analyze() — 0 NEW violations
- Open RefreshDropdown → axe.analyze() — 0 NEW violations
- Open Cmd+K with Recents/Time range/Density groups → axe.analyze() — 0 NEW violations
- Activate compare overlay on token-usage → axe.analyze() — 0 NEW violations
- Verify the v1.2 carry-overs fixed in Plan 08 (`.cmc-numeric` + `.cmc-system-health-strip__*`) are no longer flagged

Use the Phase 25 `PHASE_25_NET_CLASS_MARKERS` inversion-filter pattern: add `PHASE_26_NET_CLASS_MARKERS` for the new Phase 26 chrome classes (`.cmc-time-picker__*`, `.cmc-refresh-dropdown__*`, `.cmc-compare-toggle*`, `.cmc-reset-zoom-button`, `.cmc-sidebar__recently-visited*` — collect them).

**EXTEND `v13-portal-containment.spec.ts`** — assert:
- TimePicker popover (`[data-testid="time-picker-popover"]`) walks no ancestor with `transform: !none` (mirror Phase 24 plan 07's walker pattern)
- RefreshDropdown content portals (`[role="menu"]`) — same check
- Sonner Toaster portal (`[role="region"]` or sonner's class) — same check; viewport-bounded at every density × ≥ 1024px viewport

**EXTEND `v13-saved-views.spec.ts`** — add tests for Phase 26 integration:
- Save view with `time_from=now-7d&time_to=now` → reload → load view → URL restores both params
- Save view with `compare_panels=token-usage` → reload → load view → compare overlay restores
- Save view with all three (time_from + time_to + compare_panels) → round-trips fully

**EXTEND `v13-sidebar.spec.ts`** — Recently Visited section:
- Navigate `/activity` → `/skills` → `/cost` → sidebar shows Recently Visited with `/skills` then `/activity` (current `/cost` filtered)
- Section header always renders (even with 0 recents on a fresh-localStorage cold start)
- Collapsed sidebar (Cmd+B) still shows Recently Visited entries as icons

**EXTEND `command-palette.spec.ts`** — group order + density command interaction:
- Cmd+K → first group heading is "Recents"; last is "Actions"; full order: Recents → Saved Views → Pages → Time range → Density → Actions
- "Set density: Compact" command flips `<html data-density="compact">`
- "Last 7 days" Time range command writes URL

Run the full e2e suite: `pnpm --filter cmc-frontend test:e2e`. Capture any flake — re-run flaky specs once; if persistent flake, document as Accepted Exception with rationale in the VISUAL-CHECK.md.

**Lighthouse CI run:** `cd frontend && npx lhci autorun` (existing config). Assertions must stay 9/9 PASS — no perf regression. If LCP slips on `/activity` (likely culprit: TimePicker JS bundle on first paint), document and consider lazy-loading the TimePicker calendar (defer to Plan 09 Task 2 if needed; otherwise document and revisit).
  </action>
  <verify>
    <automated>cd frontend && pnpm test:e2e --reporter=line && pnpm tsc --noEmit && pnpm lint && cd ../backend && uv run pytest tests/test_url_contract.py</automated>
  </verify>
  <done>
- `v13-time-picker.spec.ts` ships with ≥ 18 test cases mapped 1:1 to TIME/CMDK requirements + ROADMAP success criteria
- 5 existing v13-* spec families extended for Phase 26 surfaces
- All Playwright tests green; flakes documented + retried
- Lighthouse 9/9 PASS preserved
- backend pytest 686/0/0 preserved (no backend changes)
- ResponsiveContainer count = 26 (final phase-close check)
- testid registry contains every NEW testid used in the new specs (Plan 03/04/05/06/07 registered them all; this plan only consumes)
- `pnpm tsc --noEmit` clean; `pnpm lint` clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend v13-visual-capture.spec.ts to capture 5+ new Phase 26 chrome surfaces × 3 densities × 2 themes</name>
  <files>frontend/tests/e2e/v13-visual-capture.spec.ts</files>
  <action>
Extend `frontend/tests/e2e/v13-visual-capture.spec.ts` with new visual-capture rows for the Phase 26 chrome (mirror Phase 25 plan 11's 30 NEW Phase 25 chrome PNGs pattern: 5 surfaces × 3 densities × 2 themes = 30 new images).

Phase 26 chrome surfaces to capture:
1. **time-picker-open** — TimePicker popover expanded showing preset list + calendar
2. **refresh-dropdown-open** — RefreshDropdown menu expanded showing 4 intervals
3. **compare-toggle-active** — TokenUsageCard with `?compare_panels=token-usage&range=7d` showing translucent overlay bar
4. **cmdk-with-recents** — Cmd+K palette open with populated Recents + Time range + Density groups visible (seed `cmc.recents.routes` via `page.evaluate` first)
5. **sidebar-with-recently-visited** — Sidebar showing populated Recently Visited section between Pinned and Configure (seed `cmc.recents.routes`)

For each surface, iterate `density × theme` (3 × 2 = 6 PNGs per surface = 30 total NEW images).

Path: `.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/visual-check/<surface>-<density>-<theme>.png`

EXAMPLE shape (extend existing v13-visual-capture.spec.ts):

```typescript
const PHASE_26_CHROME_MATRIX = [
  { name: 'time-picker-open', setup: async (page) => {
      await page.goto('/');
      await page.click('[data-testid="time-picker-trigger"]');
      await page.waitForSelector('[data-testid="time-picker-popover"]');
  }},
  { name: 'refresh-dropdown-open', setup: async (page) => {
      await page.goto('/');
      await page.click('[data-testid="refresh-dropdown-trigger"]');
      await page.waitForSelector('[data-testid="refresh-option-30s"]');
  }},
  { name: 'compare-toggle-active', setup: async (page) => {
      await page.goto('/?compare_panels=token-usage');
      await page.waitForSelector('[data-testid="compare-overlay-toggle-token-usage"][aria-pressed="true"]');
  }},
  { name: 'cmdk-with-recents', setup: async (page) => {
      await page.evaluate(() => window.localStorage.setItem('cmc.recents.routes',
        JSON.stringify([
          { route: '/activity', visitedAt: Date.now() - 1000 },
          { route: '/skills', visitedAt: Date.now() - 2000 },
          { route: '/cost', visitedAt: Date.now() - 3000 },
        ])));
      await page.goto('/');
      await page.keyboard.press('Meta+k');
      await page.waitForSelector('[data-testid^="cmdk-recents-route-"]');
  }},
  { name: 'sidebar-with-recently-visited', setup: async (page) => {
      await page.evaluate(() => window.localStorage.setItem('cmc.recents.routes',
        JSON.stringify([
          { route: '/activity', visitedAt: Date.now() - 1000 },
          { route: '/skills', visitedAt: Date.now() - 2000 },
          { route: '/cost', visitedAt: Date.now() - 3000 },
        ])));
      await page.goto('/');
      await page.waitForSelector('[data-testid="sidebar-section-recently-visited"]');
  }},
];

for (const surface of PHASE_26_CHROME_MATRIX) {
  for (const density of ['compact', 'comfortable', 'cozy']) {
    for (const theme of ['light', 'dark']) {
      test(`Phase 26 chrome capture: ${surface.name} × ${density} × ${theme}`, async ({ page }) => {
        await page.evaluate(([d, t]) => {
          document.documentElement.setAttribute('data-density', d as string);
          document.documentElement.setAttribute('data-theme', t as string);
          window.localStorage.setItem('cmc.density', d as string);
          window.localStorage.setItem('cmc.theme', t as string);
        }, [density, theme]);
        await surface.setup(page);
        await page.screenshot({
          path: `.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/visual-check/${surface.name}-${density}-${theme}.png`,
          fullPage: false,
        });
      });
    }
  }
}
```

Mirror Phase 24 plan 05's networkidle vs domcontentloaded discipline: routes with persistent streams (OTEL firehose on `/activity`) use `domcontentloaded` + short `waitForSelector` settle. Phase 25 plan 11 followed this; Phase 26 inherits.

Also REGENERATE existing 36 + 30 Phase 24/25 PNGs by running the matrix to confirm no Phase 26 regressions broke prior surfaces. After this task: 36 (Phase 24) + 30 (Phase 25) + 30 (Phase 26) = 96 total visual-capture PNGs.

(If file count is too high, planner may choose a smaller Phase 26 matrix — e.g. 3 surfaces × 2 densities × 2 themes = 12 PNGs — and document the choice. Default plan: 30 PNGs to match Phase 25 precedent.)
  </action>
  <verify>
    <automated>cd frontend && pnpm test:e2e --grep "Phase 26 chrome capture" --reporter=line && ls .planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/visual-check/*.png | wc -l | awk '{if($1>=15)print "OK "$1" PNGs"; else {print "FAIL — only "$1" PNGs"; exit 1}}'</automated>
  </verify>
  <done>
- 30 NEW Phase 26 chrome PNGs at `.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/visual-check/` (5 surfaces × 3 densities × 2 themes — OR a documented smaller matrix)
- Existing 36 + 30 PNGs regenerated and visually unchanged (no Phase 26 regressions in prior surfaces — operator verifies in Task 3)
- `pnpm test:e2e --grep "Phase 26 chrome capture"` all green
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator visual verification + 26-VISUAL-CHECK.md verdict</name>
  <what-built>
Phase 26 close-gate evidence cascade:
- v13-time-picker.spec.ts (18 tests covering TIME-01..05 + CMDK-02..04 + SHEL-05)
- v13-a11y.spec.ts extended (NEW Phase 26 chrome axe scans)
- v13-portal-containment.spec.ts extended (TimePicker / RefreshDropdown / Sonner Toaster portal walks)
- v13-saved-views.spec.ts extended (time_from + time_to + compare_panels round-trip)
- v13-sidebar.spec.ts extended (Recently Visited section)
- command-palette.spec.ts extended (group order + density command)
- v13-visual-capture.spec.ts extended (30 NEW Phase 26 chrome PNGs)
- Lighthouse 9/9 PASS preserved
- backend pytest 686/0/0 preserved
- ResponsiveContainer count = 26

Plus: 26-VISUAL-CHECK.md scaffold with automated evidence ROLLED UP (test counts, ROADMAP success criterion mapping, axe verdict, Lighthouse verdict, visual matrix table). Mirrors Phase 24/25 VISUAL-CHECK.md format verbatim.

Author `26-VISUAL-CHECK.md` BEFORE this checkpoint runs — it should already contain:
- Test count deltas (vitest, Playwright, backend pytest) vs Phase 25 close baseline
- 5/5 ROADMAP success criteria mapped 1:1 to evidence sources
- Axe verdict (Phase-26-attributable count; deferred Phase-27 items documented)
- Lighthouse verdict (9/9 PASS confirmation; LCP/CLS/Performance median values)
- 30 visual-capture PNG inventory (all 30 listed, awaiting operator pass/fail)
- 4 deferred-to-Phase-27 a11y items (carryover semantic — `.cmc-heatmap-cell` aria, `.cmc-otel-feed` scrollable-region, `.cmc-sessions-table-header__label`, `<select aria-label="Range filter">`)
- Any new Accepted Exceptions discovered during the close gate (document each with unblock condition + future-phase reference)
- Empty "Operator inline notes" section (9-item template like Phase 24/25)
- Empty "Operator verdict signature" block

Author the file via Write tool now; operator fills the verdict at the checkpoint.
  </what-built>
  <how-to-verify>
**Required environment:** Dev server running (`cd frontend && pnpm dev`), demo DB populated (LiveSessionsCard / OtelPanel / SessionsTable have data).

**Operator verification steps:**

1. **Cold-load each route** at every density × theme (3 × 2 = 6 combinations per route × 3 routes = 18 combinations). For each:
   - Visit `/`, `/activity`, `/sessions/compare`
   - Verify: page is bounded (no viewport overflow), panels each render as `bounded` cards with internal scroll where applicable, time picker visible in header, refresh dropdown visible, sidebar shows Recently Visited section after navigation

2. **TimePicker functional check:**
   - Open the popover; click "Last 7 days" → URL gains `?time_from=now-7d&time_to=now`
   - Cmd+Shift+C → toast.success "Time range copied"
   - Navigate to `/activity` → Cmd+Shift+V → toast.message "Pasted: last 7 days" → URL updates
   - Custom range calendar → pick a range → click Apply → URL gains ISO timestamps

3. **RefreshDropdown functional check:**
   - Select "30 seconds" → pulse dot appears next to trigger
   - Open DevTools Network tab → confirm time-anchored query requests fire every 30s
   - Brush a region on `/activity` ChartsStrip → trigger label flips to "Paused" → no further auto-refreshes fire

4. **Brush-zoom flow (success criterion 3):**
   - Drag a region on the 14-day token bar chart on `/activity`
   - Verify: URL gains absolute time_from/time_to; global TimePicker label updates; ResetZoomButton appears in chart header
   - All other panels on `/activity` re-anchor (RangeToggle's effective range now derives from the absolute URL via rangeToVocab — bridge degrades to '30d' widest cover; client-side slicing handles the rest)
   - Click "Reset zoom" → URL clears; TimePicker reverts to default; RefreshDropdown unpause

5. **Cmd+K group order + integration (success criterion 4):**
   - Cmd+K opens → 6 groups in order: Recents → Saved Views → Pages → Time range → Density → Actions
   - Recents shows last 5 routes (Pitfall 8 b: current route filtered)
   - "Set density: Compact" → entire dashboard re-spaces with no flash, no React commit-list explosion
   - "Last 7 days" → URL updates without navigating

6. **Sidebar Recently Visited (SHEL-05):**
   - Navigate `/` → `/activity` → `/skills` → `/cost` → sidebar shows `/skills`, `/activity`, `/` (top 3, current `/cost` filtered)
   - Collapse sidebar (Cmd+B) → icons visible, hover tooltips work

7. **Compare overlay (TIME-04):**
   - On `/`, toggle CompareToggle on TokenUsageCard (set range to 7d first) → URL gains `?compare_panels=token-usage&range=7d`
   - Verify: translucent prior-period bar series renders alongside primary bars
   - Save the view → reload → load it back → overlay restored

8. **Sheet viewport-bounded on /sessions/compare (success criterion 5):**
   - Visit `/sessions/compare?a={uuid}&b={uuid}` with two real sessions
   - Open the compare picker Sheet → at 1024px / 1440px / 1920px viewport widths AND each density tier (3 × 3 = 9 combinations), Sheet stays inside viewport, no clipping
   - Long session IDs + cwd paths truncate with hover-tooltip

9. **Review the 30 NEW Phase 26 visual-capture PNGs:**
   - Browse `.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/visual-check/`
   - For each PNG, eyeball: chrome renders correctly, density tokens applied, no broken overlays, no clipped overlay portals
   - Re-verify Phase 24 + Phase 25 PNGs are visually unchanged (no Phase 26 regression on prior chrome)

10. **Acknowledge Accepted Exceptions:**
    - 4 deferred-to-Phase-27 a11y items (`.cmc-heatmap-cell` aria, `.cmc-otel-feed` scrollable-region, `.cmc-sessions-table-header__label`, `<select aria-label="Range filter">`) — confirm they're listed in 26-VISUAL-CHECK.md as Accepted Exceptions with unblock condition pointing to Phase 27
    - Backend `time_from`/`time_to` acceptance deferred to Phase 27 TDBT — confirm the ADR in Plan 01 + the verification must-have are documented
    - Any close-gate-discovered defects: either inline-patch (Phase 24 plan 07 precedent) or document as Accepted Exception with operator-approved rationale

**Then:** write the Operator inline notes (9-item template) into 26-VISUAL-CHECK.md; write Operator verdict signature block (PASS / CONDITIONAL / FAIL); commit.

If any step fails: file a defect, plan inline patch, re-run cascade. If patch can land without restructuring, follow Phase 24 plan 07's pattern (multiple fix commits + 1 docs commit). If structural defect: gate fails, return to gap closure mode.
  </how-to-verify>
  <resume-signal>Type "PASS" (with operator name + date) or describe issues / Accepted Exceptions to add</resume-signal>
</task>

</tasks>

<verification>
- All Phase 26 plans (01-08) shipped with green SUMMARYs
- v13-time-picker.spec.ts ships with ≥ 18 cases mapped to TIME/CMDK/SHEL requirements
- All e2e specs green; Lighthouse 9/9 PASS; axe 0 NEW Phase-26-attributable violations
- ResponsiveContainer count = 26
- backend pytest 686/0/0 unchanged
- 30 NEW Phase 26 visual-capture PNGs ship at `.planning/phases/26.../visual-check/`
- 26-VISUAL-CHECK.md exists with operator verdict signature
- All 5 ROADMAP success criteria 1:1 mapped to evidence sources
- All 9 requirements (SHEL-05, TIME-01..05, CMDK-02..04) marked Complete in REQUIREMENTS.md after operator PASS
</verification>

<success_criteria>
1. Operator verdict PASS captured in 26-VISUAL-CHECK.md with date + signature (mirror Phase 24/25 precedent)
2. v1.3 milestone progress advances 2/5 → 3/5 phases complete; 29/45 → 38/45 active requirements satisfied
3. Phase 27 (Per-Route Adoption II + Tech Debt) is unblocked — picker contract proven, primitive adoption pattern proven, time-bridge architecture decided
4. Any close-gate-discovered defects either inline-patched or documented as Accepted Exceptions with operator approval + unblock conditions referencing future phases
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-09-SUMMARY.md` per `summary.md` template (close-gate roll-up)
2. Create `.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-VISUAL-CHECK.md` (operator verdict file — author scaffold in Task 1-2, fill verdict in Task 3)
3. Update STATE.md with Phase 26 close baseline (test counts, dep ledger, Accepted Exceptions, v1.3 progress)
4. Update REQUIREMENTS.md traceability table: mark SHEL-05, TIME-01..05, CMDK-02..04 as Complete
5. Update ROADMAP.md Phase 26 entry: mark [x] complete with date
</output>
