---
phase: 26-per-route-adoption-i-command-activity-sessions-time-cmd-k
plan: 08
type: execute
wave: 4
depends_on:
  - "26-01"
  - "26-03"
  - "26-05"
files_modified:
  - frontend/src/routes/index.tsx
  - frontend/src/routes/activity.tsx
  - frontend/src/routes/sessions_.compare.tsx
  - frontend/src/components/panels/SystemHealthStrip.tsx
  - frontend/src/components/panels/KpiRow.tsx
  - frontend/src/components/panels/LiveSessionsCard.tsx
  - frontend/src/components/panels/AttentionBar.tsx
  - frontend/src/components/panels/TokenUsageCard.tsx
  - frontend/src/components/panels/CacheEfficiencyCard.tsx
  - frontend/src/components/panels/HookActivityCard.tsx
  - frontend/src/components/panels/SessionOutcomesCard.tsx
  - frontend/src/components/panels/PressurePanel.tsx
  - frontend/src/components/panels/McpPanel.tsx
  - frontend/src/components/panels/ProductivityCard.tsx
  - frontend/src/components/panels/EditAcceptanceCard.tsx
  - frontend/src/components/panels/ToolLatencyCard.tsx
  - frontend/src/components/panels/AgentFanoutCard.tsx
  - frontend/src/components/panels/ProjectBreakdownCard.tsx
  - frontend/src/components/panels/ActivityHeatmap.tsx
  - frontend/src/components/panels/ChartsStrip.tsx
  - frontend/src/components/panels/OtelPanel.tsx
  - frontend/src/components/panels/UnifiedFailures.tsx
  - frontend/src/components/panels/TopSkills.tsx
  - frontend/src/components/panels/SessionsTable.tsx
  - frontend/src/components/panels/SessionCompareView.tsx
  - frontend/src/styles.css
autonomous: true

must_haves:
  truths:
    - "Every panel rendered on /, /activity, /sessions/compare uses BoundedPanelCard OR <PanelCard bounded> (no bare <PanelCard> on these three routes)"
    - "/ uses .cmc-page--bounded modifier on its root section (CONT-01 viewport-height constraint)"
    - "/activity uses .cmc-page--bounded on its root section"
    - "/sessions/compare uses .cmc-page--bounded on its root section"
    - "Long session IDs and cwd paths in SessionsTable + SessionCompareView truncate via TruncatedCell with tooltip-on-hover"
    - "CopyIconButton appears on session-id + cwd columns where adoption applies"
    - "v1.2-carry-over .cmc-numeric class has contrast >= AA on dark theme (color bumped from --cmc-text-subtle to --cmc-text-dim)"
    - "v1.2-carry-over .cmc-system-health-strip__* classes have contrast >= AA (same rebalance)"
    - "Per-route default range applied AT PANEL READ SITE: bare / falls back to last 24h, /activity falls back to today (≤24h window) via rangeToVocab bridge — true 1h precision deferred to Phase 27 backend vocab extension; /sessions/compare to last 7d (NOT in validator — Pitfall 13)"
    - "Time-anchored panels on these routes call rangeToVocab(time_from, time_to) at hook-read site to bridge to existing Range vocab"
    - "TIME-02 satisfied: all time-anchored panels on /, /activity, /sessions/compare sync to the global time picker via the rangeToVocab bridge; non-time-aware panels (e.g. live SSE feeds, system health strip) opt out by simply not consuming the bridge — no schema changes required"
  artifacts:
    - path: "frontend/src/routes/index.tsx"
      provides: ".cmc-page--bounded modifier on root section + per-route default 24h fallback at panel read sites"
      contains: "cmc-page--bounded"
    - path: "frontend/src/routes/activity.tsx"
      provides: ".cmc-page--bounded on root section + per-route default falls back to today (≤24h) — v1 limitation, true 1h precision deferred to Phase 27"
      contains: "cmc-page--bounded"
    - path: "frontend/src/routes/sessions_.compare.tsx"
      provides: ".cmc-page--bounded + per-route default 7d fallback"
      contains: "cmc-page--bounded"
  key_links:
    - from: "frontend/src/components/panels/TokenUsageCard.tsx"
      to: "frontend/src/lib/time/rangeToVocab.ts"
      via: "useRouterState → rangeToVocab({from,to}) → useTokens(vocab)"
      pattern: "rangeToVocab"
---

<objective>
Per-route adoption sweep on `/`, `/activity`, `/sessions/compare` — the three highest-traffic routes. **THIS PLAN IS THE TIME-02 DELIVERABLE** (per-route panel-sync to the global time picker via `rangeToVocab` bridge at panel read sites; panels not previously time-aware opt out cleanly by simply not consuming the bridge). Migrate every panel rendered on these routes to `BoundedPanelCard` (or `<PanelCard bounded>` — semantically identical per Phase 24 plan 03 lock). Add `.cmc-page--bounded` modifier to each route's root `<section>` so the viewport-height-constraint flex ladder activates. Adopt `TruncatedCell` + `CopyIconButton` primitives on the high-overflow columns (session-id, cwd, skill-name). Bridge each route's time-anchored panels: read `time_from`/`time_to` from URL via `useRouterState`, coerce via `rangeToVocab()` to the existing backend vocab, fall back to the per-route default at the panel read site (NOT in the validator — Pitfall 13 lock).

Also: fix v1.2 carry-over a11y classes from Phase 25 Accepted Exception (b) — the "lite" subset (contrast carry-overs `.cmc-numeric` + `.cmc-system-health-strip__*`) that the density-token swap naturally touches. Defer aria/semantic patterns (`.cmc-heatmap-cell` aria-prohibited-attr, `.cmc-otel-feed` scrollable-region-focusable, `.cmc-sessions-table-header__label`) to Phase 27 — those are non-contrast semantic fixes that don't ride the density-token rebalance.

Purpose: Phase 24 primitives (BoundedPanelCard / TruncatedCell / CopyIconButton / density tokens) shipped with zero consumers. This plan is the FIRST adoption sweep — validates the pattern Phase 27 will repeat on the tail-end routes. Success criterion 5 from ROADMAP: "any Sheet on /sessions/compare stays inside viewport at every density × viewport ≥1024px; long session IDs and cwd paths truncate; pages stay bounded with internal panel scroll."

This plan is mechanical (drop-in component swaps + CSS-class additions) but TOUCHES 19 panel files. Group into 2 focused tasks: (a) `/` + `/sessions/compare` route + their panels, (b) `/activity` route + its panels + a11y lite fixes (since those carry-overs live on `/` and `/activity`).

Output: 3 route files extended, 19 panel files migrated, 1 styles.css update (a11y rebalance + .cmc-page--bounded usage), zero new components or libs.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-RESEARCH.md
@.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-03-PLAN.md

# Phase 24 adoption primitives (no changes — consume verbatim)
@frontend/src/components/ui/BoundedPanelCard.tsx
@frontend/src/components/ui/PanelCard.tsx
@frontend/src/components/ui/TruncatedCell.tsx
@frontend/src/components/ui/CopyIconButton.tsx

# Plan 01 bridge consumed
@frontend/src/lib/time/rangeToVocab.ts
@frontend/src/lib/time/coerce.ts

# Routes to extend
@frontend/src/routes/index.tsx
@frontend/src/routes/activity.tsx
@frontend/src/routes/sessions_.compare.tsx

# Phase 25 Accepted Exception (b) — the 8 carry-over classes inventory
@.planning/phases/25-saved-views-backend-frontend/25-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adopt BoundedPanelCard + .cmc-page--bounded + per-route default fallback on / and /sessions/compare panels</name>
  <files>frontend/src/routes/index.tsx, frontend/src/routes/sessions_.compare.tsx, frontend/src/components/panels/SystemHealthStrip.tsx, frontend/src/components/panels/KpiRow.tsx, frontend/src/components/panels/AttentionBar.tsx, frontend/src/components/panels/LiveSessionsCard.tsx, frontend/src/components/panels/TokenUsageCard.tsx, frontend/src/components/panels/CacheEfficiencyCard.tsx, frontend/src/components/panels/HookActivityCard.tsx, frontend/src/components/panels/SessionOutcomesCard.tsx, frontend/src/components/panels/PressurePanel.tsx, frontend/src/components/panels/McpPanel.tsx, frontend/src/components/panels/ProductivityCard.tsx, frontend/src/components/panels/EditAcceptanceCard.tsx, frontend/src/components/panels/ToolLatencyCard.tsx, frontend/src/components/panels/AgentFanoutCard.tsx, frontend/src/components/panels/ProjectBreakdownCard.tsx, frontend/src/components/panels/SessionCompareView.tsx</files>
  <action>
**Step A — Route updates (`/` and `/sessions/compare`):**

`frontend/src/routes/index.tsx`:
- Add `className="cmc-page cmc-page--bounded"` to the root `<section>` (currently has only `cmc-page`)
- aria-labelledby unchanged
- All panel imports + JSX unchanged at the route level — panels do their own BoundedPanelCard adoption

`frontend/src/routes/sessions_.compare.tsx`:
- Same: root `<section className="cmc-page cmc-page--bounded">`
- `SessionCompareView` panel does its own adoption inside (Step B)

**Step B — Panel migrations:**

For EACH of the 14 panels on `/` (listed in `frontend/src/routes/index.tsx`):
- `SystemHealthStrip`, `KpiRow`, `AttentionBar`, `LiveSessionsCard`, `TokenUsageCard`, `CacheEfficiencyCard`, `HookActivityCard`, `SessionOutcomesCard`, `PressurePanel`, `McpPanel`, `ProductivityCard`, `EditAcceptanceCard`, `ToolLatencyCard`, `AgentFanoutCard`, `ProjectBreakdownCard`

AND SessionCompareView on `/sessions/compare`:

Apply the SAME mechanical change:
1. Find the JSX `<PanelCard` (or `<PanelCard<T>`) opener
2. Add the `bounded` boolean prop (preferred) OR import `BoundedPanelCard` from `'../ui/BoundedPanelCard'` and replace `PanelCard` with `BoundedPanelCard`
3. Both are SEMANTICALLY IDENTICAL per Phase 24 plan 03 lock — pick whichever is less code-noise. For panels that already pass typed generics like `<PanelCard<TokenUsageResponse>>`, adding `bounded` keeps the generic flow; for simpler call sites, `BoundedPanelCard` is cleaner.

EXAMPLE diff (KpiRow.tsx — illustration, do the same shape for every panel):
```jsx
// Before:
return <PanelCard reqId="OPNL-02" title="KPIs" query={query} empty={...}>{...}</PanelCard>

// After (preferred — just add the prop):
return <PanelCard bounded reqId="OPNL-02" title="KPIs" query={query} empty={...}>{...}</PanelCard>
```

**Step C — Time-anchored panel bridges:**

For the time-anchored panels on `/` (TokenUsageCard, CacheEfficiencyCard, HookActivityCard, SessionOutcomesCard, ProductivityCard, EditAcceptanceCard, ToolLatencyCard, AgentFanoutCard, ProjectBreakdownCard — every panel that today calls `useFooQuery(range)` with a `Range` Literal arg):

Add the bridge — read URL `time_from`/`time_to`, coerce to vocab via `rangeToVocab`, fall back to PER-ROUTE DEFAULT (24h for `/`) when both absent. EXACT shape (add this block at the top of the panel function, before existing useState/useEffect):

```typescript
import { useRouterState } from '@tanstack/react-router'
import { rangeToVocab } from '../../lib/time/rangeToVocab'

// Inside the panel function:
const location = useRouterState({ select: (s) => s.location })
const search = (location.search ?? {}) as Record<string, unknown>
const timeFrom = typeof search.time_from === 'string' ? search.time_from : undefined
const timeTo = typeof search.time_to === 'string' ? search.time_to : undefined

// Per-route default: '/' falls back to 'today' (24h) — RESEARCH §"Default range when URL has no time params":
const globalRange = timeFrom && timeTo ? rangeToVocab({ from: timeFrom, to: timeTo }) : 'today'
```

For panels that ALSO have a local `<RangeToggle>` (CONTEXT punted to discretion — RESEARCH §"State of the Art" recommendation: "Keep per-panel RangeToggle for v1.3"): the LOCAL toggle PRECEDENCE — if local `range` state is set (not the panel's initial default), use local; otherwise use `globalRange`. EXACT shape:

```typescript
const [localRange, setLocalRange] = useState<Range | null>(null)
const effectiveRange = localRange ?? globalRange
const query = useTokens(effectiveRange)
// RangeToggle onChange writes setLocalRange (overrides global)
// To reset to global: setLocalRange(null) — exposed via a tiny "Reset to global range" affordance OR simply not provided in v1 (defer)
```

Simpler v1 alternative if a panel does NOT currently have local-state for range (most don't — they only had `useState<Range>('7d')` defaulting): replace the default with `globalRange` derivation. KEEP the RangeToggle but make its `value` derive from URL + local-state-override.

For panels WITHOUT a RangeToggle today (e.g. SystemHealthStrip, KpiRow, LiveSessionsCard): just pass `globalRange` to whatever query they use. If they use a NON-range hook (e.g. live SSE, no range arg), no change needed.

**Step D — Truncation on SessionCompareView:**

For `SessionCompareView.tsx`, adopt:
- `TruncatedCell` on session ID display areas
- `CopyIconButton` next to session IDs and cwd paths (the high-overflow columns identified in CONTEXT)

Mechanical: wherever a session ID or cwd path renders as raw `<span>{value}</span>`, wrap in `<TruncatedCell title={value}>{value}</TruncatedCell>` and add `<CopyIconButton value={value} />` as a sibling.

**Step E — Per-route default for /sessions/compare:**

Same bridge pattern, but per-route default is `'7d'` (7 days) per RESEARCH §"Default range when URL has no time params".

**ResponsiveContainer count check after this task:** unchanged (no new charts). `grep -c "<ResponsiveContainer" frontend/src/components/panels/*.tsx` total = 26.

**Run BOTH the existing baseline test suite AND any newly affected snapshots:** `pnpm test --run` (full suite). Vitest count grows by ZERO (we're not adding test files in this task — adoption is mechanical). If any existing vitest snapshot fixtures contain hardcoded `cmc-card` className strings that now have `cmc-card cmc-card--bounded`, those snapshots will require update — accept as the natural cost of adoption.

CRITICAL: This task touches 16+ files. Commit after EVERY 4-5 panels to keep the diff reviewable. Each commit message: `feat(26-08): adopt BoundedPanelCard on <panel-names>` — but keep the plan as a single Task (multi-commit is fine; one Task = one logical unit).
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run && grep -c "<ResponsiveContainer" frontend/src/components/panels/*.tsx | awk -F: '{s+=$2} END {if(s==26)print "OK 26"; else {print "FAIL count="s; exit 1}}' && pnpm tsc --noEmit && pnpm lint</automated>
  </verify>
  <done>
- `/` route uses `cmc-page cmc-page--bounded`
- `/sessions/compare` route uses `cmc-page cmc-page--bounded`
- All 15 panels rendered by `/` are wrapped in `<PanelCard bounded>` or `<BoundedPanelCard>` — verified by `grep "<PanelCard\b[^>]*bounded\|<BoundedPanelCard" frontend/src/components/panels/{SystemHealthStrip,KpiRow,AttentionBar,LiveSessionsCard,TokenUsageCard,CacheEfficiencyCard,HookActivityCard,SessionOutcomesCard,PressurePanel,McpPanel,ProductivityCard,EditAcceptanceCard,ToolLatencyCard,AgentFanoutCard,ProjectBreakdownCard}.tsx | wc -l` returns 15
- SessionCompareView adopts TruncatedCell + CopyIconButton on session-id / cwd surfaces
- Time-anchored panels on `/` use `rangeToVocab` bridge with `'today'` fallback
- Time-anchored panels on `/sessions/compare` use the bridge with `'7d'` fallback
- ResponsiveContainer count = 26
- vitest baseline preserved (snapshot updates accepted where adoption-related)
- `pnpm tsc --noEmit` clean; `pnpm lint` clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Adopt BoundedPanelCard on /activity panels + adopt SessionsTable truncation/copy + fix v1.2 a11y carry-overs (lite)</name>
  <files>frontend/src/routes/activity.tsx, frontend/src/components/panels/ActivityHeatmap.tsx, frontend/src/components/panels/ChartsStrip.tsx, frontend/src/components/panels/OtelPanel.tsx, frontend/src/components/panels/UnifiedFailures.tsx, frontend/src/components/panels/TopSkills.tsx, frontend/src/components/panels/SessionsTable.tsx, frontend/src/styles.css</files>
  <action>
**Step A — Route update (`/activity`):**

`frontend/src/routes/activity.tsx`:
- Add `cmc-page--bounded` modifier to root `<section>`

**Step B — Panel migrations:**

For EACH panel on `/activity` (ActivityHeatmap, ChartsStrip, OtelPanel, UnifiedFailures, TopSkills, SessionsTable):
- Add `bounded` prop OR swap to `BoundedPanelCard` (per Task 1 pattern)
- Time-anchored panels (ChartsStrip already has the brush from Plan 05; OtelPanel uses SSE not range; UnifiedFailures uses sessions with range; TopSkills uses skills with range) get the bridge with per-route default `'today'` for `/activity` (RESEARCH §"Default range" says 1h, but the existing backend vocab tightest bucket is `today`; that is the closest fit until backend extension. **v1 limitation tracked by the same ADR as the rangeToVocab bridge** (Plan 01 objective ADR — Phase 27 backend extension will accept time_from/time_to natively and unlock true 1h precision))

**Step C — SessionsTable adoption (HIGH-impact — long session IDs + cwd paths):**

`SessionsTable.tsx`:
- For the session-id column rendering: wrap in `<TruncatedCell title={fullSid}>{shortSid}</TruncatedCell>` (existing pattern likely already truncates the SID — verify by reading the file)
- Add `<CopyIconButton value={fullSid} />` as a sibling of the session-id cell
- For the cwd column (if present): same treatment

If SessionsTable uses the `<DataTable>` primitive (Phase 24 plan 03), set `wrap: false` (truncate via TruncatedCell — default) and `copyable: true` on the `session-id` + `cwd` columns. Phase 24 plan 03 SUMMARY confirms: "DataTable.DataTableColumn<T>.wrap defaults to false and copyable defaults to false. Per-route adoption (Phase 26/27) opts in by adding copyable: true to the id/session-id/cwd/skill-name columns specifically — never globally on a table."

**Step D — Compare-picker Sheet bounded-viewport check:**

`SessionsTable` (and any compare-picker Sheet inside) — the Phase 24 Sheet primitive uses Radix Portal. CONTEXT requirement: "Sheets on /sessions/compare must stay inside viewport at every density × viewport from 1024px wide upward." If the SessionsTable opens a session-detail Sheet, verify the Sheet uses the Phase 24 Portal-mounted primitive (likely YES — Phase 24 plan 04 SHEL ensured this). No code changes required unless the audit surfaces a regression — defer to the close gate (Plan 09) visual checkpoint.

**Step E — v1.2 a11y carry-overs "lite" fix (Pitfall 7 recommendation A):**

Identify the contrast carry-overs that LAND on `/` and `/activity` per Phase 25 25-VERIFICATION.md Accepted Exception (b):
- `.cmc-numeric` (on `/` and `/activity` — KPI number displays)
- `.cmc-system-health-strip__*` (on `/` — health strip subtle text)

In `frontend/src/styles.css`, find these classes and BUMP body-text color from `--cmc-text-subtle` to `--cmc-text-dim` (the Phase 24 plan 07 commit `06f09a2` precedent — that fix used the same color swap for `Skeleton` role status + sidebar section header). Verify the result meets WCAG AA contrast on dark theme via the close-gate axe scan.

EXACT diff hint (verify against actual class definitions):

```css
/* Before — flagged for AA violation */
.cmc-numeric { color: var(--cmc-text-subtle); }
.cmc-system-health-strip__metric { color: var(--cmc-text-subtle); }
.cmc-system-health-strip__label { color: var(--cmc-text-subtle); }

/* After — Phase 26 Plan 08 contrast lift */
.cmc-numeric { color: var(--cmc-text-dim); }
.cmc-system-health-strip__metric { color: var(--cmc-text-dim); }
.cmc-system-health-strip__label { color: var(--cmc-text-dim); }
```

If `--cmc-text-dim` doesn't exist as a token, USE `--cmc-text` directly (the darker variant) — read the existing token list to confirm available variables. Whatever the chosen token, it MUST meet AA contrast against the body background; the close-gate axe scan in Plan 09 enforces this. If the swap fails axe, increase contrast further.

**DEFERRED to Phase 27** (aria/semantic — NOT this plan):
- `.cmc-heatmap-cell` aria-prohibited-attr
- `.cmc-otel-feed` scrollable-region-focusable
- `.cmc-sessions-table-header__label`
- `<select aria-label="Range filter">`

Document the deferral inline in `frontend/src/styles.css` (a comment block above the relevant class section) so Phase 27's plan can find them easily.

**Step F — Run the full test suite:**

`pnpm --filter cmc-frontend test --run` — every vitest test must pass. ResponsiveContainer count check: `grep -c "<ResponsiveContainer" frontend/src/components/panels/*.tsx` total = 26 (Plan 05 added Brush INSIDE existing ResponsiveContainer; no new wrappers in this plan).
  </action>
  <verify>
    <automated>cd frontend && pnpm test --run && grep -c "<ResponsiveContainer" frontend/src/components/panels/*.tsx | awk -F: '{s+=$2} END {if(s==26)print "OK 26"; else {print "FAIL count="s; exit 1}}' && pnpm tsc --noEmit && pnpm lint</automated>
  </verify>
  <done>
- `/activity` route uses `cmc-page cmc-page--bounded`
- All 6 panels on `/activity` (ActivityHeatmap, ChartsStrip, OtelPanel, UnifiedFailures, TopSkills, SessionsTable) use `bounded`
- SessionsTable opts in `copyable: true` on `session-id` + `cwd` columns (via DataTable column config) — long values truncate via TruncatedCell, copy via CopyIconButton
- `.cmc-numeric` + `.cmc-system-health-strip__*` color tokens swapped to a higher-contrast variable (Pitfall 7 lite recommendation A satisfied for contrast carry-overs)
- Deferred-to-Phase-27 a11y items documented in styles.css comments
- ResponsiveContainer count = 26
- `pnpm tsc --noEmit` clean; `pnpm lint` clean
- vitest baseline preserved (snapshot updates accepted)
  </done>
</task>

</tasks>

<verification>
- 21 panel files migrated to bounded mode (15 on `/` + 6 on `/activity` + SessionCompareView)
- 3 routes use `cmc-page--bounded` modifier
- Time-anchored panels bridge URL `time_from`/`time_to` → `rangeToVocab` → existing useQuery hooks
- Per-route defaults applied AT PANEL READ SITE (Pitfall 13): `/` → today, `/activity` → today, `/sessions/compare` → 7d
- Compare-overlay (Plan 07) continues to work — `compare_panels` round-trip preserved
- v1.2 a11y carry-over contrast lift: `.cmc-numeric` + `.cmc-system-health-strip__*` swapped (lite path)
- Phase 27 deferrals documented inline
- ResponsiveContainer count = 26 (Phase 24 lock); ESLint clean; tsc clean
- vitest count preserved or grows only by snapshot updates
</verification>

<success_criteria>
1. Success criterion 5 from ROADMAP Phase 26: Sheets on `/sessions/compare` stay inside viewport at all densities × viewport ≥ 1024px; long session IDs and cwd paths truncate with tooltip; pages stay bounded
2. Success criterion 1 from ROADMAP Phase 26: User sets global time picker → every time-anchored panel on /, /activity, /sessions/compare re-queries against that range (verified via the bridge + per-route fallback)
3. Phase 24 invariants preserved: no density:subtree, ResponsiveContainer count = 26, registry-only testids
4. v1.2-baseline `.cmc-numeric` + `.cmc-system-health-strip__*` axe violations no longer surface in Phase 26 axe scan (Plan 09 close gate enforces)
5. Phase 27 has a clean handoff for the remaining 4 deferred semantic a11y items
</success_criteria>

<output>
After completion, create `.planning/phases/26-per-route-adoption-i-command-activity-sessions-time-cmd-k/26-08-SUMMARY.md` per `summary.md` template.
</output>
