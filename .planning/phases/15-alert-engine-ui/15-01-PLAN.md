---
phase: 15-alert-engine-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmc/alerts/__init__.py
  - backend/cmc/alerts/detector.py
  - backend/cmc/alerts/scopes.py
  - backend/tests/test_alerts_detector.py
autonomous: true
requirements: [ALRT-03, ALRT-05]
tags: [alerts, detector, ewma, z-score, hysteresis, stdlib-math, tdd]

must_haves:
  truths:
    - "User can call cmc.alerts.detector.evaluate_threshold(rule, value, state) and receive an AlertSignal enum (FIRING / CLEAR / PENDING_FIRE / HOLD) computed from threshold_fire / threshold_clear / min_dwell_seconds / cooldown_seconds with no float drift"
    - "User can call cmc.alerts.detector.evaluate_anomaly(rule, value, state) and receive an AlertSignal computed via stdlib math.sqrt + math.fabs over EWMA mean+variance carried in alert_state.params_json — no numpy/scipy import"
    - "User can trust that anomaly rules in insufficient_data state suppress notifications during a 24h warm-up window after rule.created_at — verified by a passing test that flips firing only AFTER both sample_count >= min_samples AND now - created_at >= 86400s"
    - "User can rely on cmc.alerts.scopes._SCOPE_EXTRACTORS mapping each supported metric to (a) a SQL that returns scope_key -> value rows AND (b) a stable scope_key string format"
  artifacts:
    - path: "backend/cmc/alerts/__init__.py"
      provides: "Package marker"
      min_lines: 1
    - path: "backend/cmc/alerts/detector.py"
      provides: "evaluate_threshold + evaluate_anomaly + AlertSignal enum"
      contains: "AlertSignal"
      exports: ["AlertSignal", "evaluate_threshold", "evaluate_anomaly"]
    - path: "backend/cmc/alerts/scopes.py"
      provides: "_SCOPE_EXTRACTORS dict + ScopeExtractor protocol — one entry per v1.0 metric"
      contains: "_SCOPE_EXTRACTORS"
    - path: "backend/tests/test_alerts_detector.py"
      provides: "Hand-rolled threshold + EWMA + warm-up coverage; all ~12 cases in stdlib math only"
      min_lines: 200
  key_links:
    - from: "backend/cmc/alerts/detector.py"
      to: "stdlib math (math.sqrt, math.fabs ONLY)"
      via: "import math"
      pattern: "^import math$"
    - from: "backend/cmc/alerts/detector.py"
      to: "alert_state.params_json (carries EWMA mean / variance / sample_count for anomaly)"
      via: "state.params_json reads + writes"
      pattern: "params_json"
    - from: "backend/cmc/alerts/scopes.py"
      to: "otel_events / sessions / token_usage tables (per-metric SQL)"
      via: "AsyncSession.execute(text(...))"
      pattern: "AsyncSession"

deviations:
  - id: D-01
    item: "RESEARCH.md Open Q #1: scope_key vocabulary not pre-locked beyond one example. v1.0 must ship a finite, documented set."
    decision: "Lock 3 v1.0 metrics: (a) cost_usd_24h with scope_key='model:<model>' — joins token_usage + pricing; (b) skill_p95_latency_ms with scope_key='skill:<name>' — copies _LATENCY_SQL pattern from skills.py; (c) dispatcher_failed_tasks_5m with scope_key='<global>' — counts tasks.status='failed' rows in last 5min. Implemented as _SCOPE_EXTRACTORS dict in scopes.py. Adding a 4th metric in v1.2 = one dict entry + one helper, no detector changes."
    impact: "AlertRuleCreate (Plan 02) MUST validate metric ∈ _SCOPE_EXTRACTORS keys; unknown metric -> 422. Plan 02 imports _SCOPE_EXTRACTORS for the validator."
  - id: D-02
    item: "RESEARCH.md Open Q #2: where in heartbeat.py the alert hook lands."
    decision: "Plan 02 owns the heartbeat.py modification, but Plan 01 fixes the contract: the hook lands AFTER stamp_tick AND AFTER the emergency-stop early return. Rationale: e-stop should disable alerts too — if the user pulled the plug, we are NOT sending Telegram alerts about the dispatcher being down because we just stopped it. Plan 02's task wires this; Plan 01's evaluate_alerts (Plan 02-owned) signature accepts a single AsyncSession only — no e-stop check inside the alert engine itself."
    impact: "evaluate_threshold and evaluate_anomaly are pure functions over (rule, value, state). State persistence and e-stop coordination are Plan 02's responsibility."
  - id: D-03
    item: "Anomaly state (EWMA mean / variance / sample_count) must persist across ticks. The AlertState SQLModel has sample_count: int but no mean / variance columns."
    decision: "Carry EWMA mean + variance inside alert_state.params_json (already present, JSON column). Keys: ewma_mean: float, ewma_var: float. sample_count uses the existing int column. The detector reads + returns updated values; Plan 02's evaluate_alerts persists them via state.params_json = {**state.params_json, 'ewma_mean': new_mean, 'ewma_var': new_var}. Per RESEARCH.md detector math section: 'use params_json for anomaly state — no schema change.'"
    impact: "Plan 01 ships an explicit AnomalyState pydantic-style helper (read_anomaly_state, write_anomaly_state) to encode/decode the JSON consistently. Tests assert round-trip preservation."
---

<objective>
Lay the pure-function detector primitives (~150 LOC) for ALRT-03: hand-rolled hysteresis-aware threshold comparator + EWMA z-score anomaly detector, both stdlib-math only. Plus the scope_key vocabulary lock for v1.0 (3 metrics, one dict entry each). This plan ships ZERO IO — pure math + a SQL-extractor table. Plan 02 owns dispatcher integration; Plan 03 owns the API.

Purpose: Decouple math from IO. Detector is exhaustively unit-tested without DB / network / dispatcher fixtures. Plan 02 composes it; Plan 03's CRUD validator imports _SCOPE_EXTRACTORS to reject unknown metrics.

Output: 1 new package (cmc/alerts/), 2 modules (detector.py, scopes.py), 1 test file with ~12 tests covering threshold hysteresis, anomaly warm-up, EWMA correctness, and the 24h warm-up gate.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-alert-engine-ui/15-RESEARCH.md
@backend/cmc/db/models/alert_rules.py
@backend/cmc/db/models/alert_state.py

<interfaces>
<!-- Phase 13 schema (LOCKED — do not modify): -->

class AlertRule(SQLModel, table=True):
    rule_id: int | None
    name: str                          # display name
    kind: str                          # "threshold" | "anomaly"
    metric: str                        # must be in _SCOPE_EXTRACTORS keys (Plan 02 validates)
    threshold_fire: float | None       # nullable for anomaly rules
    threshold_clear: float | None      # hysteresis floor; defaults to threshold_fire if null
    min_dwell_seconds: int             # default 0
    min_samples: int                   # default 1; anomaly warm-up gate
    cooldown_seconds: int              # default 0; min seconds between re-notifies after firing
    enabled: bool                      # default True
    spec_version: int                  # default 1
    params_json: dict[str, Any]        # overflow + (anomaly only) ewma_mean/ewma_var
    created_at: datetime               # 24h warm-up reference
    updated_at: datetime

class AlertState(SQLModel, table=True):
    id: int | None
    rule_id: int                       # FK alert_rules.rule_id
    scope_key: str
    state: str                         # "firing" | "clear" | "acked" | "insufficient_data"
    last_value: float | None
    last_evaluated_at: datetime
    fired_at: datetime | None          # candidate fire stamp; cleared on transition to clear
    cleared_at: datetime | None
    acked_until: datetime | None       # ack suppresses re-notify until this ts (ALRT-08)
    sample_count: int                  # default 0; anomaly warm-up gate
    # UNIQUE(rule_id, scope_key) — one state row per scope.

<!-- The detector signatures (this plan SHIPS): -->

class AlertSignal(StrEnum):
    FIRING = "firing"           # emit decision + telegram (subject to cooldown + ack)
    CLEAR = "clear"             # auto-resolve (find pending decision, mark answered)
    PENDING_FIRE = "pending"    # candidate; min_dwell not yet satisfied — no emit
    HOLD = "hold"               # firing-state but cooldown OR ack window suppresses re-emit
    INSUFFICIENT = "insufficient_data"  # anomaly only; warm-up not satisfied

def evaluate_threshold(
    rule: AlertRule, current_value: float, state: AlertState, *, now: datetime,
) -> AlertSignal: ...

def evaluate_anomaly(
    rule: AlertRule, current_value: float, state: AlertState, *, now: datetime,
) -> tuple[AlertSignal, dict[str, float]]:
    """Returns (signal, {ewma_mean, ewma_var, sample_count}). Caller persists the dict
    into state.params_json + state.sample_count after the call."""
    ...

<!-- Scope extractor signature (Plan 02 calls these): -->

ScopeExtractor = Callable[[AsyncSession, datetime], Awaitable[dict[str, float]]]
# Returns mapping scope_key -> current_value for every scope this metric covers.
# E.g. cost_usd_24h returns {"model:claude-opus-4-7": 12.34, "model:claude-sonnet-4-6": 5.67}.

_SCOPE_EXTRACTORS: dict[str, ScopeExtractor] = {
    "cost_usd_24h":              extract_cost_usd_24h,
    "skill_p95_latency_ms":      extract_skill_p95_latency_ms,
    "dispatcher_failed_tasks_5m": extract_dispatcher_failed_tasks_5m,
}

<!-- EWMA math primitives (RESEARCH.md detector math section): -->
# alpha = 2 / (N + 1), N from rule.params_json.get("window_n", 50). Default 50 -> alpha ≈ 0.0392.
# Seed: when sample_count == 0, mean_0 = x, var_0 = 0. Don't compute z on the seed sample.
# Recurrence:
#   mean_t = alpha * x + (1 - alpha) * mean_{t-1}
#   var_t  = alpha * (x - mean_{t-1})**2 + (1 - alpha) * var_{t-1}
# Z-score: z = (x - mean_t) / sqrt(var_t + 1e-9)  # epsilon prevents div-zero
# Fire when math.fabs(z) > threshold_fire AND sample_count >= min_samples AND now - created_at >= 86400.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create alerts package + AlertSignal enum + threshold detector + tests</name>
  <files>
    backend/cmc/alerts/__init__.py
    backend/cmc/alerts/detector.py
    backend/tests/test_alerts_detector.py
  </files>
  <behavior>
    - cmc.alerts.detector.AlertSignal is a StrEnum with five members: FIRING, CLEAR, PENDING_FIRE, HOLD, INSUFFICIENT.
    - evaluate_threshold(rule, current_value, state, *, now) returns one of {FIRING, CLEAR, PENDING_FIRE, HOLD}.
    - State machine (per RESEARCH.md detector math):
        state.state == "clear" or "acked":
          if value > rule.threshold_fire:
            if state.fired_at is None: return PENDING_FIRE  (caller stamps fired_at = now)
            elif (now - state.fired_at).total_seconds() >= rule.min_dwell_seconds: return FIRING
            else: return PENDING_FIRE
          else: return CLEAR
        state.state == "firing":
          clear_floor = rule.threshold_clear if rule.threshold_clear is not None else rule.threshold_fire
          if value < clear_floor: return CLEAR
          # cooldown: min seconds between re-notifies; if not elapsed since fired_at, HOLD
          if state.fired_at is not None and (now - state.fired_at).total_seconds() < rule.cooldown_seconds:
            return HOLD
          return FIRING  (re-emit after cooldown)
        state.state == "insufficient_data": return CLEAR  (threshold rules have no warm-up)
    - Pitfall 3 (cooldown vs ack): ack precedence is OUT of detector scope — the dispatcher (Plan 02) checks state.acked_until > now BEFORE calling the detector and short-circuits to HOLD. Detector itself ignores acked_until.
    - All numeric comparisons MUST use float arithmetic only (no Decimal — values come from SQL aggregates as floats per scope extractors). Do NOT mix Decimal and float.
  </behavior>
  <action>
    1. Create `backend/cmc/alerts/__init__.py` with a one-line module docstring: `"""Alert engine — Phase 15. Pure-math detector + IO-side dispatcher entry."""`. No imports yet (detector.py / scopes.py will be imported by callers explicitly).
    2. Create `backend/cmc/alerts/detector.py`:
       - File header docstring citing ALRT-03 (stdlib math only).
       - `import math` at top — the ONLY math library import. Use `math.sqrt` + `math.fabs` in the anomaly path (Task 2). Threshold path uses no math import beyond stdlib comparison.
       - `from datetime import datetime, timedelta` (stdlib).
       - `from enum import StrEnum`.
       - `from typing import Any` (for params_json dict type).
       - `class AlertSignal(StrEnum):` with five members verbatim from <interfaces> above.
       - `def evaluate_threshold(rule, current_value, state, *, now) -> AlertSignal:` per the state machine in <behavior>. Type annotations use forward references for AlertRule / AlertState (avoid circular import — `from cmc.db.models.alert_rules import AlertRule` and `from cmc.db.models.alert_state import AlertState` are safe; the models do not import alerts/).
       - Stub `def evaluate_anomaly(...)` raising NotImplementedError — Task 2 implements.
    3. Create `backend/tests/test_alerts_detector.py` with full RED→GREEN coverage for evaluate_threshold:
       a. test_threshold_clear_to_pending_fire — state='clear', fired_at=None, value > threshold_fire → returns PENDING_FIRE.
       b. test_threshold_pending_fire_to_firing_after_dwell — state='clear', fired_at=now-60s, min_dwell_seconds=30, value > threshold_fire → returns FIRING.
       c. test_threshold_pending_fire_holds_under_dwell — state='clear', fired_at=now-10s, min_dwell_seconds=30, value > threshold_fire → returns PENDING_FIRE (still candidate).
       d. test_threshold_clear_stays_clear_below_fire — state='clear', value < threshold_fire → returns CLEAR (no fired_at change semantics — caller may wipe fired_at).
       e. test_threshold_firing_to_clear_below_clear_floor — state='firing', threshold_clear=5.0, value=4.0 → returns CLEAR.
       f. test_threshold_firing_holds_within_cooldown — state='firing', cooldown_seconds=600, fired_at=now-300s, value > threshold_fire → returns HOLD.
       g. test_threshold_firing_re_emits_after_cooldown — state='firing', cooldown_seconds=600, fired_at=now-700s, value > threshold_fire → returns FIRING.
       h. test_threshold_clear_floor_defaults_to_fire_when_null — rule.threshold_clear=None → uses threshold_fire as floor (asymmetric hysteresis disabled gracefully).
       i. test_threshold_insufficient_data_returns_clear — state='insufficient_data' (synthetic; threshold rules never set this state in production) → returns CLEAR.
       j. test_threshold_no_float_drift — value=0.1+0.2 (float quirk), threshold_fire=0.3 → behavior matches actual float arithmetic deterministically (assert exact AlertSignal value for documented input).
    4. Use a `_make_rule(...)` and `_make_state(...)` test factory at top of test file — keyword args with defaults so individual tests only override what they need. AlertRule and AlertState are SQLModel — instantiate WITHOUT db.add (in-memory only is fine; don't need DB for pure-function detector tests).
    5. Run `cd backend && pytest tests/test_alerts_detector.py -x -v -k threshold`. All 10 tests must pass.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_alerts_detector.py -x -v -k threshold</automated>
  </verify>
  <done>
    - `from cmc.alerts.detector import AlertSignal, evaluate_threshold` succeeds.
    - 10 threshold tests pass; no float-drift surprises.
    - `grep -c "import math" backend/cmc/alerts/detector.py` returns 1 (no other math libs).
    - Full backend suite still green: pre-existing tests = 463 from Phase 14 baseline; this plan adds ~10.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement EWMA z-score anomaly detector + warm-up gate + tests</name>
  <files>
    backend/cmc/alerts/detector.py
    backend/tests/test_alerts_detector.py
  </files>
  <behavior>
    - `evaluate_anomaly(rule, current_value, state, *, now) -> tuple[AlertSignal, dict[str, float]]` returns the signal AND the updated EWMA dict {ewma_mean, ewma_var}; caller persists.
    - Window N read from `rule.params_json.get("window_n", 50)`; alpha = 2 / (N + 1).
    - Read prior state via `_read_anomaly_state(state) -> tuple[float, float, int]` returning (mean, var, sample_count). When state.params_json is empty / missing keys, returns (0.0, 0.0, state.sample_count) — but the seed-sample short-circuit handles the all-zero case.
    - First sample (sample_count == 0):
        new_mean = current_value
        new_var = 0.0
        sample_count_new = 1
        signal = INSUFFICIENT  # always — even if current_value is wildly out of range, we have no baseline
    - Subsequent samples (sample_count >= 1):
        new_mean = alpha * current_value + (1 - alpha) * prior_mean
        new_var  = alpha * (current_value - prior_mean) ** 2 + (1 - alpha) * prior_var
        sample_count_new = prior_sample_count + 1
        z = (current_value - new_mean) / math.sqrt(new_var + 1e-9)
    - Warm-up gate (ALRT-05):
        if sample_count_new < rule.min_samples: signal = INSUFFICIENT
        elif (now - rule.created_at).total_seconds() < 86400: signal = INSUFFICIENT
        else: signal computed from z vs thresholds (same hysteresis pattern as threshold detector but on |z|):
          state.state in ("clear","acked","insufficient_data"):
            if math.fabs(z) > rule.threshold_fire:
              if state.fired_at is None: signal = PENDING_FIRE
              elif (now - state.fired_at).total_seconds() >= rule.min_dwell_seconds: signal = FIRING
              else: signal = PENDING_FIRE
            else: signal = CLEAR
          state.state == "firing":
            clear_floor = rule.threshold_clear if rule.threshold_clear is not None else rule.threshold_fire
            if math.fabs(z) < clear_floor: signal = CLEAR
            elif state.fired_at is not None and (now - state.fired_at).total_seconds() < rule.cooldown_seconds: signal = HOLD
            else: signal = FIRING
    - Return (signal, {"ewma_mean": new_mean, "ewma_var": new_var, "sample_count": sample_count_new}).
    - Caller responsibility: assign returned dict's mean/var into state.params_json (merge, don't replace overflow keys); assign sample_count into state.sample_count column.
  </behavior>
  <action>
    1. Open `backend/cmc/alerts/detector.py`. Replace the NotImplementedError stub for `evaluate_anomaly` with the full implementation per <behavior>. Add a helper `_read_anomaly_state(state) -> tuple[float, float, int]` that defensively pulls floats from state.params_json — `float(state.params_json.get("ewma_mean", 0.0))` etc., wrapped in try/except (ValueError, TypeError) returning (0.0, 0.0, 0) on parse failure (defense against hand-edited JSON).
    2. Add module-level constant `EPSILON = 1e-9` with a comment citing the rationale (prevents math.sqrt(0) division when variance has not yet diverged from seed).
    3. Add module-level constant `WARMUP_SECONDS = 86400` with comment citing ALRT-05.
    4. Append tests to `test_alerts_detector.py`:
       a. test_anomaly_first_sample_returns_insufficient — sample_count=0 → INSUFFICIENT + sample_count_new=1, ewma_mean=current_value, ewma_var=0.0.
       b. test_anomaly_warmup_gate_suppresses — rule.created_at = now - 1h, sample_count=10, min_samples=5, current_value 100σ from mean → INSUFFICIENT (warm-up not satisfied even though z is huge).
       c. test_anomaly_min_samples_gate_suppresses — rule.created_at = now - 25h (warm-up satisfied), sample_count=2, min_samples=10, current_value 100σ from mean → INSUFFICIENT (min_samples not met).
       d. test_anomaly_fires_after_warmup_and_min_samples — rule.created_at=now-25h, min_samples=10, prior state: mean=50, var=4, sample_count=20. current_value=80 → fabs(z) > 3 (assuming threshold_fire=3) → PENDING_FIRE on first crossing.
       e. test_anomaly_pending_to_firing_after_dwell — same as (d) but state.fired_at = now - rule.min_dwell_seconds - 10 → FIRING.
       f. test_anomaly_clears_below_threshold_clear — state='firing', threshold_clear=1.0, current_value at +0.5σ from mean → CLEAR.
       g. test_anomaly_returns_updated_ewma_dict — assert returned dict has 3 keys (ewma_mean, ewma_var, sample_count) with float types and reasonable ranges (|new_mean - current_value| < 2 × prior_var for typical updates).
       h. test_anomaly_ewma_recurrence_no_drift — feed 10 samples in [99,100,99,100,...] starting from mean=99.5 var=0.25 sample_count=10; assert mean stays in [99,100] and var stays bounded — verifies recurrence math.
       i. test_anomaly_no_numpy_or_scipy_imported — `import cmc.alerts.detector; assert 'numpy' not in sys.modules; assert 'scipy' not in sys.modules` (run as a subprocess to avoid test-runtime contamination from pytest plugins, OR assert via inspecting the source: `re.search(r'^(import|from) (numpy|scipy)', source, flags=re.M) is None`).
    5. Run `cd backend && pytest tests/test_alerts_detector.py -x -v`. All ~19 tests must pass.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_alerts_detector.py -x -v</automated>
  </verify>
  <done>
    - All anomaly tests pass; warm-up + min_samples gates verified independently.
    - `grep -E "(numpy|scipy)" backend/cmc/alerts/detector.py` returns nothing.
    - Returned dict round-trips: caller can json.dumps it and reload via _read_anomaly_state without precision loss (within float epsilon).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock scope_key vocabulary — _SCOPE_EXTRACTORS for 3 v1.0 metrics + tests</name>
  <files>
    backend/cmc/alerts/scopes.py
    backend/tests/test_alerts_detector.py
  </files>
  <behavior>
    - `_SCOPE_EXTRACTORS: dict[str, ScopeExtractor]` maps each supported metric to an async callable returning {scope_key: current_value} for the rule's evaluation window.
    - Three v1.0 entries (D-01):
      1. `cost_usd_24h` → scope_key format `model:<model>`. SQL: SUM token_usage rows in last 24h GROUP BY model, cost computed read-time via cmc.pricing.compute_cost. Returns {"model:claude-opus-4-7": 12.34, ...}. Empty result → empty dict.
      2. `skill_p95_latency_ms` → scope_key format `skill:<skill_name>`. Adapts the percentile window-CTE from skills.py::_LATENCY_SQL but bucketed at the 24h evaluation window (NOT the rule's range — alerts evaluate the latest 24h slice). Returns {"skill:analyze": 1234.0, ...}. Empty → {}.
      3. `dispatcher_failed_tasks_5m` → scope_key format `<global>` (literal string, no per-scope dimension). SQL: SELECT COUNT(*) FROM tasks WHERE status='failed' AND finished_at >= datetime('now','-5 minutes'). Returns {"<global>": <count>} always (even on count=0 — global always reports).
    - Each extractor is async (AsyncSession is async). All SQL parameterized — no string interpolation.
    - Module-level helper `is_known_metric(metric: str) -> bool` returning `metric in _SCOPE_EXTRACTORS`. Plan 02's CRUD validator imports this.
    - SQL constants are module-private (`_COST_USD_24H_SQL`, `_SKILL_P95_LATENCY_SQL`, `_DISPATCHER_FAILED_5M_SQL`). Mirror the cost.py / skills.py SQL conventions (sqlalchemy.text + named bindings).
  </behavior>
  <action>
    1. Create `backend/cmc/alerts/scopes.py`:
       - Module docstring: `"""Scope extractors — one per supported metric. Plan 01 ships 3 entries; v1.2 adds more without detector changes."""`.
       - Imports: `from datetime import datetime, timedelta, UTC`; `from typing import Awaitable, Callable`; `from sqlalchemy import text`; `from sqlalchemy.ext.asyncio import AsyncSession`; `from cmc.pricing import compute_cost, load_rates`.
       - Type alias: `ScopeExtractor = Callable[[AsyncSession, datetime], Awaitable[dict[str, float]]]` — second arg is `now` (UTC, naive) so tests can inject deterministic times.
       - Implement `extract_cost_usd_24h(db, now) -> dict[str, float]`:
           * `since = now - timedelta(hours=24)` (naive UTC convention from cost.py:48-54).
           * Run `_COST_USD_24H_SQL` (SELECT model, SUM(tokens_input), SUM(tokens_output), SUM(tokens_cache_read), SUM(tokens_cache_create_5m), SUM(tokens_cache_create_1h) FROM token_usage WHERE day >= DATE(:since) GROUP BY model). Same column shape as cost.py:71-81.
           * Load rates via `await load_rates(db)`. For each row, compute Decimal cost via `compute_cost(model, tokens_input, ..., rates)` and insert `{f"model:{model}": float(cost)}` into result dict. Convert Decimal -> float ONLY at the dict insert point (no Decimal in dict values; detector consumes float).
           * Return dict (empty if no rows).
       - Implement `extract_skill_p95_latency_ms(db, now)`:
           * Adapt skills.py::_LATENCY_SQL (the window-CTE percentile pattern) with `since = now - timedelta(hours=24)`.
           * Group by attrs_skill_name. Result: rows of (skill_name, p95_ms).
           * Return `{f"skill:{name}": float(p95_ms)}` for each row.
       - Implement `extract_dispatcher_failed_tasks_5m(db, now)`:
           * `since = now - timedelta(minutes=5)`.
           * SELECT COUNT(*) FROM tasks WHERE status='failed' AND finished_at >= :since. (Schema check: confirm tasks.finished_at exists. RESEARCH.md says tasks.status is the canonical column.)
           * Return `{"<global>": float(count_int)}`.
       - `_SCOPE_EXTRACTORS: dict[str, ScopeExtractor] = { "cost_usd_24h": extract_cost_usd_24h, "skill_p95_latency_ms": extract_skill_p95_latency_ms, "dispatcher_failed_tasks_5m": extract_dispatcher_failed_tasks_5m }`.
       - `def is_known_metric(metric: str) -> bool: return metric in _SCOPE_EXTRACTORS`.
    2. Append tests to `test_alerts_detector.py` (or split to a new test_alerts_scopes.py if file grows past ~400 lines — judgment call):
       a. test_scopes_known_metrics — `from cmc.alerts.scopes import _SCOPE_EXTRACTORS; assert set(_SCOPE_EXTRACTORS.keys()) == {"cost_usd_24h", "skill_p95_latency_ms", "dispatcher_failed_tasks_5m"}` — locks the v1.0 vocabulary contract.
       b. test_scopes_is_known_metric — `is_known_metric("cost_usd_24h") is True`; `is_known_metric("foo") is False`.
       c. test_extract_cost_usd_24h_empty_db_returns_empty — fresh test DB with no token_usage rows → extractor returns `{}`.
       d. test_extract_cost_usd_24h_groups_by_model — seed two token_usage rows for two different models within last 24h, seed pricing → assert dict has 2 entries with correct model: prefix and float values matching compute_cost output.
       e. test_extract_skill_p95_latency_ms_empty — no skill_activated events → returns `{}`.
       f. test_extract_skill_p95_latency_ms_per_skill — seed 5 skill_activated events for one skill at varying duration_ms → returns `{"skill:<name>": p95}` with p95 matching the Pattern 4 SQL output.
       g. test_extract_dispatcher_failed_tasks_5m_zero — no failed tasks → returns `{"<global>": 0.0}` (global ALWAYS reports, even when zero).
       h. test_extract_dispatcher_failed_tasks_5m_count — seed 3 failed tasks within last 5min + 1 outside → returns `{"<global>": 3.0}`.
    3. Tests use the existing `db_session` + `seed_pricing` fixtures from conftest.py (Phase 13 P06). Inline `_seed_token_usage`, `_seed_otel_event`, `_seed_failed_task` helpers in the test file.
    4. Run `cd backend && pytest tests/test_alerts_detector.py -x -v`. All ~27 tests must pass.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_alerts_detector.py -x -v &amp;&amp; pytest --no-header 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
    - `from cmc.alerts.scopes import _SCOPE_EXTRACTORS, is_known_metric` succeeds.
    - All 3 extractors return correct shapes on the test DB.
    - cost_usd_24h dict values match compute_cost exactly (Decimal→float conversion with no surprises).
    - Vocabulary lock test passes (the dict has exactly the 3 expected keys).
    - Full backend suite green: 463 baseline + ~27 new = ~490 passed, 0 failed.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AlertRule fields → detector input | rule.threshold_fire / threshold_clear are user-provided floats. Mitigated: detector treats non-numeric/None defensively (threshold_clear=None → falls back to threshold_fire; both being None would be a Plan 02 validation error, not a detector concern). |
| state.params_json → EWMA read | Hand-editable JSON column. Mitigated by `_read_anomaly_state` try/except returning (0.0, 0.0, 0) on parse failure — corrupt state degrades gracefully to "first sample" rather than crashing dispatcher. |
| Scope extractor SQL output → detector | SQL aggregates return floats; defended via SQLAlchemy parameterized text + COALESCE in SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | rule.params_json window_n | mitigate | `int(rule.params_json.get("window_n", 50))` wrapped in try/except, defaults to 50 on parse failure. |
| T-15-01-02 | DoS | Catastrophic-cancellation in EWMA variance | mitigate | Welford-style update used (var += alpha * (x - prior_mean)^2 + ...) — numerically stable per RESEARCH.md detector math. EPSILON=1e-9 in sqrt prevents NaN. |
| T-15-01-03 | Information Disclosure | scope_key strings exposed in Telegram | accept | Single-user tool by design; scope_keys reference user's own model/skill/global tags. No multi-tenant leak surface. |
| T-15-01-04 | Tampering | Unknown metric in AlertRule.metric | defer | Plan 02 enforces via is_known_metric() at AlertRuleCreate validation. Plan 01 ships the gate; Plan 02 wires it. |

</threat_model>

<verification>

```bash
# Per-task gate
cd backend && pytest tests/test_alerts_detector.py -x -v

# stdlib-math-only assertion
cd backend && grep -E "^(import|from) (numpy|scipy|statistics|pandas)" cmc/alerts/detector.py
# Expect: no output (only `import math` allowed beyond stdlib essentials).

# Scope vocabulary lock
cd backend && .venv/bin/python -c "from cmc.alerts.scopes import _SCOPE_EXTRACTORS; assert set(_SCOPE_EXTRACTORS.keys()) == {'cost_usd_24h', 'skill_p95_latency_ms', 'dispatcher_failed_tasks_5m'}, list(_SCOPE_EXTRACTORS.keys()); print('OK')"

# Full-suite phase gate
cd backend && pytest --no-header 2>&1 | tail -3
# Expect: 463 baseline + ~27 new = ~490 passed, 0 failed, 0 skipped (modulo 1 pre-existing SchedulesCard frontend baseline).
```

</verification>

<success_criteria>

Plan 01 complete when:

1. `from cmc.alerts.detector import AlertSignal, evaluate_threshold, evaluate_anomaly` succeeds.
2. `from cmc.alerts.scopes import _SCOPE_EXTRACTORS, is_known_metric, ScopeExtractor` succeeds.
3. evaluate_threshold passes all 10 hysteresis cases.
4. evaluate_anomaly passes all 9 EWMA + warm-up + min_samples cases.
5. _SCOPE_EXTRACTORS has exactly 3 entries (cost_usd_24h, skill_p95_latency_ms, dispatcher_failed_tasks_5m) with documented scope_key formats.
6. detector.py imports ONLY `math` from numerical libraries (no numpy/scipy/pandas/statistics).
7. Full backend test suite green: ~490 passed, 0 failed (modulo pre-existing baseline).

</success_criteria>

<output>
After completion, create `.planning/phases/15-alert-engine-ui/15-01-SUMMARY.md` per the standard template. Document:

- The 3 v1.0 metric vocabulary lock (D-01) — scope_key formats are now contract.
- The decision to carry EWMA state in alert_state.params_json (D-03) — no schema change needed.
- AlertSignal enum members (5 values) and their semantics — Plan 02 maps these to dispatcher actions.
- The evaluate_anomaly return shape (signal, ewma_dict) — Plan 02 must persist the dict.
- Patterns established: stdlib-math-only detector, scope-extractor table, defensive params_json read.
</output>

---

## Phase 15 Requirement Coverage Matrix

This matrix lives on Plan 01 (the foundation plan) but covers ALL 12 ALRT-* requirements across the 5 plans. Verifier reads this to confirm requirement→task traceability.

| Requirement | Plan | Task / Surface | Notes |
|-------------|------|----------------|-------|
| **ALRT-01** alert_rules SQLModel structural columns + params_json | (Phase 13 already shipped schema) | n/a — schema lock | Plan 02 Task 2 surfaces fields via AlertRuleRow Pydantic schema; Plan 04 surfaces TS interface AlertRule |
| **ALRT-02** alert_state lifecycle + (rule_id, scope_key) UNIQUE | (Phase 13 already shipped schema) | n/a — schema lock | Plan 02 Task 1 reads/writes state; Plan 03 Task 2 ack writes state.acked_until |
| **ALRT-03** detector.py threshold + EWMA z-score, stdlib math | 15-01 | Task 1 (threshold) + Task 2 (anomaly) | `import math` only; ~12 + ~9 tests |
| **ALRT-04** dispatcher hook after stamp_tick (try/except) | 15-02 | Task 1 — heartbeat.py edit | Placement AFTER e-stop early return (D-01) |
| **ALRT-05** insufficient_data 24h warm-up suppresses | 15-01 | Task 2 — evaluate_anomaly warmup gate | WARMUP_SECONDS=86400 + min_samples gate; verified by test_anomaly_warmup_gate_suppresses |
| **ALRT-06** stable dedup_key = alert:{rule_id}:{scope_key} | 15-02 | Task 1 — _claim_and_send pattern reuse | Pitfall 7 verified by test_evaluate_alerts_concurrent_ticks |
| **ALRT-07** auto-resolve via decisions.status='answered' answered_by='alert_engine' | 15-02 | Task 1 — CLEAR transition handler | Includes notification_log cleanup (Pitfall 5 / D-03) |
| **ALRT-08** ack_alert callback verb suppresses 1h | 15-03 | Task 1 (verb routing) + Task 2 (ack endpoint + ack-precedence test) | sha256[:8] encoding (D-01) |
| **ALRT-09** Full CRUD + events history | 15-02 | Task 2 — 5 endpoints | List, create, patch, delete, events |
| **ALRT-10** /alerts route + AlertRulesList + AlertRuleForm + 30s cadence | 15-04 (lib) + 15-05 (UI) | 15-04 Task 2 (cadence) + 15-05 Tasks 1-3 (panels + route) | Plus AlertEventsList (D-01 in Plan 05) |
| **ALRT-11** plain-text only, no parse_mode= | 15-03 | Task 2 — format_alert composer | Manual grep verifies; CI guard extension is Phase 17 (POLI-02) |
| **ALRT-12** alert engine never imports cmc.dispatcher.tasks | 15-02 | Task 1 — test_no_tasks_import (ast static check) | Invariant test in test_alerts_dispatcher.py |

## Open-Question Resolution Index

The 6 open questions surfaced by RESEARCH.md are resolved as follows:

| Open Q | Question | Resolved In | Decision |
|--------|----------|-------------|----------|
| 1 | scope_key vocabulary lock | 15-01 D-01 | 3 v1.0 metrics: cost_usd_24h (model:), skill_p95_latency_ms (skill:), dispatcher_failed_tasks_5m (<global>) |
| 2 | Heartbeat hook placement | 15-02 D-01 | AFTER stamp_tick + AFTER e-stop early return |
| 3 | ack_alert callback_data ≤64-byte encoding | 15-03 D-01 | ack_alert:{rule_id}:{sha256(scope_key)[:8]} + Python-side resolver |
| 4 | AlertEventsList placement | 15-05 D-01 | Separate panel, full-width below rules + form |
| 5 | Initial seeded rules | 15-02 D-05 | NONE — empty state on first /alerts page |
| 6 | Range Literal for events | 15-02 D-04 | Reuse Literal["1d","7d","14d","30d"] (alias as AlertRange) |
