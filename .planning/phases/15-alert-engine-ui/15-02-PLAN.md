---
phase: 15-alert-engine-ui
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - backend/cmc/dispatcher/alerts.py
  - backend/cmc/dispatcher/heartbeat.py
  - backend/cmc/api/routes/alerts.py
  - backend/cmc/api/schemas/alerts.py
  - backend/cmc/api/routes/__init__.py
  - backend/tests/test_alerts_dispatcher.py
  - backend/tests/test_alerts_router.py
autonomous: true
requirements: [ALRT-01, ALRT-02, ALRT-04, ALRT-05, ALRT-06, ALRT-07, ALRT-08, ALRT-09, ALRT-12]
tags: [fastapi, sqlmodel, dispatcher, hysteresis, dedup, decisions, notification-log, sqlite-on-conflict]

must_haves:
  truths:
    - "User can rely on cmc.dispatcher.alerts.evaluate_alerts(db) executing once per heartbeat tick AFTER stamp_tick AND AFTER the emergency-stop early return — verified by an in-memory dispatcher test that asserts evaluate_alerts is NOT called when emergency_stop=1"
    - "User can trust that one firing condition produces exactly one decision row + one notification_log row regardless of evaluation cycles, via stable dedup_key = f'alert:{rule_id}:{scope_key}' and the existing notification_log UNIQUE(kind, entity_id, chat_id) constraint — Pitfall 7 verified by asyncio.gather(evaluate_alerts(), evaluate_alerts()) test"
    - "User can hit GET /api/alerts/rules + POST /api/alerts/rules + PATCH /api/alerts/rules/{id} + DELETE /api/alerts/rules/{id} + GET /api/alerts/events?range= for full CRUD on alert rules and the firing history view"
    - "User can trust that alerts auto-resolve when underlying metric clears: evaluate_alerts writes decisions.status='answered' + answered_by='alert_engine' on a firing→clear transition AND deletes the matching notification_log row so a re-fire produces a fresh notification (Pitfall 5)"
    - "User can trust that anomaly rules in insufficient_data state suppress notifications during 24h warm-up — verified by an integration test asserting NO decision row + NO notification_log row are created while sample_count < min_samples"
    - "User can trust that the alert engine never imports cmc.dispatcher.tasks — verified by a static-import test in test_alerts_dispatcher.py"
  artifacts:
    - path: "backend/cmc/dispatcher/alerts.py"
      provides: "evaluate_alerts(db) entry — orchestrates _SCOPE_EXTRACTORS → detector → state persist → decision row → notification_log claim"
      contains: "evaluate_alerts"
    - path: "backend/cmc/dispatcher/heartbeat.py"
      provides: "Modified run_one_cycle with try/except evaluate_alerts hook AFTER stamp_tick AFTER e-stop early return"
      contains: "evaluate_alerts"
    - path: "backend/cmc/api/routes/alerts.py"
      provides: "5 endpoints — list/create/patch/delete rules + events history"
      exports: ["list_rules", "create_rule", "patch_rule", "delete_rule", "list_events"]
    - path: "backend/cmc/api/schemas/alerts.py"
      provides: "AlertRange + AlertKind + AlertRuleCreate/Patch/Row + AlertEvent + AlertEventsResponse"
      contains: "AlertRuleCreate"
    - path: "backend/cmc/api/routes/__init__.py"
      provides: "Modified all_routers() to include alerts_router"
      contains: "alerts_router"
  key_links:
    - from: "backend/cmc/dispatcher/heartbeat.py"
      to: "cmc.dispatcher.alerts.evaluate_alerts"
      via: "try/except call AFTER e-stop early return AND BEFORE sweep_stale_pids"
      pattern: "evaluate_alerts"
    - from: "backend/cmc/dispatcher/alerts.py"
      to: "cmc.alerts.detector + cmc.alerts.scopes"
      via: "Import + call evaluate_threshold / evaluate_anomaly per rule.kind"
      pattern: "from cmc.alerts"
    - from: "backend/cmc/dispatcher/alerts.py"
      to: "decisions table (partial-unique on dedup_key WHERE status='pending')"
      via: "sqlite_insert(Decision).on_conflict_do_nothing(index_elements=['dedup_key'], index_where=text(\"status = 'pending'\"))"
      pattern: "Decision"
    - from: "backend/cmc/dispatcher/alerts.py"
      to: "notification_log table (UNIQUE kind, entity_id, chat_id)"
      via: "sqlite_insert(NotificationLog).on_conflict_do_nothing — kind='alert', entity_id=dedup_key"
      pattern: "kind=\"alert\""
    - from: "backend/cmc/api/routes/alerts.py"
      to: "cmc.alerts.scopes.is_known_metric"
      via: "AlertRuleCreate validator rejects unknown metric → 422"
      pattern: "is_known_metric"

deviations:
  - id: D-01
    item: "RESEARCH.md Open Q #2: heartbeat hook placement (before or after e-stop check)"
    decision: "Hook lands AFTER stamp_tick (per ROADMAP SC#1) AND AFTER the emergency-stop early return — so e-stop disables alerts too. Rationale: if user pulled the plug, do NOT spam Telegram about the dispatcher being down — user just stopped it. Concrete site: heartbeat.py line ~77, immediately after `if row is not None and (row.value or '') == '1': return 0` and before `live_pids = sweep_stale_pids()`."
    impact: "An e-stop test must assert alerts do NOT fire when emergency_stop=1 — gate covered in test_alerts_dispatcher.py."
  - id: D-02
    item: "RESEARCH.md Pitfall 2: PATCH that changes thresholds invalidates pending decision prompts"
    decision: "On PATCH that changes threshold_fire / threshold_clear / metric / kind, DELETE matching alert_state rows for that rule_id. Next tick re-evaluates with fresh state and emits new prompts. PATCH that changes only enabled / name does NOT clear state."
    impact: "test_alerts_router covers: PATCH threshold → state cleared; PATCH enabled → state preserved."
  - id: D-03
    item: "Dedup_key format and notification_log entity_id collision risk (Pitfall 5)"
    decision: "On firing→clear auto-resolve transition, ALSO delete the matching notification_log row (kind='alert', entity_id=dedup_key, chat_id=settings.telegram_chat_id) so a subsequent re-fire produces a fresh row — mirrors notifier.py::cleanup_rerun_failures pattern. Without this, the SECOND firing's INSERT ON CONFLICT DO NOTHING would silently skip notification."
    impact: "Pitfall 5 verified by a flap test (fire→clear→fire) asserting two notification_log rows exist after the second fire (one stale-cleaned, one fresh)."
  - id: D-04
    item: "RESEARCH.md Open Q #6: range Literal for events endpoint"
    decision: "Reuse Literal['1d','7d','14d','30d'] verbatim from cost.py (CostRange). Define as AlertRange in schemas/alerts.py — separate symbol so future expansion is decoupled, but identical members for v1.0."
    impact: "GET /api/alerts/events?range=2d → 422 (auto-validation)."
  - id: D-05
    item: "RESEARCH.md Open Q #5: initial seeded rules"
    decision: "NO default rules. Empty state on first /alerts page render. Rationale: avoid surprising the user with auto-firing alerts before they understand the engine. Phase 17 polish may add seed examples behind a flag."
    impact: "Plan 02 ships ZERO Alembic seeds. AlertRulesList.tsx (Plan 04) handles empty state with EmptyState component."
  - id: D-06
    item: "decisions row shape for alert auto-resolve (ALRT-07)"
    decision: "Insert decisions row with: session_id=None, task_id=None, dedup_key=f'alert:{rule_id}:{scope_key}', prompt=composed alert text (Plan 03 owns composer; this plan stubs as f'Alert: {rule.name} fired (value={value:.2f}, scope={scope_key})'), options=[]  (no Yes/No — alerts are sensors, not actuators per ALRT-12), status='pending', created_at=now. On clear: UPDATE WHERE dedup_key=:k AND status='pending' SET status='answered', answer='auto-resolved', answered_by='alert_engine', answered_at=now. Plan 03 refines the prompt body via messages.format_alert."
    impact: "Plan 03 (Telegram composer) replaces the prompt stub with the production format_alert call."
---

<objective>
Wire the detector primitives from Plan 01 into the live dispatcher tick. Ship the cmc.dispatcher.alerts.evaluate_alerts(db) entry, hook it into heartbeat.py with the locked placement (after stamp_tick + after e-stop), and expose the full /api/alerts CRUD + events history. Every dedup pattern is copied verbatim from notifier.py::_claim_and_send and hitl.py::create_decision — no new state machine, no new constraints.

Purpose: Close the IO half of the alert engine (Plan 01 owned the math). After this plan, the engine is functional end-to-end on the backend: a rule POSTed via the API will fire the next tick, deduplicate against the existing notification_log, and auto-resolve when the metric clears. The frontend (Plans 03 + 04) renders this.

Output: 2 new backend modules (dispatcher/alerts.py + api/routes/alerts.py), 1 new schema module (api/schemas/alerts.py), 1 modified heartbeat.py + routes/__init__.py, 2 new test files (test_alerts_dispatcher + test_alerts_router) with ~25 tests covering tick integration, dedup, auto-resolve, e-stop gate, CRUD validation, and PATCH state-clearing.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-alert-engine-ui/15-RESEARCH.md
@.planning/phases/15-alert-engine-ui/15-01-PLAN.md
@backend/cmc/alerts/detector.py
@backend/cmc/alerts/scopes.py
@backend/cmc/dispatcher/heartbeat.py
@backend/cmc/db/models/alert_rules.py
@backend/cmc/db/models/alert_state.py
@backend/cmc/db/models/decisions.py
@backend/cmc/db/models/notification_log.py
@backend/cmc/telegram/notifier.py
@backend/cmc/api/routes/cost.py
@backend/cmc/api/routes/schedules.py
@backend/cmc/api/routes/hitl.py

<interfaces>
<!-- From Plan 01 (Wave 1 — already shipped at start of Wave 2): -->

# cmc/alerts/detector.py
class AlertSignal(StrEnum):
    FIRING, CLEAR, PENDING_FIRE, HOLD, INSUFFICIENT

def evaluate_threshold(rule, current_value, state, *, now) -> AlertSignal: ...
def evaluate_anomaly(rule, current_value, state, *, now) -> tuple[AlertSignal, dict]: ...

# cmc/alerts/scopes.py
_SCOPE_EXTRACTORS: dict[str, ScopeExtractor]   # 3 entries; v1.0 lock
def is_known_metric(metric: str) -> bool: ...

<!-- Existing dedup primitives (verbatim copy targets): -->

# notifier.py::_claim_and_send (lines 226-294)
stmt = (sqlite_insert(NotificationLog).values(kind=..., entity_id=..., chat_id=..., sent_at=now, status="pending")
    .on_conflict_do_nothing(index_elements=["kind", "entity_id", "chat_id"]))
result = await db.execute(stmt); await db.commit()
if (result.rowcount or 0) == 0: return False  # raced

# hitl.py decisions partial-unique pattern
stmt = (sqlite_insert(Decision).values(... dedup_key=key ..., status="pending")
    .on_conflict_do_nothing(index_elements=["dedup_key"], index_where=text("status = 'pending'"))
    .returning(Decision))

<!-- Heartbeat hook surgical site (heartbeat.py:60-94): -->

async def run_one_cycle() -> int:
    settings = load_settings()
    engine = create_engine_for_settings(settings)
    sessions = make_sessionmaker(engine)
    try:
        try:
            await stamp_tick(sessions)
            async with sessions() as db:
                row = (await db.execute(select(SystemState).where(SystemState.key == "emergency_stop"))).scalar_one_or_none()
            if row is not None and (row.value or "") == "1":
                log.info("dispatcher.emergency_stop_active")
                return 0
            # === Plan 02 INSERTS HERE (line ~77, between e-stop and sweep_stale_pids): ===
            try:
                async with sessions() as db:
                    from cmc.dispatcher.alerts import evaluate_alerts
                    await evaluate_alerts(db)
            except Exception:
                log.exception("dispatcher.alerts_failed_ignore")
            # ===
            live_pids = sweep_stale_pids()
            ...

<!-- Schema for AlertRule list/create/patch/event responses: -->

class AlertRange(StrEnum):
    d1 = "1d"; d7 = "7d"; d14 = "14d"; d30 = "30d"
# Or simply: Literal["1d","7d","14d","30d"] (matches CostRange in cost.py:19)
# RECOMMEND: Literal alias (line-for-line match with cost.py) — fewest moving parts.

class AlertKind(StrEnum):
    threshold = "threshold"
    anomaly = "anomaly"

class AlertRuleCreate(BaseModel):
    name: str = Field(..., min_length=1, max_length=120)
    kind: AlertKind
    metric: str    # validated against is_known_metric (Plan 01) — 422 on unknown
    threshold_fire: float | None = None
    threshold_clear: float | None = None
    min_dwell_seconds: int = Field(0, ge=0)
    min_samples: int = Field(1, ge=1)
    cooldown_seconds: int = Field(0, ge=0)
    enabled: bool = True
    spec_version: int = 1
    params_json: dict[str, Any] = Field(default_factory=dict)
    # Validators:
    #   - kind == 'threshold' requires threshold_fire (else 422)
    #   - threshold_clear < threshold_fire when both set (else 422)
    #   - is_known_metric(metric) (else 422)

class AlertRulePatch(BaseModel):
    # All fields optional for partial update
    name: str | None = None
    enabled: bool | None = None
    threshold_fire: float | None = None
    threshold_clear: float | None = None
    min_dwell_seconds: int | None = None
    min_samples: int | None = None
    cooldown_seconds: int | None = None
    params_json: dict[str, Any] | None = None
    # NOT patchable: kind, metric, spec_version, rule_id

class AlertRuleRow(ORMBase):
    rule_id: int
    name: str
    kind: str
    metric: str
    threshold_fire: float | None
    threshold_clear: float | None
    min_dwell_seconds: int
    min_samples: int
    cooldown_seconds: int
    enabled: bool
    spec_version: int
    params_json: dict[str, Any]
    created_at: datetime
    updated_at: datetime

class AlertRuleListResponse(BaseModel):
    items: list[AlertRuleRow]
    total: int

class AlertEvent(BaseModel):
    """One row of firing history. Joins decisions WHERE answered_by='alert_engine' OR
       (status='pending' AND dedup_key LIKE 'alert:%')."""
    decision_id: int
    rule_id: int
    rule_name: str
    scope_key: str
    fired_at: datetime
    cleared_at: datetime | None
    status: str   # 'pending' (still firing) | 'answered' (auto-resolved or acked)
    last_value: float | None

class AlertEventsResponse(BaseModel):
    range: Literal["1d","7d","14d","30d"]
    items: list[AlertEvent]
    total: int
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build cmc/dispatcher/alerts.py — evaluate_alerts orchestrator + tick test</name>
  <files>
    backend/cmc/dispatcher/alerts.py
    backend/cmc/dispatcher/heartbeat.py
    backend/tests/test_alerts_dispatcher.py
  </files>
  <behavior>
    - `evaluate_alerts(db: AsyncSession) -> int` — returns count of rules evaluated. Exception-free contract: ALL exceptions raised by detectors / scope extractors are caught + logged + skipped per-rule (one bad rule must NOT poison the cycle).
    - Orchestrator algorithm:
      1. SELECT enabled=True rules (`SELECT * FROM alert_rules WHERE enabled = TRUE ORDER BY rule_id`).
      2. For each rule:
         a. Look up extractor: `extractor = _SCOPE_EXTRACTORS.get(rule.metric)`. If None (orphan rule from disabled v1.x metric), log warning + skip.
         b. Resolve scopes: `scopes = await extractor(db, datetime.now(UTC).replace(tzinfo=None))` → dict[scope_key, value].
         c. For each (scope_key, value):
            - Load or create alert_state row for (rule_id, scope_key). On miss: INSERT with state='clear', sample_count=0, fired_at=None.
            - **Ack precedence (RESEARCH Pitfall 3):** if state.acked_until is not None and state.acked_until > now → set state.last_value=value, state.last_evaluated_at=now, COMMIT, continue (no detector call, no emit).
            - Dispatch by kind:
                rule.kind == 'threshold': signal = evaluate_threshold(rule, value, state, now=now)
                rule.kind == 'anomaly':   signal, ewma = evaluate_anomaly(rule, value, state, now=now)
                                          state.params_json = {**state.params_json, **{k:ewma[k] for k in ('ewma_mean','ewma_var')}}
                                          state.sample_count = int(ewma['sample_count'])
            - Persist state mutation per signal:
                FIRING (was clear/insufficient):    state.state='firing'; state.fired_at = state.fired_at or now; state.cleared_at=None; state.last_value=value; state.last_evaluated_at=now → emit + dedup (next sub-step).
                FIRING (was firing — re-emit after cooldown): state.last_value=value; state.last_evaluated_at=now; state.fired_at=now (stamp re-emit time) → emit + dedup.
                PENDING_FIRE: state.fired_at = state.fired_at or now (stamp candidate); state.last_value=value; state.last_evaluated_at=now → NO emit.
                HOLD: state.last_value=value; state.last_evaluated_at=now → NO emit.
                CLEAR (was firing): state.state='clear'; state.cleared_at=now; state.fired_at=None; state.last_value=value; state.last_evaluated_at=now → AUTO-RESOLVE: UPDATE decisions SET status='answered', answer='auto-resolved', answered_by='alert_engine', answered_at=now WHERE dedup_key=:k AND status='pending'; ALSO DELETE FROM notification_log WHERE kind='alert' AND entity_id=:k AND chat_id=:cid (Pitfall 5 fix).
                CLEAR (was clear): state.state='clear'; state.last_value=value; state.last_evaluated_at=now → no-op aside from telemetry.
                INSUFFICIENT: state.state='insufficient_data'; state.last_value=value; state.last_evaluated_at=now → NO emit. Per ALRT-05.
            - On emit (FIRING): build dedup_key = f"alert:{rule.rule_id}:{scope_key}". Insert decisions row via sqlite_insert(...).on_conflict_do_nothing(index_elements=['dedup_key'], index_where=text("status = 'pending'")). Insert notification_log row via sqlite_insert(...).on_conflict_do_nothing(index_elements=['kind','entity_id','chat_id']) with kind='alert', entity_id=dedup_key, chat_id=settings.telegram_chat_id, sent_at=now, status='pending'. The actual Telegram send happens in the existing notifier oneshot tick (which sweeps notification_log status='pending' rows of every kind via its existing kind→formatter table — Plan 03 wires the alert formatter into _FORMATTER).
         d. COMMIT after each rule (so a later-rule failure does not roll back earlier rule's state).
      3. Return count of rules evaluated.
    - Static-import audit: this module MUST NOT import cmc.dispatcher.tasks (ALRT-12). Test asserts via `import ast; tree = ast.parse(open('cmc/dispatcher/alerts.py').read()); assert not any(... 'tasks' in alias.name ...)`.
    - heartbeat.py modification: add the try/except call at line ~77 per <interfaces> sketch above.
  </behavior>
  <action>
    1. Create `backend/cmc/dispatcher/alerts.py`:
       - File header docstring citing ALRT-04 + ALRT-12.
       - Imports (NO `cmc.dispatcher.tasks`):
         * stdlib: `from datetime import datetime, UTC`; `import logging`.
         * sqlalchemy: `from sqlalchemy import select, text, delete, update`; `from sqlalchemy.dialects.sqlite import insert as sqlite_insert`; `from sqlalchemy.ext.asyncio import AsyncSession`.
         * project: `from cmc.config import load_settings`; `from cmc.db.models.alert_rules import AlertRule`; `from cmc.db.models.alert_state import AlertState`; `from cmc.db.models.decisions import Decision`; `from cmc.db.models.notification_log import NotificationLog`; `from cmc.alerts.detector import AlertSignal, evaluate_threshold, evaluate_anomaly`; `from cmc.alerts.scopes import _SCOPE_EXTRACTORS`.
         * messages composer is Plan 03's responsibility — for this plan, use `from cmc.telegram.messages import format_alert` only IF Plan 03 lands first; OTHERWISE use a local stub `def _format_alert_prompt(rule, scope_key, value) -> str: return f"Alert: {rule.name} fired (value={value:.2f}, scope={scope_key})"` and replace in Plan 03. **Choice: ship local stub here**; Plan 03 task swaps to `from cmc.telegram.messages import format_alert` as a one-line edit.
       - Implement `async def evaluate_alerts(db: AsyncSession) -> int:` per the algorithm in <behavior>. Settings are loaded once at top: `settings = load_settings()`; chat_id = `str(settings.telegram_chat_id or "")`. If chat_id is empty (Telegram disabled), still run detectors + decisions but SKIP the notification_log insert (defense — alerts work even without Telegram).
    2. Modify `backend/cmc/dispatcher/heartbeat.py` at line ~77 (between e-stop early-return and sweep_stale_pids):
       ```python
       # Phase 15 ALRT-04: alert engine hook. Wrapped so detector failures
       # never poison the cycle. Placement is AFTER e-stop check so a
       # tripped e-stop disables alerts too (no spam about a dispatcher
       # that the user just stopped).
       try:
           async with sessions() as db:
               from cmc.dispatcher.alerts import evaluate_alerts
               await evaluate_alerts(db)
       except Exception:
           log.exception("dispatcher.alerts_failed_ignore")
       ```
       Use a function-local import to avoid bootstrap circularity if cmc.alerts.* ever needs to import dispatcher state — defensive, not strictly required at the moment.
    3. Create `backend/tests/test_alerts_dispatcher.py` with the following coverage:
       a. test_no_tasks_import — open cmc/dispatcher/alerts.py, parse with ast, assert no Import or ImportFrom node references 'tasks' (covers ALRT-12).
       b. test_evaluate_alerts_no_rules_returns_zero — empty alert_rules table → returns 0, no decisions row, no notification_log row.
       c. test_evaluate_alerts_threshold_fires_once — seed enabled threshold rule (cooldown_seconds=0, min_dwell_seconds=0) on metric 'dispatcher_failed_tasks_5m' with threshold_fire=0.0, seed 1 failed task → run once → assert exactly 1 decisions row with dedup_key='alert:{rule_id}:<global>' AND 1 notification_log row kind='alert'.
       d. test_evaluate_alerts_idempotent_per_tick — same setup as (c), run evaluate_alerts(db) twice sequentially → still exactly 1 decisions row + 1 notification_log row (partial-unique + UNIQUE handle).
       e. test_evaluate_alerts_concurrent_ticks — Pitfall 7 — `await asyncio.gather(evaluate_alerts(db1), evaluate_alerts(db2))` (two distinct sessions on the same engine) → still exactly 1 decisions row + 1 notification_log row.
       f. test_evaluate_alerts_auto_resolve_on_clear — fire (firing → 1 decisions pending), then remove the failed task (or wait past 5min — for test, mutate to flush failed tasks) → run again → assert decisions.status='answered', decisions.answered_by='alert_engine', decisions.answer='auto-resolved' AND notification_log row deleted.
       g. test_evaluate_alerts_re_fire_after_clear — flap: fire→clear→fire → assert TWO decisions rows total (first is answered, second is pending) AND ONE notification_log row currently (the second fire's; the first was deleted by Pitfall 5 cleanup).
       h. test_evaluate_alerts_anomaly_warmup_suppressed — seed anomaly rule (kind='anomaly', metric='cost_usd_24h', threshold_fire=3.0, min_samples=10), seed token_usage with one big spike, run once → assert state row exists with state='insufficient_data' AND zero decisions / zero notification_log rows.
       i. test_evaluate_alerts_skips_disabled — rule.enabled=False → no extractor call, no state row created.
       j. test_evaluate_alerts_unknown_metric_warns — seed rule with metric='made_up_metric_foo' (bypassing the API validator by direct DB insert) → run → assert no crash, no decisions, log warning emitted.
       k. test_heartbeat_hook_after_estop — set system_state.emergency_stop='1', insert always-firing rule → call run_one_cycle() → assert NO decisions row (e-stop short-circuits before evaluate_alerts). Use the test_dispatcher.py `_bootstrap_db` helper pattern verbatim.
       l. test_heartbeat_hook_calls_evaluate_alerts — emergency_stop='0', insert always-firing rule + 1 failed task (or seed dispatcher_failed_tasks_5m's input) → call run_one_cycle() → assert exactly 1 decisions row exists. Verifies the heartbeat insertion at line 77 is wired correctly.
    4. Test fixtures: reuse `_bootstrap_db` from test_dispatcher.py (alembic upgrade + sessions). Inline `_seed_failed_task(db, finished_at=now)` and `_seed_alert_rule(db, **kwargs)` helpers — keep DB-shape brittle code colocated with the tests.
    5. Run `cd backend && pytest tests/test_alerts_dispatcher.py -x -v`. All 12 tests must pass.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_alerts_dispatcher.py -x -v</automated>
  </verify>
  <done>
    - `from cmc.dispatcher.alerts import evaluate_alerts` succeeds.
    - heartbeat.py imports + calls evaluate_alerts in the correct position (after e-stop, before sweep).
    - All 12 tick-integration tests pass; concurrent-tick race verified.
    - Auto-resolve clears the notification_log row (Pitfall 5).
    - Anomaly rules in warm-up emit zero notifications.
    - `grep -c "from cmc.dispatcher.tasks" backend/cmc/dispatcher/alerts.py` returns 0 (ALRT-12 invariant).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build /api/alerts CRUD + events history endpoint + schemas + register router</name>
  <files>
    backend/cmc/api/schemas/alerts.py
    backend/cmc/api/routes/alerts.py
    backend/cmc/api/routes/__init__.py
    backend/tests/test_alerts_router.py
  </files>
  <behavior>
    - 6 endpoints under prefix='/alerts' (mounted /api):
      * GET    /alerts/rules                — AlertRuleListResponse (paginated; limit ge=1 le=200, offset ge=0; ORDER BY rule_id DESC).
      * POST   /alerts/rules                — AlertRuleRow, status_code=201; body=AlertRuleCreate; 422 on (a) is_known_metric=False, (b) kind=threshold + threshold_fire missing, (c) threshold_clear >= threshold_fire when both set, (d) name length out of bounds.
      * PATCH  /alerts/rules/{rule_id}      — AlertRuleRow; body=AlertRulePatch; 404 if rule not found; same 422 set as POST when threshold fields touched. ON PATCH that touches threshold_fire / threshold_clear / min_dwell_seconds / min_samples / cooldown_seconds: DELETE alert_state rows for this rule_id (per D-02). PATCH that touches only name / enabled / params_json: state preserved.
      * DELETE /alerts/rules/{rule_id}      — 204 No Content; cascade-delete alert_state rows for the rule_id (FK doesn't auto-cascade since SQLModel default is RESTRICT; do explicit DELETE FROM alert_state WHERE rule_id=:id BEFORE DELETE FROM alert_rules).
      * GET    /alerts/events?range=        — AlertEventsResponse; range Literal[...]; query joins decisions WHERE dedup_key LIKE 'alert:%' AND created_at >= :since LEFT JOIN alert_rules ON CAST(SPLIT_PART(...) -- SQLite has no SPLIT_PART. Use SUBSTR + INSTR substring extraction OR (cleaner) join via dedup_key prefix match: parse rule_id from dedup_key in Python after the SQL returns rows.
      * POST   /alerts/_ack                 — AlertAckRequest body {rule_id: int, scope_hash: 8-char hex}. Resolves scope_hash to full scope_key by SELECTing all alert_state rows for rule_id and computing sha256(scope_key)[:8] in Python (Plan 03 D-01 — SQLite has no native SHA256). Sets target.acked_until = now + 1h. Returns {ok: True, acked_until: iso8601}. 404 on no match. (Plan 03 wires the Telegram callback that hits this endpoint; the endpoint itself ships HERE in Plan 02 to keep alerts.py owned by a single plan.)
    - All endpoints use `db: AsyncSession = Depends(get_session)`.
    - `_RANGE_TO_DAYS` and `_range_start` COPIED from cost.py (same convention as Phase 14 plans — keeps router files independent).
    - Validators: AlertRuleCreate uses Pydantic v2 model_validator for cross-field checks. is_known_metric imported from cmc.alerts.scopes.
    - All-routers wiring: append `alerts_router` to `routes/__init__.py::all_routers()` AFTER `notifications_router` (last in list — alphabetic-ish + grouping with other Phase 15 surfaces).
  </behavior>
  <action>
    1. Create `backend/cmc/api/schemas/alerts.py`:
       - Header docstring citing ALRT-09 (CRUD spec).
       - Imports: `from datetime import datetime`; `from decimal import Decimal` (not used here but consistent with cost.py); `from enum import StrEnum`; `from typing import Any, Literal`; `from pydantic import BaseModel, ConfigDict, Field, model_validator`; `from cmc.api.schemas.common import ORMBase`; `from cmc.alerts.scopes import is_known_metric`.
       - Define `AlertRange = Literal["1d","7d","14d","30d"]` (D-04).
       - Define `AlertKind = Literal["threshold","anomaly"]` (StrEnum or Literal — pick Literal to mirror BreakdownDim in cost.py:20).
       - Define AlertRuleCreate, AlertRulePatch, AlertRuleRow (extends ORMBase), AlertRuleListResponse, AlertEvent, AlertEventsResponse, AlertAckRequest per <interfaces>. AlertAckRequest fields: `rule_id: int = Field(..., gt=0)`, `scope_hash: str = Field(..., min_length=8, max_length=8, pattern=r"^[0-9a-f]{8}$")`.
       - AlertRuleCreate `@model_validator(mode='after')`:
         * `if self.kind == 'threshold' and self.threshold_fire is None: raise ValueError("threshold rules require threshold_fire")`.
         * `if self.threshold_clear is not None and self.threshold_fire is not None and self.threshold_clear >= self.threshold_fire: raise ValueError("threshold_clear must be < threshold_fire (hysteresis floor)")`.
         * `if not is_known_metric(self.metric): raise ValueError(f"unknown metric: {self.metric}")`.
       - AlertRulePatch model_validator: same threshold checks but only when both fields are set (allow patching threshold_fire alone).
    2. Create `backend/cmc/api/routes/alerts.py`:
       - File header docstring citing ALRT-09.
       - Imports + APIRouter setup (`router = APIRouter(prefix="/alerts", tags=["alerts"])`).
       - Implement the 5 endpoints. ORM-row → AlertRuleRow via `AlertRuleRow.model_validate(row)`.
       - `_RANGE_TO_DAYS` + `_range_start` COPIED from cost.py.
       - PATCH handler: load rule, apply set_attributes, decide whether to clear state (any of `threshold_fire`, `threshold_clear`, `min_dwell_seconds`, `min_samples`, `cooldown_seconds` was provided in the patch) → `await db.execute(delete(AlertState).where(AlertState.rule_id == rule_id))`. Update updated_at = datetime.now(UTC).replace(tzinfo=None). COMMIT.
       - DELETE handler: explicit `delete(AlertState).where(rule_id=...)` then `delete(AlertRule).where(rule_id=...)`.
       - POST /alerts/_ack handler:
         ```python
         import hashlib
         from datetime import timedelta
         from cmc.db.models.alert_state import AlertState

         @router.post("/alerts/_ack", status_code=200)
         async def ack_alert(payload: AlertAckRequest, db: AsyncSession = Depends(get_session)):
             rows = (await db.execute(
                 select(AlertState).where(AlertState.rule_id == payload.rule_id)
             )).scalars().all()
             target = None
             for r in rows:
                 if hashlib.sha256(r.scope_key.encode()).hexdigest()[:8] == payload.scope_hash:
                     target = r
                     break
             if target is None:
                 raise HTTPException(status_code=404, detail=f"no alert_state matching rule_id={payload.rule_id} scope_hash={payload.scope_hash}")
             target.acked_until = datetime.now(UTC).replace(tzinfo=None) + timedelta(hours=1)
             await db.commit()
             return {"ok": True, "acked_until": target.acked_until.isoformat()}
         ```
       - GET /alerts/events handler:
         ```python
         since = _range_start(range_)
         q = (
             select(Decision, AlertRule)
             .join(AlertRule, text("CAST(SUBSTR(decisions.dedup_key, 7, INSTR(SUBSTR(decisions.dedup_key, 7), ':') - 1) AS INTEGER) = alert_rules.rule_id"), isouter=True)
             .where(Decision.dedup_key.like("alert:%"))
             .where(Decision.created_at >= since)
             .order_by(Decision.created_at.desc())
             .limit(500)
         )
         # Note: SUBSTR(dedup_key, 7) drops "alert:" prefix; INSTR finds the second ':'.
         # Cleaner: do the join in Python — fetch all matching decisions, parse rule_id, then bulk-fetch rules.
         ```
         **Recommendation:** do the join in Python (single Decision SELECT, then SELECT AlertRule WHERE rule_id IN (...)) — the SQL JOIN with SUBSTR/INSTR is fragile across SQLite versions. Speed is fine for v1.0 (events ≤500/day).
    3. Modify `backend/cmc/api/routes/__init__.py`: import `from cmc.api.routes.alerts import router as alerts_router`; append to `all_routers()` return list AFTER `notifications_router`.
    4. Create `backend/tests/test_alerts_router.py` with the following coverage:
       a. test_list_rules_empty — fresh DB → 200 + {items: [], total: 0}.
       b. test_create_rule_valid_threshold — POST {name, kind='threshold', metric='cost_usd_24h', threshold_fire=10.0, threshold_clear=5.0} → 201 + AlertRuleRow with rule_id assigned + threshold_fire=10.0.
       c. test_create_rule_unknown_metric_422 — POST metric='nonexistent' → 422; response detail/error contains "unknown metric".
       d. test_create_rule_threshold_without_fire_422 — POST kind='threshold' + threshold_fire=None → 422.
       e. test_create_rule_clear_above_fire_422 — POST threshold_fire=5, threshold_clear=10 → 422.
       f. test_create_rule_anomaly_valid — POST kind='anomaly', metric='cost_usd_24h', threshold_fire=3.0 (z-score), min_samples=10, params_json={"window_n": 100} → 201.
       g. test_list_rules_paginated — seed 5 rules → GET ?limit=2&offset=2 returns 2 items + total=5.
       h. test_patch_rule_enabled_preserves_state — POST rule, run dispatcher to create alert_state row (or directly seed alert_state), PATCH {enabled: false} → 200 + state row STILL present.
       i. test_patch_rule_threshold_clears_state — same setup, PATCH {threshold_fire: 99.0} → 200 + alert_state rows for rule_id are GONE.
       j. test_patch_rule_threshold_validation_422 — PATCH {threshold_fire: 5, threshold_clear: 10} (clear above fire) → 422.
       k. test_patch_rule_404_unknown — PATCH /alerts/rules/9999 → 404.
       l. test_delete_rule_cascades_state — POST rule, seed alert_state row, DELETE rule → 204 + no rule row + no alert_state row.
       m. test_events_invalid_range_422 — GET ?range=2d → 422.
       n. test_events_empty — GET ?range=7d (no decisions) → {range:'7d', items:[], total:0}.
       o. test_events_returns_alert_decisions — seed 2 decision rows with dedup_key='alert:1:<global>' + matching rule → GET /alerts/events returns those 2 events with rule_name populated.
       p. test_ack_alert_sets_acked_until — seed AlertRule + AlertState (rule_id=1, scope_key='model:foo'); POST /alerts/_ack {rule_id: 1, scope_hash: sha256('model:foo')[:8]} → 200 + acked_until between now+59min and now+61min; reload state and verify.
       q. test_ack_alert_unknown_rule_id_404 — POST {rule_id: 9999, scope_hash: 'a1b2c3d4'} → 404.
       r. test_ack_alert_hash_mismatch_404 — POST with valid rule_id but scope_hash that matches no state row → 404.
       s. test_ack_alert_invalid_hash_format_422 — POST scope_hash='ZZZZ' (not hex) or len!=8 → 422.
    5. Test fixtures: reuse client + db_session from conftest.py (Phase 13 P06). Inline `_post_rule(client, **overrides)` helper for the create-then-test pattern.
    6. Run `cd backend && pytest tests/test_alerts_router.py -x -v`. All 15 tests must pass.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_alerts_router.py -x -v &amp;&amp; pytest --no-header 2>&amp;1 | tail -3</automated>
  </verify>
  <done>
    - All 5 endpoints respond with the documented status codes.
    - 422 paths exercise ALL three validators (unknown metric, threshold-without-fire, clear>=fire).
    - PATCH on threshold fields clears state; PATCH on enabled preserves state (D-02 verified).
    - DELETE cascades to alert_state.
    - GET /alerts/events surfaces alert decisions with rule_name joined.
    - alerts_router is registered in all_routers() (smoke test: `client.get('/api/alerts/rules')` returns 200 not 404).
    - Full backend suite green: ~490 baseline + ~15 new = ~505 passed, 0 failed.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /api/alerts/* | Untrusted body/path/query params crossing into SQL. Mitigated by Pydantic v2 validators (is_known_metric / threshold checks / Field bounds) + SQLAlchemy parameterized text. |
| dispatcher tick → notification_log | Concurrent ticks could double-insert. Mitigated by UNIQUE(kind, entity_id, chat_id) + on_conflict_do_nothing — verbatim copy of notifier.py pattern. |
| evaluate_alerts → telegram chat_id | Settings-loaded; if missing, notification_log insert is skipped (engine still works without Telegram). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering | Unknown metric in POST body | mitigate | is_known_metric() validator + 422 surface (verifying test_create_rule_unknown_metric_422). |
| T-15-02-02 | DoS | Many rules × many scopes per tick | accept | v1.0 expected scale <50 rules; tick budget 120s, alert eval ~1s comfortable. RESEARCH Pitfall 10 documents soft cap. |
| T-15-02-03 | Tampering | Concurrent tick double-emission | mitigate | Pitfall 7 — partial-unique on decisions + UNIQUE on notification_log. test_evaluate_alerts_concurrent_ticks verifies. |
| T-15-02-04 | Repudiation | "Why did this fire?" | mitigate | decisions.dedup_key encodes rule_id + scope_key; AlertEvent.last_value surfaces the triggering numeric value; UI displays both. |
| T-15-02-05 | Information Disclosure | Telegram chat_id leak via /alerts/events | accept | Single-user tool; chat_id is settings-private; only rule_id + scope_key + last_value surface in responses. |
| T-15-02-06 | DoS | Pitfall 5 — collision blocks re-fire | mitigate | DELETE notification_log on auto-resolve transition (D-03). Test test_evaluate_alerts_re_fire_after_clear verifies. |
| T-15-02-07 | EoP | Alert engine importing dispatcher.tasks | mitigate | ast-based static-import test in test_alerts_dispatcher.py asserts no such import (ALRT-12 invariant). |

</threat_model>

<verification>

```bash
# Per-task gates
cd backend && pytest tests/test_alerts_dispatcher.py -x -v
cd backend && pytest tests/test_alerts_router.py -x -v

# ALRT-12 static-import invariant
cd backend && grep -E "from cmc\.dispatcher\.tasks|import cmc\.dispatcher\.tasks" cmc/dispatcher/alerts.py cmc/alerts/*.py
# Expect: no output.

# Heartbeat hook surgical site verification
cd backend && grep -n "evaluate_alerts" cmc/dispatcher/heartbeat.py
# Expect: 2 lines (the import + the call), positioned between e-stop and sweep_stale_pids.

# Router registration smoke
cd backend && grep -n "alerts_router" cmc/api/routes/__init__.py
# Expect: 2 lines (the import + the all_routers() append).

# Full suite phase gate
cd backend && pytest --no-header 2>&1 | tail -3
# Expect: ~490 baseline (Plan 01) + ~27 new = ~517 passed, 0 failed.
```

</verification>

<success_criteria>

Plan 02 complete when:

1. `from cmc.dispatcher.alerts import evaluate_alerts` succeeds.
2. heartbeat.py calls evaluate_alerts AFTER e-stop early return; e-stop test asserts NO decisions row when emergency_stop=1.
3. POST /api/alerts/rules creates a rule and the next evaluate_alerts call fires it (full integration verified).
4. Concurrent ticks produce exactly 1 decisions row + 1 notification_log row (Pitfall 7 verified).
5. Auto-resolve flips status='answered' AND deletes the notification_log row (Pitfall 5 verified).
6. PATCH on threshold fields clears alert_state; PATCH on enabled preserves it (D-02 verified).
7. AlertRuleCreate validators reject (a) unknown metric, (b) threshold without fire, (c) clear >= fire — all 422.
8. cmc/dispatcher/alerts.py contains zero imports of cmc.dispatcher.tasks (ALRT-12).
9. Full backend test suite green.

</success_criteria>

<output>
After completion, create `.planning/phases/15-alert-engine-ui/15-02-SUMMARY.md` per the standard template. Document:

- The heartbeat hook placement (after e-stop early return) and the rationale (D-01 Open Q #2 resolved).
- The dedup-key format (`alert:{rule_id}:{scope_key}`) and how Pitfall 5 is addressed (notification_log cleanup on auto-resolve).
- The PATCH-clears-state policy (D-02) — only threshold fields clear; enabled / name preserve.
- The decision to ship NO seed rules (D-05) — empty state on first /alerts page.
- The /alerts/events Python-side join over SQLite SUBSTR/INSTR — robust path chosen.
- Patterns established: alert engine as pure-decision emitter (ALRT-12 invariant), notification_log cleanup on transition, Pydantic v2 model_validator for cross-field validation.
</output>
