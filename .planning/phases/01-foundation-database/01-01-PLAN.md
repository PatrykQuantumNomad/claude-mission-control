---
phase: 01-foundation-database
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/phases/01-foundation-database/01-01-SCHEMA.md
autonomous: false
requirements:
  - FOUND-02
tags:
  - documentation
  - schema
  - phase-1

must_haves:
  truths:
    - "REQUIREMENTS.md no longer lists 'ORM (SQLAlchemy models)' as Out of Scope"
    - "PROJECT.md describes the database stack as SQLAlchemy 2.0 async + SQLModel + aiosqlite (not raw SQL)"
    - "A canonical 15-table list with column-level intent is committed to the phase directory and approved by the user before any Alembic revision is written"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated Out of Scope table without the ORM line"
      contains: "SQLAlchemy"
    - path: ".planning/PROJECT.md"
      provides: "Updated 'Database uses ...' paragraph"
      contains: "SQLAlchemy"
    - path: ".planning/phases/01-foundation-database/01-01-SCHEMA.md"
      provides: "Canonical 15-table schema list with columns + types + FKs"
      contains: "## Tables"
  key_links:
    - from: ".planning/PROJECT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Both must agree that the stack is SQLAlchemy ORM + Alembic, not raw SQL"
      pattern: "SQLAlchemy"
    - from: ".planning/phases/01-foundation-database/01-01-SCHEMA.md"
      to: "downstream Alembic initial revision (Plan 05)"
      via: "Plan 05 reads this file to know which tables/columns to emit"
      pattern: "table:"
---

<objective>
Close the upstream-doc contradiction surfaced in RESEARCH.md and lock the 15-table list before any code is written.

Purpose: CONTEXT.md mandates SQLAlchemy ORM + Alembic, but REQUIREMENTS.md (line 254) and PROJECT.md (line 84) still say "raw SQL, no ORM" from the pre-discussion roadmap. Phase 2+ research will inherit the wrong stack unless we fix it here. Additionally, RESEARCH.md Assumption A1 flags the 15-table list as MEDIUM-confidence — Plan 05 cannot generate the initial Alembic revision without an approved table list.

Output: Updated REQUIREMENTS.md + PROJECT.md, plus a new `01-01-SCHEMA.md` containing the canonical table list with column-level intent. A user-decision checkpoint locks the table list before downstream plans use it.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-database/01-CONTEXT.md
@.planning/phases/01-foundation-database/01-RESEARCH.md

<interfaces>
<!-- Anchor lines the executor must edit. Read these before editing. -->

REQUIREMENTS.md line 254 (current text — to be DELETED):
| ORM (SQLAlchemy models) | Raw SQL specified — simpler for read-heavy dashboard with known schema |

PROJECT.md line 84 (current text — to be REPLACED):
**Database uses raw SQL:**
No ORM. All queries are raw SQL via Python's `sqlite3` or `aiosqlite`. Schema uses `CREATE TABLE IF NOT EXISTS` with an idempotent `_migrate_add_column` helper. 15 tables ...

PROJECT.md line 96 also references this (Constraints):
- **No ORM**: Raw SQL everywhere, `aiosqlite` for async access

PROJECT.md line 106 (Key Decisions table):
| Raw SQL instead of SQLAlchemy ORM | Prompt specifies raw SQL; ... | -- Pending |

All four locations must be reconciled in this plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile REQUIREMENTS.md with the locked Phase 1 ORM decision</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Open `.planning/REQUIREMENTS.md` and make exactly these edits:

1. **Delete the line in `## Out of Scope`** that currently reads:
   `| ORM (SQLAlchemy models) | Raw SQL specified — simpler for read-heavy dashboard with known schema |`
   Delete the entire row. Do NOT replace it — ORM is now in scope.

2. **Add a new note immediately above the `## Out of Scope` table** (or as the first row entry under `## v2 Requirements > Enhanced Analytics` is fine — pick whichever keeps the diff smallest), in italics:
   `*Note: Phase 1 (Foundation & Database) discussion locked SQLAlchemy 2.0 async + SQLModel + Alembic as the data-access stack (see .planning/phases/01-foundation-database/01-CONTEXT.md). The "CREATE TABLE IF NOT EXISTS" / "_migrate_add_column" wording in FOUND-02 and FOUND-03 is INTENT (idempotent, data-safe schema evolution); the implementation is Alembic.*`

3. Do NOT change the FOUND-02/FOUND-03 requirement text itself — leave the wording, just clarify intent in the note above.

4. Save and verify the file still parses as a markdown table (no broken pipes).

Reference: RESEARCH.md "Conflict Resolution: Locked Decisions vs. Pre-existing Project Research" section.
  </action>
  <verify>
    <automated>grep -c "ORM (SQLAlchemy models)" .planning/REQUIREMENTS.md | xargs -I{} test {} -eq 0 && grep -q "SQLAlchemy 2.0 async + SQLModel + Alembic" .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <done>
The "ORM (SQLAlchemy models)" Out-of-Scope row is gone. A clarifying note about the locked Phase 1 stack is present. FOUND-02/FOUND-03 wording unchanged. File still renders as valid markdown.
  </done>
</task>

<task type="auto">
  <name>Task 2: Reconcile PROJECT.md (3 locations) with the locked Phase 1 ORM decision</name>
  <files>.planning/PROJECT.md</files>
  <action>
Open `.planning/PROJECT.md` and make exactly these edits:

1. **Around line 84** ("**Database uses raw SQL:**" paragraph in `## Context`): Replace the entire paragraph with:

```
**Database uses SQLAlchemy 2.0 async + SQLModel:**
SQLModel classes (one per resource) define all 15 tables. The async engine uses `sqlite+aiosqlite://` with WAL mode, foreign_keys=ON, and busy_timeout pragmas applied at connect time. Schema management is Alembic (initial revision creates all 15 tables; future migrations evolve them via `op.batch_alter_table` for SQLite compatibility). FOUND-02 wording in REQUIREMENTS.md ("CREATE TABLE IF NOT EXISTS") and FOUND-03 ("_migrate_add_column helper") describe INTENT — the implementation is Alembic, which is idempotent (`alembic upgrade head` is a no-op on an already-migrated DB) and provides the same data-safe column-add pattern via `Inspector.get_columns()`.
```

2. **Around line 96** (in `## Constraints`, the bullet "**No ORM**: Raw SQL everywhere..."): Replace with:

```
- **ORM**: SQLAlchemy 2.0 async + SQLModel; raw SQL only when stepping outside the ORM is unavoidable (e.g., complex analytics queries in later phases). `aiosqlite` is the async driver under the hood.
```

3. **Around line 106** (Key Decisions table, the row "| Raw SQL instead of SQLAlchemy ORM | ... | -- Pending |"): Replace that row with:

```
| SQLAlchemy 2.0 async + SQLModel + Alembic | Locked in Phase 1 discussion — supersedes earlier "raw SQL" direction. Models give type-safe queries, Alembic gives versioned migrations, aiosqlite stays as the async driver. | 2026-04-25 |
```

4. Save the file. The "FastAPI chassis foundation" paragraph (around line 78-79) currently says "Adapted for this project's needs (raw SQL instead of SQLAlchemy ORM, ...)". Update that parenthetical to "(SQLAlchemy 2.0 async + SQLModel instead of the chassis's plain stack, SQLite-only, no auth layer)".

Reference: RESEARCH.md "Conflict Resolution" + Assumption A8.
  </action>
  <verify>
    <automated>grep -c "Raw SQL instead of SQLAlchemy" .planning/PROJECT.md | xargs -I{} test {} -eq 0 && grep -c "raw SQL via" .planning/PROJECT.md | xargs -I{} test {} -eq 0 && grep -q "SQLAlchemy 2.0 async + SQLModel" .planning/PROJECT.md && echo OK</automated>
  </verify>
  <done>
All three "raw SQL"-flavored lines are replaced with SQLAlchemy/SQLModel descriptions. The Key Decisions table shows the new decision dated 2026-04-25. The Context paragraph and Constraints bullet are consistent with the locked stack. No "raw SQL"-only assertion remains.
  </done>
</task>

<task type="auto">
  <name>Task 3: Derive a draft 15-table schema from REQUIREMENTS.md and write 01-01-SCHEMA.md</name>
  <files>.planning/phases/01-foundation-database/01-01-SCHEMA.md</files>
  <action>
Create the canonical schema reference that Plan 05 will use to generate SQLModel classes and the Alembic initial revision.

Read every requirement category in `.planning/REQUIREMENTS.md` and derive a column-level schema for each of the 15 tables. RESEARCH.md Assumption A1 lists the candidate names: sessions, token_usage, tools, otel_events, otel_metrics, tasks, schedules, decisions, inbox, activities, live_state, mcp_stats, skills, system_state, notification_log.

For each table, document:
- **Purpose** (one sentence, traced to which requirement IDs use it)
- **Columns** with name, type (use SQLModel/SQLAlchemy types: `int`, `str`, `datetime`, `bool`, `JSON`, `Optional[...]`), nullability, default
- **Primary key**
- **Foreign keys** (with target table.column)
- **Indexes** the requirements imply (e.g., `sessions.session_id` UNIQUE for upserts in INGST-04; partial UNIQUE on decisions per HITL-02)
- **Open questions** (mark `[NEEDS USER CONFIRMATION]` for any column or table where the requirement is genuinely ambiguous)

Cross-reference, by phase:
- Phase 2 (INGST-01..10): sessions, token_usage, otel_events, otel_metrics, plus a `tools` table for paired tool_use/tool_result events
- Phase 3 (SAPI/SESS/OBSV/MCP/SKIL): no new tables, but schema must support the queries; flag any column gaps
- Phase 4 (HITL/TASK/SCHD/ESTOP): tasks, schedules, decisions, inbox, system_state (KV)
- Phase 6/7: panel-driven; mostly read-only against existing tables, but live_state (sessions active <5min, SESS-03/SESS-04) is its own table per REQUIREMENTS.md
- Phase 8 (DISP-*): mcp_stats, activities (commits/PRs/loc rollups), notification_log, system_state.emergency_stop
- Phase 9 (TELE-*): notification_log UNIQUE constraint per TELE-04

Use this exact file structure:

```markdown
# Phase 1 — Canonical 15-Table Schema

**Status:** DRAFT — pending user confirmation (Task 4 checkpoint)
**Source:** Derived from REQUIREMENTS.md by reading every -* category that touches the database.

## Tables

### 1. sessions
**Purpose:** ... (cite reqs)
**Columns:**
| name | type | null | default | notes |
|------|------|------|---------|-------|
| session_id | str | no | - | PK; uuid string from JSONL |
| ... | ... | ... | ... | ... |

**Indexes:** UNIQUE(session_id), INDEX(started_at DESC), INDEX(cwd)
**FKs:** none
**Used by:** INGST-04, SESS-01..07, OBSV-01, OBSV-06

### 2. token_usage
... (continue for all 15)
```

Use your best judgment on column names — match the requirement text wherever possible (e.g., TASK-02 lists `priority, quadrant, approval, risk, dry_run, model, execution_mode, skill, scheduled_for` — those are the column names). For ambiguous tables (mcp_stats, activities), use names from the API endpoints that read them (OBSV-09 reads commits/PRs/lines counters → `activities` columns are `day, kind, value` keyed-aggregate).

This is a DRAFT — Task 4 (checkpoint) is where the user confirms. Do not over-invest; aim for ~80% coverage and clearly mark gaps with `[NEEDS USER CONFIRMATION]`.

Reference: RESEARCH.md Assumption A1 + Open Question #1 + #2.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation-database/01-01-SCHEMA.md && grep -c "^### " .planning/phases/01-foundation-database/01-01-SCHEMA.md | xargs -I{} test {} -eq 15 && echo OK</automated>
  </verify>
  <done>
`01-01-SCHEMA.md` exists with exactly 15 `### N. <table_name>` sections. Each has Purpose, Columns table, Indexes, FKs, Used by. Genuinely ambiguous columns are tagged `[NEEDS USER CONFIRMATION]`. The draft cites requirement IDs so the user can spot-check coverage.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Checkpoint — Confirm 15-table schema before generating Alembic revision</name>
  <decision>Approve the draft `01-01-SCHEMA.md` as the canonical schema for Plan 05's Alembic initial revision, OR request changes (add/remove/rename tables, adjust columns).</decision>
  <context>
Plan 05 will autogenerate the initial Alembic migration from the SQLModel classes built in Plan 04. Once that migration is committed, every downstream phase assumes the schema is stable. RESEARCH.md flagged the 15-table list as MEDIUM confidence (Assumption A1) and Open Question #1 explicitly asked for user confirmation. PROJECT.md also notes "The user already runs a version of this dashboard daily" — there may be an existing schema we should mirror rather than re-derive from REQUIREMENTS.md.

Three things to verify:
1. **Table list correctness:** Are the 15 names right? (sessions, token_usage, tools, otel_events, otel_metrics, tasks, schedules, decisions, inbox, activities, live_state, mcp_stats, skills, system_state, notification_log)
2. **Critical column coverage:** Spot-check 2-3 tables you know best — e.g., do `tasks` columns match TASK-02? Does `notification_log` have the UNIQUE columns TELE-04 needs?
3. **Existing-schema reuse:** If you have the production dashboard's schema dumped somewhere, paste it (or a path to it) — that overrides any draft we derived.
  </context>
  <options>
    <option id="approve-as-is">
      <name>Approve the draft as-is</name>
      <pros>Fastest path forward. Plan 05 can autogenerate immediately.</pros>
      <cons>Any wrong column means a Phase 2+ migration to fix it (cheap with Alembic, but not free).</cons>
    </option>
    <option id="approve-with-edits">
      <name>Approve with specific column/table edits</name>
      <pros>Catch errors early when fixing them is one Alembic revision instead of a downstream migration chain.</pros>
      <cons>Requires the user to read the draft.</cons>
    </option>
    <option id="provide-existing-schema">
      <name>Provide existing dashboard schema (DDL or path) — supersedes the draft</name>
      <pros>Highest fidelity: the production schema is the source of truth.</pros>
      <cons>None, if available. Skip the draft and use the real DDL.</cons>
    </option>
  </options>
  <resume-signal>Reply with: "approve-as-is", "approve-with-edits: &lt;list of edits&gt;", or "provide-existing-schema: &lt;DDL or path&gt;"</resume-signal>
</task>

</tasks>

<verification>
- `grep` confirms no remaining "Raw SQL"-only assertion in REQUIREMENTS.md or PROJECT.md
- `01-01-SCHEMA.md` exists with 15 table sections
- Checkpoint resolution recorded (the user's chosen option becomes the canonical schema for Plan 05)
</verification>

<success_criteria>
- REQUIREMENTS.md no longer lists ORM as out of scope
- PROJECT.md describes the stack as SQLAlchemy + SQLModel + Alembic in all three locations
- A reviewed-and-approved 15-table schema exists in the phase directory
- Plan 05 has an unambiguous source of truth for the Alembic initial revision
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database/01-01-SUMMARY.md` summarizing:
- Which lines in REQUIREMENTS.md and PROJECT.md were edited (with before/after)
- The final approved table list (post-checkpoint)
- Any deferred schema questions that surfaced during the checkpoint
</output>
