---
phase: 06-observability-activity-panels
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - frontend/src/components/panels/SystemHealthStrip.tsx
  - frontend/src/components/panels/KpiRow.tsx
  - frontend/src/components/panels/AttentionBar.tsx
  - frontend/src/components/panels/LiveSessionsCard.tsx
  - frontend/src/components/panels/index.ts
  - frontend/src/components/panels/__tests__/SystemHealthStrip.test.tsx
  - frontend/src/components/panels/__tests__/KpiRow.test.tsx
  - frontend/src/components/panels/__tests__/AttentionBar.test.tsx
  - frontend/src/components/panels/__tests__/LiveSessionsCard.test.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/styles.css
autonomous: true
requirement_ids: [OPNL-01, OPNL-02, OPNL-03, OPNL-04]

must_haves:
  truths:
    - "OPNL-01 SystemHealthStrip renders pills for status (server/otel/daemons) using StatePill, shows uptime + memory + last-otel-age + tzname; refreshes every 5s"
    - "OPNL-02 KpiRow renders 4 KpiTile tiles (sessions / tokens / tools / errors) above the panel grid on / route, sourced from /api/summary; refreshes every 15s"
    - "OPNL-03 AttentionBar renders a horizontal warning bar with badge counts (stuck_sessions, stale_dispatcher_seconds, items[]) — HIDES via `return null` when no items AND no stuck sessions AND no stale dispatcher"
    - "OPNL-04 LiveSessionsCard lists active sessions (id, model, started_at via RelativeTime, current_tool, state via StatePill); clicking a row opens a Sheet drawer showing tool timeline (from /api/sessions/{id}/details) plus a follow-up textarea+Send (POST /api/sessions/live/{sid}/message via useFollowUpMessage); refreshes every 5s"
    - "/api/sessions/live returns a bare list — code accounts for that (no items envelope)"
    - "The follow-up Send button disables while mutation is pending; shows error inline if mutation fails (409 ended session, 400 invalid sid, 404 missing); on success: textarea clears + drawer stays open + liveSessions invalidates"
    - "Routes / inserts <KpiRow /> ABOVE PlaceholderCardGrid AND removes OPNL-01, OPNL-03, OPNL-04 entries from COMMAND_SLOTS (those slots become live panels rendered alongside the remaining placeholder cards in plan 06-03)"
    - "All previous frontend tests still pass; ~16 new tests added (4 panels × ~4 cases each)"
  artifacts:
    - path: "frontend/src/components/panels/SystemHealthStrip.tsx"
      provides: "OPNL-01 panel — pulls useSystemHealth, renders pills + uptime/memory/tzname"
      exports: ["SystemHealthStrip"]
    - path: "frontend/src/components/panels/KpiRow.tsx"
      provides: "OPNL-02 panel — 4 KpiTile tiles sourced from useSummary"
      exports: ["KpiRow"]
    - path: "frontend/src/components/panels/AttentionBar.tsx"
      provides: "OPNL-03 panel — uses PanelCard with hiddenWhenEmpty=true so the whole bar disappears when nothing is wrong"
      exports: ["AttentionBar"]
    - path: "frontend/src/components/panels/LiveSessionsCard.tsx"
      provides: "OPNL-04 panel — list + Sheet drawer + follow-up form"
      exports: ["LiveSessionsCard"]
    - path: "frontend/src/components/panels/index.ts"
      provides: "Panels barrel — Phase 6 plans re-export here for /, /activity to import"
      exports: ["SystemHealthStrip", "KpiRow", "AttentionBar", "LiveSessionsCard"]
    - path: "frontend/src/routes/index.tsx"
      provides: "Command page wires SystemHealthStrip + KpiRow + AttentionBar + LiveSessionsCard ABOVE PlaceholderCardGrid; OPNL-01/03/04 removed from slot list"
      contains: "SystemHealthStrip"
  key_links:
    - from: "frontend/src/components/panels/SystemHealthStrip.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useSystemHealth() hook"
      pattern: "useSystemHealth"
    - from: "frontend/src/components/panels/KpiRow.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useSummary() hook"
      pattern: "useSummary"
    - from: "frontend/src/components/panels/AttentionBar.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useAttention() hook"
      pattern: "useAttention"
    - from: "frontend/src/components/panels/LiveSessionsCard.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useLiveSessions(), useSessionDetails(), useFollowUpMessage()"
      pattern: "useLiveSessions|useFollowUpMessage"
    - from: "frontend/src/components/panels/LiveSessionsCard.tsx"
      to: "frontend/src/components/ui/Sheet.tsx"
      via: "<Sheet> wraps drawer body — composes Phase-5 primitive"
      pattern: "Sheet"
    - from: "frontend/src/routes/index.tsx"
      to: "frontend/src/components/panels"
      via: "Imports SystemHealthStrip + KpiRow + AttentionBar + LiveSessionsCard"
      pattern: "components/panels"
---

<objective>
Land the Command-page top strip — the four panels that don't fit the analytical-card grid: SystemHealthStrip (full-width status), KpiRow (4 tiles above the grid), AttentionBar (full-width warning, hides when clear), LiveSessionsCard (drawer with tool timeline + follow-up). Wire them into routes/index.tsx ABOVE the panel grid; remove OPNL-01/03/04 from COMMAND_SLOTS so they don't double-render. Plan 06-03 (next wave) fills the analytical grid afterward.

Purpose: These four panels are the operator's "is anything on fire right now?" view. Health pills + KPI tiles + Attention bar + Live sessions = the at-a-glance command-center moment. Serialized with 06-03 (Wave 3) because both plans append to `frontend/src/components/panels/index.ts` (panels barrel), `frontend/src/styles.css`, and `frontend/src/routes/index.tsx` — per Phase 5 file-ownership rule, multiple writers to a shared barrel/CSS file must run sequentially.

Output: 4 panel files + barrel + updated routes/index.tsx + ~16 new tests.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-observability-activity-panels/06-RESEARCH.md
@.planning/phases/06-observability-activity-panels/06-01-PLAN.md

# Backend response shapes consumed:
@backend/cmc/api/schemas/system.py
@backend/cmc/api/schemas/sessions.py

# Phase 5 primitives composed:
@frontend/src/components/ui/index.ts
@frontend/src/components/ui/Sheet.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SystemHealthStrip + KpiRow + AttentionBar (3 panels)</name>
  <files>frontend/src/components/panels/SystemHealthStrip.tsx, frontend/src/components/panels/KpiRow.tsx, frontend/src/components/panels/AttentionBar.tsx, frontend/src/components/panels/index.ts, frontend/src/components/panels/__tests__/SystemHealthStrip.test.tsx, frontend/src/components/panels/__tests__/KpiRow.test.tsx, frontend/src/components/panels/__tests__/AttentionBar.test.tsx, frontend/src/styles.css</files>
  <action>
    Step 1 — `frontend/src/components/panels/SystemHealthStrip.tsx` (OPNL-01):
    Use `useSystemHealth()`. Render via `<PanelCard reqId="OPNL-01" title="System Health" empty={{ dataNoun: 'health metrics', when: () => false }} query={q}>` with no empty state (system_health always returns shape). Inside the children render-prop:
    - Compute pills: server status pill from `data.status` (ok/error/stale via mapping). One pill per daemon in `data.daemon_ages` (key as label; pill state = `ok` if `age_seconds < 90`, `stale` if `< 300`, `error` if `>= 300`). One pill for OTEL (last_otel_event_age_seconds — `ok` if < 60, else `stale`).
    - Render uptime as humanized duration (use formatRelative from Phase 5 if appropriate, or local helper to format seconds → `1h 23m`). Render memory_rss_mb with one decimal. Render tzname as a footnote in `--cmc-text-subtle`.
    - Layout: `<div className="cmc-system-health-strip">`. Pills + stats in a horizontal flex row; wraps on narrow widths.

    Step 2 — `frontend/src/components/panels/KpiRow.tsx` (OPNL-02):
    Use `useSummary()`. Render via `<PanelCard reqId="OPNL-02" title="Today" trailing={<RelativeTime ts={data.date} />} query={q} empty={{ dataNoun: 'today summary' }}>`. Inside children, render `<div className="cmc-kpi-row">` with 4 `<KpiTile>` instances: Sessions (sessions_count), Tokens (sum of all 4 token fields, formatted with thousands separators via Intl.NumberFormat), Tool Calls (tool_call_count), Errors (error_count, with red accent when > 0 — pass via tile prop or wrap in span with `--cmc-status-red`).
    - Empty rule: KpiRow is special-cased — it ALWAYS renders 4 tiles even if numbers are zero. Override `empty.when` to `() => false`. The "Today" date string from /api/summary is shown in CardDescription via `trailing`.

    Step 3 — `frontend/src/components/panels/AttentionBar.tsx` (OPNL-03):
    Use `useAttention()`. Render `<PanelCard reqId="OPNL-03" title="Attention" hiddenWhenEmpty query={q} empty={{ dataNoun: 'attention items', when: (d) => d.items.length === 0 && d.stuck_sessions === 0 && d.stale_dispatcher_seconds === null }}>`.
    - PanelCard's `hiddenWhenEmpty: true` short-circuits to `null` when the empty.when predicate is true — bar disappears.
    - In the render-prop, render a horizontal warning bar: badge "Stuck sessions" + count (when > 0); badge "Dispatcher stale {Xs}" when stale_dispatcher_seconds != null; one pill per `items[i]` showing kind/severity/count/detail.
    - DO NOT render `pending_decisions` / `failed_tasks` (Phase 7 territory; STATE.md says they're stable zero today). Document via inline comment `// Phase 7 will populate pending_decisions / failed_tasks; AttentionBar v1 ignores them.`.

    Step 4 — Append CSS classes to `frontend/src/styles.css`: `.cmc-system-health-strip` (flex row + gap + wrap), `.cmc-system-health-strip__pills` (flex row gap-sm), `.cmc-system-health-strip__stats` (flex row gap-md, mono font), `.cmc-kpi-row` (display grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap-md; max width matches container), `.cmc-attention-bar` (flex row; bg `var(--cmc-status-amber-bg)` or similar token; uses Badge primitives inside). Tokens only — no inline hex.

    Step 5 — Create `frontend/src/components/panels/index.ts` barrel:
    ```ts
    export { SystemHealthStrip } from './SystemHealthStrip'
    export { KpiRow } from './KpiRow'
    export { AttentionBar } from './AttentionBar'
    export { LiveSessionsCard } from './LiveSessionsCard'  // shipped in task 2
    ```
    (LiveSessionsCard re-export added now even though file lands in task 2 — single file write avoids re-edit.)

    Step 6 — Tests for the 3 panels at `__tests__/{Panel}.test.tsx`. Pattern: pre-seed TanStack Query cache via `queryClient.setQueryData(qk.systemHealth(), fixture)` in test setup, render the panel inside a fresh `<QueryClientProvider>`, assert content. Each test file covers:
    - Skeleton render when no cached data (queryClient empty)
    - Rendered state with realistic fixture
    - For SystemHealthStrip: stale daemon → renders `stale` StatePill
    - For KpiRow: error_count > 0 → red emphasis applied
    - For AttentionBar: empty (zero items, zero stuck, null staleness) → component returns null (assert no card heading rendered)

    Test harness pattern: a small `renderWithQueryClient(node, seedFn)` helper inside the test file (or under `frontend/src/test/utils.tsx` if reused — extending the Phase 5 utils with a `withQueryClient` wrapper is acceptable). Mock global `fetch` to a no-op resolved promise to prevent the hooks' refetchInterval from firing real requests during test.
  </action>
  <verify>
    ```bash
    cd frontend && grep -q "OPNL-01" src/components/panels/SystemHealthStrip.tsx && echo "OPNL-01 wired"
    cd frontend && grep -q "OPNL-02" src/components/panels/KpiRow.tsx && echo "OPNL-02 wired"
    cd frontend && grep -q "hiddenWhenEmpty" src/components/panels/AttentionBar.tsx && echo "AttentionBar hides when empty"
    cd frontend && npm run typecheck
    cd frontend && npm run test -- --run src/components/panels/__tests__
    ```
  </verify>
  <done>
    3 panels render correctly, hiddenWhenEmpty contract honored on AttentionBar, ~12 new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: LiveSessionsCard with Sheet drawer + follow-up mutation</name>
  <files>frontend/src/components/panels/LiveSessionsCard.tsx, frontend/src/components/panels/__tests__/LiveSessionsCard.test.tsx, frontend/src/routes/index.tsx, frontend/src/styles.css</files>
  <action>
    Step 1 — `frontend/src/components/panels/LiveSessionsCard.tsx` (OPNL-04):
    Component shape:
    - State: `const [activeSid, setActiveSid] = useState<string | null>(null)`.
    - `useLiveSessions()` for the list. Note: response is a bare `LiveSessionItem[]` (no `items:` envelope) — adjust empty.when accordingly: `(d) => Array.isArray(d) && d.length === 0`.
    - `useSessionDetails(activeSid)` (only fetches when activeSid !== null).
    - `useFollowUpMessage()` mutation.
    - PanelCard wraps the list. Each row renders: session id (last 8 chars, mono), model, RelativeTime since started_at, current_tool, StatePill. Click row → `setActiveSid(sid)`.
    - Render `<Sheet open={Boolean(activeSid)} onOpenChange={(o) => !o && setActiveSid(null)} title="Session details" description="Tool timeline + follow-up message">` (matches Phase 5 Sheet API). Inside:
      - When `details.isPending` → skeleton.
      - Header: full session id (mono), model, started_at + ended_at (RelativeTime), state pill.
      - Tool timeline: list of `details.tools` rows (already ordered ASC); each row shows tool_name, ts (RelativeTime), status, latency_ms.
      - Follow-up form (only when session is still live, i.e., `details.session.ended_at == null`): `<textarea>` bound to local state + `<Button type="submit" disabled={mutation.isPending || !text.trim()}>Send</Button>`. On submit: `mutation.mutate({ sid: activeSid!, message: text })`; reset textarea on success; show inline `<ErrorState>` on `mutation.isError` with the error message body verbatim (backend emits `{error: detail}` — display literal).
      - When session ended (ended_at != null), display a notice "This session has ended; follow-ups disabled." instead of the form.

    Step 2 — Update `frontend/src/routes/index.tsx`:
    - Import the panels barrel: `import { SystemHealthStrip, KpiRow, AttentionBar, LiveSessionsCard } from '../components/panels'`.
    - Render order in `<section className="cmc-page">`:
      1. Page header (existing).
      2. `<SystemHealthStrip />`
      3. `<KpiRow />`
      4. `<AttentionBar />`
      5. `<LiveSessionsCard />`
      6. `<PlaceholderCardGrid slots={COMMAND_SLOTS} />` (existing, but with OPNL-01/03/04 removed from the slot array).
    - Remove `OPNL-01`, `OPNL-03`, `OPNL-04` entries from `COMMAND_SLOTS`. The remaining slots stay as placeholders for plan 06-03 to replace.

    Step 3 — Append CSS to `frontend/src/styles.css`: `.cmc-live-sessions-list` (vertical flex), `.cmc-live-sessions-list__row` (flex row with hover bg), `.cmc-live-sessions-list__row[data-active]` accent, `.cmc-session-drawer` (Sheet content padding + section gaps), `.cmc-tool-timeline` + `.cmc-tool-timeline__row`, `.cmc-followup-form`. Tokens only.

    Step 4 — `frontend/src/components/panels/__tests__/LiveSessionsCard.test.tsx`:
    - Renders 2 sessions from fixture; each row shows id, model, current_tool.
    - Clicking a row opens the Sheet (assert role="dialog" appears).
    - With `details` fixture seeded, drawer shows tool timeline rows.
    - Follow-up: type into textarea, click Send → mutation invoked with correct sid + message.
    - When `details.session.ended_at != null` → form is replaced by "session has ended" notice.

    Mock the mutation by stubbing `fetch` global to return 202 + `{queued, session_id, queue_path}`. Mock pattern: `vi.spyOn(globalThis, 'fetch').mockResolvedValue(new Response(JSON.stringify({ queued: true, session_id: 'abc', queue_path: '/.tmp/...' }), { status: 202 }))`.

    Step 5 — Run full suite:
    ```bash
    cd frontend && npm run typecheck && npm run test -- --run && npm run build
    ```
    Expect ~16 new tests (4 panel files × ~4 cases) on top of ~85 from Plan 06-01 = ~101 total.
  </action>
  <verify>
    ```bash
    cd frontend && grep -q "useFollowUpMessage" src/components/panels/LiveSessionsCard.tsx
    cd frontend && grep -q "Sheet" src/components/panels/LiveSessionsCard.tsx
    cd frontend && grep -q "SystemHealthStrip\|KpiRow\|AttentionBar\|LiveSessionsCard" src/routes/index.tsx
    cd frontend && grep -c "OPNL-01\|OPNL-03\|OPNL-04" src/routes/index.tsx
    # Should be 0 since OPNL-01/03/04 entries removed from COMMAND_SLOTS (titles still appear inside panel files via reqId prop, but routes/index.tsx no longer lists them as slots)
    cd frontend && npm run typecheck
    cd frontend && npm run test -- --run
    cd frontend && npm run build
    ```
  </verify>
  <done>
    LiveSessionsCard with Sheet drawer + follow-up mutation works end-to-end (against mock fetch); routes/index.tsx renders all 4 top panels above the placeholder grid; OPNL-01/03/04 placeholders gone; ~16 new tests pass.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` exits 0
- `cd frontend && npm run test -- --run` shows ~101+ tests passing (85 from Plan 06-01 + ~16 new)
- `cd frontend && npm run build` produces dist/ with no errors
- Visit `/` in dev server (manual smoke if doing `npm run dev`): see system health pills + 4 KPI tiles + (no AttentionBar when clean) + LiveSessions list above the placeholder grid; clicking a live session opens the drawer
- AttentionBar `hiddenWhenEmpty` returns null when empty — `routes/index.tsx` doesn't render an empty card; the layout flows seamlessly
</verification>

<success_criteria>
1. OPNL-01 SystemHealthStrip renders 5s-refreshing pills + uptime/memory/otel-age/tzname (Phase 6 success criterion 1: "Page load shows system health strip…")
2. OPNL-02 KpiRow renders 4 KpiTile tiles 15s-refreshing above the panel grid (Phase 6 success criterion 1: "…KPI row…")
3. OPNL-03 AttentionBar renders or hides correctly based on attention payload — `hiddenWhenEmpty` honored (Phase 6 success criterion 1: "…and attention bar with live data (or meaningful empty states)")
4. OPNL-04 LiveSessionsCard with drawer + follow-up POST works end-to-end (Phase 6 success criterion 2: "Live sessions card shows active sessions with tool-call timeline drawer and follow-up messaging")
5. Routes/index.tsx no longer renders OPNL-01/03/04 placeholders — those slots are now live panels
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability-activity-panels/06-02-SUMMARY.md` documenting:
- Final test count for the plan
- Confirm AttentionBar `return null` behavior verified by integration test
- Confirm /api/sessions/live bare-list shape handled (no `items:` envelope)
- The Phase 6 Plan 03 entry contract: COMMAND_SLOTS in routes/index.tsx still lists OPNL-05..15 + ACTV-related slots not yet replaced; Plan 06-03 replaces all of OPNL-05..15 and DELETES the PlaceholderCardGrid usage on / when its last slot is filled
- Any UI-SPEC visual inconsistencies observed during dev-server smoke (pill colors, KPI tile typography, drawer width)
</output>
