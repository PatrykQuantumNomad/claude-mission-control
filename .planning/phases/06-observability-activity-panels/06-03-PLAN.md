---
phase: 06-observability-activity-panels
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - frontend/src/components/panels/TokenUsageCard.tsx
  - frontend/src/components/panels/CacheEfficiencyCard.tsx
  - frontend/src/components/panels/SessionOutcomesCard.tsx
  - frontend/src/components/panels/ToolLatencyCard.tsx
  - frontend/src/components/panels/HookActivityCard.tsx
  - frontend/src/components/panels/ProjectBreakdownCard.tsx
  - frontend/src/components/panels/AgentFanoutCard.tsx
  - frontend/src/components/panels/EditAcceptanceCard.tsx
  - frontend/src/components/panels/ProductivityCard.tsx
  - frontend/src/components/panels/PressurePanel.tsx
  - frontend/src/components/panels/McpPanel.tsx
  - frontend/src/components/panels/index.ts
  - frontend/src/components/panels/__tests__/TokenUsageCard.test.tsx
  - frontend/src/components/panels/__tests__/CacheEfficiencyCard.test.tsx
  - frontend/src/components/panels/__tests__/SessionOutcomesCard.test.tsx
  - frontend/src/components/panels/__tests__/ToolLatencyCard.test.tsx
  - frontend/src/components/panels/__tests__/HookActivityCard.test.tsx
  - frontend/src/components/panels/__tests__/ProjectBreakdownCard.test.tsx
  - frontend/src/components/panels/__tests__/AgentFanoutCard.test.tsx
  - frontend/src/components/panels/__tests__/EditAcceptanceCard.test.tsx
  - frontend/src/components/panels/__tests__/ProductivityCard.test.tsx
  - frontend/src/components/panels/__tests__/PressurePanel.test.tsx
  - frontend/src/components/panels/__tests__/McpPanel.test.tsx
  - frontend/src/routes/index.tsx
  - frontend/src/styles.css
autonomous: true
requirement_ids: [OPNL-05, OPNL-06, OPNL-07, OPNL-08, OPNL-09, OPNL-10, OPNL-11, OPNL-12, OPNL-13, OPNL-14, OPNL-15]

must_haves:
  truths:
    - "OPNL-05 TokenUsageCard renders stacked daily bars by **token type** (input/output/cache_read/cache_create), aggregated from the per-(day, model, source) backend response; model/source-axis stacking is a v2 affordance per 06-RESEARCH. Renders via Recharts, with today/7d/30d RangeToggle; refreshes every 60s"
    - "OPNL-06 CacheEfficiencyCard shows hit_rate as a big number (×100 displayed as %), a sparkline of trend hit_rate, a 70% target ReferenceLine, and a Low-sample Badge when low_sample is true"
    - "OPNL-07 SessionOutcomesCard renders mutually-exclusive stacked bars (errored/rate_limited/truncated/unfinished/ok) summing to day total; segments use distinct token colors"
    - "OPNL-08 ToolLatencyCard renders sortable DataTable with columns tool_name/call_count/p50/p95/max/error_rate; row Badge variant=danger when p95>5000 OR error_rate>0.05; variant=success when call_count>=10 AND p95<1000 AND error_rate=0"
    - "OPNL-09 HookActivityCard renders stacked bars by hook_name + paired_duration_ms_p50; EmptyState when total_fires=0 (per RESEARCH empty-state taxonomy)"
    - "OPNL-10 ProjectBreakdownCard uses backend `display_path` directly (NEVER re-implements the home-dir regex client-side); renders DataTable with sessions, tokens_effective, tool_calls, pct_of_total bar; range toggle includes `all`"
    - "OPNL-11 AgentFanoutCard renders rows with title (or fallback `session_id.slice(0,8) + '…'` in --cmc-text-subtle when title missing) + agent_calls + RelativeTime"
    - "OPNL-12 EditAcceptanceCard always renders 4 rows (Edit/MultiEdit/Write/NotebookEdit) with accept_rate as % and Badge variant=warning when low_sample is true"
    - "OPNL-13 ProductivityCard renders StatList with commits/PRs/lines_added/lines_removed; EmptyState when total === 0"
    - "OPNL-14 PressurePanel renders api_retries_exhausted + compaction_count + a CollapsibleSection with the last 10 api_errors (ts + RelativeTime + session_id + message)"
    - "OPNL-15 McpPanel renders server list with name/call_count/error_count/p50/p95/max/source_priority; expanding a row (CollapsibleSection) lazy-fetches /api/mcp/{server}/tools and renders per-tool drill-down with slow/fast tags (slow if p95>5000; fast if call_count>=10 AND p95<500 AND error_count===0); shipped at components/panels/McpPanel.tsx so Phase 7 SKLP-01 can import it"
    - "All 11 cards compose <PanelCard> from plan 06-01 — no card re-implements loading/empty/error/skeleton JSX"
    - "All 11 cards use the lib/queries.ts hooks — no card calls api.* directly or inlines a refetchInterval"
    - "Recharts SVG fills use `var(--cmc-*)` tokens; no inline hex anywhere"
    - "routes/index.tsx replaces OPNL-05..15 placeholder slots with the live cards; the COMMAND_SLOTS array is empty after this plan and PlaceholderCardGrid usage on / is REMOVED"
    - "All previous frontend tests still pass; ~30 new tests added (11 panels × ~2-3 cases)"
  artifacts:
    - path: "frontend/src/components/panels/TokenUsageCard.tsx"
      provides: "OPNL-05 — stacked daily bars w/ RangeToggle"
      exports: ["TokenUsageCard"]
    - path: "frontend/src/components/panels/CacheEfficiencyCard.tsx"
      provides: "OPNL-06 — hit-rate big number + sparkline + 70% target line + low-sample badge"
      exports: ["CacheEfficiencyCard"]
    - path: "frontend/src/components/panels/SessionOutcomesCard.tsx"
      provides: "OPNL-07 — stacked daily bars (mutually exclusive buckets)"
      exports: ["SessionOutcomesCard"]
    - path: "frontend/src/components/panels/ToolLatencyCard.tsx"
      provides: "OPNL-08 — sortable DataTable per-tool latency w/ flag rules"
      exports: ["ToolLatencyCard"]
    - path: "frontend/src/components/panels/HookActivityCard.tsx"
      provides: "OPNL-09 — stacked bars + p50 paired duration + zero-aggregate empty"
      exports: ["HookActivityCard"]
    - path: "frontend/src/components/panels/ProjectBreakdownCard.tsx"
      provides: "OPNL-10 — DataTable w/ display_path + tokens_effective + pct_of_total bar"
      exports: ["ProjectBreakdownCard"]
    - path: "frontend/src/components/panels/AgentFanoutCard.tsx"
      provides: "OPNL-11 — sessions running Agent tool w/ title-or-id fallback"
      exports: ["AgentFanoutCard"]
    - path: "frontend/src/components/panels/EditAcceptanceCard.tsx"
      provides: "OPNL-12 — fixed 4-row table w/ accept_rate + low-sample warning"
      exports: ["EditAcceptanceCard"]
    - path: "frontend/src/components/panels/ProductivityCard.tsx"
      provides: "OPNL-13 — StatList w/ commits/PRs/lines + zero-aggregate empty"
      exports: ["ProductivityCard"]
    - path: "frontend/src/components/panels/PressurePanel.tsx"
      provides: "OPNL-14 — retry-exhaustion + compaction + last-10 api_errors collapsible"
      exports: ["PressurePanel"]
    - path: "frontend/src/components/panels/McpPanel.tsx"
      provides: "OPNL-15 — server list w/ expandable per-tool drill-down + slow/fast tags. Phase 7 SKLP-01 reuses this component"
      exports: ["McpPanel"]
    - path: "frontend/src/routes/index.tsx"
      provides: "Command page now renders 11 live analytical cards in a grid container; PlaceholderCardGrid removed"
      contains: "TokenUsageCard"
  key_links:
    - from: "frontend/src/components/panels/TokenUsageCard.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useTokens(range) hook"
      pattern: "useTokens"
    - from: "frontend/src/components/panels/McpPanel.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useMcpServers + useMcpTools(server, expanded)"
      pattern: "useMcpServers|useMcpTools"
    - from: "frontend/src/components/panels/PressurePanel.tsx"
      to: "frontend/src/components/ui/CollapsibleSection.tsx"
      via: "<CollapsibleSection> wraps the recent api_errors list"
      pattern: "CollapsibleSection"
    - from: "frontend/src/components/panels/ProjectBreakdownCard.tsx"
      to: "backend display_path field"
      via: "Reads `data.items[i].display_path` directly — no client regex"
      pattern: "display_path"
    - from: "frontend/src/routes/index.tsx"
      to: "frontend/src/components/panels"
      via: "Imports all 11 OPNL-05..15 cards via barrel"
      pattern: "components/panels"
---

<objective>
Land the 11 analytical cards on the Command page (OPNL-05 through OPNL-15) — every panel composes the Wave 1 PanelCard shell + a TanStack Query hook from queries.ts. Wire the cards into routes/index.tsx by replacing the OPNL-05..15 placeholder slots and DELETING the PlaceholderCardGrid usage on / (its last consumer for / is gone). Plan 06-02 (prior wave) shipped the top strip.

Purpose: This is the bulk of the Command page. Token usage / cache efficiency / outcomes / latency / hooks / project breakdown / agent fanout / edit acceptance / productivity / pressure / MCP — every observability dimension that doesn't fit the top strip lives here. Each card is small (~30-50 lines + a `*.utils.ts` pure helper for any data shaping). Serialized after 06-02 because both plans append to `frontend/src/components/panels/index.ts`, `frontend/src/styles.css`, and `frontend/src/routes/index.tsx`.

Output: 11 panel files + 11 test files + barrel updates + routes/index.tsx replacement of the placeholder grid + ~30 new tests.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-observability-activity-panels/06-RESEARCH.md
@.planning/phases/06-observability-activity-panels/06-01-PLAN.md

# Backend response shapes consumed:
@backend/cmc/api/schemas/observability.py
@backend/cmc/api/schemas/mcp.py
@backend/cmc/api/routes/observability.py

# Phase 5 + Wave 1 primitives composed:
@frontend/src/components/ui/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 5 chart cards (TokenUsage, CacheEfficiency, SessionOutcomes, HookActivity) + ProjectBreakdown table</name>
  <files>frontend/src/components/panels/TokenUsageCard.tsx, frontend/src/components/panels/CacheEfficiencyCard.tsx, frontend/src/components/panels/SessionOutcomesCard.tsx, frontend/src/components/panels/HookActivityCard.tsx, frontend/src/components/panels/ProjectBreakdownCard.tsx, frontend/src/components/panels/__tests__/TokenUsageCard.test.tsx, frontend/src/components/panels/__tests__/CacheEfficiencyCard.test.tsx, frontend/src/components/panels/__tests__/SessionOutcomesCard.test.tsx, frontend/src/components/panels/__tests__/HookActivityCard.test.tsx, frontend/src/components/panels/__tests__/ProjectBreakdownCard.test.tsx, frontend/src/styles.css</files>
  <action>
    Step 1 — `TokenUsageCard.tsx` (OPNL-05):
    - Local state: `range: Range = '7d'`.
    - `useTokens(range)`. Render PanelCard with reqId="OPNL-05", title="Token Usage", trailing=<RangeToggle value={range} onChange={setRange} options={['today','7d','30d']} persistKey="token-usage" />, empty.dataNoun="token usage data", empty.when=(d)=>d.items.length===0.
    - Helper `groupTokensByDay(items)` (pure fn in `TokenUsageCard.utils.ts`): map of day → {input, output, cache_read, cache_create}.
    - Render: `<ResponsiveContainer width="100%" height={220}><BarChart><XAxis dataKey="day" stroke="var(--cmc-text-subtle)"/><YAxis stroke="var(--cmc-text-subtle)"/><Tooltip ...token-styled/><Bar dataKey="input" stackId="t" fill="var(--cmc-accent-blue)"/><Bar dataKey="output" stackId="t" fill="var(--cmc-accent-purple)"/><Bar dataKey="cache_read" stackId="t" fill="var(--cmc-status-cyan)"/><Bar dataKey="cache_create" stackId="t" fill="var(--cmc-status-green)"/></BarChart></ResponsiveContainer>`. Wrap chart in `<figure aria-label="Daily token breakdown">` and include a screen-reader-only `.cmc-sr-only` table fallback for accessibility (research §risk register).

    Step 2 — `CacheEfficiencyCard.tsx` (OPNL-06):
    - Local state: range. `useCache(range)`.
    - PanelCard reqId="OPNL-06" title="Cache Efficiency". Render: big number `(hit_rate * 100).toFixed(1)%` with KpiTile (or styled span). Below: small sparkline using Recharts `<LineChart>` of `trend[i].hit_rate` with a `<ReferenceLine y={0.7} strokeDasharray="3 3" stroke="var(--cmc-status-amber)" />`. If overall `low_sample` true OR any `trend[i].low_sample === true` is in current bucket, render `<Badge variant="warning">Low sample</Badge>` next to the big number.

    Step 3 — `SessionOutcomesCard.tsx` (OPNL-07):
    - Local state: range. `useOutcomes(range)`.
    - PanelCard reqId="OPNL-07" title="Session Outcomes". Stacked bars: dataKey errored/rate_limited/truncated/unfinished/ok, stackId='o'. Fills: errored=`var(--cmc-status-red)`, rate_limited=`var(--cmc-status-orange)` (or amber), truncated=`var(--cmc-status-amber)`, unfinished=`var(--cmc-text-dim)`, ok=`var(--cmc-status-green)`. Day label in mono on XAxis.

    Step 4 — `HookActivityCard.tsx` (OPNL-09):
    - Local state: range. `useHooks(range)`.
    - PanelCard reqId="OPNL-09" title="Hook Activity". empty.when=(d)=>d.total_fires===0 (zero-aggregate empty per RESEARCH).
    - Stacked bars by hook_name. Helper `pivotHooksByDay(items)`. Color hooks by index from a token palette.
    - Below chart: small list "p50 paired duration: PreToolUse 124ms / PostToolUse 87ms" derived from items.

    Step 5 — `ProjectBreakdownCard.tsx` (OPNL-10):
    - Local state: `range: Range | 'all' = '30d'`. `useByProject(range)`.
    - PanelCard reqId="OPNL-10" title="Projects". RangeToggle options={['today','7d','30d','all']}.
    - Render `<DataTable>` columns: display_path (mono, ellipsis), sessions, tokens_effective (Intl.NumberFormat), tool_calls, pct_of_total (rendered as a thin horizontal bar `<div className="cmc-pct-bar" style={{width: pct + '%'}}/>` in mono percent).
    - **Comment in code: `// Backend already supplies display_path (home-dir stripped) — never re-implement the regex client-side. STATE.md L201.`**

    Step 6 — Tests for the 5 panels (one per file):
    - Each test pre-seeds queryClient.setQueryData(qk.X(range), fixture) and renders the panel inside a fresh QueryClientProvider; assert title + reqId render; assert chart SVG exists (`container.querySelector('svg')` non-null) for chart cards; for ProjectBreakdownCard assert one row's display_path appears verbatim.
    - HookActivityCard: also test the zero-aggregate empty state (fixture with `total_fires: 0` → EmptyState rendered, no chart).

    Step 7 — Append CSS classes to styles.css for `.cmc-pct-bar` (background var(--cmc-surface-3); inner via ::before pseudo or inline), `.cmc-chart-fig` figure wrapper, `.cmc-sr-only` (visually-hidden recipe — left/top: -10000px style).

    Step 8 — Append exports to `panels/index.ts`:
    ```ts
    export { TokenUsageCard } from './TokenUsageCard'
    export { CacheEfficiencyCard } from './CacheEfficiencyCard'
    export { SessionOutcomesCard } from './SessionOutcomesCard'
    export { HookActivityCard } from './HookActivityCard'
    export { ProjectBreakdownCard } from './ProjectBreakdownCard'
    ```

    Step 9 — Run frontend suite:
    ```bash
    cd frontend && npm run typecheck && npm run test -- --run
    ```
  </action>
  <verify>
    ```bash
    cd frontend && grep -q "Bar dataKey=\"input\"" src/components/panels/TokenUsageCard.tsx
    cd frontend && grep -q "ReferenceLine" src/components/panels/CacheEfficiencyCard.tsx
    cd frontend && grep -q "display_path" src/components/panels/ProjectBreakdownCard.tsx
    cd frontend && grep -q "var(--cmc-" src/components/panels/SessionOutcomesCard.tsx
    cd frontend && grep -q "total_fires" src/components/panels/HookActivityCard.tsx
    cd frontend && npm run typecheck
    cd frontend && npm run test -- --run src/components/panels/__tests__/TokenUsageCard.test.tsx src/components/panels/__tests__/CacheEfficiencyCard.test.tsx src/components/panels/__tests__/SessionOutcomesCard.test.tsx src/components/panels/__tests__/HookActivityCard.test.tsx src/components/panels/__tests__/ProjectBreakdownCard.test.tsx
    ```
  </verify>
  <done>
    5 panels render correctly; chart cards produce SVG output in happy-dom; ProjectBreakdownCard reads display_path verbatim; ~12 new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: 5 stat/list cards (ToolLatency, AgentFanout, EditAcceptance, Productivity, Pressure) + McpPanel + routes/index.tsx grid wiring</name>
  <files>frontend/src/components/panels/ToolLatencyCard.tsx, frontend/src/components/panels/AgentFanoutCard.tsx, frontend/src/components/panels/EditAcceptanceCard.tsx, frontend/src/components/panels/ProductivityCard.tsx, frontend/src/components/panels/PressurePanel.tsx, frontend/src/components/panels/McpPanel.tsx, frontend/src/components/panels/index.ts, frontend/src/components/panels/__tests__/ToolLatencyCard.test.tsx, frontend/src/components/panels/__tests__/AgentFanoutCard.test.tsx, frontend/src/components/panels/__tests__/EditAcceptanceCard.test.tsx, frontend/src/components/panels/__tests__/ProductivityCard.test.tsx, frontend/src/components/panels/__tests__/PressurePanel.test.tsx, frontend/src/components/panels/__tests__/McpPanel.test.tsx, frontend/src/routes/index.tsx, frontend/src/styles.css</files>
  <action>
    Step 1 — `ToolLatencyCard.tsx` (OPNL-08):
    - Local state: `range`, `sort = {col: 'p95_ms', dir: 'desc' as const}`.
    - `useLatency(range)`. PanelCard reqId="OPNL-08" title="Tool Latency".
    - DataTable columns: tool_name, call_count, p50_ms, p95_ms, max_ms, error_rate (×100 with %). Cell-level Badge: row gets `variant="danger"` icon when `p95_ms > 5000 || error_rate > 0.05`; `variant="success"` icon when `call_count >= 10 && p95_ms < 1000 && error_rate === 0`. Use a small `<FlagBadge tool={row} />` component inside the file or render inline.

    Step 2 — `AgentFanoutCard.tsx` (OPNL-11):
    - Local state: range. `useFanout(range)`. PanelCard reqId="OPNL-11" title="Agent Fanout".
    - Render rows: title (or fallback `<span style={{ color: 'var(--cmc-text-subtle)' }}>{session_id.slice(0,8)}…</span>`) + agent_calls (mono number) + RelativeTime since started_at.

    Step 3 — `EditAcceptanceCard.tsx` (OPNL-12):
    - Local state: range. `useEdits(range)`. PanelCard reqId="OPNL-12" title="Edit Acceptance".
    - Render exactly 4 fixed rows (Edit/MultiEdit/Write/NotebookEdit) — if backend returns fewer, fill missing with placeholder `0/0 (no data)`. For each row: tool_name, accepted, rejected, accept_rate (×100, 1 decimal %), Badge variant=warning when low_sample.

    Step 4 — `ProductivityCard.tsx` (OPNL-13):
    - Local state: range. `useProductivity(range)`. PanelCard reqId="OPNL-13" title="Productivity".
    - empty.when=(d)=>d.commits + d.pull_requests + d.lines_added + d.lines_removed === 0.
    - Render `<StatList items={[ { icon: <GitCommit/>, label: 'Commits', value: d.commits }, { icon: <GitPullRequest/>, label: 'Pull Requests', value: d.pull_requests }, { icon: <Plus/>, label: 'Lines added', value: d.lines_added, trend: 'up' }, { icon: <Minus/>, label: 'Lines removed', value: d.lines_removed, trend: 'down' } ]} />`. Icons from lucide-react.

    Step 5 — `PressurePanel.tsx` (OPNL-14):
    - `usePressure()`. PanelCard reqId="OPNL-14" title="Pressure".
    - StatList for 2 metrics (api_retries_exhausted with --cmc-status-red emphasis when >0; compaction_count). CollapsibleSection (Phase 5 primitive) labeled "Recent API errors ({count})" containing a list of recent_api_errors rows: ts (RelativeTime), session_id (mono first 8 chars), message (in --cmc-text-dim, monospaced). Default collapsed; persistence key `cmc.collapsible.pressure-recent-errors`.

    Step 6 — `McpPanel.tsx` (OPNL-15):
    - `useMcpServers()`. PanelCard reqId="OPNL-15" title="MCP Servers".
    - Each row: server_name (mono), call_count, error_count, p50_ms, p95_ms, max_ms, source_priority badge. Slow tag Badge variant=danger when `p95 > 5000`; Fast tag Badge variant=success when `call_count >= 10 && p95 < 500 && error_count === 0`.
    - Each row wraps in `<CollapsibleSection title="{server_name}" persistKey={`mcp-server-${server_name}`}>` whose body renders a child `<McpToolsTable serverName={server_name} expanded={open} />`. The child uses `useMcpTools(server, expanded)` with `enabled: expanded` so tools fetch only when expanded. Tools table: tool_name, call_count, error_count, p50, p95, max, source_priority. Apply slow/fast tags per tool too.
    - Path-traversal hardening: `encodeURIComponent(server_name)` is already inside `api.mcpServerTools` (verified in Plan 06-01 task 1). No additional sanitization needed in panel.
    - File location at `components/panels/McpPanel.tsx` (NOT in routes) so Phase 7 SKLP-01 imports the same component.

    Step 7 — Update `frontend/src/routes/index.tsx`:
    - Import all 11 OPNL-05..15 cards from `'../components/panels'`.
    - **Delete** the import + usage of `PlaceholderCardGrid` and the `COMMAND_SLOTS` array.
    - Render order (replacing the placeholder grid):
      1. Page header (existing).
      2. SystemHealthStrip / KpiRow / AttentionBar / LiveSessionsCard (from plan 06-02).
      3. `<div className="cmc-card-grid">` containing all 11 OPNL-05..15 cards in array order, each as a child.
    - The `.cmc-card-grid` class already exists in styles.css (Plan 05-04); reuse it.

    Step 8 — Append the 6 panel exports to `panels/index.ts`:
    ```ts
    export { ToolLatencyCard } from './ToolLatencyCard'
    export { AgentFanoutCard } from './AgentFanoutCard'
    export { EditAcceptanceCard } from './EditAcceptanceCard'
    export { ProductivityCard } from './ProductivityCard'
    export { PressurePanel } from './PressurePanel'
    export { McpPanel } from './McpPanel'
    ```

    Step 9 — Tests for the 6 panels. Pattern matches Task 1: pre-seed queryClient, render, assert reqId + title + key data presence. Specific assertions:
    - ToolLatencyCard: row with p95=6000 renders danger badge; row with call_count=20+p95=500+error_rate=0 renders success badge.
    - AgentFanoutCard: row with `title: null` renders the fallback `<span>` with subtle color + truncated id.
    - EditAcceptanceCard: low_sample row renders warning badge.
    - ProductivityCard: zero-aggregate fixture → EmptyState rendered.
    - PressurePanel: api_retries_exhausted=5 → red emphasis applied; CollapsibleSection toggles open on click.
    - McpPanel: server row with p95=8000 renders danger; expanding a server fetches tools (mock fetch returns 2 tools; assert tool rows rendered after click). Mock useMcpTools by seeding queryClient.setQueryData(qk.mcpTools('serverA'), fixture) and asserting the rows appear once expanded.

    Step 10 — Append any missing CSS classes (e.g., `.cmc-mcp-row`, `.cmc-mcp-tools-table`, `.cmc-flag-badge`) — tokens only.

    Step 11 — Run full suite + build:
    ```bash
    cd frontend && npm run typecheck && npm run test -- --run && npm run build
    ```
    Expect ~131 tests after this plan (101 from 06-02 path + ~30 new from this plan, when run after 06-02; if running independently, ~115 from 06-01 + 30 = 145).

    Step 12 — Visual smoke (developer): `cd frontend && npm run dev`. Visit `/`. Confirm:
    - 11 cards in the grid below the top strip
    - Token usage stacked bars rendered with correct token colors
    - Cache efficiency big number renders with target line
    - MCP server rows expand to show tools
  </action>
  <verify>
    ```bash
    cd frontend && grep -q "useLatency" src/components/panels/ToolLatencyCard.tsx
    cd frontend && grep -q "fallback" src/components/panels/AgentFanoutCard.tsx
    cd frontend && grep -q "low_sample" src/components/panels/EditAcceptanceCard.tsx
    cd frontend && grep -q "lines_added" src/components/panels/ProductivityCard.tsx
    cd frontend && grep -q "CollapsibleSection" src/components/panels/PressurePanel.tsx
    cd frontend && grep -q "useMcpTools" src/components/panels/McpPanel.tsx
    cd frontend && ! grep -q "PlaceholderCardGrid" src/routes/index.tsx && echo "PlaceholderCardGrid removed from /"
    cd frontend && grep -q "TokenUsageCard\|McpPanel" src/routes/index.tsx
    cd frontend && npm run typecheck
    cd frontend && npm run test -- --run
    cd frontend && npm run build
    ```
  </verify>
  <done>
    All 11 OPNL-05..15 cards rendered correctly; routes/index.tsx no longer references PlaceholderCardGrid; ~30 new tests pass; total Phase 5+6 tests ~131+ green.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` exits 0
- `cd frontend && npm run test -- --run` shows ~131+ tests passing
- `cd frontend && npm run build` produces dist/ with no errors; recharts chunk reasonable size
- `frontend/src/routes/index.tsx` does NOT import PlaceholderCardGrid
- All 11 OPNL-05..15 cards render in the dev server visual smoke
- No card component file imports `api` directly — every fetch goes through `lib/queries.ts` (grep can't enforce; reviewer-checked)
- No inline hex colors in any panel file (`grep -E "#[0-9a-f]{3,6}"` should match nothing in panels/)
</verification>

<success_criteria>
1. OPNL-05 TokenUsageCard renders stacked daily bars + range toggle (Phase 6 success criterion 3)
2. OPNL-06..09 + OPNL-12..14 cards each render with their backend data (Phase 6 success criterion overarching: "all 15 observability panels")
3. OPNL-10 ProjectBreakdownCard uses `display_path` directly (no client-side regex; STATE.md L201 contract preserved)
4. OPNL-15 MCP panel renders server list with expandable per-tool drill-down + slow/fast tags (Phase 6 success criterion 4)
5. PlaceholderCardGrid usage on `/` route deleted; the page now renders all live panels
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability-activity-panels/06-03-SUMMARY.md` documenting:
- Final test count for the plan
- Recharts bundle delta from `npm run build` (compare to Plan 06-01 baseline)
- Confirm no panel re-implements home-dir regex (display_path used as-is)
- Confirm McpPanel file location is `components/panels/McpPanel.tsx` so Phase 7 SKLP-01 can import it without moving
- Visual smoke notes (cards align in grid, chart colors match token palette, MCP expand/collapse animates correctly)
- Any TanStack Query staleTime/refetchInterval tweaks made beyond the queries.ts defaults (none expected; defaults locked in 06-01)
</output>
