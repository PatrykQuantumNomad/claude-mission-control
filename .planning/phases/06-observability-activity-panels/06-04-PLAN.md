---
phase: 06-observability-activity-panels
plan: 04
type: execute
wave: 4
depends_on: [06-01, 06-03]
files_modified:
  - frontend/src/components/panels/ActivityHeatmap.tsx
  - frontend/src/components/panels/ChartsStrip.tsx
  - frontend/src/components/panels/SessionsTable.tsx
  - frontend/src/components/panels/index.ts
  - frontend/src/components/panels/__tests__/ActivityHeatmap.test.tsx
  - frontend/src/components/panels/__tests__/ChartsStrip.test.tsx
  - frontend/src/components/panels/__tests__/SessionsTable.test.tsx
  - frontend/src/routes/activity.tsx
  - frontend/src/styles.css
autonomous: true
requirement_ids: [ACTV-01, ACTV-02, ACTV-06]

must_haves:
  truths:
    - "ACTV-01 ActivityHeatmap renders 30-day GitHub-style daily activity grid using the <HeatmapGrid> primitive; cell color scaled by sessions count; hover tooltip shows day + sessions + tokens_effective; refreshes every 300s"
    - "ACTV-02 ChartsStrip renders 14-day stacked token bars sliced client-side from /api/usage/tokens?range=30d (the backend Literal does not accept '14d' — research §11)"
    - "ACTV-06 SessionsTable renders /api/sessions paginated list with range/source/model filters in header; client-side search filter on session id + project; pagination uses limit/offset; uses keepPreviousData via placeholderData option for snappy page transitions"
    - "ChartsStrip slices the response client-side: takes the last 14 day buckets from the 30d response so the panel works without a backend Literal change"
    - "SessionsTable uses the <DataTable> primitive (sortable, paginated) shipped in Plan 06-01; columns: session_id (mono, truncated), project, model, started_at (RelativeTime), tokens_input, tokens_output"
    - "routes/activity.tsx replaces ACTV-01, ACTV-02, ACTV-06 placeholder slots with these live panels; ACTV-03/04/05 placeholders remain (filled by plan 06-05)"
    - "All previous frontend tests still pass; ~12 new tests added (3 panels × ~4 cases)"
  artifacts:
    - path: "frontend/src/components/panels/ActivityHeatmap.tsx"
      provides: "ACTV-01 — wraps HeatmapGrid primitive + useHeatmap hook; 30-day grid"
      exports: ["ActivityHeatmap"]
    - path: "frontend/src/components/panels/ChartsStrip.tsx"
      provides: "ACTV-02 — 14-day stacked token bars (client-side slice of 30d response)"
      exports: ["ChartsStrip"]
    - path: "frontend/src/components/panels/SessionsTable.tsx"
      provides: "ACTV-06 — DataTable wrapper around /api/sessions w/ filters + pagination + client-side search"
      exports: ["SessionsTable"]
    - path: "frontend/src/routes/activity.tsx"
      provides: "Activity page wires ActivityHeatmap + ChartsStrip + SessionsTable; ACTV-01/02/06 removed from placeholder slots"
      contains: "ActivityHeatmap"
  key_links:
    - from: "frontend/src/components/panels/ActivityHeatmap.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useHeatmap(range)"
      pattern: "useHeatmap"
    - from: "frontend/src/components/panels/ActivityHeatmap.tsx"
      to: "frontend/src/components/ui/HeatmapGrid.tsx"
      via: "Composes the HeatmapGrid primitive"
      pattern: "HeatmapGrid"
    - from: "frontend/src/components/panels/ChartsStrip.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useTokens('30d') + client-side slice of last 14 days"
      pattern: "useTokens"
    - from: "frontend/src/components/panels/SessionsTable.tsx"
      to: "frontend/src/lib/queries.ts"
      via: "useSessionsList({range, source, model, limit, offset})"
      pattern: "useSessionsList"
    - from: "frontend/src/components/panels/SessionsTable.tsx"
      to: "frontend/src/components/ui/DataTable.tsx"
      via: "DataTable primitive renders rows + sort + pagination"
      pattern: "DataTable"
---

<objective>
Land the Activity page's data-heavy core: 30-day heatmap (ACTV-01), 14-day token charts (ACTV-02), and the paginated SessionsTable (ACTV-06). The OTEL firehose + UnifiedFailures + TopSkills land in plan 06-05 (next wave). Serialized after 06-03 because both plans append to `frontend/src/components/panels/index.ts` and `frontend/src/styles.css`; serialized before 06-05 because both plans modify `frontend/src/routes/activity.tsx` + the panels barrel + styles.css.

Purpose: These three panels fill the bulk of the Activity page real estate. The heatmap is the "calendar at a glance" view; ChartsStrip is the same data in chart form for the recent 14 days; SessionsTable is the deep-dive list for individual session lookup. All three are poll-based (no SSE), so they fit cleanly in one plan.

Output: 3 panel files + 3 test files + barrel updates + routes/activity.tsx wiring + ~12 new tests.
</objective>

<execution_context>
@/Users/patrykattc/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-observability-activity-panels/06-RESEARCH.md
@.planning/phases/06-observability-activity-panels/06-01-PLAN.md

# Backend response shapes:
@backend/cmc/api/schemas/observability.py
@backend/cmc/api/schemas/sessions.py
@backend/cmc/api/routes/sessions.py
@backend/cmc/api/routes/observability.py

@frontend/src/components/ui/index.ts
@frontend/src/lib/queries.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivityHeatmap + ChartsStrip (poll-based panels)</name>
  <files>frontend/src/components/panels/ActivityHeatmap.tsx, frontend/src/components/panels/ChartsStrip.tsx, frontend/src/components/panels/index.ts, frontend/src/components/panels/__tests__/ActivityHeatmap.test.tsx, frontend/src/components/panels/__tests__/ChartsStrip.test.tsx, frontend/src/styles.css</files>
  <action>
    Step 1 — `ActivityHeatmap.tsx` (ACTV-01):
    - `useHeatmap('30d')`. PanelCard reqId="ACTV-01" title="30-Day Activity" empty={{ dataNoun: '30 days of session activity', when: (d) => d.items.length === 0 }}.
    - colorScale fn (pure, in `ActivityHeatmap.utils.ts`): given (value, max), returns one of `var(--cmc-surface-2)` (zero), `var(--cmc-status-green-2)` (low), `var(--cmc-status-green-3)` (mid), `var(--cmc-status-green-4)` (high), `var(--cmc-status-green-5)` (top). Buckets: 0 / 1-25% / 25-50% / 50-75% / 75-100% of max. If the project styles.css doesn't define green-2..5 tokens, use `var(--cmc-accent-blue)` with progressive opacity instead — pick whichever exists in styles.css and document the choice.
    - Render `<HeatmapGrid cells={data.items.map(i => ({day: i.day, value: i.sessions}))} colorScale={colorScale} ariaLabel="30-day session activity heatmap" />`. Tooltip per cell shows `${day}: ${sessions} sessions, ${tokens_effective.toLocaleString()} tokens`.

    Step 2 — `ChartsStrip.tsx` (ACTV-02):
    - `useTokens('30d')`. PanelCard reqId="ACTV-02" title="14-Day Token Trend" empty.dataNoun="14 days of token usage".
    - Helper `sliceLast14Days(items)` (pure fn in `ChartsStrip.utils.ts`): groups items by day, takes last 14 distinct days, preserves stack segments.
    - Render Recharts `<BarChart>` exactly as TokenUsageCard renders, but with the sliced 14 rows. Same fill tokens for input/output/cache_read/cache_create.
    - **Code comment: `// /api/usage/tokens does not accept '14d' (Literal closed in observability.py); we overfetch 30d and slice client-side per RESEARCH §11.`**

    Step 3 — Append to `panels/index.ts`:
    ```ts
    export { ActivityHeatmap } from './ActivityHeatmap'
    export { ChartsStrip } from './ChartsStrip'
    export { SessionsTable } from './SessionsTable'   // shipped in Task 2
    ```

    Step 4 — Append CSS classes to styles.css for `.cmc-heatmap-grid` (auto-fit grid for 30 cells; gap-2xs), `.cmc-heatmap-cell` (border-radius-xs; aspect-ratio: 1; transition for hover state), `.cmc-charts-strip` wrapper.

    Step 5 — Tests:
    - ActivityHeatmap: empty fixture (items: []) → EmptyState rendered; non-empty fixture → HeatmapGrid renders 30 cells; tooltip text contains the day from fixture.
    - ChartsStrip: 30-day fixture → SVG rendered; only 14 X-axis ticks present (assert `data-key="day"` rendered tick count).
    - Both: PanelCard shell renders correct reqId + title.

    Step 6 — Run suite:
    ```bash
    cd frontend && npm run typecheck && npm run test -- --run
    ```
  </action>
  <verify>
    ```bash
    cd frontend && grep -q "useHeatmap" src/components/panels/ActivityHeatmap.tsx
    cd frontend && grep -q "sliceLast14Days" src/components/panels/ChartsStrip.tsx
    cd frontend && grep -q "useTokens.*30d" src/components/panels/ChartsStrip.tsx
    cd frontend && npm run typecheck
    cd frontend && npm run test -- --run src/components/panels/__tests__/ActivityHeatmap.test.tsx src/components/panels/__tests__/ChartsStrip.test.tsx
    ```
  </verify>
  <done>
    Heatmap renders 30 cells with token-driven colors; ChartsStrip renders 14-day slice; ~6 new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: SessionsTable + routes/activity.tsx wiring</name>
  <files>frontend/src/components/panels/SessionsTable.tsx, frontend/src/components/panels/__tests__/SessionsTable.test.tsx, frontend/src/routes/activity.tsx, frontend/src/styles.css</files>
  <action>
    Step 1 — `SessionsTable.tsx` (ACTV-06):
    - Local state:
      - `range: Range | 'all' = '7d'` (note: backend accepts `range='all'` for /api/sessions per research)
      - `source: string | undefined` (filter by source — text input)
      - `model: string | undefined` (filter by model — text input)
      - `search: string` (client-side search; not sent to backend)
      - `page = 0`, `pageSize = 50`
    - `useSessionsList({range, source, model, limit: pageSize, offset: page * pageSize})`. Hook from queries.ts already has `placeholderData: (prev) => prev` so previous page stays visible during transitions (research §risk register).
    - PanelCard reqId="ACTV-06" title="Sessions" trailing={<header chrome>: range filter + source input + model input + search input}, empty.dataNoun="session history", empty.when=(d)=>d.items.length===0 && page===0.
    - Children: `<DataTable rows={data.items} columns={[ {id:'session_id', header:'Session', cell:(r)=><span className="cmc-mono">{r.id.slice(0,8)}…</span>, sortable:true}, {id:'project', header:'Project'}, {id:'model', header:'Model'}, {id:'started_at', header:'Started', cell:(r)=><RelativeTime ts={r.started_at}/>}, {id:'tokens_input', header:'In', cell:(r)=>r.tokens_input.toLocaleString()}, {id:'tokens_output', header:'Out', cell:(r)=>r.tokens_output.toLocaleString()} ]} search={search} searchKeys={['id','project']} page={page} pageSize={pageSize} onPageChange={setPage} />`.
    - Below the table: pagination controls "Page {page+1} of {Math.ceil(data.total / pageSize)}" + Prev/Next buttons (disable Prev when page===0; disable Next when (page+1)*pageSize >= data.total).
    - Search is client-side (per RESEARCH §gotcha 9 — backend has no `q=` param). Filter rows by `id` substring match OR `project` substring match (case-insensitive) when `search` is non-empty.

    Step 2 — Update `frontend/src/routes/activity.tsx`:
    - Import: `import { ActivityHeatmap, ChartsStrip, SessionsTable } from '../components/panels'`.
    - Remove `ACTV-01`, `ACTV-02`, `ACTV-06` entries from `ACTIVITY_SLOTS`. Keep `ACTV-03`, `ACTV-04`, `ACTV-05` for plan 06-05.
    - Render order:
      1. Page header (existing).
      2. `<ActivityHeatmap />` (full-width).
      3. `<ChartsStrip />`
      4. `<SessionsTable />` (full-width).
      5. `<PlaceholderCardGrid slots={ACTIVITY_SLOTS} />` (still present — plan 06-05 deletes it after replacing the last 3 slots).

    Step 3 — Tests for SessionsTable:
    - Pre-seed queryClient with a fixture of 60 items + total=120.
    - Renders 50 rows (pageSize default).
    - Click Next → page state → rerenders with offset adjusted.
    - Type search "abc" → filters rows in DOM (only rows whose id or project contains "abc" appear).
    - Range filter change → triggers new query (assert via mock fetch call signature).
    - Source/model input typing → debouncing optional v1; for now just send on blur or update via controlled input.

    Step 4 — Append CSS for `.cmc-sessions-table-header` (filter row layout), `.cmc-sessions-table-pagination`, `.cmc-table input[type=text]` (filter input styling). Tokens only.

    Step 5 — Run suite + build:
    ```bash
    cd frontend && npm run typecheck && npm run test -- --run && npm run build
    ```
  </action>
  <verify>
    ```bash
    cd frontend && grep -q "useSessionsList" src/components/panels/SessionsTable.tsx
    cd frontend && grep -q "DataTable" src/components/panels/SessionsTable.tsx
    cd frontend && grep -q "ActivityHeatmap\|ChartsStrip\|SessionsTable" src/routes/activity.tsx
    cd frontend && grep -c "ACTV-01\|ACTV-02\|ACTV-06" src/routes/activity.tsx
    # Should be 0 since those slots are removed from ACTIVITY_SLOTS (titles still appear inside panel files via reqId)
    cd frontend && npm run typecheck
    cd frontend && npm run test -- --run
    cd frontend && npm run build
    ```
  </verify>
  <done>
    SessionsTable renders paginated /api/sessions data with filters + client-side search; activity.tsx wires the 3 panels above the remaining 3 placeholder slots; ~6 new tests pass.
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run typecheck` exits 0
- `cd frontend && npm run test -- --run` shows ~143+ tests passing (131 from 06-03 + ~12 new)
- `cd frontend && npm run build` produces dist/ with no errors
- Visit `/activity` in dev: see heatmap (top), 14-day chart strip, sessions table; remaining 3 placeholder cards (ACTV-03/04/05) still render below
- ACTV-01/02/06 placeholder kickers no longer appear on the page
</verification>

<success_criteria>
1. ACTV-01 30-day heatmap renders with cell color tied to session count (Phase 6 success criterion 5: "Activity page shows 30-day heatmap…")
2. ACTV-02 14-day token chart renders client-side-sliced from 30d response (Phase 6 success criterion 5: "…14-day token charts…")
3. ACTV-06 SessionsTable renders with search/pagination + range/source/model filters (Phase 6 success criterion 5: "…sessions table with search/pagination")
4. Backend Literal stays unchanged (no '14d' added to observability.py — research §11 mitigation honored)
5. routes/activity.tsx wires all 3 live panels above the (still-present) placeholder grid for ACTV-03/04/05
</success_criteria>

<output>
After completion, create `.planning/phases/06-observability-activity-panels/06-04-SUMMARY.md` documenting:
- Final test count for the plan
- Confirm 14-day slice happens client-side (no backend Literal change)
- Confirm SessionsTable uses placeholderData for snappy pagination
- Phase 6 Plan 05 entry contract: routes/activity.tsx still imports PlaceholderCardGrid + ACTIVITY_SLOTS for ACTV-03/04/05; plan 06-05 replaces those last 3 and DELETES PlaceholderCardGrid usage on /activity
- Visual smoke notes (heatmap colors, chart strip width, table pagination behavior on edge cases)
</output>
