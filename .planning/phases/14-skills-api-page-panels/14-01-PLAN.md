---
phase: 14-skills-api-page-panels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cmc/api/routes/skills.py
  - backend/cmc/api/schemas/skills.py
  - backend/cmc/api/sse.py
  - backend/tests/test_skills_router.py
autonomous: true
requirements: [SKIL-04, SKIL-05, SKIL-06, SKIL-07]
tags: [fastapi, pydantic-v2, decimal, sqlalchemy, sqlite, skills, percentile-cte, request-scoped-attribution]

must_haves:
  truths:
    - "User can hit GET /api/skills/usage?range=14d|30d&limit=10 and receive top-N skills by invocation count + per-day sparkline buckets"
    - "User can hit GET /api/skills/{name}/cost?range= and receive Decimal-as-JSON-string cost_usd, tokens split, 14-day daily cost trend, rates_as_of caption, AND a cost_attribution: 'request' | 'session' field that exposes whether the per-request JOIN found matches"
    - "User can hit GET /api/skills/{name}/latency?range= and receive p50/p95/max latency_ms, error_rate, sample_count, AND a server-side low_sample: bool flag (true when sample_count < 30)"
    - "User can hit GET /api/skills/{name}/runs?limit= and receive ts/session_id/cwd/request_id rows ordered ts DESC"
    - "User can pass ?range=2d to any endpoint and receive HTTP 422 (Literal validation auto-422)"
    - "SkillTimeline (Plan 04) can read attrs_skill_name from the SSE firehose payload — backend SSE forwards the column"
  artifacts:
    - path: "backend/cmc/api/schemas/skills.py"
      provides: "SkillRange + 4 response models + SkillSparklineRow + SkillRunRow"
      contains: "class SkillUsageResponse"
    - path: "backend/cmc/api/routes/skills.py"
      provides: "4 new path operations appended to the existing router"
      exports: ["skills_usage", "skill_cost", "skill_latency", "skill_runs"]
    - path: "backend/cmc/api/sse.py"
      provides: "tail_otel_events payload extended with attrs_skill_name"
      contains: "attrs_skill_name"
    - path: "backend/tests/test_skills_router.py"
      provides: "Coverage for the 4 new endpoints + SSE payload regression"
      contains: "test_skills_usage_top_n_with_sparkline"
  key_links:
    - from: "backend/cmc/api/routes/skills.py"
      to: "cmc.pricing.compute_cost"
      via: "Decimal cost compute (per row, accumulated by Decimal sum)"
      pattern: "compute_cost\\("
    - from: "backend/cmc/api/routes/skills.py"
      to: "otel_events.attrs_skill_name (indexed; landed Phase 13 Plan 02)"
      via: "WHERE event_name = 'skill_activated' AND attrs_skill_name = :name"
      pattern: "event_name\\s*=\\s*'skill_activated'"
    - from: "backend/cmc/api/routes/skills.py (skill_cost)"
      to: "otel_events (event_name='api_request')"
      via: "Self-JOIN on (session_id, request_id) extracted from body.record.attributes via json_each"
      pattern: "api_request"
    - from: "backend/cmc/api/sse.py"
      to: "OtelEvent.attrs_skill_name column"
      via: "row.attrs_skill_name in the json.dumps payload"
      pattern: "attrs_skill_name"

deviations:
  - id: D-01
    item: "Roadmap text says GET /api/skills?range=14d|30d for top-N. The existing endpoint at that path returns SkillListResponse (catalog) and is consumed by SkillsRegistry.tsx."
    decision: "New endpoint at GET /api/skills/usage. Catalog endpoint preserved. Per researcher Open Q #2 recommendation (lowest blast radius)."
    impact: "REQUIREMENTS.md SKIL-04 success-criterion text says ?range= on /api/skills; deliverable instead exposes /api/skills/usage. Frontend (Plan 02) consumes /api/skills/usage."
  - id: D-02
    item: "SPIKE.md LOCK-9 — request_id presence on skill_activated events at 2.1.116 is TENTATIVE."
    decision: "Implement DUAL-PATH skill cost. Try the (session_id, request_id) JOIN to api_request events first. When it returns zero matched tokens for a (skill_name) slice, fall back to session-scoped attribution (SUM sessions.tokens_* for sessions that fired the skill). Surface the path used in cost_attribution: 'request' | 'session' on the response. Do NOT add a live-data probe task — the dual-path handles either outcome."
    impact: "Plan-checker should assert cost_attribution field exists; gsd-verifier should NOT hard-code 'request' as the expected value."
  - id: D-03
    item: "duration_ms presence on skill_activated is TENTATIVE (SPIKE.md LOCK-3)."
    decision: "Latency SQL filters WHERE duration_ms IS NOT NULL. When sample_count = 0, return p50/p95/max as null + sample_count: 0 + low_sample: true. No exception, no 500."
    impact: "All-NULL skill events surface as empty-state in the UI, not as failure."
  - id: D-04
    item: "MIN_LATENCY_SAMPLES=30 — frontend constant per REQUIREMENTS.md SKLP-05."
    decision: "Surface server-side as low_sample: bool on SkillLatencyResponse (matches CacheEfficiencyCard pattern). Frontend re-asserts the threshold in the panel for defense-in-depth, but the source of truth is the server."
    impact: "SkillLatencyTable reads response.low_sample directly; no duplicate constant drift risk."
---

<objective>
Add 4 new read-time-computed endpoints to the existing /api/skills router (SKIL-04..07) plus a one-line SSE payload extension that lets SkillTimeline (Plan 04) label firehose events with skill name. Every SQL query reuses Phase 13 patterns: Range Literal enum (auto-422), Decimal-as-JSON-string serialization, json_each for OTEL attribute extraction (BUG-A canon), compute_cost() for Decimal money. The genuinely-novel pieces are (a) the request-scoped (session_id, request_id) JOIN refining LOCK-9 with a defensive session-scoped fallback, and (b) the SQLite window-CTE percentile pattern adapted from observability._TOOL_LATENCY_SQL.

Purpose: Lay the API contract that 4 frontend panels (Plans 03 + 04) and the /skills/$name detail route (Plan 05) consume. Without this plan, every downstream panel renders empty.

Output: 4 new path operations on the existing skills_router; 5 new response models in schemas/skills.py; +1 dict key on sse.py firehose payload; 10–12 integration tests.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-skills-api-page-panels/14-RESEARCH.md
@.planning/phases/13-cost-foundation-skill-ingest/13-04-SUMMARY.md
@.planning/research/SPIKE.md

<interfaces>
<!-- Key contracts from Phase 13 the executor consumes directly. -->

From backend/cmc/api/schemas/cost.py (Phase 13 — INHERIT, do not duplicate):
```python
CostRange = Literal["1d", "7d", "14d", "30d"]
# Phase 14: define SkillRange = Literal["14d", "30d"] (or import CostRange and constrain
# at the route signature). Researcher recommendation: SkillRange alias — tightest type.
```

From backend/cmc/pricing.py:
```python
async def load_rates(db: AsyncSession) -> tuple[dict[str, dict], date | None]
def compute_cost(model: str, input_tokens: int, output_tokens: int,
                 cache_read: int, cache_create_5m: int, cache_create_1h: int,
                 rates: dict[str, dict]) -> Decimal
```

From backend/cmc/api/routes/cost.py (Phase 13 — TEMPLATE for endpoint shape):
- _RANGE_TO_DAYS dict + _range_start(range_) helper — REPLICATE in skills.py (do NOT import private symbols across router modules; copy ~5 lines).
- Per-row pattern: load_rates(db) -> for row in result: cost = compute_cost(...) -> total += cost -> rates_as_of = max(...).

From backend/cmc/api/routes/skills.py (current — APPEND below line 162):
```python
router = APIRouter(tags=["skills"])  # already mounted under /api by routes/__init__.py
# Existing operations: list_skills (GET /skills), sync_skills (POST /skills/sync),
# patch_autonomy (PATCH /skills/{name}/autonomy). Append new operations to this router.
```

From backend/cmc/api/sse.py (line ~60 — extend payload):
```python
yield {
    "event": "otel",
    "data": json.dumps({
        "id": row.id,
        "ts": row.ts.isoformat() if row.ts else None,
        "event_name": row.event_name,
        "session_id": row.session_id,
        "attrs_mcp_server": row.attrs_mcp_server,
        "attrs_mcp_tool": row.attrs_mcp_tool,
        # Plan 14-01: ADD this line
        "attrs_skill_name": row.attrs_skill_name,
    }, separators=(",", ":")),
}
```

From backend/cmc/cli/doctor.py:583-598 (BUG-A-corrected json_each pattern — COPY EXACTLY):
```sql
(SELECT json_extract(value, '$.value.stringValue')
   FROM json_each(json_extract(body, '$.record.attributes'))
  WHERE json_extract(value, '$.key') = 'request_id'
  LIMIT 1) AS request_id
```

From backend/cmc/api/routes/observability.py:215-262 (_TOOL_LATENCY_SQL — adapt for skills):
- ROW_NUMBER() OVER (PARTITION BY skill_name ORDER BY duration_ms) AS rnk
- COUNT(*) OVER (PARTITION BY skill_name) AS n
- p50/p95 sub-CTEs with rnk = MAX(CAST(n*p AS INTEGER), 1)

From backend/tests/conftest.py (Phase 13 P06 fixtures — REUSE):
- client (FastAPI TestClient with lifespan-seeded pricing + tables)
- db_session (with client-coexistence: when both requested, client owns the lifespan)
- seed_pricing (asserts the 5 SKUs are present)

SPIKE.md attribute extraction notes (LOCK-3, LOCK-9):
- api_request body carries: input_tokens, output_tokens, cache_read_tokens, cache_creation_tokens, request_id, model — ALL as stringValue (string-typed, even for ints).
- skill_activated body MAY carry: skill_name, duration_ms, request_id — TENTATIVE at 2.1.116. Defensive code mandatory.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add response schemas + Range alias + SSE payload extension</name>
  <files>
    backend/cmc/api/schemas/skills.py
    backend/cmc/api/sse.py
    backend/tests/test_skills_router.py
  </files>
  <behavior>
    - schemas: SkillRange = Literal["14d", "30d"] alias defined.
    - schemas: SkillSparklineRow(BaseModel) with day: str (YYYY-MM-DD) + invocations: int + cost_usd: Decimal | None = None.
    - schemas: SkillUsageResponse(BaseModel) with range: SkillRange + rows: list[SkillUsageRow] (rows carry skill_name + total + sparkline: list[SkillSparklineRow]).
    - schemas: SkillCostResponse(BaseModel) with range, name: str, rates_as_of: date | None, tokens_input/output/cache_read/cache_create_5m/cache_create_1h: int, cost_usd: Decimal, cost_attribution: Literal["request", "session"], trend: list[SkillSparklineRow].
    - schemas: SkillLatencyResponse(BaseModel) with range, name, sample_count: int, p50_ms: int | None, p95_ms: int | None, max_ms: int | None, error_count: int, error_rate: float, low_sample: bool.
    - schemas: SkillRunsResponse(BaseModel) with name, rows: list[SkillRunRow] (ts: datetime, session_id: str | None, cwd: str, request_id: str | None).
    - sse: tail_otel_events payload includes "attrs_skill_name" key (sourced from row.attrs_skill_name; pre-existing column post Phase 13 Plan 02).
    - test_skills_router (RED, will be wired in Tasks 2/3): one schema-importability test asserts `from cmc.api.schemas.skills import SkillRange, SkillUsageResponse, SkillCostResponse, SkillLatencyResponse, SkillRunsResponse` works without raising.
    - SSE payload regression test: assert that an OtelEvent row with attrs_skill_name='analyze' surfaces 'attrs_skill_name': 'analyze' in the json.dumps payload (mock or fixture).
  </behavior>
  <action>
    1. Open `backend/cmc/api/schemas/skills.py` and append the 5 new response models + SkillRange + SkillSparklineRow + SkillUsageRow + SkillRunRow below the existing SkillListResponse / SkillSyncResponse / SkillAutonomyResponse / SkillRow / SkillAutonomyPatch classes. Pydantic v2 serializes Decimal as JSON string by default — DO NOT add a custom serializer. DO NOT use ConfigDict(protected_namespaces=()) unless you add a `model_*` field (you won't). Import from datetime: date, datetime; from decimal: Decimal; from typing: Literal.
    2. Open `backend/cmc/api/sse.py` and add `"attrs_skill_name": row.attrs_skill_name,` to the json.dumps payload at line ~67 (after `"attrs_mcp_tool": row.attrs_mcp_tool,`). One-line addition; nothing else changes in this file. The OtelEvent SQLModel already has the attrs_skill_name column (landed Phase 13 Plan 02); no model change needed.
    3. Create or extend `backend/tests/test_skills_router.py` with a top-of-file imports section + (a) `test_skills_router_schemas_importable` (asserts the 5 new symbols import cleanly + Decimal field round-trips as JSON string via `SkillCostResponse(...).model_dump_json()`), (b) `test_sse_firehose_includes_attrs_skill_name` (insert an OtelEvent row with attrs_skill_name='analyze' via db_session fixture; iterate `tail_otel_events` for ~1 second; assert the event payload contains the key. If the live-tail test pattern is too heavy, alternative: directly call the json.dumps inline by extracting the dict-shape contract — accept whichever pattern matches existing test_observability_router.py / test_sse_*.py idioms).
    4. Verify no other test file in `backend/tests/` imports from sse.py such that the payload-shape addition could break an existing assertion (`grep -rn "attrs_mcp_tool" backend/tests/` — if any test pins the payload key set explicitly, extend it to include attrs_skill_name).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_skills_router.py::test_skills_router_schemas_importable tests/test_skills_router.py::test_sse_firehose_includes_attrs_skill_name -x -v</automated>
  </verify>
  <done>
    - `from cmc.api.schemas.skills import SkillRange, SkillUsageResponse, SkillCostResponse, SkillLatencyResponse, SkillRunsResponse` succeeds with no warnings.
    - `SkillCostResponse(name='x', range='14d', rates_as_of=None, cost_usd=Decimal('1.234'), cost_attribution='session').model_dump_json()` emits `"cost_usd":"1.234"` (string, not float).
    - SSE payload contains `attrs_skill_name` key.
    - `cd backend &amp;&amp; pytest` — full suite still green (438 baseline + new tests passing; no pre-existing test red).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement skills_usage + skill_runs endpoints + tests</name>
  <files>
    backend/cmc/api/routes/skills.py
    backend/tests/test_skills_router.py
  </files>
  <behavior>
    - GET /api/skills/usage?range=14d&limit=10 returns top-N skills by invocation count with per-day sparkline buckets. Pre-conditions: `attrs_skill_name IS NOT NULL`, `event_name = 'skill_activated'` (BARE — Pitfall 1), `ts >= datetime(:since)`.
    - Response groups by skill_name; each row carries `total: int` and `sparkline: list[{day, invocations}]` ordered day ASC. Top-N ordered by total DESC.
    - GET /api/skills/usage?range=2d returns 422 (Literal validation auto-422).
    - GET /api/skills/usage?limit=999 returns rows clamped at limit ≤ 50; default limit = 10. Both via FastAPI Query(ge=1, le=50, default=10).
    - Empty skill set returns `{ "range": "14d", "rows": [] }`, not 404.
    - GET /api/skills/{name}/runs?limit=20 returns recent invocations of `name` ordered ts DESC.
    - Response row: `ts, session_id, cwd, request_id`. cwd LEFT JOINs sessions; missing cwd → '<unknown>'.
    - Path-name validation reuses `_SKILL_NAME_RE` from the existing list_skills endpoint (allow [a-zA-Z0-9_-]+ + explicit '..' check, mirrors line 56).
    - limit defaults 20, ge=1, le=200 (200 = arbitrary safety cap — runs is a time-series tail so a generous limit is fine).
    - Tests: top-N ordering + sparkline shape + 422 + limit clamp + empty-state + path validation rejecting "../" + recent-runs cwd join.
  </behavior>
  <action>
    1. Open `backend/cmc/api/routes/skills.py`. Below the existing patch_autonomy at ~line 162, add a small SQL constants section (private module-level `text(...)` constants) and the two endpoint handlers. Reuse the `_SKILL_NAME_RE` defined at line 47 (do NOT duplicate). Import `_RANGE_TO_DAYS` and `_range_start` by COPYING (not importing — keep router files independent) ~5 lines from `cost.py:46-58`. Import additions at top: `text` from sqlalchemy, `Depends`/`Query`/`HTTPException` (already imported), the 4 schemas (SkillRange, SkillUsageResponse, SkillUsageRow, SkillSparklineRow, SkillRunsResponse, SkillRunRow), `date`/`datetime` (datetime already imported).
    2. Define `_USAGE_TOP_SQL` per RESEARCH.md lines 504-531 (per_day CTE → totals CTE LIMIT :limit → LEFT JOIN per_day for sparkline). Iterate result rows in Python and group into `SkillUsageRow(skill_name, total, sparkline=[SkillSparklineRow(day, invocations)])` — sparkline rows already arrive ordered by `t.total DESC, p.day ASC` per the SQL.
    3. Define `_RUNS_SQL` per RESEARCH.md lines 538-554 (LEFT JOIN sessions on session_id; json_each extraction for request_id; ORDER BY ts DESC LIMIT :limit). The SQL filters on `event_name='skill_activated' AND attrs_skill_name = :name` (BARE event name — see Pitfall 1).
    4. `skills_usage(range_, limit, db)` handler: alias `Query(alias="range")`, returns SkillUsageResponse. `skill_runs(name, limit, db)` handler: validate name with _SKILL_NAME_RE + '..' rejection (raise HTTPException(400) on fail — match list_skills pattern), returns SkillRunsResponse.
    5. In `test_skills_router.py`, add tests: `test_skills_usage_top_n_with_sparkline` (seed 3 skill_activated events across 2 skills + 2 days; assert top-1 wins; assert sparkline length == days touched), `test_skills_usage_invalid_range_returns_422`, `test_skills_usage_limit_clamping` (?limit=99 returns 422 from ge/le), `test_skills_usage_empty_returns_empty_rows` (no events seeded), `test_skill_runs_recent_ordered_desc` (seed 5 events at staggered ts; assert ts DESC + cwd from joined sessions row), `test_skill_runs_path_traversal_rejected` (?name=../etc returns 400), `test_skill_runs_unknown_cwd_fallback` (event with session_id that doesn't exist in sessions table → cwd == '<unknown>').
    6. Test fixtures: extend conftest.py minimally if needed — but prefer inline `_seed_otel_event(db, event_name, attrs_skill_name, body, session_id, ts)` helper in test_skills_router.py (matches the inline `_seed_token_usage` pattern in test_cost_router.py per Phase 13 P04 SUMMARY). Synthetic `body` JSON: `{"record":{"attributes":[{"key":"request_id","value":{"stringValue":"req-1"}}]}}`.
    7. Update docs to reflect the D-01 deviation (the post-execution `gsd-verifier` will hit a literal URL mismatch otherwise):
       - `.planning/REQUIREMENTS.md` SKIL-04 line: change `GET /api/skills?range=14d|30d` to `GET /api/skills/usage?range=14d|30d` with a `(deviation D-01: avoid collision with existing catalog endpoint /api/skills consumed by SkillsRegistry.tsx)` annotation. (May already be updated by the planner — verify before editing.)
       - `.planning/ROADMAP.md` Phase 14 success criterion #1: change the literal URL the same way + add the same annotation. (May already be updated.)
    8. Run targeted test command. If green, commit per execute-plan workflow.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_skills_router.py -x -v -k "skills_usage or skill_runs"</automated>
  </verify>
  <done>
    - GET /api/skills/usage?range=14d returns SkillUsageResponse with top-N rows + sparkline buckets.
    - ?range=2d → 422; ?limit=99 → 422; ?limit=10 default; ?name=../etc → 400.
    - Empty seed → 200 with `rows: []`.
    - GET /api/skills/{name}/runs returns ordered ts DESC with cwd LEFT JOIN.
    - Existing /api/skills (catalog) endpoint still passes its tests (no regression on SkillsRegistry contract).
    - Full backend suite still green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement skill_cost (dual-path attribution) + skill_latency (window-CTE) + tests</name>
  <files>
    backend/cmc/api/routes/skills.py
    backend/tests/test_skills_router.py
  </files>
  <behavior>
    - GET /api/skills/{name}/cost?range= returns SkillCostResponse with Decimal cost_usd as JSON string, cost_attribution: "request" | "session", tokens split, rates_as_of, and a 14-day daily cost trend.
    - DUAL-PATH per D-02 (defensive against TENTATIVE LOCK-9 request_id presence):
      Path R (request-scoped): Self-JOIN otel_events (skill_activated) ↔ otel_events (api_request) on (session_id, request_id extracted via json_each from body.record.attributes). Tokens from api_request stringValue → CAST INTEGER. Per RESEARCH Pattern 3 SQL.
      Path S (session-scoped fallback): When Path R returns 0 matched-tokens for the (name, range) slice, SUM `sessions.tokens_*` for sessions that fired skill_activated events with attrs_skill_name=name. Mirrors current cost.py:147 _BREAKDOWN_BY_SKILL_SQL pattern.
      Decision rule: if Path R matched-token-sum > 0 → emit cost_attribution='request' + Path R numbers. Else → cost_attribution='session' + Path S numbers.
    - Trend (14-day daily cost): bucket events by STRFTIME('%Y-%m-%d', ts, 'localtime'); compute per-day Decimal cost via compute_cost; emit list[{day, invocations: 0, cost_usd}] ordered day ASC. (invocations=0 OK because trend's purpose is sparkline cost, not invocations.)
    - GET /api/skills/{name}/latency?range= returns SkillLatencyResponse: p50_ms / p95_ms / max_ms (int | None) + sample_count + error_count + error_rate (sample_count=0 → all None + low_sample=true + error_rate=0.0) + low_sample: bool.
    - Latency SQL adapted from observability._TOOL_LATENCY_SQL (lines 215-262) — substitute table → otel_events WHERE event_name='skill_activated' AND attrs_skill_name=:name; duration extracted from body.record.attributes via json_each (stringValue → CAST INTEGER) per Pitfall 3.
    - WHERE duration_ms IS NOT NULL filters out rows where the attribute is absent (D-03 defensive — LOCK-3 TENTATIVE presence).
    - error_rate = error_count / sample_count where error_count counts skill_activated events whose body.record.attributes has key='status' value='error' (or 'failure'/'cancel' — accept the union per SPIKE.md LOCK-8).
    - low_sample = sample_count < MIN_LATENCY_SAMPLES where MIN_LATENCY_SAMPLES = 30 is a module-level constant in skills.py with a code comment citing SKLP-05.
    - Tests: cost_decimal_as_string, cost_attribution_field_present, cost_request_path_when_request_id_present, cost_session_path_when_request_id_absent, cost_trend_shape (14 day buckets), cost_decimal_equality_no_float_drift, cost_trend_sum_equals_total_cost_usd (Decimal sum invariant — sum of trend.daily_cost == cost_usd; protects against per-bucket dual-path drift); latency_percentiles_basic (synthesize 100 events at known durations; assert p50/p95 are correct), latency_single_sample (N=1 → p50=p95=max=that_one_value, low_sample=true), latency_low_sample_under_30 (sample_count=10 → low_sample=true), latency_zero_samples_empty_state (no events with duration → all None + low_sample=true + 200 OK), latency_error_rate_basic (3 of 10 events status='error' → error_rate ≈ 0.3).
  </behavior>
  <action>
    1. In skills.py, define MIN_LATENCY_SAMPLES = 30 module-level constant with `# SKLP-05: low-sample badge threshold (server-side source of truth).` comment.
    2. Define 4 SQL constants:
       - `_COST_REQUEST_SCOPED_SQL` per RESEARCH.md Pattern 3 (lines 248-303). Use json_each + stringValue + CAST INTEGER. LEFT JOIN api_req_events on session_id + request_id. GROUP BY skill_name. Result columns: tokens_input, tokens_output, tokens_cache_read, tokens_cache_create_1h, model.
       - `_COST_SESSION_SCOPED_SQL` mirroring current cost.py:147 _BREAKDOWN_BY_SKILL_SQL but filtered to a single :name (single-skill version, not the multi-row breakdown). Uses sessions.tokens_* SUMs, MAX(s.model) as model.
       - `_COST_TREND_SQL` per-day buckets — IMPORTANT: the trend MUST derive from the SELECTED attribution branch (whichever Path R / Path S won the request-vs-session test in the main cost query). DO NOT run the dual-path test independently per daily bucket — that would let different days land on different branches and break the Decimal sum invariant `sum(trend.daily_cost) == cost_usd`. Concretely: define TWO trend SQL constants — `_COST_TREND_REQUEST_SCOPED_SQL` and `_COST_TREND_SESSION_SCOPED_SQL`. Each is the matching cost SQL with `GROUP BY STRFTIME('%Y-%m-%d', e.ts, 'localtime')` added (preserve the same JOIN / token extraction as the parent branch). The handler picks the branch that matches the main `cost_attribution` value, then runs the matching trend SQL. Phase 13 cost router does NOT emit a per-day cost timeseries today, so this SQL is fresh.
       - `_LATENCY_SQL` per RESEARCH Pattern 2 (lines 188-237) adapted: events CTE extracts duration_ms via json_each + stringValue + CAST INTEGER; ranked CTE applies ROW_NUMBER OVER (PARTITION BY skill_name ORDER BY duration_ms); p50/p95 sub-CTEs use `rnk = MAX(CAST(n*p AS INTEGER), 1)`; agg CTE emits sample_count + max_ms. Final SELECT joins all four; WHERE filtered to single :name.
       - `_ERROR_COUNT_SQL`: separate count of skill_activated events whose status attribute (json_each-extracted) is in ('error','failure','cancel'). Could be a subquery in the same call or a separate text() — separate keeps the SQL readable.
    3. `skill_cost(name, range_, db)` handler:
       - Validate name via _SKILL_NAME_RE.
       - Compute since via _range_start(range_).
       - Run _COST_REQUEST_SCOPED_SQL → if any (input+output+cache_read+cache_create) > 0 → use those + cost_attribution='request'.
       - Else run _COST_SESSION_SCOPED_SQL → use those + cost_attribution='session'.
       - rates, max_eff = await load_rates(db); cost = compute_cost(model, ...) using the picked-path tokens; total accumulates as Decimal.
       - Run the trend SQL matching the SELECTED branch (`_COST_TREND_REQUEST_SCOPED_SQL` if cost_attribution=='request', else `_COST_TREND_SESSION_SCOPED_SQL`) so trend cost matches main cost_usd by the Decimal sum invariant `sum(trend.daily_cost) == cost_usd`. Verify the invariant in a unit test (see Test list below).
       - Return SkillCostResponse with all fields populated. rates_as_of = max_eff if not None.
       - Empty case (no skill_activated events at all for the slice) → 200 + cost_usd=Decimal('0') + cost_attribution='session' (the conservative branch) + tokens all 0 + trend=[].
    4. `skill_latency(name, range_, db)` handler:
       - Validate name.
       - Run _LATENCY_SQL → if no row returned → sample_count=0, all percentiles None, error_count=0, error_rate=0.0, low_sample=True.
       - Else extract sample_count, max_ms, p50_ms, p95_ms from the joined CTE result; CAST p50/p95/max to int (SQLite returns INTEGER).
       - Run _ERROR_COUNT_SQL → error_count.
       - error_rate = error_count / sample_count if sample_count > 0 else 0.0.
       - low_sample = sample_count < MIN_LATENCY_SAMPLES.
       - Return SkillLatencyResponse.
    5. Append integration tests covering every behavior bullet. Test seed pattern: `_seed_otel_event(db, event_name='skill_activated', attrs_skill_name='analyze', body=_make_skill_body(duration_ms=120, request_id='req-1'), session_id='sess-1')` plus `_seed_otel_event(db, event_name='api_request', body=_make_api_request_body(input_tokens=1000, output_tokens=500, cache_read=0, cache_create=0, request_id='req-1', model='claude-opus-4-7'), session_id='sess-1')` — these stay inline in test_skills_router.py.
    6. Run the full test_skills_router.py suite + full backend suite. Commit per execute-plan workflow.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/test_skills_router.py -x -v &amp;&amp; pytest --no-header 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - GET /api/skills/{name}/cost returns Decimal-as-string cost_usd + cost_attribution + tokens + 14-day trend + rates_as_of.
    - When request_id is populated on seeded skill events → cost_attribution='request' + Path R numbers.
    - When request_id is NULL/empty on seeded skill events → cost_attribution='session' + Path S numbers.
    - GET /api/skills/{name}/latency returns p50/p95/max + error_rate + sample_count + low_sample.
    - sample_count=10 → low_sample=true; sample_count=30 → low_sample=false.
    - sample_count=0 → 200 with all percentiles null + low_sample=true.
    - Window-CTE percentile test: 100 events at duration_ms = 1..100 → p50=50, p95=95, max=100 (or off-by-one at the documented MAX(CAST(n*p AS INTEGER), 1) clamp — assert exact values).
    - Full backend suite: 438 baseline + ~12 new tests, 0 failed.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → /api/skills/* | Untrusted query/path params crossing into SQL. Mitigated by Literal-typed Range (auto-422) + _SKILL_NAME_RE validation + parameterized SQL via SQLAlchemy text(:name, :since, :limit) bindings. |
| otel_events.body (JSON BLOB) → SQL extractors | Untrusted upstream OTLP payloads. Mitigated by json_each + stringValue extraction (no eval) + CAST INTEGER (returns NULL on parse failure, doesn't crash). Pitfall 3 already documented. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering / Injection | skill name path param → SQL | mitigate | _SKILL_NAME_RE = `^[a-zA-Z0-9_-]+$` PLUS explicit '..' rejection, replicated from existing list_skills/sync_skills pattern (line 47-56). FastAPI rejects via HTTPException(400). |
| T-14-01-02 | Tampering | range / limit query params → SQL | mitigate | FastAPI Query(ge=1, le=50) clamps int; Literal["14d","30d"] rejects unknown ranges with 422. No string interpolation into SQL — only :param bindings. |
| T-14-01-03 | Information Disclosure | otel_events.body may contain sensitive content (cwd paths, request IDs) | accept | The same exposure exists today in observability.py and cost.py. Single-user tool by design (REQUIREMENTS.md Out of Scope: "multi-user / leaderboard"). No new disclosure surface. |
| T-14-01-04 | DoS | sample_count = 1M skill events → percentile CTE OOM | mitigate | SQLite window functions stream; observability._TOOL_LATENCY_SQL has shipped against millions of rows in production with no incident (Phase 6 baseline). LIMIT clauses on usage/runs endpoints cap result size. Latency endpoint has no row-cap because percentile result is a single row, but the CTE materializes O(N) — the existing cmc doctor #6 (otel_events row count) is the early warning if N grows pathologically. |
| T-14-01-05 | Repudiation | Cost attribution dispute (user can't tell why a number landed) | mitigate | cost_attribution: "request" \| "session" field on SkillCostResponse exposes the path used. UI surfaces this as a caption. |

</threat_model>

<verification>

```bash
# Per-task gate (all 3 tasks)
cd backend && pytest tests/test_skills_router.py -x -v

# Schema importability (Task 1)
cd backend && .venv/bin/python -c "from cmc.api.schemas.skills import SkillRange, SkillUsageResponse, SkillCostResponse, SkillLatencyResponse, SkillRunsResponse; print('OK')"

# Decimal-as-string regression (Task 1)
cd backend && .venv/bin/python -c "from decimal import Decimal; from cmc.api.schemas.skills import SkillCostResponse; r = SkillCostResponse(name='x', range='14d', rates_as_of=None, cost_usd=Decimal('1.234'), cost_attribution='session'); assert '\"cost_usd\":\"1.234\"' in r.model_dump_json(); print('Decimal-as-string OK')"

# 422 regression (Task 2)
cd backend && pytest tests/test_skills_router.py::test_skills_usage_invalid_range_returns_422 -x -v

# Path traversal regression (Task 2)
cd backend && pytest tests/test_skills_router.py::test_skill_runs_path_traversal_rejected -x -v

# Dual-path attribution regression (Task 3)
cd backend && pytest tests/test_skills_router.py -x -v -k "cost_attribution"

# Window-CTE percentile correctness (Task 3)
cd backend && pytest tests/test_skills_router.py::test_skill_latency_percentiles_basic -x -v

# Full-suite phase gate
cd backend && pytest --no-header 2>&1 | tail -3
# Expect: 438 baseline + ~14 new tests = ~452+ passed, 0 failed.
```

</verification>

<success_criteria>

Plan 01 complete when:

1. `from cmc.api.schemas.skills import SkillRange, SkillUsageResponse, SkillCostResponse, SkillLatencyResponse, SkillRunsResponse` succeeds with no warnings.
2. `GET /api/skills/usage?range=14d&limit=10` returns top-N rows with sparkline; `?range=2d` returns 422. (REQUIREMENTS.md SKIL-04 + ROADMAP.md Phase 14 SC #1 are updated to reference `/api/skills/usage` with D-01 annotation — done in Task 2 step 7.)
3. `GET /api/skills/{name}/cost?range=14d` returns SkillCostResponse with `cost_usd` as JSON string, `cost_attribution: "request" | "session"` field present, 14-day trend.
4. `GET /api/skills/{name}/latency?range=14d` returns p50/p95/max + error_rate + sample_count + low_sample (true when sample_count < 30).
5. `GET /api/skills/{name}/runs?limit=20` returns recent invocations with cwd LEFT JOIN.
6. SSE firehose payload includes `attrs_skill_name` key (regression-tested).
7. Path-traversal-style names (`../etc`) return 400 from all 3 per-name endpoints.
8. Full backend test suite green: ~438 baseline + ~14 new tests, 0 failed, 0 skipped.

</success_criteria>

<output>
After completion, create `.planning/phases/14-skills-api-page-panels/14-01-SUMMARY.md` per the standard template (frontmatter requires: phase, plan, subsystem=api, tags, requires/provides/affects, tech-stack, key-files, key-decisions, patterns-established, duration, completed). Document:

- The dual-path attribution decision and which path was preferred in the test fixtures.
- The MIN_LATENCY_SAMPLES = 30 server-side surface (so Plan 03/04 know to read response.low_sample, not duplicate the constant).
- The /api/skills/usage path choice (D-01 deviation from roadmap text — preserves existing /api/skills catalog endpoint).
- Patterns established: dual-path defensive attribution, SQLite window-CTE percentile adapted from observability, SSE payload extension for firehose-consuming panels.
</output>
