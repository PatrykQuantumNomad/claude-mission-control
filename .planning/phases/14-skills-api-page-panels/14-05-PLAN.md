---
phase: 14-skills-api-page-panels
plan: 05
type: execute
wave: 4
depends_on: [14-03, 14-04]
files_modified:
  - frontend/src/routes/skills.$name.tsx
  - frontend/src/routes/__tests__/skills.$name.test.tsx
  - frontend/src/components/panels/SkillRunsTable.tsx
  - frontend/src/components/panels/__tests__/SkillRunsTable.test.tsx
  - frontend/src/components/panels/index.ts
  - frontend/src/routeTree.gen.ts
autonomous: false
requirements: [SKLP-07, SKIL-07]
tags: [frontend, tanstack-router, file-based-route, dynamic-segment, route-tree, checkpoint]

must_haves:
  truths:
    - "User can navigate to /skills/analyze (or any valid skill name) and see a /skills/$name detail page"
    - "Detail page composes 3 sections: per-skill cost (SkillCostCard with name=params.name), per-skill latency (a single-skill latency view derived from useSkillLatency, distinct from the multi-row SkillLatencyTable), and recent runs (NEW SkillRunsTable consuming useSkillRuns)"
    - "Recent runs rows show ts (RelativeTime), session_id (truncated), cwd (project surrogate); clicking a row opens the existing SessionsDetailsSheet at that session_id (Plan 04 D-09 pattern — NO new /sessions/$sid route)"
    - "TanStack Router routeTree.gen.ts is regenerated to include the new /skills/$name route (auto-regen via vite plugin on dev build)"
    - "Clicking from Activity-page TopSkills row (Plan 03) drills in to /skills/$name and lands on the page (closes the cross-plan link)"
    - "Invalid skill name (not matching backend _SKILL_NAME_RE) navigates to the route but the underlying API calls return 400; the page renders an error state"
  artifacts:
    - path: "frontend/src/routes/skills.$name.tsx"
      provides: "NEW file-based dynamic route — first one in the project"
      min_lines: 60
      contains: "createFileRoute('/skills/$name')"
    - path: "frontend/src/components/panels/SkillRunsTable.tsx"
      provides: "NEW — per-skill recent invocations with linked-session drill-in"
      min_lines: 50
    - path: "frontend/src/routeTree.gen.ts"
      provides: "Auto-regenerated route tree including /skills/$name"
      contains: "skills.$name"
  key_links:
    - from: "frontend/src/routes/skills.$name.tsx"
      to: "useParams({ from: '/skills/$name' })"
      via: "TanStack Router params extraction"
      pattern: "useParams"
    - from: "frontend/src/routes/skills.$name.tsx"
      to: "panels/SkillCostCard.tsx + SkillRunsTable.tsx"
      via: "Imports from '../components/panels'"
      pattern: "SkillCostCard\\|SkillRunsTable"
    - from: "frontend/src/components/panels/SkillRunsTable.tsx"
      to: "frontend/src/lib/queries.ts (useSkillRuns)"
      via: "useSkillRuns(name, limit)"
      pattern: "useSkillRuns"
    - from: "frontend/src/components/panels/SkillRunsTable.tsx"
      to: "Existing SessionsDetailsSheet pattern (LiveSessionsCard.tsx)"
      via: "useState<sid|null> + Sheet open=onClick row"
      pattern: "Sheet"

deviations:
  - id: D-09
    item: "RESEARCH.md Open Q #5 — recent-runs links target /sessions/$sid which doesn't exist."
    decision: "Mirror the LiveSessionsCard.tsx Sheet drawer pattern (line 223) — clicking a session_id in SkillRunsTable opens the existing SessionsDetailsSheet (or its equivalent — confirm by reading LiveSessionsCard.tsx implementation). Do NOT add a /sessions/$sid file-based route in this phase. That's a v1.2 follow-up; no Phase 14 requirement covers it."
    impact: "User experiences a drawer slide-in instead of a route navigation. Acceptable per researcher recommendation; matches the existing UX pattern for session inspection."
---

<objective>
Ship the first file-based dynamic route in the project (`/skills/$name`) — composes the SkillCostCard + a per-skill latency snapshot + a NEW SkillRunsTable into a single detail page. Recent-runs rows drill into the existing SessionsDetailsSheet drawer (D-09) instead of a not-yet-existent /sessions/$sid route. After this plan ships, clicking a TopSkills row (Plan 03) lands on a real page rather than a 404.

Purpose: Close SKLP-07 + SKIL-07. Establish the file-based dynamic route pattern for future detail pages (sessions, projects).

Output: 1 NEW route file + 1 NEW panel + 2 NEW test files + index.ts export + routeTree.gen.ts regeneration. 1 checkpoint:human-verify task at end.
</objective>

<execution_context>
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/workflows/execute-plan.md
@/Users/patrykattc/work/git/claude-mission-control/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-skills-api-page-panels/14-RESEARCH.md
@.planning/phases/14-skills-api-page-panels/14-03-SUMMARY.md
@.planning/phases/14-skills-api-page-panels/14-04-SUMMARY.md

<interfaces>
<!-- Hooks + components from Plans 02 + 04 the executor consumes directly. -->

From frontend/src/lib/queries.ts (Plan 02):
```ts
export const useSkillCost: (name: string, range: SkillRange) => UseQueryResult<SkillCostResponse>
export const useSkillLatency: (name: string, range: SkillRange) => UseQueryResult<SkillLatencyResponse>
export const useSkillRuns: (name: string, limit?: number) => UseQueryResult<SkillRunsResponse>
```

From frontend/src/lib/api.ts (Plan 02):
```ts
export interface SkillRunRow { ts: string; session_id: string | null; cwd: string; request_id: string | null }
export interface SkillRunsResponse { name: string; rows: SkillRunRow[] }
```

From @tanstack/react-router (file-based dynamic route — first in project):
```tsx
import { createFileRoute, useParams } from '@tanstack/react-router'

export const Route = createFileRoute('/skills/$name')({ component: SkillDetailPage })

function SkillDetailPage() {
  const { name } = useParams({ from: '/skills/$name' })
  // compose panels
}
```

Vite plugin auto-regenerates routeTree.gen.ts on dev/build (verified RESEARCH.md A5: vite.config.ts:11 has tanstackRouter() with autoCodeSplitting: true).

From frontend/src/components/panels/SkillCostCard.tsx (Plan 04):
```tsx
export function SkillCostCard({ name }: { name: string }): JSX.Element  // signature locked by Plan 04 D-08
```

From frontend/src/components/panels/LiveSessionsCard.tsx (existing — line 223 Sheet pattern):
- Uses `<Sheet open={...} onOpenChange={...}>...</Sheet>` from '../ui'.
- useSessionDetails(sid) hook fetches details when drawer is open.
- See lines 128-128 for the hook usage and 223-234 for the Sheet JSX.

From frontend/src/components/ui/RelativeTime.tsx + ../ui index — existing primitives for time formatting.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SkillRunsTable.tsx + tests</name>
  <files>
    frontend/src/components/panels/SkillRunsTable.tsx
    frontend/src/components/panels/__tests__/SkillRunsTable.test.tsx
    frontend/src/components/panels/index.ts
  </files>
  <behavior>
    - SkillRunsTable.tsx NEW component:
      - Signature: `export function SkillRunsTable({ name }: { name: string })`.
      - Query: `const query = useSkillRuns(name, 25)`.
      - Render via PanelCard<SkillRunsResponse> with reqId="SKIL-07", title="Recent Runs", empty.when=(d)=>d.rows.length===0.
      - DataTable columns: ts (RelativeTime), session_id (8-char prefix), cwd (truncated to last 2 path segments), request_id (8-char prefix or '—').
      - Row click → open Sheet with SessionsDetailsSheet content (mirror LiveSessionsCard.tsx:128 + 223-234 pattern). State: `const [openSid, setOpenSid] = useState<string | null>(null)`.
      - Empty state caption: "No invocations yet. Run sync from the header to refresh."
    - SkillRunsTable.test.tsx NEW:
      - Mock useSkillRuns to return 5 rows.
      - Test 1: renders 5 rows.
      - Test 2: clicking a row sets openSid (assert via Sheet visibility).
      - Test 3: empty state when rows: [].
      - Test 4: row with session_id=null renders cleanly (no crash; '—' or similar).
    - panels/index.ts MODIFIED: add `export { SkillRunsTable } from './SkillRunsTable'`. Preserve existing.
  </behavior>
  <action>
    1. Read templates: `cat frontend/src/components/panels/LiveSessionsCard.tsx | head -250` to understand the Sheet drawer pattern + useSessionDetails wiring (the SkillRunsTable reuses the same drawer).
    2. Create SkillRunsTable.tsx. Note: Sheet from '../ui'; useSessionDetails from '../../lib/queries' (it already exists per LiveSessionsCard.tsx line 19 import).
    3. Decide drawer content: option (a) reuse LiveSessionsCard's inline drawer JSX verbatim (extract into a sibling component if it lives inside LiveSessionsCard); option (b) just import the existing drawer-content rendering function. PREFER (b) — if no extracted component exists, accept (a) inline duplication for this plan and flag the refactor opportunity in the SUMMARY.
    4. Create SkillRunsTable.test.tsx. Mock useSkillRuns + useSessionDetails (the latter so the Sheet content doesn't crash on render).
    5. Open `frontend/src/components/panels/index.ts`. Add the new export.
    6. Run `cd frontend && pnpm vitest run components/panels/__tests__/SkillRunsTable && pnpm tsc --noEmit`.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm vitest run components/panels/__tests__/SkillRunsTable --reporter=basic 2>&amp;1 | tail -10 &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | (grep -E "SkillRunsTable" || echo "no errors in SkillRunsTable")</automated>
  </verify>
  <done>
    - SkillRunsTable.tsx exists; useSkillRuns + Sheet drawer wired.
    - panels/index.ts exports SkillRunsTable.
    - SkillRunsTable.test.tsx covers the 4 behaviors.
    - vitest passes; tsc clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create skills.$name.tsx file-based dynamic route + tests + routeTree regeneration</name>
  <files>
    frontend/src/routes/skills.$name.tsx
    frontend/src/routes/__tests__/skills.$name.test.tsx
    frontend/src/routeTree.gen.ts
  </files>
  <behavior>
    - skills.$name.tsx NEW file-based route:
      - createFileRoute('/skills/$name')({ component: SkillDetailPage }).
      - SkillDetailPage extracts `const { name } = useParams({ from: '/skills/$name' })`.
      - Page layout: section.cmc-page with cmc-page__header (label "Skills • {name}", h1 "{name}", subtitle "Per-skill cost, latency, and recent invocations.").
      - Body composition (3 panels in a .cmc-card-grid OR a single column if the panels are wide):
        1. <SkillCostCard name={name} /> (Plan 04 reactivated panel).
        2. A per-skill latency snapshot — render KpiTiles for sample_count / p50_ms / p95_ms / max_ms / error_rate by inline-consuming useSkillLatency(name, '14d'). NO need to ship a SkillLatencySnapshot component — the JSX inline in skills.$name.tsx is fine; this is a one-off per-skill view, not a reusable panel. (Reusing SkillLatencyTable would force a multi-row hook — wrong shape for one skill.)
        3. <SkillRunsTable name={name} /> (NEW from Task 1).
      - Optional but useful: a back-link "← Back to Skills" → `<Link to='/skills'>` at the top of the page.
    - skills.$name.test.tsx NEW (or skip if frontend/src/routes/__tests__/ doesn't exist):
      - If routes/__tests__ dir exists: mock useParams to return name='analyze'; mock all 3 hooks to return synthetic data; assert the 3 panels render (find by reqId or title text).
      - If dir doesn't exist: skip, accept Playwright-equivalent coverage as a v1.2 / TEST-05 deferral. Note in plan SUMMARY.
    - routeTree.gen.ts: regenerated by `pnpm dev` or `pnpm build` running the @tanstack/router-plugin/vite. The plugin runs on file watch in dev. To force regeneration explicitly, run `pnpm build` (vite build invokes the plugin). Commit the regenerated file.
    - VERIFY by `cd frontend && pnpm dev` (background) → wait for `vite ready` → kill — OR by `pnpm build` (preferred, explicit). The new route should appear in routeTree.gen.ts.
  </behavior>
  <action>
    1. Verify the @tanstack/router-plugin/vite is configured: `grep -n "tanstackRouter\|autoCodeSplitting" frontend/vite.config.ts`. Expected per RESEARCH.md A5.
    2. Create `frontend/src/routes/skills.$name.tsx` per the behavior block. Imports: `createFileRoute, useParams, Link` from '@tanstack/react-router'; `SkillCostCard, SkillRunsTable` from '../components/panels'; `useSkillLatency` from '../lib/queries'.
    3. Run `cd frontend && pnpm build` to trigger routeTree.gen.ts regeneration. Inspect via `grep -c "skills.\\\$name\|skillsName" frontend/src/routeTree.gen.ts` — expect ≥1 match (TanStack uses sanitized identifiers; the literal `$` may be encoded).
    4. If routeTree.gen.ts didn't update, fall back to running `pnpm dev` for a few seconds in background (the vite plugin watches files). Or check the plugin docs for an explicit CLI command.
    5. Create the test file (or skip if routes/__tests__ doesn't exist; document in SUMMARY).
    6. Run full vitest + tsc.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; pnpm tsc --noEmit 2>&amp;1 | tail -10 &amp;&amp; pnpm build 2>&amp;1 | tail -10 &amp;&amp; grep -cF '$name' frontend/src/routeTree.gen.ts &amp;&amp; grep -cF 'skills' frontend/src/routeTree.gen.ts &amp;&amp; pnpm vitest run --reporter=basic 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    - `frontend/src/routes/skills.$name.tsx` exists with createFileRoute('/skills/$name').
    - `frontend/src/routeTree.gen.ts` references the new route (grep match ≥1).
    - The 3 sections (cost, latency snapshot, runs) compose correctly when rendered with mocked hooks.
    - Full frontend test suite green; tsc clean.
    - `pnpm build` succeeds (no broken route resolutions).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — Phase 14 end-to-end flow</name>
  <files>
    (none — verification only)
  </files>
  <what-built>
    Phase 14 is end-to-end across 4 backend endpoints + 4 frontend panels + 1 file-based dynamic route. Plans 01-04 have automated test coverage; Plan 05 ships the route with limited routes/__tests__/ coverage. This checkpoint verifies the integrated flow visually before phase close.
  </what-built>
  <action>
    Pause execution. The user must run the verification steps below in `<how-to-verify>` and respond with "approved" or describe failures. No code changes are performed in this task.
  </action>
  <how-to-verify>
    1. **Start the backend:** `cd backend && .venv/bin/uvicorn cmc.api.app:app --reload --port 8788` (or whatever port the project standardizes — check `cmc/api/app.py` or scripts). Confirm lifespan auto-seed messages: "Loaded 5 SKUs from data/pricing.json" or equivalent.
    2. **Smoke the endpoints (any non-empty seed data is fine — production DB will likely have skill_activated=0 rows; that's OK, the empty-state branches are the regression target):**
       - `curl -s 'http://localhost:8788/api/skills/usage?range=14d' | jq` — expect `{ "range": "14d", "rows": [] }` (empty if no production data) or rows.
       - `curl -s 'http://localhost:8788/api/skills/usage?range=2d'` — expect 422.
       - `curl -s 'http://localhost:8788/api/skills/test-skill/cost?range=14d' | jq` — expect SkillCostResponse with cost_attribution field.
       - `curl -s 'http://localhost:8788/api/skills/test-skill/latency?range=14d' | jq` — expect SkillLatencyResponse with low_sample field.
       - `curl -s 'http://localhost:8788/api/skills/test-skill/runs?limit=5' | jq` — expect SkillRunsResponse.
    3. **Start the frontend:** `cd frontend && pnpm dev`. Wait for "Local: http://localhost:5173" or similar.
    4. **Navigate to /activity:** Confirm TopSkills panel renders (no "Coming in v2" text). If no skill data exists, expect the empty-state caption.
    5. **Navigate to /skills:** Confirm SkillCostCard, SkillLatencyTable, SkillTimeline appear in the .cmc-card-grid alongside existing panels (DecisionsCard, InboxCard, TaskBoard, SchedulesCard, SkillsRegistry, McpPanel, ContextHealthCard).
    6. **Navigate to /skills/analyze (or any name):** Confirm the detail page renders with 3 sections (cost, latency snapshot, runs) — even with empty data, each section's empty-state message should appear cleanly without React errors.
    7. **Cross-link test:** From /activity, if any TopSkills row exists, click it. Should navigate to /skills/$name. (If no rows, manually navigate to /skills/foo to verify the route resolves.)
    8. **Pause/resume test (SkillTimeline):** On /skills, click pause on the SkillTimeline panel. Confirm the firehose stops new events from appearing. Click resume. Events resume.
    9. **Filter test (SkillTimeline):** Type in the skill-name filter input. Confirm filtering happens client-side immediately.
    10. **Browser console:** Open devtools → Network tab → filter to /api/skills. Confirm request/response shapes match the mocked test types (TanStack Query devtools also useful).
    11. **Browser console errors:** Should be ZERO red errors. Warnings about missing data are fine.
    12. **TypeScript build:** `cd frontend && pnpm build` exits 0 with no errors.
    13. **Backend full suite:** `cd backend && pytest --no-header 2>&1 | tail -3` — expect ~452+ passed (438 baseline + new Phase 14 tests).
    14. **Frontend full suite:** `cd frontend && pnpm vitest run --reporter=basic 2>&1 | tail -5` — green.

    **What to look for that would indicate a regression:**
    - "Coming in v2" string appearing on /activity or /skills (means a panel rewrite reverted).
    - 500 errors from any /api/skills/* endpoint (means the dual-path attribution or window-CTE broke at runtime).
    - SkillTimeline rows showing '—' for skill name even when the upstream event clearly has attrs_skill_name (means the SSE payload extension didn't ship).
    - /skills/$name returning 404 in TanStack Router (means routeTree.gen.ts wasn't regenerated).
    - Browser console errors about Sheet.open or useSessionDetails (means the SkillRunsTable drawer wiring is broken).
  </how-to-verify>
  <verify>
    Manual user verification — no automated command. The user runs the 14 steps above and confirms.
  </verify>
  <done>
    User responds "approved" after all 14 verification steps pass.
  </done>
  <resume-signal>Type "approved" if all 14 verification steps pass, or describe any failure (panel name, browser console error text, curl response, etc.).</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL path param `$name` → useParams → API URL | Untrusted user input. Mitigated by encodeURIComponent in fetcher (Plan 02) + backend _SKILL_NAME_RE regex rejection (Plan 01). |
| Session ID rendered in SkillRunsTable | From OTEL ingest; could be any string. React text-content escaping handles XSS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering | $name path param injects into URL | mitigate | encodeURIComponent + backend regex (defense-in-depth across Plan 02 + Plan 01). |
| T-14-05-02 | Information Disclosure | Drawer reveals session_id details | accept | Single-user tool; existing LiveSessionsCard already shows this. |

</threat_model>

<verification>

```bash
# Plan 05 gates
cd frontend && pnpm tsc --noEmit
cd frontend && pnpm vitest run --reporter=basic 2>&1 | tail -5
cd frontend && pnpm build 2>&1 | tail -10
# Expect: 0 errors, full suite green, build succeeds.

# Route file presence
ls frontend/src/routes/skills.\$name.tsx
# Expect: file exists.

# routeTree regen check (primary gate is `pnpm tsc --noEmit` above; secondary uses literal-string grep to avoid \$name shell-expansion)
grep -cF '$name' frontend/src/routeTree.gen.ts
grep -cF 'skills' frontend/src/routeTree.gen.ts
# Expect: both ≥1.

# SkillRunsTable export
grep -c "SkillRunsTable" frontend/src/components/panels/index.ts
# Expect: ≥1.

# Drawer wiring (Sheet usage)
grep -c "Sheet" frontend/src/components/panels/SkillRunsTable.tsx
# Expect: ≥1.

# Phase 14 full backend regression
cd backend && pytest --no-header 2>&1 | tail -3
# Expect: 438 baseline + Phase 14 new tests, 0 failed.
```

</verification>

<success_criteria>

Plan 05 complete when:

1. `frontend/src/routes/skills.$name.tsx` exists and uses createFileRoute('/skills/$name').
2. routeTree.gen.ts regenerated to include the new route.
3. SkillRunsTable.tsx NEW with useSkillRuns + Sheet drawer (D-09 — no /sessions/$sid route).
4. SkillRunsTable exported from panels/index.ts.
5. /skills/$name composes SkillCostCard + per-skill latency snapshot + SkillRunsTable.
6. Full frontend test suite green; tsc clean; build succeeds.
7. Human checkpoint approved (Task 3).

</success_criteria>

<output>
Create `.planning/phases/14-skills-api-page-panels/14-05-SUMMARY.md` documenting:
- The Sheet-drawer-instead-of-route pattern (D-09).
- routeTree.gen.ts regeneration confirmation (the file-based route is now committed and visible in the auto-generated tree).
- Notes on routes/__tests__/ — if the dir didn't exist, the route-level test was skipped; flag for v1.2 / TEST-05 Playwright coverage.
- Phase 14 closeout: every requirement (SKIL-04..07, ACTV-04, SKLP-02, SKLP-05, SKLP-06, SKLP-07) traces to ≥1 passing test.
- Patterns established: file-based dynamic route, drawer-instead-of-detail-route pattern for entity inspection.
</output>
