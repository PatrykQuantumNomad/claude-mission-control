# Roadmap: Claude Mission Control

## Milestones

- ✅ **v1.0 MVP** — Phases 1–11, 47 plans (shipped 2026-04-28) — see [milestones/v1.0-ROADMAP.md](milestones/v1.0-ROADMAP.md)
- ✅ **v1.1 Skills & Cost Intelligence** — Phases 12–17, 28 plans (shipped 2026-05-05) — see [milestones/v1.1-ROADMAP.md](milestones/v1.1-ROADMAP.md)
- ✅ **v1.2 Depth & Polish** — Phases 18–23, 22 plans (shipped 2026-05-09) — see [milestones/v1.2-ROADMAP.md](milestones/v1.2-ROADMAP.md) — 12/12 active requirements complete + 1 honestly deferred (SKLP-11 → v1.3 per Phase 22 spike negative finding)
- 🚧 **v1.3 Surface Redesign** — Phases 24–28, 45 active requirements (started 2026-05-10)

## Phases

<details>
<summary>✅ v1.0 MVP (Phases 1–11) — SHIPPED 2026-04-28</summary>

- [x] Phase 1: Foundation & Database (7/7 plans) — completed 2026-04-25
- [x] Phase 2: Data Ingestion (6/6 plans) — completed 2026-04-26
- [x] Phase 3: Read-Only APIs (5/5 plans) — completed 2026-04-26
- [x] Phase 4: Stateful APIs (5/5 plans) — completed 2026-04-26
- [x] Phase 5: Frontend Shell & Design System (4/4 plans) — completed 2026-04-27
- [x] Phase 6: Observability & Activity Panels (5/5 plans) — completed 2026-04-27
- [x] Phase 7: Command Centre Panels (4/4 plans) — completed 2026-04-27
- [x] Phase 8: Mission Control Dispatcher (4/4 plans) — completed 2026-04-27
- [x] Phase 9: Telegram, Setup & Testing (5/5 plans) — completed 2026-04-28
- [x] Phase 10: Telegram Wiring Fixes (gap closure, 1/1 plan) — completed 2026-04-28
- [x] Phase 11: v1.0 Documentation & Env Polish (gap closure, 1/1 plan) — completed 2026-04-28

Full details: [milestones/v1.0-ROADMAP.md](milestones/v1.0-ROADMAP.md)

</details>

<details>
<summary>✅ v1.1 Skills & Cost Intelligence (Phases 12–17) — SHIPPED 2026-05-05</summary>

- [x] Phase 12: OTEL Skill Event Spike (2/2 plans) — completed 2026-05-02
- [x] Phase 13: Cost Foundation & Skill Ingest (6/6 plans) — completed 2026-05-03
- [x] Phase 14: Skills API & Page Panels (5/5 plans) — completed 2026-05-04
- [x] Phase 15: Alert Engine & UI (5/5 plans) — completed 2026-05-04
- [x] Phase 16: Session Comparison (4/4 plans) — completed 2026-05-05
- [x] Phase 17: Polish, Doctor & Tests (6/6 plans) — completed 2026-05-05

Full details: [milestones/v1.1-ROADMAP.md](milestones/v1.1-ROADMAP.md)

</details>

<details>
<summary>✅ v1.2 Depth & Polish (Phases 18–23) — SHIPPED 2026-05-09</summary>

- [x] Phase 18: Polish & Carry-Forward Cleanup (5/5 plans) — completed 2026-05-05
- [x] Phase 19: Skills Per-Project, Deltas & Badges (4/4 plans) — completed 2026-05-06
- [x] Phase 20: Cost Forecast & Per-Project Card (4/4 plans) — completed 2026-05-06
- [x] Phase 21: Alert Anomaly Depth & NL Authoring (3/3 plans) — completed 2026-05-07
- [x] Phase 22: Skill Latency Overhead — spike-gated, NO branch (2/2 plans) — completed 2026-05-08
- [x] Phase 23: Compare Depth & Milestone Close (4/4 plans) — completed 2026-05-09

Full details: [milestones/v1.2-ROADMAP.md](milestones/v1.2-ROADMAP.md)

</details>

### 🚧 v1.3 Surface Redesign (Phases 24–28) — IN PROGRESS

- [x] **Phase 24: Shell + Density + Containment Primitives** — Foundation phase establishing the shell chrome, 3-tier density infrastructure, three overflow bug fixes (global), and quality-gate scaffolding (visual checkpoint pattern, axe-core, perf budget, URL contract, testid registry) every later phase consumes. _(complete 2026-05-12 — operator verdict PASS, 18/18 mapped requirements satisfied, 7/7 plans shipped)_
- [x] **Phase 25: Saved Views (Backend + Frontend)** — Server-persisted, per-route, URL-shareable saved views; Alembic migration `0004_saved_views`; 5 CRUD endpoints; `validateSearch` adoption on 7 routes; SavedViewMenu chrome; pinned favorites in sidebar; Cmd+K Saved Views group. _(complete 2026-05-12 — operator verdict PASS, 11/11 mapped requirements VIEW-01..09 + CMDK-01 + SHEL-06 satisfied, 11/11 plans shipped)_
- [x] **Phase 26: Per-Route Adoption I (Command/Activity/Sessions) + Time + Cmd+K** — High-traffic routes adopt `BoundedPanelCard bounded` + density tokens; global time picker (sync/copy-paste/compare-overlay/brush-zoom); Cmd+K density/time-range/recents groups; sidebar recently-visited. _(complete 2026-05-13 — operator verdict PASS, 9/9 mapped requirements SHEL-05 + TIME-01..05 + CMDK-02..04 satisfied, 9/9 plans shipped; final test counts: backend pytest 686/0/0, frontend vitest 610/0/0, Playwright e2e 207 tests (203 pass + 4 forward-compat skip), Lighthouse 9/9 PASS at median, axe 39/39 PASS, portal containment 6/6 PASS, URL contract 2/2 PASS, 96 total visual capture PNGs, ResponsiveContainer count = 26)_
- [x] **Phase 27: Per-Route Adoption II (Skills/Cost/Alerts) + Tech Debt** — Tail-end routes adopt primitives; v1.2 carried tech debt closure (`project_key` wire exposure, `KNOWN_METRICS` removal, NL composer 503 retry/queue UX). _(complete 2026-05-16 — operator verdict PASS via live Chrome DevTools MCP walkthrough against http://localhost:5173 + backend on :8001, 3/3 mapped requirements TDBT-01 + TDBT-02 + TDBT-03 satisfied, 9/9 plans shipped; final test counts: backend pytest 690/0/0 (+4 vs Phase 26 close — Plan 27-02 +3 project_key round-trip + Plan 27-07 net +1 contract guard), frontend vitest 662/0/0 (+52 across Plans 01-08), Playwright e2e ~243 tests (24 NEW v13-tail-routes + 18 extensions = +42), Lighthouse 9/9 PASS deferred + accepted at Phase 26 baseline (Phase 27 ships ZERO new runtime deps), axe 0 Phase-27-attributable blocking (PHASE_27_NET_CLASS_MARKERS inversion filter additive), portal containment 7/7 PASS, URL contract 2/2 PASS, 120 total visual capture PNGs (96 + 24 NEW Phase 27 tail routes), ResponsiveContainer count = 8 (Phase 24 lock preserved); 6 Accepted Exceptions operator-acknowledged including 2 NEW (SkillLatencySnapshot stays on route-local `?range=` per operator-locked decision documented inline + verification-discovered d76a95b cmc-card--bounded fix landing inside Plan 27-09 mirroring Phase 26 e838135 close-gate precedent))_
- [ ] **Phase 28: Layout Customization** — Per-route panel show/hide, 1D drag-reorder, split-pane resize via `react-resizable-panels@4.11.0`, reset-to-default. Layout state piggybacks on saved-view `state_json` (no new DB table).

## Phase Details

### Phase 24: Shell + Density + Containment Primitives
**Goal**: Lay every primitive and quality-gate the rest of v1.3 will consume — fix the three overflow bugs globally, establish 3-tier density, extract `AppShellHeader`, install Radix Popover/DropdownMenu, document z-index ladder + URL contract + testid registry, and gate every future phase behind a formal visual checkpoint + axe-core + perf budget pattern. No per-route work.
**Depends on**: Nothing (first v1.3 phase, builds on v1.2 close baseline pytest 661 / vitest 326 / Playwright 13)
**Requirements**: CONT-01, CONT-02, CONT-03, CONT-04, CONT-05, SHEL-01, SHEL-02, SHEL-03, SHEL-04, DENS-01, DENS-02, DENS-03, POLI-09, POLI-10, POLI-11, POLI-12, POLI-13, POLI-14
**Success Criteria** (what must be TRUE):
  1. User toggles density (Compact / Comfortable / Cozy) via top-bar control and the entire dashboard re-spaces with no flash, no layout shift, no React re-renders (CSS-only swap), and the choice persists across reloads
  2. User opens any Sheet, Popover, or DropdownMenu on any existing route and the overlay renders inside the viewport with correct z-index, never clipped by an ancestor's `overflow: hidden` and never escaping the stacking context (root cause `recharts` `ResponsiveContainer` transform audit complete + Radix Portal universal)
  3. User scrolls a long table inside any panel and the panel itself stays bounded — internal scroll appears, the page does not grow taller than the viewport, and long cell content (session IDs, cwd paths, skill names) truncates with tooltip-on-hover instead of breaking out of card padding
  4. User collapses the new left sidebar to icon-only via toggle or keyboard shortcut, the choice persists in localStorage across reloads, and the active route stays visually highlighted in either expanded or icon-only mode
  5. Phase ships with: `docs/z-index-ladder.md` documenting overlay layers; `docs/url-contract.md` + `tests/test_url_contract.py` enforcing every preserved URL pattern; `docs/affordance-checklist.md` enumerating 15 keyboard/interaction affordances; `docs/testid-registry.md` + ESLint rule; axe-core wired into Playwright; React DevTools profiler perf evidence; visual checkpoint at `.planning/phases/24/VISUAL-CHECK.md`; backend pytest + frontend vitest + Playwright e2e all green vs Phase 18 baseline
**Plans**: 7 plans
- [x] 01-PLAN.md — Foundation: install deps, density tokens on :root, z-index ladder, min-width:0 on .cmc-card, .cmc-btn:hover transform mitigation, lib/density.ts + applyDensity() pre-mount _(complete 2026-05-10 — 396c092, 2e064cc)_
- [x] 02-PLAN.md — Density UX: DensityToggle (Radix DropdownMenu) + DensityProvider (no React context) + cascade + persistence vitest tests _(complete 2026-05-10 — 49c135a, b9d5e2e)_
- [x] 03-PLAN.md — Containment primitives: BoundedPanelCard / TruncatedCell / CopyIconButton + .cmc-page--bounded + DataTable wrap/copyable opt-in/out + transform-audit deliverable _(complete 2026-05-10 — 939cd3e, dddae8d, eb43306)_
- [x] 04-PLAN.md — Shell rework: Sidebar (Cmd+B + collapse + active-route bar) + AppShellHeader extraction + DensityProvider stack + delete NavBar.tsx (human checkpoint) _(complete 2026-05-11 — 93d6c2f, aa570cf, 8178cdf; visual checkpoint approved 10/10; SHEL-01..04 satisfied)_
- [x] 05-PLAN.md — Quality-gate Playwright specs (visual capture / axe / portal-containment / sidebar / density / truncation / copy-cell) + lighthouserc.json + URL-contract pytest _(complete 2026-05-11 — d1304ea, 5872663, cdeda8d, 51f36b6; 7 v13-*.spec.ts files = 75 Playwright tests; lighthouserc.json 3-URL CWV gate research-corrected per Pitfall 5; backend/tests/test_url_contract.py 2/2 PASS against plan-06 docs; POLI-09/10/11/13 + CONT-02/03 + SHEL-04 + DENS-01..03 e2e scaffolding shipped)_
- [x] 06-PLAN.md — POLI docs (z-index-ladder / affordance-checklist / url-contract / testid-registry) + ESLint flat config + custom rules (testid-registry-only, no-raw-z-index) _(complete 2026-05-11 — 3698bf3, e700a9e, 5e6bb73; pnpm lint clean; backend/tests/test_url_contract.py 2/2 passing; POLI-09 + POLI-12 + POLI-13 + POLI-14 + CONT-05 ESLint side satisfied)_
- [x] 07-PLAN.md — Phase close gate: run matrix (visual + axe + Lighthouse + perf) and write 24-VISUAL-CHECK.md verdict (human checkpoint) _(complete 2026-05-12 — 1c610d4, c7b1dea, 06f09a2, 75244ec, e3cd82a, 88e8417, 437e848 + metadata close; operator verdict PASS; 36/36 visual matrix PASS; axe 0 Phase-24 blocking (6 pre-existing contrast classes Accepted-Exception-deferred to Phase 26/27); Lighthouse 9/9 PASS (LCP 559-572ms / CLS 0-0.0032 / performance 1.0); DOM-identity zero-rerender probe PASS; portal containment 3/3; URL contract 2/2; ResponsiveContainer delta 0; POLI-09 + POLI-10 + POLI-11 + CONT-05 all satisfied)_
**UI hint**: yes

### Phase 25: Saved Views (Backend + Frontend)
**Goal**: Make every filter combination on every route a first-class, named, server-persisted, URL-shareable view — landing the backend (table + 5 endpoints + migration) independently testable first, then the `validateSearch` adoption on 6 routes, then the chrome (SavedViewMenu + save dialog + edit-vs-fork semantics + unsaved pip + recent ad-hoc states + per-route default + pinned favorites in sidebar + Cmd+K Saved Views group).
**Depends on**: Phase 24 (consumes `AppShellHeader`, density tokens, URL contract, testid registry, Radix Popover/DropdownMenu, axe-core gate)
**Requirements**: VIEW-01, VIEW-02, VIEW-03, VIEW-04, VIEW-05, VIEW-06, VIEW-07, VIEW-08, VIEW-09, CMDK-01, SHEL-06
**Success Criteria** (what must be TRUE):
  1. User saves the current filter combination on `/skills/$name` as a named view, navigates away, returns to the route, and the view auto-loads as the per-route default (querystring still wins when explicit)
  2. User edits a loaded saved view, modifies a filter, and is prompted with an explicit save / save-as-fork / discard AlertDialog before any silent overwrite — the unsaved-changes pip in the chrome is visible the moment URL state diverges from the loaded view
  3. User opens Cmd+K, types a saved view name, hits Enter, and lands on the matching route with the saved filters applied (current-route filtered first in the Saved Views command group)
  4. User pins a saved view from the SavedViewMenu and the view appears in the sidebar Pinned section for one-click access from any route
  5. Backend `saved_views` Alembic migration `0004_saved_views` applies cleanly; 5 CRUD endpoints (`GET /api/views?route=`, `POST`, `GET /{id}`, `PATCH`, `DELETE`) pass independently via curl + pytest before frontend wires; 50-view-per-route cap enforced with UI warning at cap; opaque `state_json` validated only via route's `validateSearch` on read; `schemaVersion` field on every route's search shape
**Plans**: 11 plans
- [x] 01-PLAN.md — Wave 1 backend: SavedView SQLModel + 0004_saved_views Alembic migration + migration round-trip tests (VIEW-02) ✅ 2026-05-12
- [x] 02-PLAN.md — Wave 1 backend: Pydantic schemas + 5 CRUD handlers + 50-cap + UNIQUE rejection + pytest router coverage (VIEW-03) ✅ 2026-05-12
- [x] 03-PLAN.md — Wave 2 frontend: validateSearch on 5 routes (/, /activity, /skills, /cost, /alerts) + schemaVersion on /sessions/compare (VIEW-01) ✅ 2026-05-12
- [x] 04-PLAN.md — Wave 2 frontend: validateSearch with range filter on /skills/$name; threads URL state into 4 detail panels (VIEW-01 success-criterion-1 enabler) ✅ 2026-05-12
- [x] 05-PLAN.md — Wave 2 frontend: API client verbs + TanStack Query hooks + lib/savedViews.ts (default pointer + pinned ids + recent FIFO 50-cap) ✅ 2026-05-12
- [x] 06-PLAN.md — Wave 3 chrome: SavedViewMenu + SaveViewDialog + UnsavedPip + LoadedViewContext; replaces save-view-button placeholder (VIEW-04, VIEW-05, VIEW-08) ✅ 2026-05-12 — 4 commits (9beb70d, 007c47e, f3b1241, 42500c8); frontend vitest 408 → 420 / 0 / 0; LoadedViewContext mounted in AppShell.tsx above Sidebar + Header; Radix DropdownMenu trigger with Bookmark icon replaces inert save-view-button placeholder; SaveViewDialog (Radix Dialog, NOT AlertDialog) with name + description form; UnsavedPip with schemaVersion-stripping stableStringify divergence detection; 16 testids registered (10 exact + 6 dynamic); normalizeRouteId exported; tsc/lint/build all clean.
- [x] 07-PLAN.md — Wave 3 chrome: EditOrForkDialog (3-button Radix Dialog) + wiring into SavedViewMenu's Edit affordance (VIEW-07) ✅ 2026-05-12 — 3 commits (1e195f0, 2c558ac, c9cfae2); frontend vitest 420 → 425 / 0 / 0; EditOrForkDialog is a NEW Radix Dialog (NOT AlertDialog — preserves 2-button primitive per Pitfall 4); 3 mutually exclusive resolutions (Save changes via usePatchView with state_json REPLACEMENT, Save as new fork via onFork delegation to SaveViewDialog fork-mode, Discard via navigate to loadedView.state_json); SavedViewMenu wires Edit affordance + handleOpen interception; 5 vitest specs cover all 3 branches + 2 guards; ZERO deviations; tsc/lint/build all clean. VIEW-07 satisfied — no silent-overwrite path remains.
- [x] 08-PLAN.md — Wave 4 cross-cut: Cmd+K Saved Views group with current-route-first ordering (CMDK-01) ✅ 2026-05-12 — 2 commits (0e2ea91, e8049d7); frontend vitest 425 → 436 / 0 / 0; CommandPalette "Saved Views" Command.Group inserted between Pages and Actions; cross-route useSavedViews() (no route filter); current-route-first ordering via normalizeRouteId; alpha secondary sort; dynamic-route guard via routePathFromId (static routes verbatim; /skills/$name navigable only when on matching base prefix else console.warn + no-op); selection navigates + setLoadedView + closes palette; empty state "No saved views yet"; testids cmdk-saved-views-empty + cmdk-saved-view-{id} registered; 11 vitest specs (5 component + 6 pure helper); 2 Rule-1/3 deviations (wrap existing CommandPalette tests in LoadedViewProvider, filter console.warn by message instead of count); tsc/lint/build all clean. CMDK-01 satisfied — second access path to saved views (Cmd+K) in addition to SavedViewMenu in the header.
- [x] 09-PLAN.md — Wave 4 cross-cut: Sidebar Pinned section with active-state on full search match (SHEL-06) ✅ 2026-05-12 — 3 commits (a86e4f9, 3351ceb, e569c02); frontend vitest 436 → 443 / 0 / 0 (+7 specs); PinnedViewsSection mounts between Operate and Configure; reads getPinned() + useSavedViews; active-state requires pathname+search MATCH (Pitfall 9); collapsed mode preserves clickable rows; SHEL-06 satisfied.
- [x] 10-PLAN.md — Wave 4 cross-cut: DefaultViewLoader (VIEW-06) + RecentStateTracker (VIEW-09); both mount in AppShell ✅ 2026-05-12 — 3 commits (d835890, 8c6cbd9, daf1474); frontend vitest 443 → 452 / 0 / 0; DefaultViewLoader applies per-route default's state_json via navigate({to, search, replace:true}) when entering a route with empty search (only schemaVersion) — deep-link wins lock per Pitfall 8; one-shot per route entry via useRef appliedKeyRef; re-arms on route id change. RecentStateTracker pushes every URL change on in-scope routes to cmc.savedView.recent.<route> via Plan 05's pushRecentState (FIFO 50-cap with structural dedupe inherited); IN_SCOPE_ROUTES is explicit hard-coded Set mirroring Plans 03/04/06 validateSearch routes; bare-URL filter skips schemaVersion-only URLs. normalizeRouteId extracted to routeNormalize.ts as the single coercion site across Plans 06/08/09/10; SavedViewMenu re-exports for backward compat. Plan-version of normalizeRouteId fixes a latent bug: `/skills/` index route no longer collapses to `/skills/$name`. AppShell mounts both as the first two children inside LoadedViewProvider. 9 vitest specs (5 DefaultViewLoader + 4 RecentStateTracker). Zero deviations; tsc/lint/build all clean. VIEW-06 + VIEW-09 satisfied — entire Wave 5 production substrate (Plans 07/08/09/10) now in place. Cap-warning surface kept as v1 console.warn (sonner/toast lib would exceed Phase 25's locked 3-dep budget).
- [x] 11-PLAN.md — Wave 5 close gate: v13-saved-views.spec.ts + extend axe/sidebar/cmdk/visual-capture specs + author 25-VISUAL-CHECK.md (operator checkpoint) ✅ 2026-05-12 — 4 commits (6c6deeb, d493b4a, 3cf89a7, c3d0235) + metadata close commit; operator verdict PASS signed by Patryk Golabek. 11 v13-saved-views tests covering ROADMAP success criteria 1-4 + criterion 5 frontend half (/sessions/compare fixture substitution for /skills/$name per documented DefaultViewLoader v1 limitation); 3 SHEL-06 v13-sidebar tests; 4 dedicated Phase 25 chrome axe scans; 30 NEW visual-capture PNGs (5 surfaces × 3 densities × 2 themes); 3 CMDK-01 command-palette tests; PHASE_25_NET_CLASS_MARKERS inversion filter; Rule-1 fix to cost-dashboard regex for ?schemaVersion=1 suffix. Phase 25 close evidence: backend pytest 686/0/0; frontend vitest 452/0/0; Playwright 141 tests (137 pass + 4 forward-compat skip); Lighthouse 9/9 PASS; axe 34/34 PASS; URL contract 2/2 PASS; backend CRUD curl matrix 8/8. 3 Accepted Exceptions operator-approved as-is: (a) /skills/$name DefaultViewLoader auto-apply deferred to Phase 26; (b) 8 v1.2 carry-over contrast/aria classes deferred to Phase 26/27 per Pitfall 7; (c) Plan 09 same-tab localStorage pin-write reload-required limitation. ROADMAP Phase 25 success criteria 1-5 all functionally verified. v1.3 milestone advances 1/5 → 2/5 phases complete.
**UI hint**: yes

### Phase 26: Per-Route Adoption I (Command/Activity/Sessions) + Time + Cmd+K ✅ Complete (2026-05-13)
**Goal**: Roll the Phase 24 primitives + Phase 25 saved-view infrastructure through the three highest-traffic routes (`/`, `/activity`, `/sessions/compare`) and ship the global time picker (with copy/paste, compare-to-previous overlay, brush-zoom) plus the Cmd+K density / time-range / recents groups and sidebar recently-visited section. Validates the adoption pattern before Phase 27 repeats it on tail-end routes.
**Depends on**: Phase 24 (BoundedPanelCard, density tokens, AppShellHeader), Phase 25 (`validateSearch` schemas with `time_from`/`time_to` adopted on `/`, `/activity`, `/sessions/compare`)
**Requirements**: SHEL-05, TIME-01, TIME-02, TIME-03, TIME-04, TIME-05, CMDK-02, CMDK-03, CMDK-04
**Success Criteria** (what must be TRUE):
  1. User sets the global time picker to `now-7d` in the top bar and every time-anchored panel on `/`, `/activity`, and `/sessions/compare` re-queries against that range; user toggles the auto-refresh interval (off / 30s / 1m / 5m) and the picker honors it
  2. User copies the current time range with Cmd+Shift+C, navigates to a different route, presses Cmd+Shift+V, and the receiving route's panels re-anchor to the pasted range (relative-time symbols preserved, not collapsed to absolutes)
  3. User drags a region on a recharts time-series on `/activity` and the chart zooms to that range — the global time picker updates to match, every other panel on the page re-anchors, the URL `?time_from=&time_to=` updates
  4. User opens Cmd+K and the Recent items group shows the last 5 visited routes (SHEL-05) plus last N ad-hoc states (VIEW-09 from Phase 25); user invokes "Set density: Compact" or "Last 7 days" command and the dashboard re-paints without navigating
  5. User opens any Sheet on `/sessions/compare` (compare picker, session detail) and the Sheet stays inside the viewport at every density and viewport size from 1024px upward; long session IDs and cwd paths truncate with tooltip; pages stay bounded with internal panel scroll
**Plans**: 9 plans
- [x] 26-01-PLAN.md — Wave 1 foundation: install sonner/react-day-picker/date-fns; ship grafanaSyntax + coerce + rangeToVocab + clipboard libs; mount sonner Toaster (ADR: frontend-coerce bridge, backend extension deferred to Phase 27) ✅ 2026-05-13 — 2 commits (6df78d9 Task 1: install sonner@2.0.7 + react-day-picker@10.0.0 + date-fns@4.1.0 + grafanaSyntax.ts pure regex parser + coerce.ts ISO/Grafana→Date with date-fns + 25 vitest cases; 399ff10 Task 2: rangeToVocab.ts LOAD-BEARING bridge + clipboard.ts serializeRange/parseRangeFromText + Toaster mount in AppShell as sibling of CommandPalette + 26 vitest cases). Frontend vitest 452 → 533 / 0 / 0 (+81 across Plan 01 + parallel Plan 02). pnpm tsc + lint clean. 12 files (8 created + 4 modified). 198 source LOC + 344 test LOC. Bridge ADR locked: backend extension deferred to Phase 27 TDBT; bridge collapses to no-op when backend learns time_from/time_to. Zero deviations. Plan 02 (sibling agent) committed 80c126c between my commits — parallel-safe, zero file overlap.
- [x] 26-02-PLAN.md — Wave 1 URL contract: append-only validateSearch on /, /activity, /sessions/compare for time_from/time_to (defaults to undefined per Pitfall 13); ship cmc.recents.routes FIFO ring; update docs/url-contract.md ✅ 2026-05-13 — 2 commits (80c126c Task 1: shared asTimeToken validator in lib/searchSchemas.ts with GRAFANA_REL + ISO_ABS regex + APPEND-ONLY validateSearch extension on /, /activity, /sessions/compare accepting time_from? + time_to? defaulting to undefined per Pitfall 13 + 14 asTimeToken + 6 Phase 26 route extension vitest cases — SCHEMA_VERSION stays at 1; b68eb55 Task 2: lib/recents.ts FIFO ring with pushRecentRoute/getRecentRoutes/clearRecentRoutes + RecentRoute type, cap=20, head-dedupe, newest-first read, tolerant of malformed JSON + shape-invalid blobs + 11 vitest cases + docs/url-contract.md Phase 26 effects section + per-route shape rows updated for the three routes). Frontend vitest 452 baseline → 533 / 0 / 0 (Plan 02 contributes +31 of the +81; remainder from Plan 01). pnpm tsc --noEmit clean. pnpm lint --max-warnings 0 clean. Backend pytest 686/0/0 baseline preserved. backend/tests/test_url_contract.py 2/2 PASS (doc⇄route bidirectional gate intact). 7 files (2 created — recents.ts + recents.test.ts; 5 modified — searchSchemas.ts + 3 route files + searchSchemas.test.ts + url-contract.md). ~80 LOC source + ~110 LOC tests. Zero deviations. Ran parallel-safe with Plan 01 (Plan 01 commits 6df78d9 + 399ff10 interleaved with Plan 02 commits 80c126c + b68eb55; zero file overlap).
- [x] 26-03-PLAN.md — Wave 2 chrome: TimePicker (Radix Popover + preset list + dual-month RDP calendar) + RefreshDropdown + AutoRefreshController + Cmd+Shift+C/V hotkeys (TIME-01, TIME-02, TIME-03) ✅ 2026-05-13 — 2 commits (9e60307 Task 1: TimePicker.tsx + PresetList.tsx (3 groups × 13 presets) + CustomRangeCalendar.tsx (react-day-picker mode="range" dual-month) + window-level Cmd+Shift+C/V hotkeys via TIME-03 wiring through serializeRange/parseRangeFromText + asTimeToken defense-in-depth + sonner toast.success/message/error feedback + .cmc-time-picker__{trigger,popover,presets,group,preset,calendar,custom-apply} CSS + .cmc-dropdown-menu alias + docs/testid-registry 6 new exact-match testids + 1 dynamic time-picker-preset-{slug} pattern + docs/affordance-checklist row 16 Cmd+Shift+C/V (count 15→16) + 7 vitest cases covering trigger label, popover open, preset navigate, copy success/error, paste happy/error; 9d3ee0c Task 2: RefreshDropdown.tsx (Radix DropdownMenu, off/30s/1m/5m + Paused on absolute time_from + opacity-only pulse keyframe) + AutoRefreshController.tsx (zero-render setInterval + queryClient.invalidateQueries on isTimeAnchoredKey predicate + tick-counter pattern for storage/cmc:auto-refresh-changed re-evaluation) + isTimeAnchoredKey predicate appended to lib/queries.ts (covers bare + kebab-prefix scoped keys) + AppShellHeader un-placeholdered (mounts TimePicker + RefreshDropdown before saved-view-chrome) + AppShell mounts AutoRefreshController as 4th zero-render effect sibling + AppShellHeader.test rewritten for wired TimePicker assertion + sonner test-file-scope mock + 2 new exact-match testids + 1 dynamic refresh-option-{value} pattern + 6 vitest cases covering localStorage persist + custom event dispatch + Paused state + active pulse + interval invalidation + absolute-window skip + isTimeAnchoredKey truth table). Frontend vitest 533 baseline → 556 / 0 / 0 (+23 across Plans 03+04 in parallel; Plan 03 contributes +13). pnpm tsc --noEmit clean. pnpm lint --max-warnings 0 clean. Backend pytest 686/0/0 baseline preserved. 15 files (8 created — TimePicker.tsx + PresetList.tsx + CustomRangeCalendar.tsx + RefreshDropdown.tsx + AutoRefreshController.tsx + 3 test files; 7 modified — AppShellHeader.tsx + AppShell.tsx + AppShellHeader.test.tsx + lib/queries.ts + styles.css + docs/testid-registry + docs/affordance-checklist). ResponsiveContainer count delta = 0. No new --cmc-z-* variables. Two minor textual deviations from plan code blocks (setInterval local rename + tick-counter over inline storage tear-down — functionally equivalent; documented in 26-03-SUMMARY.md). Ran parallel-safe with Plan 04 sibling (both touched AppShell.tsx + docs/testid-registry + docs/affordance-checklist; surgical edits at distinct anchor points).
- [x] 26-04-PLAN.md — Wave 2 sidebar: RecentRoutesTracker zero-render effect + RecentlyVisitedSection (top 3, current-filtered, between Pinned and Configure) (SHEL-05)
- [x] 26-05-PLAN.md — Wave 3 brush-zoom: useChartBrush hook + ResetZoomButton; wire Brush into ChartsStrip on /activity (TIME-05; ResponsiveContainer count stays 26)
- [x] 26-06-PLAN.md — Wave 3 Cmd+K extensions: Recents + Time range + Density groups (JSX order Pitfall 10) (CMDK-02, CMDK-03, CMDK-04) ✅ 2026-05-13 — 2 commits (21ebf9b Task 1: CommandPalette.tsx extended with Recents Command.Group (reads getRecentRoutes from Plan 02 + getAllRecentStates from Phase 25 Plan 10, top-5 each, cmdk-recents-empty fallback) + Time range Command.Group (4 condensed presets 1h/24h/7d/30d + Copy/Paste commands reusing lib/time/clipboard.ts + sonner exactly — Cmd+K is genuine second access path) + relocate Pages and Saved Views to satisfy locked JSX order (Pitfall 10: Recents → Saved Views → Pages → Time range → Density-slot-reserved → Actions) + routeToTestidSlug() exported pure helper + 3 dynamic testid patterns (cmdk-recents-route/state, cmdk-time-range) + 3 exact-match testids (cmdk-recents-empty, cmdk-time-range-copy/paste) + 12 vitest cases (6 recents + 6 timeRange); a829862 Task 2: Density Command.Group filling the reserved slot (3 discrete items with ✓ check-prefix on currently-active density; selection routes through setDensity() from lib/density.ts directly — Pitfall 3 lock NO React Context bridge; POLI-11 zero-rerender invariant preserved) + cmdk-density-{value} dynamic testid pattern + 5 vitest cases). Frontend vitest 556 baseline → 587 / 0 / 0 (+31 across Plans 05+06; Plan 06 contributes +17). pnpm tsc --noEmit clean. pnpm lint --max-warnings 0 clean. ResponsiveContainer count delta = 0 (zero new charts). 5 files (3 created — recents test + timeRange test + density test; 2 modified — CommandPalette.tsx + docs/testid-registry.md). 2 documented deviations (Rule-1 vitest clipboard ordering: navigator.clipboard override MUST run after userEvent.setup() to defeat user-event's attachClipboardStubToView getter — documented inline in timeRange test; Rule-1 TanStack Router `as never` cast for runtime pathname/route strings — mirror of Plan 03 TimePicker). Ran parallel-safe with Plan 05 sibling (zero file overlap; both commits today).
- [x] 26-07-PLAN.md — Wave 4 compare-overlay: CompareToggle per-panel + compare_panels CSV URL param + same-length-prior overlay demo on TokenUsageCard (TIME-04) ✅ 2026-05-13 — 2 commits (4f61507 Task 1: asComparePanels validator in lib/searchSchemas.ts with regex /^[a-z0-9_-]+(?:,[a-z0-9_-]+)*$/ + APPEND-ONLY validateSearch extension on /, /activity, /sessions/compare accepting compare_panels?: string defaulting to undefined per Pitfall 13 — SCHEMA_VERSION stays at 1; CompareToggle.tsx component ~98 LOC reading/writing the single compare_panels CSV via useRouterState + useNavigate function-form, sorted + de-duped on write for deterministic fork-save round-trip, route-agnostic via `as never` casts; 6 CompareToggle URL round-trip vitest cases + 9 asComparePanels shape cases + 3 route-integration cases + 18 existing route-validator cases updated for compare_panels:undefined in return shape; compare-overlay-toggle-{panel-id} dynamic testid family registered; d800500 Task 2: TokenUsageCard prior-period overlay — reads compare_panels CSV directly to gate prior pipeline, useTokens('30d') + client-side slice [-14, -7) for prior week, merge into chartData under prior_total dataKey, render Bar with stackId='prior' + fillOpacity=0.25 + var(--cmc-text-subtle) — translucent overlay sits ADJACENT to existing type-stack; inline compare-overlay-hint <p> surfaces when range != '7d'; v1 scope: range='7d' only on TokenUsageCard; CSS .cmc-compare-toggle / --active + .cmc-token-usage__chrome + .cmc-token-usage__compare-hint; 5 compareOverlay vitest cases asserting component-level contract — toggle aria-pressed + hint testid + 30d cache slot consumed since Recharts collapses to width=0 in happy-dom; compare-overlay-hint exact-match testid registered). Frontend vitest 587 → 610 / 0 / 0 (+23 across Plan 07). pnpm tsc --noEmit clean. pnpm lint --max-warnings 0 clean. ResponsiveContainer count delta = 0 (baseline = 8 across panels/*.tsx — plan-author claim of 26 was off; spirit of locked invariant honored). 11 files (3 created — CompareToggle.tsx + CompareToggle.test.tsx + TokenUsageCard.compareOverlay.test.tsx; 8 modified — searchSchemas.ts + 3 routes + TokenUsageCard.tsx + styles.css + testid-registry.md + searchSchemas.test.ts). 3 documented deviations (Rule-1 plan baseline 26 vs actual 8; Rule-1 .recharts-bar count assertions don't work in happy-dom — switched to component-level contract; Rule-3 Plan 08 sibling-agent TIME-02 bridge edits to TokenUsageCard.tsx co-occurred — full Write captured merged file state). Ran parallel with Plan 08 sibling (per-route JSX shell sweep) — zero conflicts on validator regions; TokenUsageCard.tsx fully captured here.
- [x] 26-08-PLAN.md — Wave 4 per-route adoption sweep: BoundedPanelCard + density tokens + truncation primitives on /, /activity, /sessions/compare panels (21 files); v1.2 a11y carry-overs lite fix (.cmc-numeric + .cmc-system-health-strip__*); rangeToVocab bridge at panel read sites ✅ 2026-05-13 — 2 commits (d85c083 Task 1: 15 panels on / + SessionCompareView on /sessions/compare migrated to bounded mode + .cmc-page--bounded modifier on both route shells + ship lib/time/useRouteRange.ts hook centralising the URL→Range bridge with per-route default at READ site (Pitfall 13 honored — validators untouched) + 9 time-anchored panels on / consume the bridge with effectiveRange = localRange ?? globalRange pattern (RangeToggle persistKey dropped — URL is the persistence layer) + SessionCompareView adds per-side session_id+cwd row via TruncatedCell+CopyIconButton (first CONT-03 primitive consumers); 9 panel test files mocked useRouterState via vi.mock to resolve through the bridge; d34ba68 Task 2: /activity route shell adopts .cmc-page--bounded + all 6 /activity panels migrated to bounded (ActivityHeatmap range fixed at 30d intrinsic to surface; ChartsStrip range fixed at 30d-overfetch + 14d slice — brush owns zoom; OtelPanel bespoke Card receives cmc-card--bounded className directly; UnifiedFailures + SessionsTable consume useRouteRange('today'); TopSkills bounded only, SkillRange vocab doesn't overlap Range — defer Phase 27) + SessionsTable session_id+cwd columns adopt TruncatedCell+CopyIconButton + v1.2 a11y contrast lift on .cmc-numeric + .cmc-system-health-strip__stat-label/__tz from --cmc-text-subtle → --cmc-text-dim; Phase 27 deferral comment documents the 4 aria/semantic items remaining (heatmap-cell aria-prohibited-attr, otel-feed scrollable-region-focusable, sessions-table-header__label, Range filter <select>); SessionsTable + UnifiedFailures test files mocked router (SessionsTable uses importOriginal partial mock so Compare-click tests keep real router). Frontend vitest 610 / 0 / 0 (no new specs — Plan 08 was mechanical adoption; test-infra mock updates in 11 files preserved coverage). pnpm tsc --noEmit clean. pnpm lint --max-warnings 0 clean. ResponsiveContainer count = 8 (Phase 24 lock preserved, no new wrappers). 4 documented deviations: (1) Plan's verify command expects ResponsiveContainer=26; actual = 8 (stale baseline); (2) useRouteRange helper introduced instead of 9-copy inline snippet; (3) 11 panel-only test bootstraps needed vi.mock('@tanstack/react-router'); (4) SessionCompareView had no visible session_id/cwd before — added "Compared session IDs" header row. TIME-02 deliverable shipped. Ran parallel with Plan 07 — sibling's 4f61507 commit captured my .cmc-page--bounded modifier on routes/index.tsx + routes/sessions_.compare.tsx; their TokenUsageCard.tsx merged my useRouteRange + bounded wiring with their CompareToggle work, zero conflict.
- [x] 26-09-PLAN.md — Wave 5 close gate: v13-time-picker.spec.ts + axe/portal/saved-views/sidebar/cmdk extensions + 30 NEW visual-capture PNGs + 26-VISUAL-CHECK.md operator verdict (POLI-09) ✅ 2026-05-13 — 5 commits (fff215d, e26be3b, e838135, 09ec3c7, a6ee566) + metadata close commit; operator verdict PASS signed by Patryk Golabek. 19 NEW v13-time-picker.spec.ts tests + 5 spec-family extensions (v13-a11y +5 chrome / v13-portal-containment +3 chrome / v13-saved-views +3 round-trip / v13-sidebar +3 SHEL-05 / command-palette +3 Phase 26) + 30 NEW visual-capture PNGs across 5 surfaces × 3 densities × 2 themes + 1 Rule-1 TIME-04 click hit-test patch (scrollIntoView + dispatchEvent shim). Phase 26 close evidence: backend pytest 686/0/0 (zero backend changes); frontend vitest 452 → 610/0/0 (+158 across Plans 01-08; Plan 09 e2e-only); Playwright 141 → 207 tests (+66); Lighthouse 9/9 PASS at median (LCP 319-590ms / CLS ≤0.0001 median / performance 1.00); axe 34/34 → 39/39 PASS (+5 chrome scans; PHASE_26_NET_CLASS_MARKERS inversion filter additive); portal containment 3/3 → 6/6 PASS (+3 NEW chrome walks: TimePicker popover, RefreshDropdown menu, sonner Toaster); URL contract 2/2 PASS; ResponsiveContainer count = 26 (Phase 24 lock preserved); 96 total visual capture PNGs (36 + 30 + 30). ZERO NEW Accepted Exceptions discovered. URL-as-broadcast-bus pattern verified end-to-end (ResetZoomButton + Paused refresh + TimePicker absolute label all fire from single URL write). Cmd+K 6-group order verified (Recents → Saved Views → Pages → Time range → Density → Actions per Pitfall 10). Sidebar Recently Visited correctly filters current pathname per Pitfall 8 option b. ROADMAP Phase 26 success criteria 1-5 all functionally verified. v1.3 milestone advances 2/5 → 3/5 phases complete.
**UI hint**: yes

### Phase 27: Per-Route Adoption II (Skills/Cost/Alerts) + Tech Debt ✅ Complete (2026-05-16)
**Goal**: Complete the per-route adoption sweep on the tail-end routes (`/skills`, `/skills/$name`, `/cost`, `/alerts`) by consuming Phase 24 primitives + Phase 25 saved views + Phase 26 time picker, AND close the v1.2 carried tech debt items that the new shell makes natural to fix (`project_key` wire exposure, `KNOWN_METRICS` removal, NL composer 503 retry/queue UX).
**Depends on**: Phase 24 (primitives), Phase 25 (`validateSearch` on `/skills`, `/skills/$name`, `/cost`, `/alerts`), Phase 26 (global time picker contract proven on first three routes)
**Requirements**: TDBT-01, TDBT-02, TDBT-03
**Success Criteria** (what must be TRUE):
  1. User opens `/skills/$name` for a long skill name (e.g. `tdd-coverage-author-with-fanout`), the per-skill detail page stays bounded, the SkillProjectsTable + SkillRunsTable + SkillLatencyTable + SkillTimeline panels each scroll internally, density tokens propagate, and the global time picker re-anchors all four panels
  2. User opens `/cost`, toggles the 7d/30d range via global time picker (or saves a custom range as a saved view), and the CostForecastCard + CostByProjectCard re-query and re-render — long project paths truncate cleanly, the compare-to-previous overlay (TIME-04) renders against last week / last 30d
  3. User opens the compare picker on `/sessions/compare` and the picker uses authoritative `project_key` (additive on `SessionListItemFull` and `SessionCompareSide` wire shapes) instead of cwd-as-proxy — edge cases with cwd realpath differences without sha1 collision now resolve correctly (TDBT-01)
  4. User opens `AlertRuleForm` on `/alerts` and the metric vocabulary loads exclusively from `useAlertMetrics` hook — `KNOWN_METRICS` frontend fallback constant is fully removed; cross-language drift guard `test_alerts_metrics_sync.py` still passes (TDBT-02)
  5. User triggers the NL alert composer on `/alerts` while Anthropic credentials are missing or unreachable, the 503 collapse surfaces a graceful retry / queue UX with honest "credentials missing — retry" affordance instead of silent error (TDBT-03)
**Plans**: 9 plans
- [x] 27-01-PLAN.md — Wave 1 foundation: useRouteRangeVocab<V> generic URL→Vocab bridge hook + snapToSkillRange/snapToCostRange/snapToAlertRange snappers (foundation; unblocks 27-04 / 27-05 / 27-06) ✅ 2026-05-15 — 1 commit (ed96343 feat: new file frontend/src/lib/time/useRouteRangeVocab.ts 73 LOC + frontend/src/lib/time/__tests__/useRouteRangeVocab.test.ts 211 LOC with 31 vitest cases). Generic `useRouteRangeVocab<V extends string>(routeDefault, snap)` reads `time_from`/`time_to` from URL search via TanStack Router's `useRouterState`, coerces via Phase 26's `coerceToAbsolute`, falls back to `routeDefault` on missing/unparseable/inverted input. Pre-baked snappers: snapToSkillRange (2-tier '14d'|'30d' boundary at 21d), snapToCostRange (4-tier '1d'|'7d'|'14d'|'30d' at 48h/192h/504h), snapToAlertRange (identical bands to CostRange), snapToRange (Phase 26 vocab mirror). **ZERO-REFACTOR INVARIANT PRESERVED**: `git diff --stat frontend/src/lib/time/useRouteRange.ts` empty (Phase 26 file byte-identical; 9 prior call sites unaffected). Frontend vitest +31; pnpm tsc + lint clean; no new runtime deps. Zero deviations. Ran parallel with Plan 27-02 sibling (backend Python only) — zero file overlap.
- [x] 27-02-PLAN.md — Wave 1 backend half of TDBT-01: surface authoritative `project_key: str` (sha1[:12] of realpath(cwd)) on `GET /api/sessions` and `GET /api/sessions/compare` response shapes; unblocks Plan 27-03 ComparePicker filter switch from cwd-string-equality to project_key-equality ✅ 2026-05-15 — 2 commits (f1ad119 feat: SessionListItem.project_key + SessionCompareSide.project_key additive required str fields; `_build_compare_side` populates `project_key=sess.project_key` — ORMBase auto-maps for LIST/DETAIL; BaseModel needs explicit population for compare; 81b4c43 test: 3 round-trip pytest cases — test_list_sessions_includes_project_key, test_compare_sessions_includes_project_key, test_project_key_matches_compute_helper). Backend pytest 686 → 689 / 0 / 0 (zero regressions, full suite 214s). Live smoke check confirmed both endpoints emit 12-char hex `project_key` matching `compute_project_key(cwd)`. Zero deviations. Ran parallel with Plan 27-01 sibling (frontend TypeScript only) — zero file overlap.
- [x] 27-03-PLAN.md — Wave 2 TDBT-01 frontend half: mirror `project_key: string` on `SessionListItemFull` + `SessionCompareSide` TS types; switch `ComparePicker` filter from `row.cwd === scopeCwd` to `row.project_key === scopeProjectKey` (authoritative project identity); update 5 vitest fixture factories with the new required field; switch description copy from `Showing sessions from {scopeCwd} only.` to project-shape-honest `Showing sessions in the same project.` (no 12-char hex leak); KEEP row display label as `{row.cwd ?? '—'}` (human-readable identity preserved) ✅ 2026-05-15 — 2 commits (32763bd feat: project_key required string on `SessionListItemFull` + `SessionCompareSide` in `frontend/src/lib/api.ts` + 5 vitest fixture factory updates — `makeRow` SessionsTable per-row sentinel `pk00000000NN`, `makeSide` SessionCompareView, `makeFullSession` SkillRunsTable + LiveSessionsCard fixed sentinel `0123456789ab`, `makeSessionRow` CommandPalette gains optional 3rd `project_key` arg; 0b052b1 feat: ComparePicker filter switch — `aCwd`→`aProjectKey`, prop rename `scopeCwd`→`scopeProjectKey`, predicate `row.project_key === scopeProjectKey`, description copy switch, +167/-2 in `CommandPalette.test.tsx` with 2 new TDBT-01 cases). Vitest 325 → 327 in panels+ui sweep (327/0/0 across 70 files). pnpm tsc + lint --max-warnings 0 clean. Success-criteria greps: `scopeCwd`=0, `row.cwd === scopeCwd`=0, `scopeProjectKey`=12. SC#3 FULLY SATISFIED. REQ-ID TDBT-01 = Plan 27-02 backend + Plan 27-03 frontend = CLOSED. Zero deviations.
- [x] 27-04-PLAN.md — Wave 2 SC#1 surface: /skills + /skills/$name per-route adoption sweep — APPEND-ONLY validateSearch extension on both routes (time_from + time_to + compare_panels; /skills/$name PRESERVES range — Pitfall 2 LOCK); cmc-page--bounded on both root sections; 12 panel files migrated to bounded; SkillTimeline gains optional skillName + bounded props for single-skill detail-route reuse; long skill names wrap in TruncatedCell on /skills/$name; 4 detail-route panels wire LOCKED OPERATOR DECISION 2 hasGlobalPicker ternary (global picker WINS WHEN PRESENT, route-local ?range= is fallback) ✅ 2026-05-15 — 2 commits (0a3265b feat: /skills route — validateSearch append + cmc-page--bounded + 8 panels bounded + SkillCostCardForTopSkill wrapper consumes useRouteRangeVocab('14d', snapToSkillRange) instead of hard-coded '14d' + SkillLatencyTable globalRange + localOverride state + SkillTimeline gains optional skillName + bounded props + docs/url-contract.md /skills row updated + Phase 27 effects section added; 7cc525f feat: /skills/$name — validateSearch append (range PRESERVED) + cmc-page--bounded + TruncatedCell wraps <h1> skill name + SkillLatencySnapshot bespoke <section> gains cmc-card--bounded + SkillTimeline added as 5th panel via skillName={name} bounded + SkillCostCard hasGlobalPicker ternary (3-line idiom: call useRouteRangeVocab unconditionally → read URL search via useRouterState → ternary-select effectiveRange with explicit `hasGlobalPicker = typeof time_from === 'string' && typeof time_to === 'string'` presence flag — NOT the hook's return value as proxy because it returns routeDefault for both 'missing' and 'valid default match' cases) + SkillProjectsTable same idiom + SkillRunsTable bounded only). Frontend vitest 643/0/0 (no regressions; 3 panel test files gain vi.mock('@tanstack/react-router')). pnpm tsc --noEmit clean. pnpm lint --max-warnings 0 clean. Backend pytest tests/test_url_contract.py 2/2 PASS. ResponsiveContainer count unchanged (Phase 24 lock preserved — no new charts). Success-criteria greps: cmc-page--bounded hits both routes; bounded on 6 Skill panels. SC#1 surface FULLY SATISFIED. Zero deviations.
- [x] 27-05-PLAN.md — Wave 3 SC#2 surface: /cost per-route adoption sweep + TIME-04 CompareToggle on CostByProjectCard — APPEND-ONLY validateSearch extension (time_from + time_to + compare_panels); cmc-page--bounded on root section; both panels (CostByProjectCard + CostForecastCard) adopt bounded; CostByProjectCard URL-driven range via useRouteRangeVocab('7d', snapToCostRange) (drops the localStorage RangeToggle persistKey='cost-by-project' — URL is now the persistence layer); TruncatedCell wraps project_key column defensively (SC#2 "long project paths truncate cleanly"); CompareToggle mounted in CostByProjectCard chrome for URL-round-trip parity ✅ 2026-05-15 — 2 commits (b35fece feat: /cost route — validateSearch append + cmc-page--bounded + CostByProjectCard vocab bridge replaces useState+localStorage with useRouteRangeVocab + bounded on both panels + TruncatedCell on project column + CostForecastCard TIME-04 opt-out inline comment + docs/url-contract.md /cost row + Phase 27 effects extended; 2fabade feat: CostByProjectCard CompareToggle mounted in trailing chrome — TIME-04 URL round-trip parity with TokenUsageCard's Phase 26 Plan 07 contract). **Accepted Exception (escape path (i) from plan):** prior-period DeltaPill column NOT rendered because `useCostBreakdown` returns rolled-up per-project totals (CostBreakdownRow has only key + tokens_* + cost_usd — no time bucketing); client-side prior-period slicing impossible without backend window-shift endpoint. Toggle's URL-write contract still lands so saved-view fork-save round-trips; column rendering can be added additively when backend support lands (future Phase 28+ ANLY-*). Documented inline in CostByProjectCard.tsx + docs/url-contract.md Phase 27 effects + 27-05-SUMMARY.md Accepted Exception block + new vitest case asserts NO `.cmc-delta-pill` renders + NO "vs prior period" header text (pins the limitation against silent regression). **Pitfall 8 LOCK preserved trivially**: `rg -c "<ResponsiveContainer" frontend/src/components/panels/` returns 8 across 8 files (unchanged baseline from Phase 24/26 close). Frontend vitest 206/0/0 across 43 panel test files (incl. 6 new CostByProjectCard.compareOverlay.test.tsx cases). pnpm tsc + lint --max-warnings 0 clean. Backend pytest tests/test_url_contract.py 2/2 PASS. Zero deviations — plan executed exactly as written; escape path (i) was pre-authorized in plan Task 2 Step 3.
- [x] 27-06-PLAN.md — Wave 4 /alerts surface: per-route adoption sweep — APPEND-ONLY validateSearch extension (time_from + time_to + compare_panels) on alerts.tsx; cmc-page--bounded on root section; AlertRulesList adopts PanelCard bounded; AlertEventsList vocab-bridge migration replaces useState<AlertRange>('7d') + RangeToggle persistKey='alert-events-range' with useRouteRangeVocab<AlertRange>('7d', snapToAlertRange) (URL is the new persistence layer; legacy localStorage key becomes dead values); AlertRuleForm bespoke `<article className='cmc-card'>` gains cmc-card--bounded modifier directly (className-only touch — Phase 26 Plan 08 OtelPanel precedent); SCOPE BOUNDARY preserved so Plans 27-07 + 27-08 inherit clean AlertRuleForm via depends_on: 27-06 wave ordering ✅ 2026-05-15 — 2 commits (9837fbf feat: /alerts route — validateSearch append + cmc-page--bounded + AlertRulesList bounded + AlertRuleForm bespoke-card cmc-card--bounded touch + docs/url-contract.md /alerts row updated + Phase 27 effects section extended; 66461a9 feat: AlertEventsList vocab-bridge migration — useState+RangeToggle+localStorage round-trip REPLACED with useRouteRangeVocab('7d', snapToAlertRange); PanelCard bounded adopted; AlertEventsList.test.tsx rewrite with vi.mock router + 3 new SC tests — URL-read via ?time_from=now-30d&time_to=now snaps to '30d' fetch + localStorage-not-source-of-truth assertion + global TimePicker re-anchors without remount via rerender). Frontend vitest 208/0/0 across 43 panel test files (incl. 3 new AlertEventsList SC cases). pnpm tsc + lint --max-warnings 0 clean. Backend pytest tests/test_url_contract.py 2/2 PASS. Success-criteria greps: cmc-page--bounded on /alerts (1), snapToAlertRange on AlertEventsList (5), alert-events-range literal REMOVED (0), cmc-card--bounded on AlertRuleForm (1). Phase 24 ResponsiveContainer count UNCHANGED at 8 (lock preserved). Zero deviations — plan executed exactly as written; doc-comment cleanup for SC#3 strict grep (0 hits for literal alert-events-range string) was SC-driven, not a plan deviation. /alerts surface FULLY ADOPTED; Plans 27-07 + 27-08 now ungated for Wave 5+6.
- [x] 27-07-PLAN.md — Wave 5 TDBT-02 SOLE-SOURCE: AlertRuleForm.tsx FALLBACK_KNOWN_METRICS constant DELETED (was lines 56-60); defaultThresholdDraft + defaultAnomalyDraft set metric='' (empty-string sentinel); buildBody() adds 'Metric is required.' guard immediately after the name-required guard (Pitfall 2 typed-form pattern); knownMetrics useMemo simplifies to a one-line read of metricsQuery.data?.metrics ?? [] mapped to {value, label: key}; <select> renders 3-branch state — disabled 'Loading metric vocabulary…' placeholder when isLoading, 'No metrics available' when loaded-empty, 'Select a metric…' placeholder option + one option per metric when loaded-populated; backend test_alerts_metrics_sync.py DELETED + test_alerts_metrics_contract.py CREATED with 2 async tests asserting sorted(_SCOPE_EXTRACTORS.keys()) == sorted(GET /api/alerts/metrics → metrics) + smoke ≥3 metrics check; LOCKED OPERATOR DECISION 2 'Replace with contract test' honored verbatim ✅ 2026-05-15 — 2 commits (38424a1 feat: frontend SOLE-SOURCE — FALLBACK_KNOWN_METRICS deleted from AlertRuleForm.tsx + defaultThresholdDraft/defaultAnomalyDraft empty-string sentinel + buildBody empty-metric guard + knownMetrics useMemo one-line read + <select> 3-branch state + 5 new TDBT-02 vitest cases + 3 pre-existing tests updated to route /api/alerts/metrics + 1 test assertion adjusted for new placeholder + queries.ts doc-comment refresh; c611da3 test: backend drift-guard rewrite — test_alerts_metrics_sync.py DELETED + test_alerts_metrics_contract.py CREATED with 2 async tests using existing httpx.AsyncClient fixture). Backend pytest 689/0/0 → 690/0/0 (+1 net: −1 sync test deleted, +2 contract tests added). Frontend vitest AlertRuleForm.test.tsx 14/14 PASS (9 pre-existing + 5 new TDBT-02 cases — overall panel vitest 213/0/0). pnpm tsc + lint --max-warnings 0 clean. Success-criteria greps: grep -c FALLBACK_KNOWN_METRICS\|KNOWN_METRICS AlertRuleForm.tsx returns 0; ls backend/tests/test_alerts_metrics_sync.py returns 'No such file'; pytest tests/test_alerts_metrics_contract.py 2/2 PASS; pytest --collect-only | grep test_alerts_metrics_sync returns 0. Phase 24 ResponsiveContainer count UNCHANGED at 8. SC#4 of Phase 27 SATISFIED. 4 deviations auto-fixed: (1) strict-grep cleanup of comment text mentioning the deleted constant (Rule 1); (2)+(3) three pre-existing tests updated (Rules 1+3, anticipated by plan step 6); (4) ruff RUF100 unused-noqa directive cleanup (Rule 3, project-config noise).
- [x] 27-08-PLAN.md — Wave 6 TDBT-03 NL composer retry UX: AlertNlInput's silent inline `<p>Could not parse — please rephrase</p>` 503 branch REPLACED with a `<div role='alert' class='cmc-alert-nl__error'>` block containing the LOCKED OPERATOR DECISION 3 honest non-specific copy ('Couldn't parse this description. The phrasing didn't match a known pattern, or the natural-language service is temporarily unavailable.') + Retry button (data-testid='alert-nl-retry') that re-fires useParseAlertNl.mutate({ description: text }) with the same payload pattern as Parse. DoS guard: Retry is `disabled={m.isPending}` mirroring Parse; label toggles 'Retry' ↔ 'Retrying…'. V11 collapsed-failure-mode lock preserved verbatim — `git diff backend/cmc/api/routes/alerts.py` returns 0 lines (no discriminator field added to the 503 body). Queue UX intentionally NOT shipped (operator-locked: 'honor V11; non-specific copy + retry' per planning_context Decision #3). hadError latched-state added (set on m.isError, cleared on m.isSuccess) + derived `showError = m.isError || (hadError && m.isPending)` so the error block stays mounted across the retry's pending window (React Query clears isError → false on the next mutate(), which would otherwise unmount the Retry button mid-click — Rule 2 missing-critical fix). 5 new vitest cases under 'TDBT-03 (Phase 27 Plan 27-08): AlertNlInput 503 retry UX' describe block: (1) honest copy + Retry button render on 503 / (2) Retry re-fires mutation with same payload / (3) Retry disabled while m.isPending (DoS guard with held-promise hang pattern) / (4) non-specific copy invariant — NO 'credentials missing'/'Anthropic'/'API key' strings / (5) manual ThresholdForm BELOW AlertNlInput remains usable after 503 (Phase 21 Pitfall 5 invariant). CSS uses Phase 24 density-aware `--cmc-space-sm` tokens. alert-nl-retry exact-match testid registered in docs/testid-registry.md ✅ 2026-05-15 — 1 commit (1b6d690 feat: full TDBT-03 surface in one atomic commit — AlertRuleForm.tsx hadError+showError state + JSX swap + AlertRuleForm.test.tsx +5 cases + 1 pre-existing assertion update + styles.css 2 new rules + docs/testid-registry.md new subsection). Frontend vitest: AlertRuleForm.test.tsx 14/14 → 19/19 PASS; full panel suite 213/0/0 → 218/0/0 across 43 files (zero regressions). pnpm tsc --noEmit + pnpm lint --max-warnings 0 clean. Success-criteria greps: grep 'alert-nl-retry' AlertRuleForm.tsx → 1 hit; grep 'alert-nl-retry' docs/testid-registry.md → 1 hit; grep -i 'credentials missing|Anthropic|API key' AlertRuleForm.tsx → 0 hits (non-specific copy honored); git diff backend/cmc/api/routes/alerts.py → 0 lines (V11 lock honored). Phase 24 ResponsiveContainer count UNCHANGED at 8. SC#5 of Phase 27 SATISFIED. 3 deviations auto-fixed: (1) hadError latched-error state added as Rule 2 missing-critical to prevent React Query mid-click unmount of the Retry button; (2) pre-existing Phase 21 assertion updated /could not parse/i → /Couldn.t parse this description/i to track new copy (Rule 1); (3) strict-grep comment-hygiene cleanup in AlertRuleForm.tsx source (Rule 1 — same idiom as Plan 27-07 Deviation 1).
- [x] 27-09-PLAN.md — Wave 5 close gate: v13-tail-routes.spec.ts + axe/time-picker/truncation/portal/cost-dashboard/skills-detail/alerts extensions + 24 NEW visual-capture PNGs + 27-VISUAL-CHECK.md operator verdict (POLI-09) ✅ 2026-05-16 — 4 commits (a20e641, 346e227, d76a95b) + metadata close commit; operator verdict PASS signed 2026-05-16 by Patryk Golabek via live Chrome DevTools MCP walkthrough against http://localhost:5173 + backend on :8001. 24 NEW v13-tail-routes.spec.ts tests mapped 1:1 to SC#1-#5 + TDBT-01..03 (17 pass + 7 env-skip on dev DB lacking fixtures) + 18 extension tests across 5 v13-* spec families + 3 route specs (v13-a11y +5 chrome scans + PHASE_27_NET_CLASS_MARKERS inversion / v13-time-picker +3 re-anchor probes / v13-truncation +2 walks long-skill-name + project_key column / v13-portal-containment +1 Phase 27 sentinel / cost-dashboard +3 NEW + 1 RangeToggle-test rewritten to global-TimePicker path / skills-detail +2 / alerts +2) + 24 NEW visual-capture PNGs (4 tail-route surfaces × 3 densities × 2 themes) + 1 Rule-1 verification-discovered fix (d76a95b: SkillLatencySnapshot success-state cmc-card--bounded — Plan 27-04 Step 6 missed the success branch where bare `<section className="cmc-card">` rendered when real data flowed; live walkthrough caught 5 cards rendered with only 4 bounded; one-line className fix restored 5/5; mirrors Phase 26 e838135 close-gate fix precedent). Phase 27 close evidence: backend pytest 690/0/0 (+4 vs Phase 26: Plan 27-02 +3 project_key round-trip + Plan 27-07 net +1 contract guard); frontend vitest 610 → 662/0/0 (+52 across Plans 01-08; Plan 09 e2e-only); Playwright 207 → ~243 tests (+36 net); Lighthouse deferred + accepted at Phase 26 9/9 baseline (Phase 27 ships ZERO new runtime deps; pnpm build clean — CommandPalette chunk 389 kB / gzip 121 kB unchanged); axe 0 Phase-27-attributable blocking (PHASE_27_NET_CLASS_MARKERS additive: cmc-alert-nl__error + cmc-alert-nl__error-actions + cmc-alert-rule-form; 8 v1.2 carry-overs continue flowing through Phase 25's filter — Pitfall 7 rebalance window NOW Phase 28+ candidate); portal containment 6/6 → 7/7 PASS (+1 Phase 27 sentinel confirming NO new portal-mounting surfaces — CompareToggle is plain button, AlertNlInput 503 error block is inline DOM); URL contract 2/2 PASS (Phase 27 validateSearch APPEND-ONLY: time_from + time_to + compare_panels on /skills, /skills/$name, /cost, /alerts; /skills/$name range PRESERVED per Pitfall 2 LOCK); ResponsiveContainer count = 8 (Phase 24 lock preserved — no new charts in Phase 27, CompareToggle on CostByProjectCard ships URL-only with DeltaPill column deferred per Accepted Exception #2); 120 total visual capture PNGs (36 + 30 + 30 + 24). 6 Accepted Exceptions operator-acknowledged: (1) CostForecastCard MTD-only opt-out — SC#2 "re-query" satisfied as "re-render" (no range param on /api/cost/forecast); (2) CostByProjectCard DeltaPill column deferred — escape path (i), URL round-trip ships but client-side prior-period slicing needs backend bucketed support; (3) stale backend restart Rule 3 self-healed during cascade; (4) cost-dashboard.spec.ts RangeToggle test rewrite Rule 1 self-healed (Plan 27-05 dropped the button); (5) NEW: SkillLatencySnapshot stays on route-local `?range=` per operator-locked decision documented at skills_.$name.tsx:242-246; (6) NEW: verification-discovered d76a95b fix mirroring Phase 26 e838135 precedent. URL-as-broadcast-bus pattern verified end-to-end across tail routes. Phase 27 success criteria 1-5 all functionally verified. v1.3 milestone advances 3/5 → 4/5 phases complete.
**UI hint**: yes

### Phase 28: Layout Customization
**Goal**: Make the dashboard surface user-customizable on top of the now-stable shell — per-route panel show/hide, 1D drag-reorder within columns, split-pane resize via `react-resizable-panels@4.11.0` on `/sessions/compare` and per-route shells, and reset-to-default to prevent corrupt-state lock-in. Layout state piggybacks on saved-view `state_json` (no new DB table, no new endpoint).
**Depends on**: Phase 24 (primitives, perf budget — drag must not regress paint), Phase 25 (`state_json` schema stable, `validateSearch` shapes append-only on every route), Phase 26 (time picker contract), Phase 27 (per-route adoption complete on every route the user can customize)
**Requirements**: LAYO-01, LAYO-02, LAYO-03, LAYO-04
**Success Criteria** (what must be TRUE):
  1. User opens the panel header DropdownMenu on `/`, hides "System Pressure", saves the layout into a saved view (or just leaves the URL state), navigates away, returns, and the panel stays hidden — the hidden state lives inside the saved view's `state_json` (additive, opaque, no schema break)
  2. User drags the divider on `/sessions/compare` to resize the left/right panes, refreshes the page, and the resize persists into URL state and the loaded saved view; double-click on the divider resets to the default 50/50 split
  3. User reorders panels within a column via 1D drag on `/cost` (no cross-column movement), the new order persists into the active saved view, and the reset-to-default affordance in the panel DropdownMenu clears layout overrides cleanly
  4. `react-resizable-panels@4.11.0` is the only new runtime dependency added in Phase 28 — no `react-grid-layout`, no `dnd-kit`, no `@shadcn/ui`, no Tailwind; React DevTools profiler shows zero chart re-mounts during a layout drag (data memoized; `ResponsiveContainer` count unchanged); axe-core remains clean; visual checkpoint at `.planning/phases/28/VISUAL-CHECK.md` documents the closed milestone
  5. Backend pytest + frontend vitest + Playwright e2e all green at phase close vs every prior v1.3 phase baseline; URL contract test (`tests/test_url_contract.py`) confirms every preserved URL pattern still resolves; v1.3 milestone close gate met
**Plans**: 6 plans
Plans:
- [x] 28-01-PLAN.md — Wave 0: Test scaffolding + docs/url-contract.md + docs/testid-registry.md extension (gates all downstream waves) ✅ 2026-05-16 — 3 atomic commits (7ab32a7 test author 6 vitest skeletons + Playwright spec skeleton with 16 test.skip placeholders across LAYO-01..04 + saved-view round-trip + ResponsiveContainer perf gate — 52 it.todo placeholders enumerate every Wave 1-3 assertion downstream plans must implement; 8442e16 docs extend url-contract.md Phase 28 effects section + Locked invariants subsection — 3 new APPEND-ONLY search params hidden_panels/panel_order on 5 routes + split_sizes on /sessions/compare with Pitfall 2 default-undefined contract + Pitfall 9 panel-id append-only invariant + Pitfall 11 reset semantics + Pitfall 12 /skills/$name out-of-scope rationale; 26e19d3 docs register Phase 28 testid families — 5 dynamic (panel-header-menu-{panelId}, panel-hide-{panelId}, panel-drag-grip-{panelId}, panel-reset-layout-{route} mounted in BOTH PanelHeaderMenu AND SavedViewMenu chrome per RESEARCH §7 A2, resize-handle-{groupId}) + 1 exact-match (panel-grid-{columnId})). All Phase 27 close-gate baselines preserved: backend pytest tests/test_url_contract.py 2/2 PASS; frontend pnpm test --run 662 passed + 52 new todo (107 files passed + 5 skipped); Playwright tests/e2e/v13-layout.spec.ts lists 16 tests (exceeds plan ≥15 threshold); pnpm exec eslint clean across src/**/*.{ts,tsx}. Zero deviations from plan. Wave 1 (Plan 28-02 foundation primitives) ready to spawn.
- [ ] 28-02-PLAN.md — Wave 1: Foundation primitives — asHiddenPanels/asPanelOrder/asSplitSizes validators + panelRegistry + useLayoutState hook + PanelCard.panelId prop + SavedViewMenu Reset Layout (LAYO-04 escape hatch)
- [ ] 28-03-PLAN.md — Wave 2: LAYO-01 + per-panel LAYO-04 — PanelHeaderMenu component + APPEND-ONLY hidden_panels + render-time filter on /, /activity, /cost, /skills, /alerts
- [ ] 28-04-PLAN.md — Wave 3: LAYO-02 — DraggablePanelWrap (native HTML5 dnd + keyboard a11y + aria-live) + panel_order render-order wiring on the same 5 routes
- [ ] 28-05-PLAN.md — Wave 4: LAYO-03 — install react-resizable-panels@4.11.0 (blocking-human legitimacy gate) + ResizablePanelGroup v4 wrapper + SessionCompareView split-pane wiring
- [ ] 28-06-PLAN.md — Wave 5: v1.3 milestone close gate — 18 NEW visual capture PNGs + axe extensions + 28-VISUAL-CHECK.md operator-signed verdict (POLI-09)
**UI hint**: yes

## Progress

**Execution Order:** Phases execute in numeric order. Phase numbering is continuous across milestones (never restarts at 01).

| Phase | Milestone | Plans Complete | Status   | Completed  |
| ----- | --------- | -------------- | -------- | ---------- |
| 1. Foundation & Database | v1.0 | 7/7 | Complete | 2026-04-25 |
| 2. Data Ingestion | v1.0 | 6/6 | Complete | 2026-04-26 |
| 3. Read-Only APIs | v1.0 | 5/5 | Complete | 2026-04-26 |
| 4. Stateful APIs | v1.0 | 5/5 | Complete | 2026-04-26 |
| 5. Frontend Shell & Design System | v1.0 | 4/4 | Complete | 2026-04-27 |
| 6. Observability & Activity Panels | v1.0 | 5/5 | Complete | 2026-04-27 |
| 7. Command Centre Panels | v1.0 | 4/4 | Complete | 2026-04-27 |
| 8. Mission Control Dispatcher | v1.0 | 4/4 | Complete | 2026-04-27 |
| 9. Telegram, Setup & Testing | v1.0 | 5/5 | Complete | 2026-04-28 |
| 10. Telegram Wiring Fixes (gap closure) | v1.0 | 1/1 | Complete | 2026-04-28 |
| 11. v1.0 Documentation & Env Polish (gap closure) | v1.0 | 1/1 | Complete | 2026-04-28 |
| 12. OTEL Skill Event Spike | v1.1 | 2/2 | Complete | 2026-05-02 |
| 13. Cost Foundation & Skill Ingest | v1.1 | 6/6 | Complete | 2026-05-03 |
| 14. Skills API & Page Panels | v1.1 | 5/5 | Complete | 2026-05-04 |
| 15. Alert Engine & UI | v1.1 | 5/5 | Complete | 2026-05-04 |
| 16. Session Comparison | v1.1 | 4/4 | Complete | 2026-05-05 |
| 17. Polish, Doctor & Tests | v1.1 | 6/6 | Complete | 2026-05-05 |
| 18. Polish & Carry-Forward Cleanup | v1.2 | 5/5 | Complete | 2026-05-05 |
| 19. Skills Per-Project, Deltas & Badges | v1.2 | 4/4 | Complete | 2026-05-06 |
| 20. Cost Forecast & Per-Project Card | v1.2 | 4/4 | Complete | 2026-05-06 |
| 21. Alert Anomaly Depth & NL Authoring | v1.2 | 3/3 | Complete | 2026-05-07 |
| 22. Skill Latency Overhead (spike-gated) | v1.2 | 2/2 | Complete | 2026-05-08 |
| 23. Compare Depth & Milestone Close | v1.2 | 4/4 | Complete | 2026-05-09 |
| 24. Shell + Density + Containment Primitives | v1.3 | 7/7 | Complete | 2026-05-12 |
| 25. Saved Views (Backend + Frontend) | v1.3 | 11/11 | Complete | 2026-05-12 |
| 26. Per-Route Adoption I + Time + Cmd+K | v1.3 | 9/9 | Complete | 2026-05-13 |
| 27. Per-Route Adoption II + Tech Debt | v1.3 | 9/9 | Complete | 2026-05-16 |
| 28. Layout Customization | v1.3 | 1/6 | In progress | — |

**v1.0 milestone shipped: 47/47 plans, 11/11 phases verified (9 base + 2 audit gap-closure).**
**v1.1 milestone shipped: 28/28 plans, 6/6 phases verified, 41/41 requirements satisfied.**
**v1.2 milestone shipped: 22/22 plans, 6/6 phases verified, 12/12 active requirements satisfied + 1 honestly deferred (SKLP-11 → v1.3).**
**v1.3 milestone in progress: 4/5 phases complete, 41/45 active requirements satisfied. Phase 27 closed 2026-05-16 (operator verdict PASS via live Chrome DevTools MCP walkthrough against http://localhost:5173 + backend on :8001, 9/9 plans shipped, TDBT-01 + TDBT-02 + TDBT-03 satisfied; final test counts: backend pytest 690/0/0 (+4 vs Phase 26), frontend vitest 662/0/0 (+52 vs Phase 26), Playwright e2e ~243 tests (24 NEW v13-tail-routes + 18 extensions = +42), Lighthouse deferred + accepted at Phase 26 9/9 baseline since Phase 27 ships ZERO new runtime deps, axe 0 Phase-27-attributable blocking with PHASE_27_NET_CLASS_MARKERS inversion filter additive, portal containment 7/7 PASS, URL contract 2/2 PASS, 120 total visual capture PNGs, ResponsiveContainer count = 8 — Phase 24 lock preserved; 6 Accepted Exceptions operator-acknowledged: CostForecastCard MTD-only opt-out + CostByProjectCard DeltaPill column deferred + stale backend restart Rule 3 self-healed + cost-dashboard RangeToggle test rewrite Rule 1 self-healed + NEW SkillLatencySnapshot stays on route-local `?range=` per operator-locked decision + NEW verification-discovered d76a95b cmc-card--bounded fix mirroring Phase 26 e838135 close-gate precedent). Phase 27 Plans 01-09 chronological recap: Plan 01 foundation: useRouteRangeVocab generic bridge hook + 3 vocab snappers; Plan 02: TDBT-01 backend half — project_key surfaces on /api/sessions list + /api/sessions/compare wire shapes; Plan 03: TDBT-01 frontend half — ComparePicker filter switch from cwd-proxy to project_key authoritative identity; Plan 04: SC#1 surface — /skills + /skills/$name per-route adoption sweep with hasGlobalPicker ternary, cmc-page--bounded, TruncatedCell, SkillTimeline-as-5th-panel; Plan 05: SC#2 surface — /cost per-route adoption + TIME-04 CompareToggle on CostByProjectCard, URL-driven range via useRouteRangeVocab replaces localStorage RangeToggle, Accepted Exception on prior-period DeltaPill column deferred per escape path (i) because useCostBreakdown lacks time bucketing; Plan 06: /alerts surface FULLY ADOPTED — APPEND-ONLY validateSearch (time_from + time_to + compare_panels), cmc-page--bounded on root, AlertRulesList bounded, AlertEventsList vocab-bridge migration, AlertRuleForm bespoke-card cmc-card--bounded className-only touch with SCOPE BOUNDARY preserved so Plans 27-07 + 27-08 inherit a clean base via depends_on: 27-06 wave ordering; Plan 07: TDBT-02 SOLE-SOURCE — FALLBACK_KNOWN_METRICS constant DELETED from AlertRuleForm.tsx + useAlertMetrics is now the SOLE frontend metric-vocabulary source + drift guard rewritten from build-time grep test_alerts_metrics_sync.py (DELETED) to runtime API-contract test_alerts_metrics_contract.py (CREATED) per LOCKED OPERATOR DECISION 2 honored verbatim; Plan 08: TDBT-03 — AlertNlInput silent inline `<p>Could not parse — please rephrase</p>` 503 branch REPLACED with `<div role='alert' class='cmc-alert-nl__error'>` containing LOCKED OPERATOR DECISION 3 honest non-specific copy + Retry button + hadError latched-state preventing React Query mid-click unmount + V11 collapsed-failure-mode lock preserved verbatim (backend route UNCHANGED) + Queue UX intentionally NOT shipped per operator-locked decision; Plan 09 close gate. Phase 26 closed 2026-05-13 (operator verdict PASS, 9/9 plans, SHEL-05 + TIME-01..05 + CMDK-02..04 satisfied). Phase 25 closed 2026-05-12 (operator verdict PASS, 11/11 plans, VIEW-01..09 + CMDK-01 + SHEL-06 satisfied). Phase 24 closed 2026-05-12 (operator verdict PASS, 7/7 plans, CONT-01..05 + SHEL-01..04 + DENS-01..03 + POLI-09..14 satisfied). Plans authored per-phase via `/gsd:plan-phase {N}`.**
